# FUNCTIONAL_TEST_REPORT — ที่ดินชัวร์

วันที่ทดสอบ 19–20 กันยายน 2569 · ทดสอบบน production (อ่านอย่างเดียว) + รันชุดเทสต์ในเครื่อง
**ไม่มีการส่งแบบฟอร์มใด ๆ เข้า production ระหว่างทดสอบ** ตามกติกางานที่ 18

---

## 1. ชุดเทสต์อัตโนมัติ (baseline)

### 1.1 เว็บสาธารณะ — `nj-land-listings`

ไม่มี `package.json` · รันทีละไฟล์ด้วย `node <ไฟล์>.test.js`

| ไฟล์ | ผล | จำนวนข้อ |
|---|---|---|
| `agency.test.js` | ผ่าน | 69 |
| `compare.test.js` | ผ่าน | 10 |
| `contracts.test.js` | ผ่าน | 194 |
| `feecalc.test.js` | ผ่าน | 50 |
| `loancalc.test.js` | ผ่าน | 25 |
| `njchat.test.js` | ผ่าน | 51 |
| `valuecalc.test.js` | ผ่าน | 28 |
| `verified.test.js` | ผ่าน | 58 |
| `zonechip.test.js` | ผ่าน | 18 |
| `zoneguide.test.js` | ผ่าน | 39 |
| **รวม** | **ผ่านทั้งหมด** | **542 · ไม่ผ่าน 0** |

### 1.2 ระบบหลังบ้าน — `nj-survey-system`

```
npm run lint   → ผ่านทั้งหมด (4 กฎ)
npm test       → exit code 0
```

| | ผล |
|---|---|
| ไฟล์เทสต์ที่รัน | 63 ไฟล์ (ตาม `scripts.test`) |
| ข้อที่ผ่าน | ประมาณ **4,345** |
| ข้อที่ไม่ผ่าน | **0** |
| เวลาที่ใช้ | ประมาณ 6 นาที |

หมายเหตุ: ชุดเทสต์เตือนว่าฐานข้อมูลทดสอบยังใช้รหัสผ่านตั้งต้น (`admin`, `sales`, `gov`, `field`, `docs`, `acct`, `somchai`) — เป็นข้อมูลทดสอบในเครื่อง ไม่ใช่ production แต่ควรยืนยันกับเจ้าของว่า production เปลี่ยนรหัสครบแล้ว

### 1.3 ตัวตรวจลิงก์

```
node build/linkcheck.js  →  ลิงก์เสีย 0 · หน้ากำพร้า 3
```

| หน้ากำพร้า | มีลิงก์เข้าจาก |
|---|---|
| `inspect.html` | JS เท่านั้น (`land.js`) |
| `land.html` | JS เท่านั้น (7 ไฟล์) |
| `partner-apply.html` | **ไม่มีลิงก์จากที่ใดเลย** |

จุดยึด 4 จุดที่มีจริงเฉพาะหลัง JS ทำงาน: `videos.html#clip1` ถึง `#clip4`

---

## 2. ทดสอบเส้นทางผู้ใช้จริง (production)

### 2.1 ค้นหาจากหน้าแรก

| ข้อ | ผล | หมายเหตุ |
|---|---|---|
| โหลดประกาศจาก API | **ผ่าน** | 9 ใบบนหน้าแรก (แบ่งหน้า) |
| ค้นคำว่า "สมุทรปราการ" | **ผ่าน** | 9 → 5 รายการ |
| ข้อความสรุปผล | **ผ่าน** | "ผลการค้นหา สมุทรปราการ — พบ 5 รายการ" |
| ค้นคำที่ไม่มีผล | **ผ่าน** | ขึ้น empty state พร้อม CTA "ฝากหาที่ดินฟรี" |
| ตัวแบ่งหน้า `#listing-pager` | **ผ่าน** | มีจริง |
| **URL เปลี่ยนตามคำค้น** | **ไม่ผ่าน** | URL ยังเป็น `https://njteedinsure.com/` เหมือนเดิม |
| **แชร์ผลการค้นหาได้** | **ไม่ผ่าน** | ผลจากข้อบน |
| **ปุ่มย้อนกลับคืนค่าตัวกรอง** | **ไม่ผ่าน** | ไม่มี history entry ให้ย้อน |
| Autocomplete จังหวัด/อำเภอ/ตำบล | **ไม่มี** | ช่องค้นหาเป็น `<input type=search>` เปล่า ไม่มี `datalist` |

### 2.2 หน้ารวมประกาศ `/listings.html`

| ข้อ | ผล | หมายเหตุ |
|---|---|---|
| จำนวนประกาศทั้งหมด | 22 รายการ | |
| ตัวกรอง 7 ชุด | **ผ่าน** | `f-type` `f-province`(11) `f-prop`(9) `f-floors`(4) `f-deed`(5) `f-zone`(14) `f-sort`(5) |
| กรองจังหวัด "กรุงเทพมหานคร" | **ผ่าน** | 22 → 5 รายการ |
| รายชื่อจังหวัดสร้างจากของจริง | **ผ่าน** | 10 จังหวัดที่มีแปลงจริง ไม่ใช่ 77 จังหวัด |
| เรียงลำดับ | **ผ่าน** | 5 ตัวเลือก |
| **URL เปลี่ยนตามตัวกรอง** | **ไม่ผ่าน** | `location.search` ไม่เปลี่ยนเลย |
| **ตัวกรองแบบ Bottom Sheet บนมือถือ** | **ไม่มี** | ตัวกรองเรียงต่อกันเป็นแถวยาวแทน |
| แบ่งหน้า | **ไม่มี** | แสดงทุกแปลงในหน้าเดียว (ตามที่ออกแบบไว้เดิม) |
| ข้อความ "ถูกซ่อนเพราะยังไม่ได้ระบุ" (`#unknown-note`) | ไม่ขึ้นในรอบที่ทดสอบ | ตรรกะมีอยู่ แต่ยังไม่มีกรณีที่ทริกเกอร์ |

### 2.3 การ์ดประกาศ

ตรวจ DOM จริงของการ์ดใบหนึ่ง (`OP-026`)

| องค์ประกอบที่งานที่ 7 ต้องการ | มี/ไม่มี |
|---|---|
| ภาพ | มี (พร้อม `loading="lazy"`) |
| ราคา | มี |
| ทำเล | มี |
| ประเภททรัพย์ | มี (แสดงเมื่อทีมกรอกแล้ว) |
| เนื้อที่ | มี |
| ราคา/ตารางวา | มี (เมื่อคำนวณได้) |
| ระดับการตรวจสอบ | มี — ป้าย `n/5 ระดับตรวจสอบ` + ป้ายคะแนนความพร้อม |
| **รหัสทรัพย์** | **ไม่แสดงให้ผู้ใช้เห็น** (มีแต่ใน `data-id` / `data-njcmp`) |
| **ปุ่มบันทึก** | **ไม่มี** |
| ปุ่มเปรียบเทียบ | **มี** — `.njcmp-btn` พร้อม `aria-pressed` (ฉีดโดย `compare.js`) |
| ปุ่ม "ดูรายละเอียด" | ไม่มีปุ่มแยก — ทั้งการ์ดกดได้ + หัวข้อเป็นลิงก์ |

**ปัญหาที่พบเพิ่ม:** `alt` ของรูปการ์ดยาวมาก เพราะใส่คำโปรยทั้งท่อน
ตัวอย่างจริง: "แขวงคลองขวาง เขตภาษีเจริญ กรุงเทพมหานคร 10160 · 98200 - ขายบ้านเดี่ยวแปลงมุม 95.2 ตร.ว. หมู่บ้านนิศาชล บางแวก 79 · 0-0-95.2 ไร่"
โปรแกรมอ่านหน้าจอจะอ่านซ้ำกับข้อความในการ์ดที่อยู่ถัดไปทันที

**ปัญหาที่ยืนยัน:** การ์ดใบเดียวกันติดป้ายสองระบบที่ตอบคนละคำถาม — `◐ ข้อมูลเบื้องต้น` (tier 1) บนรูป และ `n/5 ระดับตรวจสอบ` ในตัวการ์ด

### 2.4 หน้ารายละเอียดแปลง `/land.html?id=OP-072`

| ข้อ | ผล |
|---|---|
| โหลดข้อมูลแปลง | **ผ่าน** |
| `<title>` เปลี่ยนตามแปลง | **ผ่าน** (ด้วย JS) |
| `canonical` เปลี่ยนตามแปลง | **ผ่าน** (ด้วย JS) |
| H1 เปลี่ยนตามแปลง | **ผ่าน** (ด้วย JS) แต่ยาวทั้งย่อหน้า |
| JSON-LD `RealEstateListing` | **ผ่าน** (ด้วย JS) |
| **meta description เปลี่ยนตามแปลง** | **ไม่ผ่าน** — เป็นข้อความกลางทุกแปลง |
| **`og:title` เปลี่ยนตามแปลง** | **ไม่ผ่าน** — "รายละเอียดแปลงที่ดิน \| ที่ดินชัวร์" ทุกแปลง |
| **`og:url`** | **ไม่มี** |
| **ประเภททรัพย์ตรงกับข้อมูลจริง** | **ไม่ผ่าน** — title ว่า "ขายที่ดิน" แต่ H1 ว่า "บ้านแฝด 2 ชั้น" |
| Breadcrumb | **ไม่มี** |
| Sticky Contact Bar บนมือถือ | **ไม่มี** |
| Similar Properties | **ไม่มี** |
| Recently Viewed | **ไม่มี** |
| นัดชมทรัพย์ | **มี** |
| ปุ่มแชร์ | **ไม่มี** |
| Save Property | **ไม่มี** |
| Compare Property | **มี** |
| Report Listing | **ไม่มี** |
| `land.html?id=<ไม่มีจริง>` | **คืน HTTP 200** (soft 404) |

### 2.5 ฟอร์ม (ตรวจจากโค้ด ไม่ได้ส่งจริง)

| ฟอร์ม | ไฟล์ | Validation ไทย | กันกดซ้ำ | Save Draft | Progress | Auto-format | Honeypot | Consent |
|---|---|---|---|---|---|---|---|---|
| ฝากขาย | `consign.js` | มี | **มี** | **มี** | บางส่วน | บางส่วน | **มี** | มี |
| ตรวจทรัพย์ | `verify.js` | มี | มี | ไม่มี | ไม่มี | ไม่มี | **มี** | มี |
| ฝากหา | `wanted.js` | มี | มี | ไม่มี | ไม่มี | ไม่มี | **มี** | มี |
| นัดตรวจแปลง | `inspect.js` | มี | มี | ไม่มี | **มี** | ไม่มี | ไม่มี | มี |
| สมัครพันธมิตร | `partner-apply.js` | มี | มี | ไม่มี | **มี** | ไม่มี | ไม่มี | มี |
| ฟอร์มข้อมูลแปลงร่วม | `landform.js` | — | ไม่มี | ไม่มี | ไม่มี | บางส่วน | ไม่มี | — |
| Newsletter | **ไม่มีในเว็บ** | — | — | — | — | — | — | — |

**ฝั่งเซิร์ฟเวอร์**

| ข้อ | สถานะ |
|---|---|
| Rate limit | **มี** — `uploadThrottled(ip)` คืน 429 ในทุก endpoint สาธารณะที่เขียนข้อมูล |
| ล็อกบัญชีเมื่อใส่รหัสผิดหลายครั้ง | **มี** — 429 |
| Consent log | **มี** — `consentStatus` และกติกา "การกรอกฟอร์มบนเว็บไม่ใช่ความยินยอม" ถูกบังคับที่เซิร์ฟเวอร์ (`consentStatus: 'none'` ตายตัว) |
| Lead เข้าหลังบ้าน | **มี** — ทุกฟอร์มสร้างเรื่องในระบบและมีเทสต์ครอบ |
| Success page | มีเป็นสถานะในหน้าเดิม ไม่ใช่หน้าแยก |
| **UTM / Source / Landing page / First touch / Last touch** | **ไม่มีเลย** |
| ไม่ส่ง PII เข้า Analytics | **ผ่าน** — `/api/public/track` ส่งแค่ `type`, `ref`, `listingId` |

### 2.6 เครื่องมือคำนวณ

| เครื่องมือ | ไฟล์ | เทสต์ | หน้าที่ใช้ |
|---|---|---|---|
| ค่าโอน/ภาษีวันโอน | `feecalc.js` | 50 ข้อ ผ่าน | index · guides · land · tools |
| ค่างวดสินเชื่อ | `loancalc.js` | 25 ข้อ ผ่าน | tools |
| ประเมินมูลค่า | `valuecalc.js` | 28 ข้อ ผ่าน | guides |
| ค่ารังวัด | `surveyquote.js` | อยู่ในชุด contracts | index · consign |
| คู่มือผังสี | `zoneguide.js` | 39 ข้อ ผ่าน | tools |

**ไม่มี event ติดตามการใช้เครื่องคำนวณเลย** (`use_calculator` ยังไม่มีในระบบ)

### 2.7 แชทบอต

| ข้อ | ผล |
|---|---|
| ปุ่มลอยปรากฏ | **ทันทีที่หน้าโหลด** — ไม่ได้รอ 15–30 วินาที และไม่ได้รอให้เลื่อนจอ |
| เปิดแผงแชทเอง | **ไม่เปิดเอง** (ถูกต้อง) |
| ส่ง context ของหน้า | ส่งแค่ชื่อไฟล์ (เช่น `land.html`) |
| **ส่งรหัสทรัพย์อัตโนมัติ** | **ไม่ส่ง** |
| ปุ่มคุยเจ้าหน้าที่ | มีเป็น chip "คุยกับเจ้าหน้าที่" |
| Event | มี `chat_open` · **ไม่มี** `chat_to_human` |
| บังแบนเนอร์คุกกี้ / แถบติดต่อ | ปุ่มลอย z-index 790 สูงกว่าแบนเนอร์คุกกี้ (200) และแถบล่าง (150) — บนจอ 375 px อยู่ห่างจากก้นจอ 228 px จึงยังไม่ทับกันตรง ๆ แต่กินพื้นที่จอแรกร่วมกัน |

### 2.8 แบนเนอร์คุกกี้

| ข้อ | ผล |
|---|---|
| ปุ่ม "ยอมรับ" | มี |
| ปุ่ม "ปฏิเสธ" | มี |
| **ปุ่ม "ตั้งค่าคุกกี้"** | **ไม่มี** |
| จำการเลือก | มี (`localStorage` คีย์ `njts_consent`) |
| ไม่โหลด Analytics/Marketing ก่อนยินยอม | **ผ่าน** — Meta Pixel / GA4 โหลดหลังกดยอมรับเท่านั้น |
| ความสูงบนมือถือ | **140 px** — บังปุ่ม CTA รองของ hero |
| **ลิงก์ไปนโยบายคุกกี้** | **ไม่มี เพราะไม่มีหน้านโยบาย** |
| สไตล์ | เขียนซ้ำ 4 ไฟล์ (`consent.css`, `home.css`, `inspect.css`, `ui.css`) แต่ทุกหน้ามีสไตล์ครบ ไม่พบหน้าที่แบนเนอร์เพี้ยน |

---

## 3. ตรวจ Console / Network / API

| ข้อ | ผล |
|---|---|
| Console error บน `/` | **ไม่มี** |
| Console error บน `/listings.html` | **ไม่มี** |
| Console error บน `/land.html?id=…` | **ไม่มี** |
| HTTP error ใน network | **ไม่มี** — ทุกคำขอ 200 |
| `/api/public/listings` | 200 · ประมาณ 844 ms |
| `/api/public/track` | 200 (sendBeacon) |
| รูปประกาศจาก `app.njteedinsure.com` | 200 · `cache-control: public, max-age=31536000, immutable` |
| ลิงก์เสีย | 0 |
| `robots.txt` / `sitemap.xml` | 200 ทั้งคู่ |
| `/no-such-page-xyz` | **404 จริง** (ถูกต้อง) |
| `/land.html?id=NOPE-999` | **200** (soft 404 — ต้องแก้) |

> ข้อสังเกตด้านวิธีทดสอบ: การเรียก `fetch()` ไปยังรูปบน `app.njteedinsure.com` จากหน้า `njteedinsure.com` ถูกบล็อกด้วย CORS (ไม่มี `Access-Control-Allow-Origin`) แต่แท็ก `<img>` ไม่ต้องใช้ CORS จึงโหลดได้ตามปกติ — ยืนยันแล้วด้วยการเปิดไฟล์รูปตรง ๆ (1800×1013 · 458 KB · HTTP 200) **ไม่ใช่บั๊กของ production**

---

## 4. สรุปผล

| หมวด | ผ่าน | ไม่ผ่าน / ขาด |
|---|---|---|
| ชุดเทสต์อัตโนมัติ | 542 + 4,345 ข้อ | 0 |
| ลิงก์ | 0 ลิงก์เสีย | 3 หน้ากำพร้า |
| ค้นหา/กรอง | ทำงานถูกต้อง | URL ไม่แชร์ได้ · ไม่มี autocomplete · ไม่มี bottom sheet |
| การ์ดประกาศ | 7 จาก 10 องค์ประกอบ | รหัสทรัพย์ · ปุ่มบันทึก · ปุ่มดูรายละเอียด |
| หน้ารายละเอียด | 5 จาก 18 องค์ประกอบ | meta/og ไดนามิก · breadcrumb · sticky bar · similar · recently viewed · share · save · report |
| ฟอร์ม | validation + กันกดซ้ำ + rate limit + consent | draft · progress · auto-format · UTM |
| แชทบอต | ไม่เปิดเอง · มีปุ่มคุยเจ้าหน้าที่ | ปรากฏเร็วเกินไป · ไม่ส่งรหัสทรัพย์ · ไม่มี event `chat_to_human` |
| คุกกี้ | จำการเลือก · ไม่โหลด tracker ก่อนยินยอม | ไม่มีปุ่มตั้งค่า · ไม่มีลิงก์นโยบาย · บัง CTA |

**ยังไม่ได้ทดสอบ** (ต้องใช้ staging + ข้อมูลทดสอบ ตามกติกางานที่ 18)

- การส่งแบบฟอร์มจริงทั้ง 6 ชุด
- การอัปโหลดไฟล์แนบ
- การเข้าสู่ระบบและหน้าหลังบ้าน (ต้องใช้สิทธิ์ของเจ้าของ)
- Permission test ระหว่างบทบาทผู้ใช้
- ทดสอบข้ามเบราว์เซอร์จริง (Safari / Edge / Mobile Safari)
