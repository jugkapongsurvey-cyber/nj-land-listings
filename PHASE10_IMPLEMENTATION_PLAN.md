# PHASE10_IMPLEMENTATION_PLAN.md — แผนพัฒนา Phase 10: SEO Marketing and User Growth

**วันที่:** 18 กันยายน 2569 · **สาขา:** `feature/phase-10-seo-growth` (แตกจาก `origin/main` = `64c7f37`)
**อ่านคู่กับ** `PHASE10_SEO_AUDIT.md` (ผลตรวจและหลักฐาน)
**สถานะ:** ยังไม่เริ่มแก้โค้ด — รออนุมัติแผนและคำตอบ 6 ข้อในหัวข้อ 8 ของไฟล์ audit

---

## 1. สรุประบบเดิม (ตามข้อกำหนดข้อ 1 และ 6)

| ชั้น | ของจริงที่ใช้อยู่ |
|---|---|
| เว็บสาธารณะ | HTML/CSS/JS สแตติกล้วน 20 หน้า · ไม่มี build step · ไม่มี framework · ไม่มี `package.json` · GitHub Pages |
| Routing | ชื่อไฟล์ตรงๆ + query string (`land.html?id=OP-072`) |
| ฐานข้อมูล | **เว็บสาธารณะไม่มีของตัวเอง** · ระบบหลังบ้านเป็น JSON blob (`lib/db.js`) บน Postgres |
| Authentication | JWT ที่ระบบหลังบ้าน · เว็บสาธารณะไม่มีล็อกอิน (ใช้ลิงก์มีตั๋วแทน) |
| Admin | SPA หน้าเดียวที่ `app.njteedinsure.com` · สิทธิ์ 47 พื้นที่ (`lib/auth.js` `AREAS`) · สวิตช์ฟีเจอร์ต่อรอบใน `lib/featureflags.js` |
| Phase 1–9 | ครบและอยู่บน production — ของที่ Phase 10 ต่อยอด: `leads.js` (CRM) · `savedsearch.js` (แจ้งเตือนแปลง) · `kb.js` (คลังความรู้มีขั้นตอนตรวจ/เผยแพร่) · สถิติภายใน `POST /api/public/track` |

**กติกาที่แผนนี้ต้องไม่ละเมิด** (จากเจ้าของและจาก `CLAUDE.md` ของทั้งสอง repo)
- ห้าม Deploy Production จนกว่าจะสั่ง · ห้ามเชื่อมบริการภายนอกหรือ API ที่มีค่าใช้จ่ายโดยไม่อนุมัติ
- ห้ามสร้างข้อมูลกฎหมาย ราคา หรือผลตรวจสอบที่ไม่มีแหล่งอ้างอิง · **ห้ามมีข้อมูลแปลงสมมติในโค้ด**
- ห้ามเผยเลขโฉนด ชื่อเจ้าของ หรือข้อมูลส่วนบุคคล · ห้ามส่ง PII เข้าระบบสถิติ
- ห้ามติดตั้ง Tracking Script เพิ่มโดยไม่อนุมัติ · รักษาฟังก์ชัน Phase 1–9 ครบ

---

## 2. ปัญหาที่พบ (ย่อจาก audit)

**Critical** C1 ระบบหลังบ้านถูกเก็บดัชนีได้ + soft 404 ไม่จำกัด · C2 หน้าแปลงส่ง HTML เปล่า · C3 ทุกหน้าแปลงใช้ title/description/og ชุดเดียวกัน
**High** ไม่มีหน้าจังหวัด/อำเภอ · หน้ากำพร้า 2 หน้า · ไม่มี sitemap ประกาศ · ไม่มี Search Console/GA4 · ตั้ง Security Header และ 301/410 ที่โฮสต์ปัจจุบันไม่ได้ · metadata hard-code 20 ไฟล์ · ไม่มี CMS บทความ · ลีดไม่เก็บที่มา (UTM)
**Medium/Low** 12 + 6 ข้อ — ดูตารางในไฟล์ audit

---

## 3. การตัดสินใจเชิงสถาปัตยกรรม (ตัวเลือกและเหตุผลที่เลือก)

### 3.1 หน้าเว็บที่เสิร์ชเอนจินอ่านได้ — เลือก "สร้างไฟล์ HTML จริงด้วยสคริปต์" (prerender)

| ทางเลือก | ผล | ตัดสิน |
|---|---|---|
| **ก. สคริปต์ Node ใน repo เว็บ ดึง API แล้วเขียนไฟล์ HTML จริง แล้ว commit** | ไม่มีค่าใช้จ่าย · ไม่ต้องย้ายโฮสต์ · ไม่มี dependency ใหม่ · ตรวจผลก่อนขึ้นได้ (เห็นใน diff) | ✅ **เลือกทางนี้** |
| ข. ให้ Express ที่ `app.njteedinsure.com` เรนเดอร์หน้าสาธารณะแทน | ต้องย้ายโดเมนหลักไปเซิร์ฟเวอร์ที่มีค่าใช้จ่าย · กระทบ Phase 1–9 · เสี่ยงสูงสุด | ❌ |
| ค. Cloudflare Worker / บริการ prerender | บริการภายนอก + อาจมีค่าใช้จ่าย → ต้องอนุมัติ | ❌ ไม่เสนอ |

**หลักการสำคัญ**: หน้า prerender เก็บ "ภาพนิ่ง ณ เวลาสร้าง" ไว้ใน HTML เพื่อให้บอตและ LINE/Facebook อ่านได้
แต่ **JS เดิมยังดึงข้อมูลสดมาทับเสมอเมื่อผู้ใช้จริงเปิดหน้า** — ผู้ใช้จึงไม่มีทางเห็นราคาเก่า
และถ้าแปลงถูกถอดออกจาก API แล้ว หน้าจะแสดงว่า "ประกาศนี้ปิดแล้ว" พร้อมแปลงใกล้เคียง ไม่ใช่ข้อมูลค้าง

### 3.2 รูปแบบ URL — เลือกใช้ภาษาไทยใน URL

ข้อกำหนดงานที่ 4 ให้ใช้ `/properties/{property-type}/{province}/{district}/{slug}` และ "สร้าง Slug แบบรองรับภาษาไทยหรือภาษาอังกฤษตามระบบเดิม"

- ระบบเดิมเก็บชื่อจังหวัด/อำเภอ/ตำบล **เป็นภาษาไทยเท่านั้น** (`data/thai-admin.json` มีแค่ `name_th` + รหัสไปรษณีย์)
- การแปลงเป็นอักษรโรมันเองโดยไม่มีแหล่งอ้างอิง = สร้างข้อมูลที่ไม่มีที่มา ซึ่งกติกาห้ามไว้
- → **เลือก slug ภาษาไทย** เช่น `/properties/ที่ดิน/สมุทรปราการ/พระสมุทรเจดีย์/OP-072/`
  Google รองรับ URL ยูนิโคดเต็มรูปแบบ และลูกค้าคนไทยอ่านออกทันที
- ถ้าเจ้าของอยากได้ URL อักษรอังกฤษ: ทำได้โดยดึงชื่ออังกฤษจากชุดข้อมูลเดิม (`kongvut/thai-province-data` มีช่อง `name_en`) แล้วสร้าง `thai-admin.json` ใหม่ — **บอกได้ก่อนเริ่มสปรินต์ 3 จะเปลี่ยนให้**

### 3.3 URL เดิมห้ามพัง (ข้อกำหนดข้อ 3)

- `land.html?id=OP-072` **ยังทำงานตลอดไป** ไม่ลบ ไม่ปิด — ลิงก์ที่ทีมส่งในไลน์ไปแล้วนับพันลิงก์ต้องเปิดได้เหมือนเดิม
- หน้านั้นจะได้ `<link rel="canonical">` ชี้ไปยัง URL ใหม่ + พาผู้ใช้ไปหน้าใหม่ด้วย `location.replace` (โฮสต์ปัจจุบันทำ 301 จริงไม่ได้ — ดู H6)
- ตารางจับคู่ URL เก่า→ใหม่ทั้งหมดจะอยู่ในไฟล์ `redirects.json` และมีเทสต์คุมว่าทุก URL เก่าต้องมีปลายทาง

### 3.4 ทะเบียน SEO กลาง (งานที่ 2) อยู่ที่ระบบหลังบ้าน

เพราะข้อกำหนดสั่งว่า "ข้อมูลทั้งหมดต้องแก้ไขจาก Admin ได้" และเว็บสาธารณะไม่มีฐานข้อมูล
→ เก็บใน DB ของระบบหลังบ้าน · แก้จากหน้าแอดมิน · เปิดออกทาง `GET /api/public/seo/pages` (ไม่มี PII) · สคริปต์ build ของเว็บอ่านค่าจากตรงนั้นมาเขียนลงไฟล์

---

## 4. แผนรายสปรินต์ (แบ่ง commit ตามสปรินต์ตามที่สั่ง)

> ทุกสปรินต์: รันชุดทดสอบทั้งสอง repo ให้ผ่านครบก่อน commit → รายงานผล → **รอคำสั่งก่อน merge/deploy**

### Sprint 1 — หยุดเลือดก่อน + วางเครื่องมือวัด
| งาน | ที่ไหน | หมายเหตุ |
|---|---|---|
| แก้ C1: `/robots.txt` จริง + `X-Robots-Tag: noindex` + `meta robots` + 404 จริง | `nj-survey-system` | **Critical — ทำก่อนทุกอย่าง** |
| เพิ่ม `404.html` ของเราเอง (M9) | เว็บ | ไม่ต้องย้ายโฮสต์ |
| เขียนตัวไต่ลิงก์ในเว็บ (Node ล้วน ไม่มี dependency) → รายงานลิงก์เสีย + หน้ากำพร้า | เว็บ `build/linkcheck.js` | ปิดช่องว่างข้อ "Broken Links" ของงานที่ 1 |
| ล้าง `sitemap.xml`: ตัด `priority`/`changefreq` · `lastmod` จากวันที่ commit จริง (M5) | เว็บ | |
| เตรียมไฟล์ยืนยันสิทธิ์ Search Console + ช่อง GA4 ให้พร้อมเปิด | เว็บ | **เจ้าของเป็นคนกดยืนยัน** (Q2/Q3) |
| บันทึกเส้นฐานสถิติภายในปัจจุบัน (ลีด/เหตุการณ์ 30 วันล่าสุด) | เอกสาร | ใช้เทียบผลตอนจบ Phase 10 |

### Sprint 2 — ทะเบียน SEO กลาง + Metadata + Structured Data
- `lib/seo.js` (ตรรกะล้วน): ตรวจความถูกต้องของช่อง SEO ทั้ง 13 ช่องที่สั่ง + คิด `seo_score` จากกฎที่เขียนไว้ชัด (ความยาว title/description, มี canonical, มีรูป OG, มี schema) — **ไม่ใช่คะแนนลอยๆ**
- เก็บใน DB ใหม่ `seoPages` + API แอดมิน + หน้าจอ "SEO และการตลาด" ในระบบหลังบ้าน (สิทธิ์ใหม่ `marketing`)
- `GET /api/public/seo/pages` (สาธารณะ อ่านอย่างเดียว)
- ฝั่งเว็บ: `build/seo.js` เขียน `<title>/description/canonical/og/twitter` ลงไฟล์ HTML ทั้ง 20 หน้าจากทะเบียน (แทนการแก้มือ 20 จุด) + เติม `twitter:card`/`og:url` ที่ขาด (M1, M2)
- JSON-LD: `Organization`/`ProfessionalService`, `WebSite`, `BreadcrumbList` ทุกหน้า, `FAQPage` เฉพาะ 5 หน้าที่มีคำถามจริง, `VideoObject` จากคลิปที่มีจริง (M3, M4, L3, L4)
- **Database migration ครั้งที่ 1** (ดูหัวข้อ 5)

### Sprint 3 — หน้าประกาศทรัพย์ + หน้าจังหวัด/อำเภอ
- `build/prerender.js`: สร้าง `/properties/…/` ต่อแปลงจาก API จริง (แก้ C2, C3)
  ครบตามงานที่ 6: title/description อัตโนมัติและแก้เองได้ · canonical · breadcrumb · จังหวัด-อำเภอ-ตำบล · ราคาและราคาต่อ ตร.ว. (จากเซิร์ฟเวอร์ ห้ามหารเอง) · เอกสารสิทธิ · เนื้อที่ · แผนที่แบบเคารพความเป็นส่วนตัว (หมุดที่ทีมกดเลือกเท่านั้น) · รูปพร้อม alt · แปลงใกล้เคียง · บริการที่เกี่ยวข้อง · วันเผยแพร่/อัปเดต · สถานะ
- สถานะ Sold/Expired: **ค่าเริ่มต้น = คงหน้าไว้ + ป้ายสถานะชัดเจน + แนะนำแปลงใกล้เคียง** (ทางที่ข้อกำหนดอนุญาตและโฮสต์ปัจจุบันทำได้) · ทางเลือก redirect/410 ขึ้นกับคำตอบ Q1
- `sitemaps/` ครบ 6 ไฟล์ + sitemap index (งานที่ 3) — `lastmod` จาก `updatedAt` จริง
- หน้าจังหวัด 6 จังหวัด + อำเภอที่มีของจริง — เนื้อหาจริงต่อพื้นที่ ไม่ใช่เปลี่ยนชื่อจังหวัด · **ต้องให้แอดมินกดเผยแพร่ก่อน** ตามข้อกำหนด
- allowlist ตัวกรองที่ให้เก็บดัชนี · ตัวกรองอื่น `noindex, follow` + canonical กลับหน้าหลัก

### Sprint 4 — Knowledge CMS + Internal Linking
- ต่อยอด `lib/kb.js` เดิม (มีขั้นตอน draft→reviewed→published→archived + เวอร์ชัน + ผู้ตรวจ + วันทบทวนอยู่แล้ว) ด้วยช่องใหม่สำหรับเผยแพร่บนเว็บ: slug · หมวด (7 เสาเนื้อหาตามที่สั่ง) · แท็ก · ผู้เขียน/ผู้ตรวจ + คุณวุฒิ · แหล่งอ้างอิง · ภาพหน้าปก + alt · สารบัญ · บทความ/บริการ/แปลงที่เกี่ยวข้อง · FAQ · ตั้งเวลาเผยแพร่
- **ไม่สร้างระบบ CMS ที่สอง** — ของเดิมมีประวัติและการตรวจอยู่แล้ว สร้างใหม่ = มีสองที่ให้ลืมแก้
- ระบบลิงก์ภายใน + รายงานหน้ากำพร้า + แอดมินอนุมัติลิงก์ที่ระบบแนะนำ (งานที่ 9) · แก้ H2 ไปในตัว

### Sprint 5 — เครื่องมือเก็บลีด + ที่มาของลีด
- เครื่องมือที่ **มีอยู่แล้ว** และแค่ต้องเชื่อม/เปิดออกหน้าเว็บ: เครื่องคำนวณค่าโอน · ค่างวดสินเชื่อ · ประเมินค่ารังวัด · เปรียบเทียบแปลง · บันทึกแปลง · Saved Search + แจ้งแปลงใหม่/ราคาลด (`lib/savedsearch.js` ทำไว้ครบแล้วใน Phase 7)
- ของใหม่: แบบประเมินทรัพย์ 10 นาที · เช็กลิสต์ตรวจที่ดินก่อนซื้อ · ฟอร์มขอวิเคราะห์ราคาขาย · ดาวน์โหลดตัวอย่างรายงาน · รับข่าวสาร
- **ที่มาของลีด 11 ช่อง** (`source` `medium` `campaign` `content` `term` `landing_page` `referrer` `first_touch` `last_touch` `conversion_event` `consent_status`) เข้า `lib/leads.js` + เชื่อม CRM เดิม (แก้ H9) — **Database migration ครั้งที่ 2**
- ⚠️ `landing_page` และ `referrer` เก็บเฉพาะเส้นทางของเว็บเราและโดเมนผู้อ้างอิง **ตัด query string ที่อาจมีตั๋วหรือข้อมูลส่วนตัวทิ้งเสมอ**

### Sprint 6 — แดชบอร์ดการตลาด · ประสิทธิภาพ · ทดสอบ · คู่มือ
- แดชบอร์ดตามงานที่ 12 — ตัวเลขที่มาจากระบบเราเองแสดงได้ทันที · ตัวเลขฝั่ง Search แสดงเมื่อเชื่อม Search Console แล้ว · **ช่องที่ยังไม่มีข้อมูลต้องขึ้นว่า "ยังไม่ได้เชื่อม" ห้ามแสดงเลข 0 ให้เข้าใจผิด**
- Export CSV/Excel + เลือกช่วงวันที่
- ประสิทธิภาพ (งานที่ 13): วัดก่อน–หลัง บันทึกใน `PHASE10_PERFORMANCE_REPORT.md` · แตะ M7/M8/M11/M12 · **ห้ามซ่อนเนื้อหาเพื่อทำคะแนน**
- ชุดทดสอบครบ 14 ชนิดตามงานที่ 15 + คู่มือ 3 ฉบับที่สั่ง

---

## 5. แผน Database Migration (ข้อกำหนดข้อ 5 และ 6)

ฐานข้อมูลเป็น JSON blob ก้อนเดียว → การเพิ่มของใหม่เป็นการ **เพิ่มคีย์ ไม่ใช่แปลงข้อมูลเดิม** ซึ่งเป็นรูปแบบที่ปลอดภัยที่สุดที่ระบบนี้ใช้มาตลอด

**ก่อนลงมือทุกครั้ง (ทั้ง 2 ครั้ง)**
1. สั่งสำรองจากหน้าแอดมิน (ระบบสำรองข้อมูลของ Phase 9) แล้ว **บันทึกรหัสไฟล์สำรองลงในรายงานรอบนั้น**
2. ทดลองกู้คืนลง `DATA_DIR` ชั่วคราว **ห้ามกู้คืนทับฐานข้อมูลจริงเด็ดขาด** (กติกา Phase 9)
3. รัน migration บนสำเนา → รันชุดทดสอบ → ค่อยขึ้นจริง

| ครั้ง | สปรินต์ | เพิ่มอะไร | ย้อนกลับอย่างไร |
|---|---|---|---|
| 1 | 2 | `seoPages[]` (ทะเบียน SEO) · `meta.featureFlags.seo_center` | ลบคีย์ทิ้ง — ไม่มีของเดิมถูกแตะ |
| 2 | 5 | `leads[].attr{}` (ที่มา 11 ช่อง) · `kb[].web{}` (ช่องเผยแพร่เว็บ) | ช่องใหม่ทั้งหมดเป็น optional · โค้ดเดิมอ่านข้าม |

**ลีดเก่าจะไม่ถูกเติมที่มาย้อนหลัง** — เราไม่รู้ว่ามาจากไหนจริง การเดาแล้วใส่ = ตัวเลขการตลาดที่ผิดแต่ดูน่าเชื่อ
ลีดก่อนวันเปิดใช้จะแสดงว่า "ไม่ทราบที่มา (ก่อน Phase 10)"

**Seed data**: มีเฉพาะสิ่งที่ไม่ใช่ข้อมูลธุรกิจ — โครงหมวดบทความ 7 เสา และแม่แบบ SEO ต่อชนิดหน้า
**ไม่มี** บทความตัวอย่าง ไม่มีแปลงสมมติ ไม่มีจังหวัดสมมติ

---

## 6. รายชื่อไฟล์ที่จะกระทบ (ประเมินก่อนเริ่ม)

### `nj-land-listings` (เว็บสาธารณะ)
**สร้างใหม่** `404.html` · `build/seo.js` · `build/prerender.js` · `build/sitemap.js` · `build/linkcheck.js` · `build/slug.js` · `redirects.json` ·
`properties/**` (หน้าแปลง prerender) · `locations/**` (หน้าจังหวัด/อำเภอ) · `knowledge/**` (บทความ) · `sitemaps/*.xml` ·
`seo.test.js` `sitemap.test.js` `schema.test.js` `redirect.test.js` `prerender.test.js`
**แก้** `sitemap.xml` · `robots.txt` · HTML ทั้ง 20 หน้า (เฉพาะส่วน head ที่สคริปต์เขียนให้) · `land.js` · `listingcard.js` · `listings.js` · `analytics.js` · `CLAUDE.md`

### `nj-survey-system` (ระบบหลังบ้าน)
**สร้างใหม่** `lib/seo.js` · `lib/seocontent.js` · `lib/attribution.js` · `lib/marketingdash.js` + เทสต์คู่ละ 1 ไฟล์ (ตรรกะ + API)
**แก้** `server.js` (robots/noindex/404, API SEO, API แดชบอร์ด) · `public/index.html` (meta robots) · `public/app.js` (หน้าจอการตลาด) · `lib/auth.js` (`marketing`) · `lib/featureflags.js` (สวิตช์ Phase 10) · `lib/leads.js` (ที่มา) · `lib/kb.js` (ช่องเผยแพร่เว็บ) · `public/sw.js` (เลขเวอร์ชันทุกครั้งที่แตะหน้าจอ) · `package.json` (ขึ้นทะเบียนเทสต์) · `CLAUDE.md`

**รวมประมาณ**: ไฟล์ใหม่ ~25 · ไฟล์ที่แก้ ~35 · กระจายใน 6 commit ตามสปรินต์

---

## 7. สิ่งที่ต้องได้รับอนุมัติก่อน (มีค่าใช้จ่ายหรือส่งข้อมูลออกนอกบริษัท)

| เรื่อง | สถานะ | ค่าใช้จ่าย |
|---|---|---|
| Google Search Console | ต้องเจ้าของยืนยันสิทธิ์โดเมน | ฟรี |
| Google Analytics 4 | ต้องอนุมัติ (สคริปต์ภายนอก + ส่งข้อมูลพฤติกรรมออกนอกบริษัท) | ฟรี |
| Google Tag Manager | **เสนอให้ข้าม** — เพิ่มชั้นที่ทำให้แก้สคริปต์ได้จากนอก repo ซึ่งขัดกับวิธีทำงานของโปรเจกต์นี้ | ฟรี |
| Meta Conversion API | ต้องอนุมัติ + ต้องมีเซิร์ฟเวอร์รับ (มีอยู่แล้ว) · **ส่ง PII ที่ถูกแฮชไปเมตา → ต้องพิจารณา PDPA ก่อน** | ฟรี (แต่กระทบ PDPA) |
| ย้ายเว็บสาธารณะออกจาก GitHub Pages | รอคำตอบ Q1 | Render Static Site = ฟรี · แบบมี header/redirect ครบอาจมีค่าใช้จ่าย |
| Lighthouse ในเครื่อง | รอคำตอบ Q4 | ฟรี (แต่เป็น dependency ก้อนใหญ่) |
| เครื่องมือหาคำค้น (Ahrefs/Semrush ฯลฯ) | **ไม่เสนอ** | มีค่าใช้จ่ายสูง |

---

## 8. สิ่งที่แผนนี้จะ "ไม่ทำ" และเหตุผล

1. **ไม่สร้างคะแนนหรือรีวิวปลอมใน Structured Data** — ข้อกำหนดห้าม และ Google ตัดสิทธิ์ทั้งเว็บ
2. **ไม่สร้างหน้าจังหวัดที่เปลี่ยนแค่ชื่อ** — จังหวัดที่ยังไม่มีเนื้อหาจริงจะยังไม่เผยแพร่ แม้อยู่ในรายชื่อ 6 จังหวัด
3. **ไม่เดาข้อมูลสำนักงานที่ดิน ค่าธรรมเนียม หรือข้อกฎหมาย** ที่ไม่มีแหล่งอ้างอิง (Q5)
4. **ไม่ส่งชื่อ เบอร์ เลขโฉนด เลขบัตร หรือ ID แปลงที่ผูกกับบุคคล เข้าระบบสถิติใดๆ**
5. **ไม่เปลี่ยน URL เดิม** — ของเดิมอยู่ครบ มีแต่เพิ่ม URL ใหม่ที่อ่านเข้าใจง่ายขึ้น
6. **ไม่แตะประตูกัน PDPA ของ `/api/public/listings`** — แปลงที่ยังไม่ได้รับความยินยอมจะไม่มีวันขึ้น sitemap

---

## 8b. สปรินต์ 1 — ทำแล้ว (18 กันยายน 2569)

เจ้าของกิจการสั่ง "ทำตามแนะนำ" จึงเริ่มด้วยค่าตั้งต้นที่ปลอดภัยของทุกคำถาม:
**อยู่บน GitHub Pages ต่อ (Q1)** · เตรียม Search Console ให้เจ้าของกดเอง (Q2) · **ยังไม่เปิด GA4 · ไม่ใช้ GTM** (Q3) ·
**ไม่ติดตั้ง Lighthouse** ใช้ PageSpeed Insights แทน (Q4) · ไม่เขียนข้อมูลสำนักงานที่ดินที่ไม่มีแหล่ง (Q5) ·
หน้าจังหวัดยืนด้วยเนื้อหาบริการ ไม่ใช่จำนวนแปลง (Q6)

| งาน | ผล |
|---|---|
| **C1 · ระบบหลังบ้านห้ามขึ้นผลค้นหา** | `GET /robots.txt` จริง · หัว `X-Robots-Tag: noindex, nofollow` ทุกคำขอ **ยกเว้น `/uploads`** (รูปประกาศต้องขึ้นการค้นรูปได้ตามเดิม) · `<meta name="robots">` ใน `public/index.html` · ขอไฟล์ที่ไม่มีจริง (`/wp-login.php` `/.env`) ได้ **404** แทน 200 |
| เทสต์ใหม่ | `test/p10-robots-api.test.js` (28 ข้อ · พอร์ต 8904) — รวมข้อที่ล็อกว่า `/uploads` ต้องไม่ถูก noindex และ `pricing.js`/`buildingprice.json` ยังเปิดข้ามโดเมนได้ |
| **หน้า 404 ของเราเอง** | `404.html` (noindex, follow) พร้อมทางไปต่อ 8 หน้า — GitHub Pages ใช้ไฟล์นี้เอง ไม่ต้องย้ายโฮสต์ |
| **ตัวไต่ลิงก์** | `build/linkcheck.js` (Node ล้วน ไม่มี dependency) — ลิงก์เสีย · จุดยึดที่ JS สร้าง · หน้ากำพร้า |
| **🐞 ลิงก์เสียที่เจอและแก้แล้ว** | `index.html#team` **ไม่มีจุดยึดนี้อยู่จริง** (หน้าแรกใช้ `id="ทีมเรา"`) — ใครกด "ทีมเรา" จากหน้าใน จะไปโผล่หัวหน้าแรกแทนส่วนทีมงาน · **แก้ 18 ลิงก์ใน 11 ไฟล์** |
| **สร้าง sitemap.xml จากของจริง** | `build/sitemap.js` — `lastmod` จากวันที่ commit จริง (ไฟล์ที่แก้แล้วยังไม่ commit ใช้วันนี้) · **ตัด `priority`/`changefreq` ทิ้งตามข้อกำหนด** · หน้า noindex หายเองโดยไม่ต้องมีรายชื่อ |
| **เส้นฐานประสิทธิภาพ** | `PHASE10_PERFORMANCE_REPORT.md` — วัดน้ำหนักจริงทุกหน้าหลัก · Core Web Vitals ยังว่างพร้อมวิธีเติม |
| **คู่มือขึ้นระบบ** | `PHASE10_DEPLOYMENT_GUIDE.md` — ลำดับ deploy สองส่วน · คำสั่งตรวจหลังขึ้น · ขั้นตอน Search Console |

**ที่ยังไม่แก้ในสปรินต์นี้โดยตั้งใจ**
- หน้ากำพร้า 3 หน้า (`partner-apply.html` ไม่มีลิงก์เข้าเลย · `inspect.html`/`land.html` มีแต่ลิงก์จาก JS) — เป็นงานที่ 9 ในสปรินต์ 4
- จุดยึด `videos.html#clip1..4` สร้างด้วย JS — ผู้ใช้กดได้ บอตรอบแรกไม่เห็น · แก้พร้อมงานหน้าวิดีโอ
- ทุกอย่างของสปรินต์ 2 ขึ้นไป (ทะเบียน SEO · หน้าแปลง prerender · หน้าจังหวัด)

## 9. ขั้นตอนถัดไป

1. เจ้าของตอบคำถาม 6 ข้อ (หัวข้อ 8 ของ `PHASE10_SEO_AUDIT.md`) — อย่างน้อย **Q1 และ Q6** ต้องตอบก่อนเริ่มสปรินต์ 1
2. อนุมัติแผนนี้ (หรือสั่งตัด/เพิ่มสปรินต์)
3. เริ่มสปรินต์ 1 → รันเทสต์ → รายงาน → รอคำสั่งก่อน commit/merge ทุกรอบ
