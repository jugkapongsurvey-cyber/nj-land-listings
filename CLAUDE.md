# Claude Code Guide — ที่ดินชัวร์

## เป้าหมายผลิตภัณฑ์

เว็บตลาดซื้อขายที่ดินของบริษัท เอ็นเจ แอนด์ คอนซัลติ้ง จำกัด จุดต่างหลักคือข้อมูลแนวเขตจากงานรังวัดจริง ไม่ใช่เว็บรวมประกาศทั่วไป

## โครงสร้างสำคัญ

- `index.html`, `home.css` หน้าแรก — โทนน้ำเงินเข้ม–ทอง (Sarabun + Trirong)
  · **หน้าแรกไม่โหลด `marketplace.css`** คนละชุดสีกัน โหลดคู่กันแล้วฟอนต์และพื้นหลังตีกัน
    สไตล์การ์ดแปลงที่ดินถูกเขียนซ้ำไว้ใน `home.css` แทน — แก้หน้าตาการ์ดต้องแก้ที่นี่ ไม่ใช่ `marketplace.css`
  · โครงหน้าเป็น inline style ตามที่ดีไซน์ส่งมา · `home.css` เก็บเฉพาะ base, media query,
    คลาส hover และคอมโพเนนต์ที่ดีไซน์ไม่ได้ให้มา
  · **คลาส `.hv-*` ต้องใช้ `!important`** เพราะ inline style ชนะ class เสมอ — ถอด `!important` ออกเมื่อไหร่ hover ตายทันที
  · **โรงฉายวิดีโอ `.nj-stage` ถูกถอดออกแล้ว 2026-09-07** — เจ้าของสั่งเอาภาพโมชั่นกราฟิก 3D ออก
    แล้วใช้คลิปพรีเซนเตอร์แทนในตำแหน่งเดิม (ระหว่างเมนูค้นหาด่วนกับ `#listings`)
    · HTML กลายเป็นแถบ `.nj-intro` ตัวที่สอง (`#ตรวจก่อนโอน`) ดูหัวข้อ `njintro.js` ข้างล่าง
    · ตัวควบคุม IntersectionObserver ท้าย `surveyquote.js` ถูกลบทิ้งพร้อมกัน (ไม่มี element ให้คุมแล้ว)
    · **CSS `.nj-stage*` ยังอยู่ท้าย `home.css`** ตั้งใจไม่ลบ เผื่ออยากได้แถบคลิปเงียบวนลูปกลับมา
      ไฟล์ `video/njsurvey.mp4` + โปสเตอร์ก็ยังอยู่ใน repo เหมือนกัน
- `njintro.js` + แถบ `.nj-intro` **คลิปพรีเซนเตอร์แนวตั้ง 9:16 — ตอนนี้มี 2 แถบ** (2026-09-07)
  · `#ตรวจก่อนโอน` โฆษณาที่ดินชัวร์ 30 วิ (`video/teedinsure-ad.mp4` ~4 MB) — บนสุด แทนที่ `.nj-stage` เดิม
  · `#แนะนำระบบ` แนะนำระบบทำงาน 1:18 (`video/nj-intro.mp4` ~11 MB) — คั่นระหว่าง "บริการ" กับ "ขั้นตอน"
  · **`njintro.js` วนทุก `.nj-intro` ไม่ผูก id เดี่ยวแล้ว** เพิ่มแถบที่สามก็แค่เขียน HTML ตามโครงเดิม
    หาปุ่ม/วิดีโอ "ในแถบของตัวเอง" ด้วย `sec.querySelector` — ห้ามกลับไปใช้ `getElementById`
  · **กดเล่นแถบหนึ่ง ต้องหยุดอีกแถบ** ไม่งั้นเสียงพูดสองคลิปทับกัน (คลิปทั้งคู่มีเสียง)
  · **คลิปมีเสียงพูดทั้งคู่ จึงห้ามเล่นเอง**
    (`preload="none"` ด้วย — ไฟล์ 11 MB เล่นเองคือกินเน็ตมือถือลูกค้าฟรีๆ)
  · **HTML ใส่ `controls` ไว้ตั้งแต่แรก แล้ว `njintro.js` เป็นคนถอดออก ห้ามทำกลับด้าน**
    ไฟล์ JS โหลดไม่สำเร็จเมื่อไหร่ ต้องเหลือวิดีโอที่กดเล่นได้ตามปกติ ไม่ใช่ภาพนิ่งที่กดอะไรไม่ได้เลย
    · ปุ่มเล่นที่เราวาดเองก็ซ่อนไว้ใน CSS จนกว่า JS จะใส่คลาส `.js-ready` ให้ ด้วยเหตุผลเดียวกัน
    · **สองสถานะห้ามซ้อนกัน**: ยังไม่เล่น = โปสเตอร์ + ปุ่มของเรา · เล่นแล้ว = แถบควบคุมของเบราว์เซอร์
      มีพร้อมกันเมื่อไหร่ แถบควบคุมจะทับปุ่มมุมล่างจนอ่านไม่ออก (เจอจริงตอนพรีวิว)
  · ⚠️ **`.nj-intro-frame video` ต้องมี `height:auto`** — แอตทริบิวต์ `height="1920"` ใน HTML
    กลายเป็น `height:1920px` ซึ่งชนะ `aspect-ratio` แล้วจอวิดีโอยืดเป็นแถบเปล่ายาวทั้งหน้า
  · ⚠️ `@media (max-width:900px)` ของแถบนี้ต้องมี `padding-left:0 !important` เพื่อล้มกฎรวม
    `section{padding:18px !important}` ที่ 640px ไม่งั้นคลิปเยื้องไปทางขวาบนมือถือ
- **ปุ่ม "ดูคลิปแนะนำระบบ" ที่ hero** (`[data-nj-intro-play]` ใน `index.html` · ตัวจัดการอยู่ใน `njintro.js`)
  · ทำเป็น**ลิงก์เปล่า + วงกลมทองมีสามเหลี่ยมเล่น ไม่ใช่ปุ่มกรอบเต็มใบ** — สองปุ่มแรกในแถวนั้น
    (ฝากขาย / คู่มือ) เป็น CTA หลัก ถ้าปุ่มที่สามหน้าตาเท่ากัน แถวจะกลายเป็นสามทางเลือกที่ดูสำคัญ
    เท่ากันหมด แล้วปุ่มฝากขายซึ่งเป็นเป้าหมายจริงของหน้าจะจม
  · **ต้องเรียก `start()` ในจังหวะคลิกทันที ห้ามรอให้เลื่อนจอถึงแถบคลิปก่อน** — นโยบายสื่อของเบราว์เซอร์
    อนุญาตให้เล่นคลิปที่มีเสียงเฉพาะตอนที่ผู้ใช้เพิ่งกดเท่านั้น หน่วงเมื่อไหร่ `play()` ถูกปฏิเสธ
    แล้วผู้ใช้จะเลื่อนไปเจอจอนิ่งที่ต้องกดซ้ำอีกที
  · **ไม่ `preventDefault`** ปล่อยให้ `href="#แนะนำระบบ"` พาไปตามปกติ — `njintro.js` ไม่ทำงานเมื่อไหร่
    ลิงก์ยังต้องพาไปดูคลิปได้เหมือนลิงก์ธรรมดา
  · ผูกด้วย `querySelectorAll('[data-nj-intro-play]')` ไม่ใช่ id — จะเพิ่มปุ่มดูคลิปที่จุดอื่นของเว็บ
    ก็แค่ติดแอตทริบิวต์นี้ ไม่ต้องแก้ JS
  · **ค่าของแอตทริบิวต์ = id ของแถบที่จะให้เล่น** (2026-09-07) · ไม่ใส่ค่า = `#แนะนำระบบ` ตามเดิม
    ปุ่มที่ hero จึงยังเล่นคลิปแนะนำระบบเหมือนเดิม ไม่ใช่คลิปโฆษณาที่มาใหม่
- `marketplace.css` design system ของ `guides.html` `land.html` `videos.html` (หน้าแรกไม่ใช้แล้ว)
- `marketplace.js` โหลดประกาศ ค้นหา กรอง และบันทึกรายการบนหน้าจอ
  · **ผูกกับ id เหล่านี้แบบไม่เช็ก null**: `search-form` `search-input` `type-filter` `price-filter`
    `listing-grid` `result-note` `listings` `year` + `[data-purpose]` `[data-quick]`
    ขาดตัวใดตัวหนึ่งคือ JS ตายทั้งไฟล์ ประกาศไม่ขึ้นเลย — จัดหน้าแรกใหม่ต้องเก็บครบทุกตัว
- `listingcard.js` **ตัวเรนเดอร์การ์ดกลาง** (`window.NJListing`) — `card()` `normalize()` `emptyHtml()`
  `loadFailedHtml()` `fetchListings()` `bindGrid()` · ใช้ร่วมกันทั้งหน้าแรกและหน้ารวมประกาศ
  · **ต้องโหลดก่อน `marketplace.js` / `listings.js` เสมอ** (ทั้งสองไฟล์อ่าน `window.NJListing` ตอนเริ่มทำงานทันที)
  · **ห้ามก๊อป `card()` กลับไปไว้ในไฟล์ของหน้าใดหน้าหนึ่ง** — กติกาข้อ 4/5 จะกลายเป็นมีสองที่ให้ลืมแก้
  · ⚠️ สไตล์การ์ดยังแยกสองชุดตามชุดสี (`home.css` หน้าแรก · `marketplace.css` หน้าอื่น)
    เพิ่มคลาสใหม่ต้องเพิ่ม**ทั้งสองไฟล์** ใส่ที่เดียว = อีกหน้าไม่มีสไตล์โดยไม่มี error
- `listings.html`, `listings.js`, `listings.css` **หน้ารวมประกาศ + ตัวกรอง** (เพิ่ม 2026-09-04)
  · เหตุผลที่ต้องมี: หน้าแรกมีแค่ช่องค้นหา + กรองราคาชั้นเดียว และเป็นหน้าขายแบรนด์ ไม่ใช่หน้าไล่ดูของ
    เวลายิงโฆษณาหาผู้ซื้อจึงไม่มีหน้าปลายทางให้ส่งคนไปลง
  · กรองได้: คำค้น · ประเภท · จังหวัด · ช่วงราคา · ช่วงเนื้อที่ · เอกสารสิทธิ์ · ผังสี · สิ่งที่แปลงมี
    เรียงได้: อัปเดตล่าสุด · ราคารวม · ราคาต่อ ตร.ว. · เนื้อที่
  · **รายชื่อจังหวัดสร้างจากแปลงที่มีอยู่จริง ไม่ใช่รายชื่อ 77 จังหวัด** — เลือกจังหวัดที่ไม่มีของ
    แล้วเจอผลว่างเปล่าจะดูเหมือนเว็บพัง
  · ⚠️ **กติกาที่ห้ามผ่อน — "ไม่ตรงเงื่อนไข" กับ "ยังไม่ได้ระบุ" ต้องแยกกัน**
    แปลงที่ยังไม่ได้กรอกช่องที่กำลังกรอง จะไม่ถูกนับว่าตรง (บอกว่าตรงทั้งที่ไม่รู้ = โกหกผู้ซื้อ)
    **แต่ต้องขึ้นข้อความบอกจำนวนที่ถูกซ่อนด้วยเหตุนี้เสมอ** (`#unknown-note`)
    ไม่งั้นแปลงหายเงียบ ผู้ซื้อคิดว่าไม่มีของ และทีมงานก็ไม่รู้ว่าต้องไปกรอกอะไรเพิ่ม
  · ค่าใน dropdown ต้องตรงกับ `LAND_DEEDS` / `LAND_ZONES` / `PARCEL_FEATURES` ใน `server.js` เป๊ะ
    ไม่ตรง = กรองไม่เจออะไรเลยทั้งที่ข้อมูลมีอยู่
  · **"น.ส.3ก" ในตัวกรองหมายถึง "น.ส.3ก ขึ้นไป"** — โฉนดนับว่าตรงด้วย (กติกาเดียวกับฝั่ง server)
- `consign.html`, `consign.js`, `ui.css` หน้าฝากขายเดิม ห้ามแก้โดยไม่ทดสอบฟอร์ม
- **กล่อง "รังวัดยืนยันเขตก่อนประกาศขาย" ในฟอร์มฝากขาย** (`#cs-survey` · `setupSurvey()` · เพิ่ม 2026-09-06)
  · เกาะช่องที่มีอยู่แล้ว 2 ช่อง ไม่ได้เพิ่มช่องให้กรอกใหม่: **จังหวัด** → ตัดสินว่าบังคับรังวัดไหม ·
    **เนื้อที่** → ยิงเข้าตารางราคางานรังวัดจริง ได้ค่ารังวัดประมาณการทันทีขณะพิมพ์
  · ⚠️ **บังคับเฉพาะ กทม. + ปริมณฑล 5 จังหวัด** (`SURVEY_REQUIRED_PROVINCES`) —
    **ต้องตรงกับรายชื่อชื่อเดียวกันใน `server.js` เป๊ะ** ไม่ตรง = หน้าเว็บบอกว่าเลือกได้
    แต่พอบันทึกแล้วค่ากลายเป็น "ต้องรังวัด" โดยไม่มีคำอธิบาย
  · ⚠️ **"บังคับ" = บังคับก่อนขึ้นประกาศ ไม่ใช่บังคับก่อนส่งฟอร์ม — ห้ามเอาไปบล็อกปุ่มบันทึก**
    คนที่ยังไม่พร้อมจ่ายค่ารังวัดก็ยังเป็นลีดที่ทีมขายคุยต่อได้ (กติกาเดียวกับหน้าฝากหาที่ดิน)
  · ในเขตบังคับ: ล็อกเหลือ "ต้องการรังวัด" ทางเดียว แต่ยังแสดงตัวเลือกอื่นแบบจาง
    ให้เห็นว่าเป็นข้อจำกัดของพื้นที่ ไม่ใช่ฟอร์มพัง · และ**ซ่อนตารางข้อดี–ข้อเสีย**
    (ไม่ต้องโน้มน้าวคนที่ไม่มีทางเลือก — อ่านแล้วเหมือนขายของทั้งที่เลือกไม่ได้)
  · นอกเขตบังคับ: เลือกได้ 3 ทาง (`yes`/`no`/`undecided`) พร้อมตาราง **ข้อดีและสิ่งที่ต้องแลก**
    เขียนทั้งสองด้านตามจริง — เจ้าของที่รู้ต้นทุนล่วงหน้าแล้วยังเลือกรังวัด คือคนที่ไม่ถอยตอนได้ใบเสนอราคา
  · ตอนเปิดใบเดิมกลับมาใช้ `d.surveyRequired` ที่เซิร์ฟเวอร์ส่งมาเสมอ (`applyFromLead`) ไม่คำนวณเองซ้ำ
  · **ปุ่ม "ฝากขายแปลงถัดไป" ต้องเรียก `SV.reset()`** — `form.reset()` คืนค่า radio ให้ก็จริง
    แต่ธง "บังคับ" ของแปลงก่อนหน้ายังค้าง แปลงใหม่ในต่างจังหวัดจะขึ้นว่าบังคับรังวัด
- `surveyquote.js` **เปิด `window.NJSurveyQuote` ให้หน้าอื่นเรียกได้แล้ว** (2026-09-06)
  · `load()` แคช promise ของ `pricing.js` ไว้ตัวเดียว (กติกาเดียวกับ `NJFeeCalc.loadBuildingPrices`)
  · `quoteFromWa(totalWa, jobType, {combo:true})` — คิดส่วนลด "รังวัด + ฝากขาย 5%" ผ่าน `adjust`
    ของ `pricing.js` ไม่ใช่ลบเอาเองทีหลัง ไม่งั้นบรรทัดแจกแจงจะไม่มีส่วนลดโผล่
  · ⚠️ **ตารางราคายังอยู่ที่ระบบหลังบ้านที่เดียวเหมือนเดิม ห้ามก๊อปตัวเลขมาไว้ในเว็บ**
    โหลดไม่ได้ = ไม่แสดงราคาเลย แล้วให้ทักไลน์แทน ห้าม fallback เป็นตัวเลขที่เดา
  · `consign.html` โหลดไฟล์นี้ด้วยแล้ว (กล่องเช็กราคาและโรงฉายวิดีโอข้างในเช็ก element ของตัวเองก่อนทำงาน
    หน้าฝากขายไม่มีทั้งคู่ จึงไม่ทำอะไรนอกจากเปิด API ให้)
- `landform.js` ช่อง "ที่ตั้ง · เนื้อที่ · ราคา" ของฟอร์มฝากขาย (พิมพ์ค้นหา/เลือก จังหวัด-อำเภอ-ตำบล + คำนวณราคาอัตโนมัติ)
  · ใช้ `input + datalist` **ไม่ใช่ `select`** — พิมพ์ 2-3 ตัวอักษรแล้วเบราว์เซอร์กรองให้ เร็วกว่าเลื่อนหาใน 77 จังหวัดมาก
  · เป็นช่องพิมพ์ได้อิสระ ค่าที่ไม่ตรงรายชื่อก็ส่งไปให้ทีมขายอยู่ดี (ขึ้นข้อความเตือนตอนออกจากช่อง ไม่บล็อกการส่ง)
  · คณิตศาสตร์อยู่ใน `NJLandForm.calc` แยกจาก DOM เพื่อทดสอบด้วย node ได้ตรงๆ
  · **`initAreaPrice` มี `opt.onChange` แล้ว** (2026-09-06) ยิงทุกครั้งที่เนื้อที่/ราคาเปลี่ยน —
    กล่องรังวัดในหน้าฝากขายใช้ตัวนี้คำนวณค่ารังวัดตามเนื้อที่ใหม่ (`initAddress` มี `onChange` อยู่ก่อนแล้ว)
    · ⚠️ **ตัวแจ้งอยู่ในตัวห่อ `render()` ไม่ใช่ต่อท้าย `paint()`** — `paint()` มี early return หลายทาง
      (ยังไม่ใส่เนื้อที่ · ยังไม่ใส่ราคา) ซึ่งเป็นทางที่ผู้เรียกต้องรู้พอๆ กับทางที่คำนวณได้
      เอาไปต่อท้ายเมื่อไหร่ ค่ารังวัดบนหน้าฝากขายจะค้างตัวเลขเก่าตอนผู้ใช้ลบเนื้อที่ทิ้ง
- `data/thai-admin.json` รายชื่อ 77 จังหวัด / 930 อำเภอ / 7,452 ตำบล พร้อมรหัสไปรษณีย์ (~271 KB, gzip ~57 KB)
  · สร้างด้วย `node data/build-admin.js` จากชุดข้อมูล kongvut/thai-province-data · **ห้ามแก้ไฟล์ json ด้วยมือ**
  · โหลดแบบไม่บล็อกหน้า · โหลดไม่สำเร็จก็ยังกรอกได้ เพราะช่องเป็น `input` ธรรมดาอยู่แล้ว (ไม่ต้องมีช่องสำรองแยก)
- **หน้าฝากขายเป็น 2 จังหวะ ไม่ใช่กดส่งครั้งเดียวจบ** (แก้เมื่อ 2026-09-02)
  1. **"บันทึกข้อมูล"** → สร้างใบฝากขายในระบบ · **ฟอร์มไม่ถูกซ่อน** ยังแก้ทุกช่องแล้วกดบันทึกซ้ำได้ไม่จำกัด
     (ปุ่มเปลี่ยนข้อความเป็น "💾 บันทึกการแก้ไข" · ยิง `PATCH` แทน `POST`)
  2. **"ส่งให้ทีมงานตรวจสอบ"** (`#cs-send`) → ยิงแจ้งเตือนหาทีมงาน · กดซ้ำได้หลังแก้ข้อมูลเพิ่ม
  · กุญแจคือ **ตั๋ว** (`uploadToken`) ที่ได้ตอนบันทึกครั้งแรก — ไม่มีระบบสมาชิก ไม่มีรหัสผ่าน
    เก็บใน `localStorage['njConsignTicket']` (อ่าน/เขียนใน try/catch เสมอ · โหมดส่วนตัวเขียนไม่ได้)
    เปิดหน้านี้ใหม่ภายใน 14 วันแล้ว `restoreLead()` เติมค่ากลับลงฟอร์มให้ครบ · รองรับลิงก์ `?id=OP-xxx&t=<ตั๋ว>` ด้วย
  · **`fillForm()` ต้องเติมที่ตั้งตามลำดับ จังหวัด → อำเภอ → ตำบล** และ dispatch `input` ทุกช่อง
    เพราะรายชื่อชั้นล่างถูกสร้างจากชั้นบนใน `landform.js` · ติ๊ก `pdpa` คืนให้ด้วย (ยินยอมไปแล้วตอนบันทึกครั้งแรก)
  · **ไม่ยิงสถิติเหตุการณ์ใหม่ตอนกดส่ง** — ลีดถูกนับไปแล้วตอนบันทึกครั้งแรก (`consign_submit` ฝั่งเซิร์ฟเวอร์)
    เพิ่มชนิดเหตุการณ์ใหม่ต้องขึ้นทะเบียนทั้ง `analytics.js` และ `server.js` ก่อน ไม่งั้นถูกทิ้งเงียบๆ
- **ปุ่ม "ยกเลิกการฝากขาย" เป็นทางออกของเจ้าของ ไม่ใช่ปุ่มของทีมงาน** (`#cs-cancel` · `cancelLead()` · เพิ่มเมื่อ 2026-09-02)
  · **กดได้ทุกขั้น รวมถึงหลังประกาศขึ้นเว็บแล้ว — ห้ามผูกปุ่มนี้กับ `d.editable` เด็ดขาด**
    ช่วงที่ต้องใช้จริงที่สุดคือ "ขายได้เองแล้ว" ซึ่งเป็นช่วงที่เจ้าของแก้ข้อมูลเองไม่ได้แล้วพอดี
  · 2 จังหวะเสมอ: ลิงก์ตัวเล็กสีเทา → กล่องยืนยันโทนแดง (กรอกเหตุผลได้ ไม่บังคับ) → กดยืนยันถึงยิงจริง
    กดคืนเองไม่ได้ ต้องให้พนักงานปลดให้ในระบบหลังบ้าน จึงห้ามทำเป็นปุ่มแดงเต็มใบที่เผลอกดระหว่างกรอกฟอร์ม
  · **`applyCancelled()` ต้องถูกเรียกจาก `setLead()` ที่เดียว** — ทุกเส้นทาง (เปิดใบเดิม · บันทึก · แนบไฟล์ ·
    กดยกเลิก) วิ่งผ่าน `setLead()` หมด · ไปเรียกกระจายเองเมื่อไหร่ หน้าจอจะค้างสถานะ "ยังฝากขายอยู่"
    ทั้งที่ฝั่งเซิร์ฟเวอร์ยกเลิกไปแล้ว
  · ยกเลิกแล้วต้อง**ปิดทุกทางที่เขียนข้อมูลบนหน้าจอ**ให้ตรงกับที่เซิร์ฟเวอร์ปิดไปแล้ว
    (ซ่อน `#cs-up` `#cs-send` `#cs-keep` + `disabled` ทั้งฟอร์ม) — ไม่ปิดฝั่งนี้ = กดได้แต่เจอ 409 ทุกครั้ง
    ซึ่งอ่านแล้วเหมือนระบบพัง ไม่ใช่เหมือนถูกปิด
  · **`d.cancelled` กับ `d.editable === false` คนละเรื่องกัน ห้ามใช้ปนกัน** — `editable` เป็นเท็จได้จาก
    "ทีมงานรับเรื่องไปแล้ว" ด้วย ซึ่งต้องขึ้นข้อความคนละแบบโดยสิ้นเชิง
  · ยกเลิกไม่สำเร็จ **ห้ามเงียบ** ต้องขึ้นข้อความ + บอกให้ทักไลน์ ไม่งั้นเจ้าของเข้าใจว่ายกเลิกแล้ว
    ทั้งที่ประกาศยังอยู่บนเว็บ
- **รูป/เอกสารที่แนบแล้วขึ้นเป็นการ์ดพร้อมปุ่มลบ** (`.cs-thumbs` / `.cs-thumb` ใน `ui.css`)
  · **แยก 2 สถานะบนหน้าจอตามสิทธิ์จริงที่ฝั่งเซิร์ฟเวอร์**: รอตรวจ = มีปุ่ม ✕ ลบเองได้ ·
    ตรวจแล้ว = แถบเขียว "ทีมงานตรวจแล้ว" ลบเองไม่ได้ (รูปที่ผ่านการตรวจอาจขึ้นหน้าประกาศไปแล้ว)
  · ปุ่มลบใช้ **event delegation** ที่กล่อง ไม่ใช่ผูกรายใบ — การ์ดถูกวาดใหม่ทุกครั้งที่ข้อมูลเปลี่ยน
  · **ชื่อไฟล์มาจากผู้ใช้ ใส่ผ่าน `textContent` เท่านั้น** ห้ามต่อเป็นสตริง HTML
- **หน้าฝากขายมีช่องแนบไฟล์หลังบันทึกฟอร์มสำเร็จ** (`#cs-up` ใน `consign.html` · `setupUpload()` ใน `consign.js`)
  · แนบได้เฉพาะ**หลัง**บันทึกฟอร์ม เพราะต้องใช้ตั๋วที่ได้จาก response — ไม่ใช่ช่องอัปโหลดลอยๆ ที่ใครก็ยิงไฟล์เข้ามาได้
  · **แยก 2 ช่องเด็ดขาด**: รูปที่ดิน (ขึ้นเว็บได้หลังแอดมินอนุมัติ) กับ เอกสาร (ทีมงานเห็นเท่านั้น ไม่มีทางขึ้นเว็บ)
  · ย่อรูปด้วย canvas ก่อนส่งเสมอ (ยาวสุด 1800px) — รูปมือถือใบละ 3-8 MB อัปดิบๆ คือรอเป็นนาทีบนเน็ตมือถือ
  · ไฟล์ทุกไฟล์เข้ากล่อง "รอตรวจ" ในระบบหลังบ้าน **ไม่ขึ้นเว็บทันที** (กติกาล็อกไว้ที่ `server.js`)
- `wanted.html`, `wanted.js` **หน้าฝากหาที่ดิน — ลีดฝั่งผู้ซื้อ** (เพิ่ม 2026-09-04)
  · กลับด้านกับหน้าฝากขาย: ผู้ซื้อบอกโจทย์ว่าอยากได้ที่ดินแบบไหน ส่งเข้า `POST /api/public/buyer-request`
  · **หน้านี้ทำงานได้แม้คลังแปลงยังว่าง** เพราะสิ่งที่เสนอคือ "เราจะไปหาให้" ไม่ใช่ "มาดูของที่เรามี"
    จึงเป็นหน้าเดียวที่ยิงโฆษณาหาผู้ซื้อได้ตั้งแต่ตอนที่ยังมีแปลงขึ้นเว็บไม่กี่แปลง
  · **ทำเลใส่ได้ถึง 3 ย่าน** — สร้าง `NJLandForm.initAddress` แยกคนละชุดต่อย่าน (ไม่มีสถานะร่วมกัน)
    ย่านที่ 2-3 ซ่อนไว้ก่อน กดเพิ่มเอง · `areaValues()` **ต้องข้ามย่านที่ยังซ่อนอยู่**
    ไม่งั้นค่าที่เบราว์เซอร์เติมอัตโนมัติไว้จะถูกส่งไปเป็นโจทย์ที่ผู้ซื้อไม่ได้กรอก
  · **หน่วยเนื้อที่ส่งค่าดิบ + `areaUnit` ให้เซิร์ฟเวอร์แปลงเอง ห้ามแปลงที่ฝั่งนี้**
    แปลงสองที่เมื่อไหร่ ตัวเลขจะเพี้ยนคนละทางโดยไม่มีอะไรเตือน
  · **ไม่บังคับกรอกทำเล/งบ/เนื้อที่** — คนที่ยังไม่รู้ว่าตัวเองอยากได้อะไรก็ยังเป็นลีดที่คุยต่อได้
    บังคับให้กรอกครบ = เขากดถอยตั้งแต่ยังไม่ได้คุยกับใคร
  · ไม่ยิง `buyer_request_submit` จากเบราว์เซอร์ — เซิร์ฟเวอร์บันทึกให้แล้วตอนสร้างใบ (นับที่เดียว)
- **กล่อง "ไม่พบแปลง" ต้องยื่นปุ่มฝากหาที่ดินเป็นทางออกหลัก** (`app.js` หน้าแรก · `marketplace.js`)
  · คนที่ยืนอยู่หน้ารายการแปลงคือ**ผู้ซื้อ** ยื่นให้แต่ปุ่มฝากขายคือตอบไม่ตรงกับสิ่งที่เขามาหา
  · จังหวะ "ค้นแล้วไม่เจอ" คือจังหวะที่รู้โจทย์ของผู้ซื้อชัดที่สุดในทั้งเว็บ
    ปล่อยให้จบแค่ "ลองเปลี่ยนคำค้น" คือทิ้งลีดที่บอกความต้องการมาแล้วเต็มๆ
- `njservices.js`, `njservices.css` **บริการเพิ่มเติมหลังซื้อที่ดิน** (`window.NJServices` · เพิ่ม 2026-09-06)
  · จุดต่างที่เว็บประกาศทั่วไปไม่มี — ผู้ซื้อติ๊กบอกได้ตั้งแต่วันแรกว่าหลังซื้อแล้วอยากให้ดูแลอะไรต่อ
    (โอนกรรมสิทธิ์ · ขออนุญาตก่อสร้าง · ผู้รับเหมา) แล้วส่งเข้า `POST /api/public/inquiry`
  · อยู่ 2 หน้า: `index.html#บริการเพิ่มเติม` (ไม่ผูกกับแปลง) และ `land.html` (แนบรหัสทรัพย์ให้อัตโนมัติ)
  · **รายการบริการอยู่ใน `LIST` ที่เดียว ห้ามเขียนรายการซ้ำในหน้าใดหน้าหนึ่ง** —
    ไม่ตรงกันเมื่อไหร่ สถิติ "คนสนใจบริการไหนมากที่สุด" อ่านไม่ได้เลย
  · **คีย์ต้องตรงกับ `NJ_SERVICES` ใน `server.js` เป๊ะ** เซิร์ฟเวอร์ทิ้งคีย์ที่ไม่รู้จักโดยไม่มี error
  · ⚠️ **`PARTNERS` ตั้งใจปล่อยว่างไว้จนกว่าจะมีสัญญาจริง** — บริการที่ `by:'partner'`
    จะขึ้นป้ายว่า "ทีมงานจัดหาบริษัทพันธมิตรให้" **ห้ามใส่ชื่อบริษัทสมมติเพื่อให้หน้าเว็บดูสมบูรณ์**
    (กติกาข้อ 4 และ 9) · มีสัญญาแล้วเติมชื่อลง `PARTNERS` ที่เดียว ทุกหน้าขึ้นชื่อจริงพร้อมกัน
  · บริการที่ `by:'nj'` 4 ตัวแรกคือสิ่งที่แถบ "บริการ" หน้าแรกประกาศไว้อยู่แล้วว่าบริษัททำเอง
    (ฝากขาย · ตรวจกรรมสิทธิ์ · รังวัด · ยื่นจัดสรร · ประสานงานโอน) จึงเขียนชื่อทีมงานได้ตามจริง
  · หน้าไหนอยากได้ฟอร์มนี้ ใส่ `<div data-njsv-form>` พอ (ดู `boot()`) —
    หน้ารายละเอียดแปลงเรียก `mount()` เองเพราะต้องรอรหัสแปลงจาก API ก่อน
  · ⚠️ **`.njsv-sec` (แถบบนหน้าแรก) ต้องเป็นพื้นสว่างเท่านั้น** — มันวางระหว่างแถบ "บริการ" (`#E7EEF5`)
    กับ `.nj-intro` (`#050F1D`) ทำเป็นพื้นเข้มเมื่อไหร่ = พื้นเข้มสองแถบติดกัน จังหวะสีของหน้าแรกพัง
- `compare.js`, `compare.css`, `compare.html`, `comparepage.js` **เปรียบเทียบแปลง** (เพิ่ม 2026-09-06)
  · `compare.js` = ตัวเก็บรายการที่เลือก + ปุ่ม "เทียบ" บนการ์ด + แถบลอยด้านล่าง (`window.NJCompare`)
    `comparepage.js` = ตัววาดตารางในหน้า `compare.html` · โหลด `compare.js` ก่อนเสมอ
  · **ปุ่มบนการ์ดถูกแปะทีหลังด้วย `decorate()` ไม่ได้เขียนไว้ใน `listingcard.js`** —
    หน้าที่ไม่ได้โหลด `compare.js` (เช่น `/s/`) จึงได้การ์ดหน้าตาเดิมเป๊ะ
    และสไตล์ปุ่มอยู่ใน `compare.css` ที่เดียว ไม่ต้องเขียนซ้ำใน `home.css` + `marketplace.css`
  · **ตัวดักคลิกต้อง `stopPropagation` ในเฟส capture** — `bindGrid()` ดักคลิกทั้งการ์ดเพื่อพาไปหน้ารายละเอียด
    ไม่หยุดไว้ = กดปุ่ม "เทียบ" แล้วเด้งออกจากหน้าทันที
  · เก็บใน **sessionStorage ไม่ใช่ localStorage** — การเทียบเป็นเรื่องของการเข้าเว็บรอบนี้ ไม่ใช่ข้ามวัน
  · ⚠️ **ป้ายจุดแข็งมอบให้เฉพาะแปลงที่มีข้อมูลช่องนั้นจริง** (`winners()` ข้ามแปลงที่ค่าเป็น null)
    ไม่รู้ ≠ ชนะ และไม่รู้ ≠ แพ้ · ช่องที่ไม่มีข้อมูลขึ้น "—" และ**ข้อความอธิบายว่า "—" แปลว่า
    ยังไม่ได้ระบุ ไม่ใช่ไม่มี ห้ามถอด** (กติกาข้อ 5 · กติกาเดียวกับ `#unknown-note` ของหน้ารวมประกาศ)
  · ป้าย "รังวัดยืนยันแล้ว" เป็นข้อเท็จจริงของแปลงนั้นเอง **ไม่ใช่ผลการเทียบ** จึงขึ้นได้แม้เทียบแปลงเดียว
  · เทียบได้สูงสุด 4 แปลง · `compare.html` ตั้ง `noindex` เพราะเนื้อหาขึ้นกับสิ่งที่ผู้ใช้เลือกเอง
- `portal.html`, `portal.css` **ระบบติดตามงานรังวัด — หน้าอธิบาย + ทางเข้าพอร์ทัลลูกค้า** (เพิ่ม 2026-09-08)
  · ตัวระบบจริงคือ `https://app.njteedinsure.com` (ระบบหลังบ้าน `nj-survey-system` บัญชี `role=customer`)
    หน้านี้เป็นแค่หน้าอธิบาย + ปุ่มพาไป **ไม่ดึง API และไม่มีสถานะงานของใครอยู่ในหน้านี้เลย**
  · ⚠️ **ห้ามใส่ฟอร์มเข้าสู่ระบบในหน้านี้เด็ดขาด** เว็บนี้เป็นสแตติกบน GitHub Pages ไม่มีฝั่งเซิร์ฟเวอร์
    ที่จะถือ token ได้ปลอดภัย · และการรับรหัสผ่านที่โดเมนหนึ่งแล้วส่งข้ามไปอีกโดเมน = สอนลูกค้า
    ให้กรอกรหัสในที่ที่ไม่ใช่ระบบจริง ซึ่งเป็นพฤติกรรมเดียวกับหน้าฟิชชิ่ง
    (โทเคนของระบบผูกกับชื่อโดเมนอยู่แล้ว กรอกที่นี่ก็ใช้ที่นั่นไม่ได้)
  · ⚠️ **หัวข้อ "เข้าไปแล้วเห็นอะไร" 4 ข้อ = 4 แท็บจริงในพอร์ทัล** (สถานะงาน · รายงาน/ไฟล์ ·
    ใบแจ้งหนี้/ชำระ · แผนที่แปลง — ดู `portalBody()` ใน `nj-survey-system/public/app.js`)
    และ "11 ขั้นตอน" มาจาก `SURVEY_STEPS` ที่นั่น · ระบบเปลี่ยนเมื่อไหร่ต้องแก้หน้านี้ตาม
    เขียนสิ่งที่ระบบยังไม่มี = ลูกค้าเข้าไปหาไม่เจอแล้วโทรถาม ซึ่งตรงข้ามกับเหตุผลที่ทำหน้านี้
  · ⚠️ **ปุ่มติดต่อในหน้านี้ห้ามใส่ `data-contact` และห้ามยิงสถิติติดต่อ** — คนที่กดคือลูกค้าเดิม
    ที่มาขอรหัส/ลืมรหัส ไม่ใช่ลีดใหม่ · นับรวมเมื่อไหร่ ยอดลีดที่ใช้ตัดสินเกณฑ์ Phase 2 จะสูงกว่าความจริง
    (กติกาข้อ 6 มองจากอีกด้าน — ข้อนั้นกันลีดตกหล่น ข้อนี้กันลีดปลอม)
  · ทางเข้า: ลิงก์ `.nj-portal-link[data-njportal]` บนหัวเว็บ **8 หน้า** (สไตล์อยู่ใน `menu.css`)
    · **หน้าแรกไม่มีบนหัวเว็บ** เพราะแถวหัวเว็บเต็มพอดีจนโลโก้ถูกบีบอยู่แล้ว (เมนู 982px + โลโก้ 136
      + ปุ่ม 118 + ช่องไฟ 52 = 1288 บนพื้นที่จริง 1216 ที่จอ 1440) เติมอีกอันเมนูตัดสองบรรทัดทันที
      — หน้าแรกใช้ปุ่มบนการ์ด 04 "ติดตามผลผ่านระบบ" ในแถบบริการ + ลิงก์ในฟุตเตอร์แทน
    · จอ ≤640px ซ่อนลิงก์บนหัวเว็บ แล้ว `menu.js` ย้ายไปท้ายลิ้นชัก (`.njmenu-portal`)
      **`[data-njportal]` ต้องมีจุดเดียวต่อหน้า** — `menu.js` ใช้ `querySelector` ตัวเดียว
- `menu.js`, `menu.css` **เมนูมือถือ (ลิ้นชัก)** — โหลดครบทั้ง 9 หน้า (เพิ่ม 2026-09-07)
  · ปัญหาที่แก้: **สไตล์ทั้งสามชุดซ่อนเมนูหลักทิ้งบนจอเล็กโดยไม่มีอะไรมาแทน**
    `home.css` @1040px `header nav{display:none !important}` · `marketplace.css` @1079px `.topbar nav{display:none}` (เดิม 900 · ขยับ 2026-09-10 ดูเหตุผลท้ายไฟล์นั้น)
    · `ui.css`/`style.css` `.header-nav{display:none}` @984px (เดิม 860 · ขยับ 2026-09-10 · ดูเหตุผลท้าย `style.css`) — เหลือแค่แถบล่าง 5 ปุ่ม คนบนมือถือจึงเข้า
    มาตรฐาน · ทีมเรา · บริการ · บริการหลังซื้อที่ดิน · ขั้นตอน · ตรวจทรัพย์ก่อนซื้อ · วิดีโอ · ที่ดินประกาศขาย **ไม่ได้เลย**
  · ⚠️ **สร้างรายการจาก `<nav>` ของหน้านั้นเอง ห้ามพิมพ์รายการเมนูซ้ำใน `menu.js` เด็ดขาด**
    9 หน้ามีเมนูไม่เหมือนกัน · พิมพ์ซ้ำ = วันหนึ่งเพิ่มเมนูบนเดสก์ท็อปแล้วมือถือไม่มี โดยไม่มี error
    (กับดักเดียวกับสไตล์การ์ดสองไฟล์ และ `messenger_click` ที่เคยมีฝั่งเดียว)
  · ⚠️ **ตัดสินว่าจะโชว์ปุ่มไหม จาก `getComputedStyle(nav).display` ไม่ใช่เลข breakpoint**
    สามชุดใช้จุดตัดคนละค่า เขียนเลขไว้ = ต้องแก้ให้ตรงกันสี่ที่ และจะมีช่วงความกว้างที่
    "เมนูหายแต่ปุ่มยังไม่มา" หรือ "มีทั้งสองอัน" โดยไม่มีใครสังเกต · เช็คซ้ำตอน resize ด้วย
  · ⚠️ **z-index ต้องสูงกว่า 900** — `.cs-bottombar` (800) และแบนเนอร์คุกกี้ (900) เป็น fixed ทั้งคู่
    ต่ำกว่านั้นแถบล่างจะทับลิ้นชักแล้วดูเหมือนเมนูพัง (เจอจริงตอนพรีวิวหน้าฝากขาย)
  · ปุ่มสร้างด้วย JS ทั้งหมด — ไฟล์โหลดไม่สำเร็จ = ไม่มีปุ่มที่กดแล้วไม่มีอะไรเกิดขึ้น (กลับไปสภาพเดิมพอดี)
  · คีย์บอร์ด: Escape ปิด · โฟกัสถูกขังในลิ้นชัก (ประกาศ `aria-modal` เฉยๆ เบราว์เซอร์ไม่กันให้)
    · ปิดแล้วโฟกัสกลับไปที่ปุ่มเสมอ ไม่ปล่อยค้างบนของที่เพิ่งถูกซ่อน
  · **ตัดปุ่ม "ฝากขายฟรี" ออกจากหัวเว็บที่ ≤640px** — ที่ 390px ชื่อแบรนด์กับปุ่มถูกบีบจนตัดบรรทัดกลางคำ
    ทางเข้ายังมีอีก 3 ทางบนจอเดียวกัน (แถบล่าง · ปุ่มใน hero · ท้ายลิ้นชัก)
    และหน้าที่ใช้ `marketplace.css` ซ่อน `.post-btn` ที่ 640px อยู่ก่อนแล้ว กติกาจึงตรงกันทั้งเว็บ
  · **ลิ้นชักหยิบลิงก์นอก `<nav>` มาได้ 3 อย่างเท่านั้น** — ปุ่มหลัก (`CTA_SELECTORS`)
    · ทางเข้าพอร์ทัลลูกค้า (`PORTAL_SELECTOR` = `[data-njportal]`) · ทางเข้าแชท (`CHAT_SELECTOR` = `[data-njchat]`
    ใช้เฉพาะ `index.html` · ข้ามให้เองถ้าหน้านั้นมี href เดียวกันใน `<nav>` แล้ว) · ทั้งสามอ่าน `href`/ข้อความ
    จากลิงก์จริงบนหน้า **ไม่ได้พิมพ์ค่าซ้ำไว้ใน `menu.js`** จึงไม่ขัดกับกติกาห้ามพิมพ์เมนูซ้ำข้างบน
- `analytics.js` API base, analytics, consent banner และ **ที่อยู่ช่องทางติดต่อ** ใช้ร่วมกันทุกหน้า
  · `NJ_MESSENGER_URL` = `m.me/<ชื่อผู้ใช้เพจ>` — **ผูกกับชื่อผู้ใช้เพจ ไม่ใช่ชื่อเพจ** เปลี่ยนชื่อผู้ใช้เมื่อไหร่ลิงก์พังทันที
- `guides.html`, `guides.css` หน้าคู่มือที่ดิน 7 หัวข้อ (มี anchor รายหัวข้อ)
- `valuecalc.js`, `valuecalc.css` เครื่องมือ "ประเมินหลักการราคาที่ดิน" ใน `guides.html#valuation`
  · **คนละเรื่องกับ `feecalc.js` อย่าเอามาปนกัน** — `feecalc.js` คำนวณ "ราคาประเมินราชการ" เพื่อภาษี/ค่าธรรมเนียม
    ส่วนตัวนี้จำลอง "หลักการประเมินราคาตลาด" แบบ WQS (ให้คะแนนปัจจัยแล้วถ่วงน้ำหนัก) เพื่อการศึกษา
  · น้ำหนักปัจจัย (ทำเล 50% / รูปร่าง 25% / สาธารณูปโภค 15% / อื่นๆ 10%) และเพดาน ±40% เป็นค่ากลางแบบง่าย
    **ไม่ใช่สูตรทางการของสภาวิชาชีพการประเมินมูลค่าทรัพย์สิน** — คำเตือนใต้ผลลัพธ์ห้ามถอด
  · ส่วนสิ่งปลูกสร้างเรียก `NJFeeCalc.estimateBuilding()` ตัวเดียวกับเครื่องคำนวณค่าโอน จะได้ไม่ให้ตัวเลข 2 ที่ขัดกัน
    จึงต้องเลือกจังหวัดเหมือนกัน ไม่เลือก = คืน `null` ต้องขึ้นข้อความบอกเหตุผล **ห้ามเดาตัวเลขแทน**
  · บัญชีราคาโหลดผ่าน `NJFeeCalc.loadBuildingPrices()` ซึ่งแคช promise ไว้ — 2 เครื่องมือบนหน้าเดียวกันยิงเน็ตครั้งเดียว
  · แก้สูตรเมื่อไหร่ต้องรัน `node valuecalc.test.js` ให้ผ่านครบก่อนเสมอ (28 ข้อ)
- `warm.css` ชั้น "คนและความอบอุ่น" ของ `guides.html` `land.html` `videos.html`
- `photos.js` **ยังไม่มีหน้าไหนเรียกใช้** ตั้งแต่หน้าแรกเปลี่ยนดีไซน์ (2026-08-29)
  หน้าแรกใหม่ฝัง `<img src="photos/...">` ตรงๆ ไม่ได้ผ่านตัวจัดการช่องรูปของ `photos.js`
  พอมีรูปถ่ายจริงครบแล้วจะต่อช่องรูปกลับเข้าหน้าแรกหรือทิ้ง `photos.js` ไปเลย ให้ตัดสินใจตอนนั้น
- **รูป 6 ใบบนหน้าแรกตอนนี้เป็นภาพสต็อก ไม่ใช่รูปทีมงานจริง** (เจ้าของสั่งใช้เอง 2026-08-29)
  `hero.jpg` `survey.jpg` `site.jpg` ครอปจากภาพช่างรังวัดหน้าไซต์ก่อสร้าง ·
  `marker.jpg` `marker-wide.jpg` `house.jpg` ครอปจากภาพหมุดหลักเขตในสนามหญ้า
  · **ผ่านข้อ 9 ได้เพราะ `alt` ไม่ได้อ้างว่าเป็นทีมงานเราหรือแปลงลูกค้า** (ข้อ 9 แก้ใหม่ 2026-09-07)
    เขียน `alt` ใหม่ให้อ้างความเป็นเจ้าของเมื่อไหร่ = ผิดข้อ 9 ทันที · ยังขัดกับ `photos/README.md` อยู่
    เก็บบรรทัดนี้ไว้ให้คนที่มาทีหลังรู้ว่าไม่ใช่ความพลาด
  · คำบรรยาย `alt` เลี่ยงการอ้างว่าเป็นทีมงานของเราหรือแปลงของลูกค้า อย่าเขียนใหม่ให้อ้างแบบนั้น
  · ถ่ายรูปจริงมาแล้วให้ทับไฟล์ชื่อเดิมได้เลย ขนาด/สัดส่วนอยู่ในแอตทริบิวต์ `width`/`height` ของแต่ละ `<img>`
- `photos/` รูปจริงของทีมงาน · **ดู photos/README.md ก่อนเพิ่มรูป**
  ตั้งชื่อไฟล์ตามสเปกแล้วรูปขึ้นเอง ไม่ต้องแก้โค้ด
  · **เว็บจริง**: ช่องที่ยังไม่มีรูปถูกซ่อน (ทั้งกลุ่มไม่มีรูป = ซ่อนทั้งกลุ่ม) ลูกค้าไม่เห็นกรอบว่าง
  · **ดูว่าขาดรูปไหน**: เติม `?photos=dev` ท้าย URL → กรอบประบอกสเปกทุกช่อง
  · **ดูว่าลูกค้าเห็นอะไร** (ตอนเปิดจาก localhost): เติม `?photos=live`
- `land.html`, `land.js`, `land.css` หน้ารายละเอียดแปลง — เรียกด้วย `land.html?id=OP-xxx`
  · **ลำดับบนหน้าตั้งใจให้ราคาและค่าโอนอยู่บนสุด**: รูป → ราคา → เครื่องคำนวณค่าโอน → ชื่อแปลง →
    ข้อมูลแปลง → แผนที่ → ผลตรวจ → ปุ่มติดต่อ → ปุ่ม PDF (ย้ายขึ้นมาเมื่อ 2026-08-30 ตามคำสั่งเจ้าของ)
  · **สไลด์ภาพ** `galleryHtml()`/`initGallery()` — เลื่อนเองทุก 4.2 วิ · ผู้ใช้แตะอะไรก็หยุดทันที
    เคารพ `prefers-reduced-motion` (ไม่เลื่อนเอง) · รูปเดียวไม่สร้างปุ่มควบคุมเลย
  · **สถานที่ใกล้เคียง** `nearbyHtml()` — แสดงเมื่อ API ส่ง `land.nearby` มาเท่านั้น ไม่มีก็ซ่อนทั้งหัวข้อ (กติกาข้อ 5)
  · **แผนที่** `mapHtml()` มี 2 โหมด — มีหมุด `pinLat/pinLng` = ปักตำแหน่งจริง + ลิงก์ขอเส้นทาง ·
    ไม่มีหมุด = ค้นด้วย `locality` ได้แผนที่ระดับพื้นที่ · **ห้ามเปลี่ยนไปอ่าน `land.lat/lng`** (ดูกติกาข้อ 3)
  · **ปุ่ม PDF** ใช้ `window.print()` + `@media print` ใน `land.css` ไม่ได้ใช้ไลบรารีใดๆ
    สไตล์พิมพ์ซ่อนเมนู/ฟุตเตอร์/ฟอร์มคำนวณ/แผนที่ (iframe พิมพ์ไม่ติดในเบราว์เซอร์ส่วนใหญ่)
  · **แถบรหัสทรัพย์ `.ld-code`** (`codeHtml()` · เพิ่ม 2026-09-06) — วางใต้ชื่อแปลง
    เดิมผู้ซื้อทักไลน์มาว่า "สนใจที่แปลงนึงในเว็บ" โดยไม่บอกว่าแปลงไหน ทีมจับคู่กลับไม่ได้
    แปลว่า **เจ้าของที่ดินไม่เคยรู้ว่ามีคนสนใจแปลงของตัวเอง** ซึ่งเป็นสิ่งที่เราสัญญากับเขาไว้
    · **ห้ามซ่อนตอนสั่งพิมพ์** — คนที่พิมพ์ประกาศออกมาอ่านคือคนที่ต้องใช้รหัสนี้ตอนโทรมาที่สุด
    · ปุ่มคัดลอกต้องมีทางถอย (`prompt()`) เสมอ — `navigator.clipboard` ใช้ไม่ได้บน http:// และในเว็บวิวบางตัว
      ล้มแล้วเงียบ = ผู้ซื้อคิดว่าคัดลอกแล้ว วางในไลน์ไม่ติด แล้วก็ไม่แจ้งรหัสอยู่ดี
  · **กล่อง "สนใจแปลงนี้" `.ld-inq`** — `NJServices.mount()` พร้อม `listingId` ที่ล็อกไว้อ่านอย่างเดียว
    ผู้ซื้อจึงไม่มีทางพิมพ์รหัสผิดจนแอดมินตรวจไม่เจอ · ซ่อนตอนพิมพ์ (`@media print`)
- `s/index.html`, `s/share.css` **หน้ารับคนดูจากลิงก์ที่พนักงานแชร์** — `njteedinsure.com/s/?e=<รหัสพนักงาน>`
  · หน้าเดียวในเว็บที่ไม่ใช้ `marketplace.css` โดยตั้งใจ — คนมาจาก TikTok/Facebook ไม่ได้มาหาแปลงที่ดิน
    เมนู แถบค้นหา และ bottom nav คือสิ่งรบกวนล้วนๆ · **ตัวแปรสีถูกคัดลอกไว้ ไม่ได้ import** แก้โทนแบรนด์ต้องแก้ 2 ไฟล์
  · `?e=` = รหัสพนักงานผู้แชร์ · `?v=clipN` = เลือกคลิป (ค่าตั้งต้น `clip5`)
    **รหัสถูกเอาไปต่อในข้อความที่ลูกค้าจะกดส่งเข้า LINE** จึงกรองเหลือ `[a-z0-9]{2,8}` เท่านั้น ผิดรูปแบบให้ทิ้ง
  · เก็บรหัสใน **sessionStorage ไม่ใช่ localStorage** — เครดิตผูกกับการเข้าครั้งนี้ ไม่ใช่ข้ามเดือน
  · แบนเนอร์ PDPA บนหน้านี้อยู่ **ด้านบน** ต่างจากหน้าอื่น เพราะแบบล่างทับปุ่ม "ทักไลน์" พอดี (วัดแล้วบนจอ 375×812)
  · ปุ่มติดต่อมี `href` ใช้งานได้ตั้งแต่ใน HTML แล้ว JS ค่อยเติมรหัสให้ — `analytics.js` ล่มแล้วปุ่มต้องไม่ตาย
- `brand/` โลโก้ favicon และ social preview

## น้องเอ็นเจชัวร์ — แชท AI ผู้ช่วยลูกค้า (2026-09-09)

`njchat.js` · `njchat.css` · `chat.html` (หน้าเต็ม) · `njchat.test.js` (42 ข้อ) · `njchat.e2e.js` (Playwright 55 ข้อ · รันมือ)
วิดเจ็ตมุมจอโหลดครบทั้ง 12 หน้า (`index listings land compare guides portal consign verify wanted videos deal inspect`)
· ฝั่งหลังบ้าน: `POST /api/public/njchat` + `lib/njchat.js` + `test/njchat.test.js` ใน `nj-survey-system`

### ทำงานสองชั้น — กฎก่อน แล้วค่อย AI
1. **กฎในไฟล์ (`route()`) ตอบเองก่อน** ไม่มีค่าใช้จ่าย ทำงานได้แม้ AI ล่ม:
   ค้นแปลง (จังหวัด/อำเภอ/งบ/เนื้อที่/เอกสารสิทธิ์/ผังสี/สิ่งที่แปลงมี/เรียง) · เทียบแปลง · ค่ารังวัด ·
   ขั้นตอนฝากขาย/ยื่นรังวัด · เอกสาร · คิว · ค่าโอน (ลิงก์เครื่องคำนวณ) · ราคาประเมิน · ตรวจทรัพย์ · ฝากหา · พอร์ทัล · ติดต่อ
   · **ข้อพิพาทแนวเขต/เพื่อนบ้านรุกล้ำ** (เพิ่ม 2026-09-11) — ถามบ่อยและเป็นงานหลักของเราโดยตรง ปล่อยให้ AI ตอบแล้วเคยได้
     คำมั่ว ("ส่วนราชการ (อสม./สมุนไขมัน)") บนของจริง · กฎนี้ตอบเองและ **ห้ามให้คำแนะนำทางกฎหมาย**
     ตอบได้แค่ "ยืนยันแนวเขตด้วยการรังวัดก่อน" แล้วส่งต่อทนาย/ทีมงาน (มีเทสต์ล็อก)
     ⚠️ "เพื่อนบ้าน/ข้างบ้าน" เดี่ยวๆ ไม่พอ ต้องมีคำที่บ่งว่ามีปัญหา หรือคู่กับคำเรื่องแนวเขต
     ไม่งั้น "ที่ดินข้างบ้านขายไหม" (คนอยากซื้อ) จะถูกดูดมาตอบผิดเรื่อง
2. **จับกฎไม่ได้ → `POST /api/public/njchat`** ให้ Claude ตอบจากฐานความรู้ + แปลงที่ประกาศอยู่จริง
   · เซิร์ฟเวอร์ถือ `ANTHROPIC_API_KEY` (เว็บนี้สแตติก **ห้ามมี key ที่นี่** — กติกาเดียวกับ `portal.html`)
   · ตอบ `{ok:false}` (ไม่มี key / AI ล่ม / โควตา) = หน้าเว็บถอยไปให้ทักไลน์/โทร ไม่พัง · ข้อความจาก AI ติดป้าย "🤖 ตอบโดย AI"

### กติกาที่ห้ามผ่อน (สืบทอดจากข้อ 4 · 5 · 10 · 14)
- **แปลงมาจาก `NJListing.fetchListings()` เท่านั้น** การ์ดใช้ `NJListing.card()` (ห้ามก๊อป) · `compare.js` แปะปุ่ม "เทียบ" บนการ์ดในแชทให้เอง
- **"ยังไม่ระบุ" ≠ "ไม่ตรง"** — `applySearch()` ถอดจาก `listings.js` ทั้งก้อน: ช่องที่ว่างไม่นับว่าตรง
  แต่ต้องบอกจำนวนที่ถูกซ่อนเสมอ (`hiddenUnknown`) · ตารางเทียบในแชทขึ้น "—" พร้อมคำอธิบาย
- **ประเภทสิ่งปลูกสร้าง + จำนวนชั้น กรองได้จริงตั้งแต่ 2026-09-11** (`land.propertyType` · `land.floors`)
  ตัวกรองบนหน้ารวมประกาศคือ `#f-prop` + `#f-floors` · `njchat.js` อ่านเป็น `f.prop`/`f.floors` แล้วกรองด้วยกติกาเดียวกัน
  · **เดิมข้อนี้เขียนว่า "API ไม่มีช่องให้กรอง ต้องบอกตามจริง"** — ตอนนั้น `f.building` เป็นแค่ธง
  แล้วคัดตามทำเลให้แทน พร้อมนับแปลงที่ "ประกาศเอ่ยถึงสิ่งปลูกสร้าง" จาก `parcelInfo`/`blurb` · ตอนนี้ไม่ต้องแล้ว
  · ⚠️ **ค่าว่าง = ยังไม่ได้กรอก ไม่ใช่ "ที่ดินเปล่า"** — แปลงที่ยังว่างต้องถูกนับเป็น `hiddenUnknown`
  แล้วบอกจำนวน เหมือนช่องอื่นทุกช่อง ห้ามเดาแทนหรือซ่อนเงียบๆ (ตอนเขียนข้อนี้ ทั้ง 21 แปลงบนเว็บยังว่างหมด)
  · ⚠️ ห้ามใช้คำว่า "บ้าน" โดดๆ จับโจทย์ — "ที่ดินข้างบ้าน" คือคนอยากซื้อที่ดิน ไม่ได้อยากได้บ้าน
  · ⚠️ `landvocab.js` ต้องโหลด**ก่อน** `listings.js` — ตัวกรองเติมชื่อไทยตอนสร้างตัวเลือก ถ้ามาทีหลังช่องจะว่างเงียบๆ
- **ค่ารังวัดคิดจาก `NJPricing.computeQuote` เท่านั้น** (โหลด `pricing.js` แบบเดียวกับ `surveyquote.js` · ส่ง `nights` ของโซนเสมอ)
  โหลดไม่ได้ = ไม่มีตัวเลข ให้ทักไลน์ · ไม่บอกจังหวัด = เตือน "ยังไม่รวมค่าเดินทาง" · ทุกคำตอบราคากำกับว่าเป็นประมาณการ
  · กทม.+ปริมณฑล อ่านจาก `P.ZONE_PROVINCES.A` (`METRO_FALLBACK` ใช้เฉพาะตอนตารางยังไม่มา — ตรงกับ `SURVEY_REQUIRED_PROVINCES`)
- **ไม่มีตัวเลขค่าโอน/ภาษี/ค่ารังวัดพิมพ์ไว้ในไฟล์** — `KB` มีแค่ข้อเท็จจริงที่หน้าเว็บพิมพ์อยู่แล้ว (ค่านายหน้า 3% · 1 วันทำการ · เวลาทำการ)
- น้องบอกว่าเป็น AI ตั้งแต่ข้อความแรก · ห้ามรับปากแทนทีมงาน · "ขอใบเสนอราคาจริง" = ส่งต่อไลน์ ไม่คิดซ้ำ
- **สถิติ**: `chat_open` ขึ้นทะเบียนแล้วทั้ง `NJ_INTERNAL_EVENTS` และ `PUBLIC_EVENT_TYPES` (ไม่ใช่ลีด · ห้ามบวกเข้า `leads`)
  ปุ่มติดต่อในแชทใช้แมป `CONTACT_TRACK` เดียวกับการ์ด (กติกาข้อ 6) · **บทสนทนาไม่ส่งไป `/api/public/track`** (ไม่มี PII)
  · ประวัติเก็บ `sessionStorage['njchat']` (กติกาเดียวกับ `njCompare`)
- คำที่ใช้จับโหมดค้นต้องเจาะจง — **ห้ามใช้ "หา" "น้ำ" "ถม" "บ้าน" โดดๆ** ("ปัญหา" "น้ำท่วม" "ข้างบ้าน" จะหลุดเข้าโหมดค้น) มีเทสต์ล็อกไว้

### ตำแหน่งบนจอ (กับดักที่เจอตอนทดสอบจริง)
- z-index ปุ่ม 790 · แผง 890 — **ต่ำกว่าแบนเนอร์คุกกี้ (900)** ซึ่งเป็นเรื่องกฎหมายต้องอยู่บนสุด
  → แบนเนอร์บังปุ่มจนกดไม่ได้ทั้งหน้าที่ 390px · แก้ด้วย `dodgeConsent()` ยกปุ่ม/แผงขึ้นเหนือแบนเนอร์
  (วัดจาก `#nj-consent` จริง · เฝ้า `body` childList เพื่อคืนตำแหน่งเมื่อผู้ใช้ตอบ · แก้แค่ `style` จึงไม่วนลูปแบบ compare.js)
- จอเล็ก: ปุ่มยกพ้น `.mobile-nav` (69px) และ `.cs-bottombar` (74px) · แผงเต็มจอ + `html.njchat-open{overflow:hidden}`
- สไตล์การ์ด `.njchat-cards .land-card` ประกาศครบชุดใน `njchat.css` เพราะหน้า `ui.css` (ฝากขาย/ตรวจทรัพย์/ฝากหา)
  ไม่มีสไตล์ `.land-card` เลย — **ใช้เฉพาะในกล่องแชท** ไม่ใช่ชุดที่สามของเว็บ · เพิ่มคลาสใหม่ให้การ์ดต้องมาเพิ่มที่นี่ด้วย
- `njchat.js` ต้องมาหลัง `analytics.js` · `listingcard.js` (+`verified.js` ก่อนหน้า) · `landvocab.js` · `compare.js` และก่อน `menu.js`
  · `njchat.css` โหลดคู่ `compare.css` + `verified.css` ทุกหน้า (หน้าที่ไม่เคยมีการ์ดถูกเติมให้แล้ว)
- **ลิงก์ "แชทกับน้อง" ในเมนู `<nav>`** อยู่ครบ 12 หน้า (7 หน้าชุด `marketplace.css` ใน `<nav aria-label="เมนูหลัก">` ·
  5 หน้าชุด `ui.css` ใน `<nav class="header-nav">` รวม `deal.html` กับ `inspect.html` ที่ได้วิดเจ็ตแชทครบแล้ว
  เช่นกัน) — ลิ้นชักจอเล็กได้ตามไปเองเพราะ `menu.js` สร้างรายการจาก `<nav>` จริง
  · **ยกเว้น `index.html`** ที่แถวหัวเว็บเต็มความกว้างพอดีแล้ว (มีคำเตือนไว้ในไฟล์) — วัดจริงที่ 1440 แล้วเติมลิงก์เดียว
  โลโก้ถูกบีบจาก 153 เหลือ 97px และเมนูทั้ง 10 ข้อขึ้นสองบรรทัด จึงวางลิงก์ไว้ในฟุตเตอร์พร้อม `data-njchat`
  แล้วให้ `menu.js` หยิบเข้าลิ้นชักแทน (กติกาเดียวกับ `data-njportal` · เช็ก href ซ้ำกับใน `<nav>` ก่อนเติม)
  · ⚠️ แถวหัวเว็บทั้งสองชุดพอดีตัวมาก (marketplace เหลือ ~15px ที่จอ 1440) — ช่วงที่แคบกว่านั้น flex จะบีบ
  ข้อความ **ทุกข้อ** ในเมนูให้ขึ้นสองบรรทัดพร้อมกัน ไม่ใช่แค่ข้อที่เพิ่งเติม จึงซ่อนเฉพาะลิงก์แชท
  ที่ `max-width:1224px` (marketplace · กฎอยู่ใน `njchat.css`) และ `1079px` (ui.css · กฎอยู่ใน `style.css`
  ไม่ใช่ `njchat.css` เพราะ `deal.html`/`inspect.html` มีลิงก์แต่ไม่ได้โหลดไฟล์นั้น)
  ส่วนตัวแถบเมนูเองซ่อนที่ `1079px` และ `984px`
  ตามลำดับ — ตัวเลขทั้งหมดวัดจากของจริงโดยยึดหน้าที่แย่ที่สุดของแต่ละชุด ที่มาอยู่ในคอมเมนต์ท้าย
  `njchat.css` · `marketplace.css` · `style.css` (`deal.html` ไม่ต้องขยับ เพราะแถวหัวเว็บไม่มีปุ่มหลัก)
  ลิ้นชักยังมีรายการนี้เสมอเพราะ `menu.js` อ่านจาก DOM ไม่สนใจ `display` · **เพิ่มลิงก์เมนูอีกไม่ได้แล้วโดยไม่วัดใหม่**

### ทดสอบ
`node njchat.test.js` (42 ข้อ · node:vm · ตารางราคาจาก `../nj-survey-system` ถ้ามี) · `node contracts.test.js` ยังผ่าน 86 ข้อ ·
`node njchat.e2e.js` (Playwright · เซิร์ฟเวอร์ปลอมในไฟล์ · pricing.js ของจริง): มือถือ 390 + เดสก์ท็อป 1440 · ทั้ง 10 หน้า ·
โจทย์ "บ้าน 1 ชั้น กทม.ปริมณฑล เปรียบเทียบ" · เทียบ · ค่ารังวัดตรงกับ `computeQuote` ทุกบรรทัด (25,000+10,000+72,000) ·
API ล่ม vs count:0 ขึ้นคนละข้อความ · AI ใช้ไม่ได้ถอยไปไลน์ · ไม่ล้นแนวนอน · ไม่มี console error

## กติกาการพัฒนา

1. รักษาภาษาไทยเป็นภาษาหลักและออกแบบ mobile-first
2. จุดขาย “รังวัดยืนยันแล้ว” ต้องเห็นบนการ์ดทุกแปลง
3. ห้ามเผยเลขโฉนด ชื่อเจ้าของ หรือข้อมูลส่วนบุคคลโดยไม่ได้รับความยินยอม
   · **เรื่องพิกัดเปลี่ยนกติกาแล้ว 2026-08-30** — API ส่ง `land.pinLat`/`land.pinLng` ออกมาได้
     ซึ่งเป็น**หมุดที่ทีมงานกดเลือกเองรายแปลง**ในระบบหลังบ้าน ไม่ใช่พิกัดจากงานรังวัด
     `land.lat`/`land.lng` (พิกัดภายใน) ยังถูกตัดออกเสมอเหมือนเดิม — ห้ามเปลี่ยนมาอ่านสองค่านั้น
   · แปลงที่ไม่ได้ปักหมุด = `mapHtml()` ถอยไปค้นด้วยข้อความ `locality` ได้แผนที่ระดับพื้นที่แทน
4. **ประกาศทุกแปลงมาจาก `GET /api/public/listings` เท่านั้น — ห้ามมีข้อมูลแปลงตัวอย่างในโค้ดเด็ดขาด**
   ถ้า API ตอบ `count: 0` ให้แสดงกล่อง "ยังไม่มีแปลงประกาศ" พร้อมทางไปต่อ (ฝากขาย/ทักไลน์) ไม่ใช่แสดงแปลงสมมติ
   เหตุผล: การ์ดทุกใบติดป้าย "รังวัดยืนยันแล้ว" ผู้ซื้อจึงอ่านทุกบรรทัดเป็นข้อเท็จจริงที่ตรวจสอบได้
   แปลงสมมติบนเว็บจริงทำลายสิ่งเดียวที่แบรนด์นี้ขาย และขัดกับประตูกันการเผยแพร่ที่ล็อกไว้ใน server.js
   (เคยมีของจริงหลุดเข้ามาแล้วครั้งหนึ่ง — แปลงสมมติ 3 แปลงพร้อมราคาและทำเลจริง ถูกถอดออกเมื่อ 2026-08-25)
5. **ห้ามเติมข้อความแทนช่องที่ API ไม่ได้ส่งมา** เช่น "มีทางเข้าออก" "โฉนด" "ตรวจสอบเนื้อที่แล้ว"
   หรือจำนวนรูปขั้นต่ำ — ช่องไหนไม่มีข้อมูลให้ซ่อนบรรทัดนั้นไป
   API ส่งมาแค่: `id · type · parcelInfo · estValue · blurb · photos · updatedAt · tier · land`
   **· เพิ่ม 2026-09-04: `totalWa` · `pricePerWa` · `pricePerRai`** — คำนวณที่เซิร์ฟเวอร์เสมอ
     **ห้ามหารเองในเบราว์เซอร์** ไม่งั้นหน้ารวมกับหน้ารายละเอียดได้ตัวเลขคนละค่าเมื่อปัดเศษต่างกัน
     ไม่มีเนื้อที่หรือไม่มีราคา = ส่ง 0 มา ต้องซ่อนบรรทัดนั้น ห้ามแสดงเป็น "฿0/ตร.ว."
   · ใน `land` มี: `deedArea zoning locality frontage pinLat pinLng nearby verified verifiedAt verifiedBy checks`
     **· เพิ่ม 2026-09-04 (ช่องแบบเลือกจากรายการ ใช้ทำตัวกรอง): `deedType` `zoneColor` `roadSurface`
       `facing` `roadLanes` `features[]` `province` `amphoe` `tambon`**
       ทุกช่องว่างได้ · **ว่าง = "ยังไม่ได้ระบุ" ซึ่งไม่เหมือน "ไม่มี"** ห้ามแสดงว่าไม่มี
       `zoneColor` เป็นค่าตายตัวที่กรองได้ **แยกจาก `zoning` ที่เป็นข้อความอิสระ ห้ามยุบรวม**
       `features[]` ใช้คีย์ชุดเดียวกับหน้าฝากหาที่ดิน (`BR_FEATURES`) เพื่อให้จับคู่ "คนหา ↔ แปลง" ได้
     (ไม่มี `lat` `lng` `ravaeng` `landNo` `aiAnalysis` `sellerType` `holdYears` — ถูกตัดที่ `publicLand()` ใน server.js)
   · `land.nearby` = สถานที่ใกล้เคียงจาก Google Places `{groups:[{key,label,icon,items:[{name,km}]}],total,at}`
     **เป็นข้อมูลบุคคลที่สาม ไม่ใช่ผลตรวจของทีมช่างรังวัด** — หน้าตาต้องต่างจากแผงผลตรวจ 7 หัวข้อ
     และต้องบอกที่มา + วันที่สำรวจเสมอ ห้ามนำเสนอเป็นคำยืนยันว่าวันนี้สถานที่นั้นยังอยู่
10. **ระดับความน่าเชื่อถือของประกาศมี 2 ระดับ ห้ามแสดงปนกัน**
   - `tier: 1` = ยังไม่รังวัด → ป้าย**เหลือง** “◐ ข้อมูลเบื้องต้น” · **ห้ามขึ้นป้าย “รังวัดยืนยันแล้ว” เด็ดขาด**
   - `tier: 2` = ตรวจเชิงลึกแล้ว → ป้าย**เขียว** “✓ รังวัดยืนยันแล้ว” + ผลตรวจ 7 หัวข้อ + วันที่ตรวจ
   - ผลตรวจที่ `status` ว่าง = **“ยังไม่ได้ตรวจ”** ต้องแสดงเป็นขีด “—” เท่านั้น
     ห้ามแสดงเป็นติ๊กถูกหรือซ่อนบรรทัดไป เพราะ “ยังไม่ตรวจ” กับ “ตรวจแล้วไม่มีปัญหา” คนละเรื่องกัน
   - ข้อความผลตรวจเรื่องอายัด/คดี/ภาษี เป็นข้อเท็จจริง **ณ วันที่ตรวจ** ต้องแสดงวันที่เสมอ
     และห้ามใช้คำรับประกัน — ส่วนนี้ควรให้ที่ปรึกษากฎหมายตรวจก่อนแก้ข้อความ
6. ค่าติดต่อหลักคือ `02-162-0405` (สำนักงาน) และ `084-915-8601` (มือถือ) · LINE `@716lffzt` · Messenger `m.me/NJTeeDinSure`
   · **สองเบอร์นี้ต้องมาคู่กันทุกที่** — ที่ไหนมีเบอร์สำนักงาน ที่นั่นต้องมีเบอร์มือถือด้วย (สั่งโดยเจ้าของ 12 ก.ย. 69)
     ค่าคงที่ชื่อ `COMPANY_TEL` / `COMPANY_TEL_ALT` (`consign.js` `verify.js` `wanted.js` `app.js`) ·
     `TEL_TXT` / `TEL2_TXT` (`land.js` `njchat.js`) · `TEL` / `TEL2` (`listingcard.js`)
   · ลิงก์โทรของเบอร์มือถือใช้ `data-contact="tel"` และเหตุการณ์ `tel_click` **ตัวเดิม** —
     เบอร์ที่สองไม่ใช่ช่องทางใหม่ จึงไม่ต้องขึ้นทะเบียนเพิ่มที่ `analytics.js` / `server.js`
   **เพิ่มช่องทางติดต่อใหม่ต้องแก้ 4 ที่เสมอ** ไม่งั้นตัวเลขลีดจะต่ำกว่าความจริงแล้วสรุปผิดว่าไม่ผ่านเกณฑ์ Phase 2:
   (1) ปุ่มบนหน้าเว็บ + `data-contact` (2) **`NJ_INTERNAL_EVENTS` ใน `analytics.js`**
   (3) `PUBLIC_EVENT_TYPES` ใน `server.js` (4) ผลรวม `leads` ใน `/api/public/track/summary`
   · ตัวดักคลิกต้องเป็น **แมป** ไม่ใช่ `if/else` สองทาง — ของเดิมเป็น if/else ช่องทางที่ 3 เลยถูกนับเป็น "กดโทร"
   · ที่ (2) เคยตกหล่นมาก่อน: `messenger_click` มีอยู่ใน `server.js` แต่ไม่มีใน `analytics.js`
     คลิก Messenger ทุกครั้งจึงถูกทิ้งตั้งแต่ฝั่งเบราว์เซอร์ **โดยไม่มี error ให้เห็น** (แก้แล้ว 2026-08-28)
7. คง semantic HTML, keyboard navigation, contrast และ `prefers-reduced-motion`
8. หลีกเลี่ยง framework จนกว่าจะมีเหตุผลด้าน routing, authentication หรือ state ที่ชัดเจน
9. **ภาพและวิดีโอ — แยกตามว่าสิ่งนั้น "อ้างอะไร" ไม่ใช่แยกตามว่าสร้างด้วยอะไร** (แก้ 2026-09-07)
   หลักการเดิมจากข้อ 4 ไม่เปลี่ยน: เว็บนี้ขายความจริงที่ตรวจสอบได้ · สิ่งที่ห้ามคือ**ภาพที่ทำให้คนเข้าใจผิด
   ว่าเป็นหลักฐาน** ไม่ใช่ห้ามงานที่ AI ช่วยสร้างทุกชิ้น · ของเดิมเขียนห้ามแบบเหมารวม แล้วเราก็ทำผิดกติกา
   ตัวเองอยู่ 2 จุด (ภาพสต็อก 6 ใบ · คลิปพรีเซนเตอร์ 2 ตัว) กติกาที่ไม่มีใครทำตามได้คือกติกาที่ใช้ไม่ได้
   · **ห้ามเด็ดขาด — ภาพที่อ้างถึงแปลงใดแปลงหนึ่ง** รูปแปลง รูปหมุด รูปแนวเขต รูปเอกสารสิทธิ์
     ต้องเป็นภาพถ่ายจริงของแปลงนั้นเท่านั้น **ห้าม AI ห้ามสต็อก ห้ามยืมรูปแปลงอื่น**
     ผู้ซื้ออ่านรูปพวกนี้เป็นหลักฐาน ปลอมเมื่อไหร่คือโกหกเรื่องทรัพย์ที่เขากำลังจะจ่ายเงินซื้อ
   · **ห้าม — ภาพที่อ้างว่าเป็นทีมงานหรือผลงานจริง** เช่น "ช่างของเราลงพื้นที่" "ผลงานที่เราทำ"
     ถ้ายังไม่มีรูปจริง ให้เขียน `alt` แบบไม่อ้างความเป็นเจ้าของ (ดูหมายเหตุภาพสต็อก 6 ใบข้างบน)
   · **อนุญาต — คลิป/ภาพเชิงแบรนด์และโฆษณาที่ไม่ได้อ้างว่าเป็นหลักฐาน** เช่น พรีเซนเตอร์พูดอธิบายบริการ
     กราฟิกอธิบายขั้นตอน ภาพประกอบเชิงแนวคิด · ใช้ AI สร้างได้ **ภายใต้ 3 เงื่อนไข**:
     1. **ใบหน้าที่ใช้ต้องได้รับความยินยอมจากเจ้าของใบหน้าก่อนเสมอ (PDPA)** — รวมถึงกรณีสร้างจากรูปถ่ายจริง
        เจ้าของสั่งใช้อย่างเดียวไม่พอ ต้องเป็นความยินยอมของ**คนในรูป**
     2. **ห้ามให้พรีเซนเตอร์พูดอ้างข้อเท็จจริงที่ตรวจสอบไม่ได้** เช่น ตัวเลขลูกค้า สถิติ หรือคำรับประกันผล
        พูดได้เฉพาะสิ่งที่บริษัททำจริงและมีหลักฐานรองรับ (ใบอนุญาต · บริการที่มีอยู่จริง)
     3. **ห้ามทำให้ดูเหมือนฟุตเทจหน้างานจริง** — ฉากต้องอ่านออกว่าเป็นสตูดิโอ/กราฟิก ไม่ใช่การถ่ายทำในสนาม
   · **ของที่ใช้อยู่จริงตอนนี้** (ทั้งคู่เข้าเงื่อนไขข้อ "อนุญาต" ข้างบน):
     `video/nj-intro.mp4` พรีเซนเตอร์ผู้ชาย = ภาพลักษณ์ของเจ้าของบริษัทเอง ·
     `video/teedinsure-ad.mp4` พรีเซนเตอร์ผู้หญิง = เจ้าของยืนยันความยินยอมของเจ้าของภาพแล้ว 2026-09-07
     · ทั้งสองคลิปสร้างด้วย Airova (omni_flash) จากรูปถ่ายจริง — **ไม่ใช่ฟุตเทจหน้างาน**
11. **ราคารวมที่ฟอร์มคำนวณให้เป็นค่าประมาณ ห้ามนำเสนอเป็นราคาที่ตกลงแล้ว**
   หน่วยที่ดินไทย: 1 ไร่ = 4 งาน = 400 ตร.ว. · ถ้าเจ้าของกรอกราคาต่อหน่วยแต่ยังไม่กรอกเนื้อที่
   ต้องขึ้นข้อความว่ายังคำนวณไม่ได้ **ห้ามเดาเป็นราคารวม** เพราะจะกลายเป็นตัวเลขผิดที่ดูเหมือนตัวเลขจริง
   และเซิร์ฟเวอร์คำนวณราคารวมใหม่เองเสมอ ไม่เชื่อ `estValue` ที่หน้าเว็บส่งไป

12. **เครื่องคำนวณค่าโอน (`feecalc.js`) ห้ามเติมราคาประเมินราชการให้อัตโนมัติ**
   · **ตอนนี้อยู่ 3 หน้า**: `land.html` (mount เอง ใต้ราคา) · `guides.html#valuation` · `index.html` (หน้าแรก)
     หน้าแรกใช้ `bootPage()` ในตัว `feecalc.js` เอง — ปุ่ม `[data-fc-open]` ในแถบบริการกางกล่อง `#fee-calc`
     mount ครั้งเดียวตลอดอายุหน้า ถ้า mount ซ้ำตัวเลขที่ผู้ใช้กรอกค้างไว้จะหายหมด
   · **`bootPage()` ต้องมี `typeof document !== 'undefined'` ครอบเสมอ** — `feecalc.test.js` รันไฟล์นี้ด้วย
     node ที่ไม่มี DOM ถอดเงื่อนไขออกเมื่อไหร่เทสต์ 50 ข้อพังทันทีตั้งแต่บรรทัดแรก (เคยพลาดมาแล้ว)
   ค่าธรรมเนียมโอนคือ 2% ของราคาประเมิน ใครเห็นตัวเลขค่าโอนของแปลงก็หารกลับได้ราคาประเมินทันที
   ถ้าเติมให้ = เท่ากับประกาศราคาประเมินของแปลงนั้นต่อสาธารณะ เจ้าของที่ดิน (ลูกค้าเรา) เสียเปรียบตอนต่อรอง
   ระบบหลังบ้านตั้งใจไม่ส่งราคาประเมินออกทาง `/api/public/*` อยู่แล้ว (ดู `publicLand()` ใน `server.js`)
   · หน้าเว็บเติมให้แค่ "ราคาซื้อขาย" ซึ่งเป็นตัวเลขที่ประกาศอยู่แล้ว
   · ไม่กรอกราคาประเมิน = ใช้ราคาขายแทน ได้ตัวเลข**สูงกว่าความจริง** ต้องบอกผู้ใช้ตรงๆ ทุกครั้ง
   · **ที่ดินเปล่าไม่ได้สิทธิลดค่าโอนเหลือ 0.01%** (มาตรการนั้นใช้เฉพาะที่อยู่อาศัย ≤ 7 ล้านบาท)
     ห้ามคำนวณให้เป็น 0.01% เพราะผู้ซื้อจะไปเจอตัวเลขจริงที่สำนักงานที่ดินแล้วดีลพัง
   · แก้สูตรภาษีเมื่อไหร่ ต้องรัน `node feecalc.test.js` ให้ผ่านครบก่อนเสมอ (50 ข้อ เทียบกับการคำนวณมือ)

14. **เครื่องประเมินราคารังวัด (`surveyquote.js`) ต้องถามจังหวัดเสมอ และต้องบวกค่าดำเนินการนอกพื้นที่**
   (เพิ่ม 2026-09-08 · อยู่ 2 ที่: กล่อง "เช็กราคางานรังวัด" บนหน้าแรก และกล่องประเมินในหน้าฝากขาย)
   · **เคสจริงที่ทำให้ต้องเขียนกติกานี้:** โซนเดินทาง 7 ขั้นขึ้นระบบหลังบ้าน 8 ก.ย. 2569 แต่เว็บ
     ไม่ได้บวกให้เลย — 1 ไร่ สอบเขต จ.เชียงใหม่ เว็บบอก **฿25,000** ขณะที่ใบเสนอราคาจริงคือ
     **฿97,000** (ต่ำไป 72,000) ลูกค้าที่เชื่อตัวเลขนี้แล้วมาคุยกับเซลส์ = ดีลพังตั้งแต่ยังไม่เริ่ม
   · **ตารางโซนและรายชื่อจังหวัดอ่านจาก `pricing.js` เท่านั้น** (`P.zoneOf` · `P.TRAVEL_ZONES` ·
     `P.ZONE_PROVINCES`) กติกาเดียวกับตารางราคา — ห้ามก๊อปมาไว้ฝั่งเว็บ **รวมถึงตัวเลขในข้อความ**
     ช่วง "12,000–75,000" ต้องมาจาก `travelRangeText()` ไม่ใช่พิมพ์ลงไปในประโยค
   · **ต้องส่ง `nights` ไปด้วยเสมอ = จำนวนคืนที่แนะนำของโซนนั้น** ให้ตรงกับ `travelNightsDefault()`
     ในฟอร์มใบเสนอราคาของระบบหลังบ้าน · ไม่ส่ง = `computeTravel` คิดคืนพักเป็น 0
     แล้วเว็บบอกราคาต่ำกว่าใบจริง 4,000–16,000 บาทแบบเงียบๆ
   · **ส่วนลดคอมโบ 5% ห้ามกินค่าเดินทาง** — คิดจาก `comboBase()` ที่หัก `travelTotal` ออกแล้ว
     กติกาเดียวกับที่ `pricing.js` กันส่วนลดลูกค้าเดิม 40% ออกจากค่าเดินทาง
     (ค่าโซน = ค่าน้ำมัน ค่าที่พัก ค่าเสียเวลาทีมบนถนน — เป็นเงินสดที่จ่ายออกจริง ลดแล้วขาดทุนจริง)
   · **ยังไม่เลือกจังหวัด = ต้องขึ้นคำเตือนว่ายอดนี้ยังไม่รวมค่าเดินทาง** ห้ามปล่อยให้อ่านเหมือนราคาครบ
   · หมายเหตุที่ลูกค้าเห็นต้องบอกด้วยว่าค่าโซนคิดเป็นขั้นตาม**จังหวัด** ไม่ได้วัดจากที่ตั้งแปลงจริง
   · ล็อกไว้ด้วย `node contracts.test.js` หัวข้อ 6 (9 ข้อ) ซึ่ง `require('pricing.js')` มาคิดจริง

13. **เครื่องคำนวณรองรับสิ่งปลูกสร้างแล้ว — 2 ค่านี้ห้ามคำนวณให้เองโดยผู้ใช้ไม่ได้สั่ง**
   · **อัตราลดค่าธรรมเนียมตามมาตรการรัฐ** ค่าตั้งต้นคือ **2% ตามกฎหมาย** เสมอ
     ผู้ใช้ต้องติ๊กเองถึงจะลดเหลือ 0.01% (หรือ 1%) — เพราะแหล่งข้อมูลสาธารณะ ณ ส.ค. 2569
     ยังขัดกันเองทั้งวันสิ้นสุด (30 มิ.ย. 2569 หรือ 2570) และอัตรา (0.01% หรือ 1%)
     ล็อกไว้ด้วยว่าติ๊กได้เฉพาะประเภทที่เป็น "ที่อยู่อาศัย" (`home:true` ใน `TYPES`)
     ที่ดินเปล่าและโรงเรือน/โกดังติ๊กแล้วต้องไม่มีผล — มีเทสต์คุมไว้
   · **ราคาประเมินสิ่งปลูกสร้าง** ให้กรอกเองเป็นช่องหลัก ตัวช่วยประมาณเป็นทางเลือกรอง
     เรตต่อ ตร.ม. **เป็นบัญชีจริงของกรมธนารักษ์แล้ว** (2026-08-30) โหลดจาก
     `https://app.njteedinsure.com/buildingprice.json` — 67 ประเภท × 77 จังหวัด
     สร้างด้วย `node landprice/refresh-building.js` ใน repo nj-survey-system
     · ราคาต่างกันตามจังหวัด **จึงบังคับให้เลือกจังหวัดก่อน** ไม่มีค่ากลางทั้งประเทศให้ใช้
     · โหลดบัญชีไม่ได้ หรือไม่เลือกจังหวัด = ประมาณไม่ได้ ต้องกรอกราคาเอง **ห้าม fallback เป็นตัวเลขที่เดา**
     · **ค่าเสื่อมตามอายุยังเป็นค่าประมาณ** (`decay`/`cap` ใน `TYPES`) เพราะกรมธนารักษ์
       ไม่ได้เปิดตารางหักค่าเสื่อมเป็นข้อมูลเปิด — คำเตือนที่ขึ้นต้องพูดถึงเฉพาะค่าเสื่อม
       ไม่ใช่บอกว่าราคาทั้งก้อนเป็นค่าประมาณ เพราะจะทำให้ผู้ใช้ไม่เชื่อตัวเลขที่จริงอยู่แล้ว
     · เทสต์ใส่บัญชีราคาเองผ่าน `setBuildingPrices()` จะได้ไม่ต้องต่อเน็ตและผลไม่เปลี่ยนตามรอบบัญชี
   · ยกเว้นภาษีธุรกิจเฉพาะเมื่อมีชื่อในทะเบียนบ้าน ≥ 1 ปี ใช้ได้เฉพาะที่อยู่อาศัยและบุคคลธรรมดา
     ที่ดินเปล่าไม่มีทะเบียนบ้าน ติ๊กแล้วต้องไม่มีผล

## กับดักที่เคยเจอมาแล้ว

- **เขียน `textContent` ทับด้วยค่าเดิม ไม่ใช่ "การไม่เปลี่ยนอะไร" — มันคือลบ text node แล้วสร้างใหม่**
  ซึ่งนับเป็น childList mutation · จับคู่กับ `MutationObserver` ที่เฝ้า `childList + subtree`
  ของ element เดียวกันเมื่อไหร่ = **ลูปไม่รู้จบ กินซีพียู 100% แท็บค้างจนต้องปิด**
  · เจอจริง 2026-09-06 ใน `compare.js`: `decorate()` แปะปุ่มลงใน `#listing-grid` แล้วเฝ้าตะแกรงเดียวกัน
    `syncMarks()` เขียนป้ายปุ่มทับด้วยค่าเดิม → observer ยิง → วน · **หน้าแรกกับหน้ารวมประกาศ
    ค้างทั้งสองหน้าตั้งแต่ประกาศโหลดเสร็จ** (หน้าอื่นไม่เป็นเพราะไม่มี `#listing-grid`)
  · อาการหลอกมาก: ไม่มี error ใน console เลย · พาเนลเบราว์เซอร์ในเครื่องมือก็แค่ "timeout"
    ซึ่งอ่านเหมือนเครื่องมือเสีย ไม่ใช่เว็บเสีย — เสียเวลาไล่ผิดทางอยู่นาน
  · แก้ 2 ชั้น: (1) เทียบค่าเดิมก่อนเขียนเสมอ (2) `mo.disconnect()` ก่อนแก้ DOM แล้วค่อย observe ใหม่
  · **มีเทสต์คุมไว้แล้ว — `node compare.test.js`** ยืนยันแล้วว่าเวอร์ชันเก่าทำเทสต์แดง 6 ข้อ

- **`[hidden]` ใช้ไม่ได้กับ element ที่ถูกตั้ง `display` ในสไตล์ชีตของเรา** — `.cs-field{display:grid}`
  ชนะกฎ `[hidden]{display:none}` ของเบราว์เซอร์ ทำให้ช่องที่ตั้งใจซ่อนไว้โผล่ขึ้นเว็บจริง
  แก้ด้วย `[hidden]{display:none !important}` ใน `ui.css` แล้ว — อย่าลบกฎนี้ทิ้ง
  · **เจอซ้ำอีกครั้ง 2026-08-30** ที่ `.fc-rate{display:flex}` ใน `feecalc.css`
    หน้าที่โหลด `feecalc.css` โดยไม่ได้โหลด `ui.css`/`home.css` จะไม่มีกฎ `!important` นั้น
    จึงต้องประกาศซ้ำไว้ใน `feecalc.css` เอง — อย่าลบออก

- **หลายเซสชันแก้ repo นี้พร้อมกัน — ตรวจ `git diff --stat` ก่อนเชื่อว่า working tree เป็นของเราคนเดียว**
  (2026-09-06 เจอจริง: กำลังจะ commit ปุ่ม hero แล้วพบว่า `index.html` มีงานแถบ "บริการเพิ่มเติม"
  ของอีกเซสชันเพิ่มเข้ามาระหว่างทาง รวม 12 ไฟล์ที่เขายังทำไม่เสร็จ)
  · คัดเฉพาะก้อนของตัวเองเข้า commit — `git commit -a` จะลากงานที่เขายังทำไม่เสร็จติดไปด้วย
  · ⚠️ **`git apply --cached --unidiff-zero` วางบรรทัดผิดตำแหน่งได้** patch แบบ `-U0` ไม่มีบริบท
    ให้ git ยึด รอบนี้บรรทัดไปโผล่นอกแถวปุ่ม 3 บรรทัด **ต้องอ่าน `git diff --cached` ยืนยันตำแหน่ง
    ก่อน commit เสมอ อย่าเชื่อว่า apply ผ่านแล้วถูก**
  · วิธีที่ชัวร์กว่าเมื่อของเราเป็นการแก้ไม่กี่บรรทัด: ประกอบเนื้อไฟล์ = `git show HEAD:<ไฟล์>`
    บวกบรรทัดของเรา แล้ว `git hash-object -w --stdin` + `git update-index --cacheinfo`
    — stage ได้ตรงเป๊ะโดยไม่แตะไฟล์ในโฟลเดอร์ทำงานที่อีกเซสชันกำลังใช้อยู่
  · **repo นี้เก็บไฟล์เป็น LF แต่ `core.autocrlf=true` แปลงเป็น CRLF ในโฟลเดอร์ทำงาน**
    สคริปต์ที่แก้ไฟล์ในโฟลเดอร์ต้องใช้ตัวจบบรรทัดแบบ CRLF ส่วนสคริปต์ที่สร้าง blob ให้ git
    ต้องใช้ LF — ใช้ผิดตัวแล้ว anchor จะหาไม่เจอเงียบๆ

## Design tokens

**เว็บมี 2 ชุดสีอยู่พร้อมกันตอนนี้** เพราะหน้าแรกเปลี่ยนดีไซน์ก่อนหน้าอื่น (2026-08-29)
หน้าอื่นยังเป็นชุดเดิม — จะไล่เปลี่ยนตามทีหลังหรือคงไว้ ให้ตัดสินใจก่อนแก้ ไม่ใช่เผลอผสมสองชุดในหน้าเดียว

ชุดใหม่ — หน้าแรก (`home.css`)
- Ink `#14170F`: พื้นเข้ม/ตัวหนังสือหลัก
- Gold `#DFC98F` บนพื้นเข้ม · Bronze `#8A6A2E` บนพื้นสว่าง · Brass `#C0A163` เส้นขอบ hover
- Paper `#F7F4EE` พื้นหลังหน้า · Sand `#EDE8DD` พื้นส่วนบริการ
- Thai/Latin: Sarabun (300/400/500) · หัวข้อ: Trirong (400/500)
- ไม่มี border-radius — ดีไซน์นี้ใช้มุมเหลี่ยมทั้งหมด

ชุดเดิม — `guides.html` `land.html` `videos.html` `consign.html` (`marketplace.css`, `ui.css`)
- Navy `#0B2549`: ความน่าเชื่อถือและข้อความหลัก (หน้ารอง)
- **หน้าแรกใช้ชุดสีของตัวเองใน `home.css`** โทนน้ำเงินเข้ม–ทอง (เปลี่ยนจากครีม–ทอง 2026-08-30)
  `--ink:#0B1F3A` · `--gold:#D8B96B` · `--bronze:#1B4B7A` · `--paper:#F4F7FA`
  · สีฝังเป็น inline style ใน `index.html` เยอะมาก เปลี่ยนโทนทีต้องแก้ทั้ง 2 ไฟล์ให้ตรงกัน
  · `s/share.css` คัดลอกตัวแปรสีไว้เอง **ยังเป็นโทนเดิมอยู่** ถ้าจะให้ทั้งเว็บโทนเดียวกันต้องแก้ไฟล์นั้นด้วย
- Green `#1CA45C`: verified/action
- Mist `#F4F7F8`: พื้นหลังตลาด
- Thai font: IBM Plex Sans Thai
- Latin numbers: Manrope
- Card radius: 12px; panel radius: 16px

## การทดสอบขั้นต่ำ

- **`node compare.test.js` ต้องผ่านครบก่อนเสมอ** (10 ข้อ · เพิ่ม 2026-09-06)
  ล็อกบั๊กลูป MutationObserver ที่เคยทำให้ 2 หน้าค้างทั้งแท็บ (ดู "กับดักที่เคยเจอมาแล้ว")
  · ใช้ DOM ปลอมที่เขียนเองใน `node:vm` **ไม่พึ่งไลบรารีภายนอก** — repo นี้เป็นเว็บสแตติกล้วน
    ไม่มี `package.json` และไม่ควรมี `node_modules` เพียงเพื่อเทสต์ไม่กี่ข้อ
- **`node contracts.test.js` ต้องผ่านครบก่อนเสมอ** (27 ข้อ · เพิ่ม 2026-09-06 · +9 ข้อเรื่องโซนเดินทาง 2026-09-08)
  เทียบ "รายการค่าตายตัวที่ผูกกันข้าม repo" ระหว่างเว็บกับ `nj-survey-system`:
  ชนิดเหตุการณ์สถิติ · คีย์บริการเพิ่มเติม · จังหวัดที่บังคับรังวัด · `LAND_DEEDS`/`LAND_ZONES`/`PARCEL_FEATURES`
  · เขียนขึ้นเพราะความผิดพลาดที่แพงที่สุดของโปรเจกต์นี้ไม่ใช่โค้ดพัง แต่คือ
    **รายการสองฝั่งเลื่อนออกจากกันโดยไม่มี error ให้เห็น** (เคยเกิดจริงกับ `messenger_click` 2026-08-28)
  · หา `nj-survey-system` จาก `../nj-survey-system` · อยู่ที่อื่นให้ส่ง path เป็นอาร์กิวเมนต์แรก
    ไม่เจอ = ข้ามเงียบๆ (exit 0) ไม่ใช่ล้มเหลว — คนที่เช็คเอาต์เฉพาะ repo เว็บก็ยังรันเทสต์อื่นได้
- เปิดผ่าน local HTTP server (ไม่เปิดด้วย `file://`)
- ทดสอบที่ความกว้าง 390px, 768px และ 1440px
- **ทุกหน้าต้องมีทางเข้าเมนูบนจอเล็ก** — ย่อจอให้แคบกว่าจุดตัดของหน้านั้นแล้วปุ่มแฮมเบอร์เกอร์ต้องโผล่
  และรายการในลิ้นชักต้องตรงกับเมนูเดสก์ท็อปของหน้านั้นเป๊ะ (`menu.js` สร้างจาก `<nav>` จริง)
- ทดสอบค้นหา กรองราคา ปุ่มบันทึก โทร LINE และลิงก์ฝากขาย
- **หน้าฝากขาย**: เลือกจังหวัดในเขตบังคับ (เช่น กรุงเทพมหานคร) แล้วตัวเลือกรังวัดต้องล็อกเหลือ "ต้องการรังวัด"
  · เลือกจังหวัดนอกเขต (เช่น ระยอง) แล้วต้องปลดล็อก + ขึ้นตารางข้อดี–ข้อแลกเปลี่ยน
  · ใส่เนื้อที่แล้วต้องขึ้นค่ารังวัดประมาณการจาก `pricing.js` จริง (ไม่ใช่ตัวเลขที่ฝังไว้)
- **หน้าเปรียบเทียบ**: กด "เทียบ" บนการ์ดแล้วต้อง **ไม่** เด้งเข้าหน้ารายละเอียด (ดูเหตุผลที่ `compare.js`)
- ตรวจ console และ network error · **API ล่มต้องแสดงว่า "โหลดไม่สำเร็จ" ไม่ใช่แสดงแปลงตัวอย่าง**
  (คนละกรณีกับ API ตอบ count: 0 ซึ่งต้องแสดงกล่อง "ยังไม่มีแปลงประกาศ" — สองอย่างนี้ห้ามสลับกัน)

## Teedin Sure Verified — บันได 5 ระดับ · รายงานสุขภาพแปลง · แผนที่แนวเขต (2026-09-09 · Phase 1)

branch `phase1/verified-status` · ✅ **merge เข้า main และขึ้นเว็บจริงแล้ว** (ตรวจกับ git และเว็บจริง 2026-09-11)

### ⚠️ กติกาข้อแรกของทั้งระลอกนี้ — แปลงเก่าต้องหน้าตาเหมือนเดิมเป๊ะ

ทั้งสามแผงคืน **สตริงว่าง** เมื่อ API ไม่ได้ส่งข้อมูลมา → หน้าเว็บซ่อนทั้งหัวข้อ (กติกาข้อ 5)
**ห้ามเปลี่ยนเป็นวาดกล่องว่างพร้อมข้อความ "ยังไม่มีข้อมูล"** — บันไดว่าง 5 ขีดสีเทาอ่านแล้วเหมือน
แปลงถูกตัดสินไปแล้ว ทั้งที่ความจริงคือยังไม่มีใครไปตรวจ · แปลงที่ประกาศอยู่ตอนนี้อยู่ในกลุ่มนี้ทั้งหมด

⚠️ **ตารางสุขภาพกั้นด้วย `L.health` ไม่ใช่กั้นด้วย "ประกอบแถวสีเขียวได้ไหม"** — แปลงเก่ามี
`deedType`/`deedArea`/`checks` อยู่แล้วทุกแปลง ถ้ากั้นแบบหลัง ตารางจะโผล่ทุกแปลงในวันที่ deploy
ทั้งที่ยังไม่มีใครไปตรวจอะไรเพิ่ม และซ้ำกับแผงผลตรวจ 7 หัวข้อเดิมที่อยู่ล่างลงไป
(กรอกช่องไหนก็ได้แค่ช่องเดียวฝั่งพนักงาน ตารางก็ขึ้นทั้งใบพร้อมข้อมูลเดิมที่มีอยู่แล้ว)

### ไฟล์ใหม่

| ไฟล์ | ทำอะไร |
|---|---|
| `landvocab.js` | **คำศัพท์ชุดกลาง** (`window.NJVocab`) — ไฟล์ใหม่ทุกไฟล์ให้ใช้ชุดนี้ ห้ามก๊อปเพิ่ม |
| `verified.js` / `verified.css` | บันได 5 ระดับ (`NJVerified.ladderHtml` / `badgeHtml`) |
| `health.js` / `health.css` | รายงานสุขภาพแปลง 14 หัวข้อ 4 สถานะ (`NJHealth.tableHtml`) |
| `parcelmap.js` / `parcelmap.css` | แผนที่แนวเขต SVG (`NJParcelMap.mapHtml` / `init`) |
| `verified.test.js` | 58 ข้อ · รันด้วย `node verified.test.js` |

⚠️ **ยังมีสำเนาคำศัพท์เดิมอยู่ใน `listings.js` และ `comparepage.js`** (เขียนไว้ก่อน `landvocab.js` จะมี)
`contracts.test.js` หัวข้อ 7 เทียบสำเนาทั้งหมดให้แล้ว — แก้ที่ `landvocab.js` แล้วเทสต์จะบอกเองว่าต้องตามแก้ที่ไหนอีก

### ลำดับการโหลดสคริปต์ (ห้ามสลับ)

`land.html`: `landvocab.js` → `verified.js` → `health.js` → `parcelmap.js` → `land.js`
· `health.js` อ่านคำศัพท์จาก `NJVocab` · `land.js` เรียกทั้งสามตัวตอนวาดหน้า
`index.html` / `listings.html`: `verified.js` **ต้องมาก่อน** `listingcard.js` (ป้ายระดับบนการ์ด)

### แผนที่แนวเขตเป็น SVG ล้วน ไม่เพิ่มไลบรารีแผนที่

เหตุผล: (1) กติกาข้อ 10 ห้ามเพิ่มไลบรารีใหญ่โดยไม่จำเป็น ทั้งเว็บไม่มีไลบรารี JS เลยสักตัว
(2) **โหมดเริ่มต้นที่เซิร์ฟเวอร์ส่งมาไม่มีพิกัดเลย** (ส่งเป็นระยะเป็นเมตรจากจุดกึ่งกลาง เพื่อไม่ให้
แนวเขตระดับรังวัดหลุดสู่สาธารณะ) ไม่มีพิกัด = วางบนกระเบื้องแผนที่ไม่ได้อยู่แล้ว

- วาดรูปแปลง หมุดพร้อมเลขกำกับ ความยาวแต่ละด้าน ไม้บรรทัด และเข็มทิศ
- **หมุดเป็น `<button>` จริง** ไม่ใช่จุดเล็กๆ บน SVG ที่ต้องจิ้มให้แม่น — ได้การใช้คีย์บอร์ดและ
  การแตะบนมือถือมาฟรี · กดแล้วเปิดแผงรูป/หมายเหตุช่าง/วันที่สำรวจ
- ⚠️ **ห้ามคำนวณความยาวด้าน/พื้นที่/การเรียงหมุดเองที่ฝั่งนี้** เซิร์ฟเวอร์คิดมาให้แล้ว
  คิดซ้ำ = สองที่ปัดเศษไม่เหมือนกัน แล้วตัวเลขบนรูปกับในตารางไม่ตรงกันโดยไม่มีอะไรเตือน
- ⚠️ **y ของ SVG ชี้ลง แต่ y ของเราคือทิศเหนือซึ่งชี้ขึ้น ต้องกลับด้าน** ไม่กลับ = รูปแปลงพลิกบน-ล่าง
  ซึ่งดูเหมือนถูกจนกว่าจะเอาไปเทียบกับของจริง
- ⚠️ **`.njp-svg` ต้องมีเพดานความสูง** แปลงแคบยาว (พบบ่อยมาก เช่นแปลงริมถนน) จะได้ SVG
  สูงหลายพันพิกเซล แล้วดันทุกอย่างใต้แผนที่ตกจอไปหมด
- ข้อจำกัดที่บอกผู้ใช้ตรงๆ: ไม่มีชั้นภาพถ่ายดาวเทียม ไม่มีถนนโดยรอบ และไม่ได้วางทับพิกัดจริงบนโลก

### รายงานสุขภาพแปลง — 4 สถานะ ทุกสถานะมีข้อความกำกับ

เขียว ตรวจแล้วไม่พบประเด็นสำคัญ · เหลือง ต้องตรวจเพิ่มเติม · แดง พบประเด็นที่ผู้ซื้อควรทราบ · เทา ยังไม่มีข้อมูล

- ⚠️ **ห้ามใช้สีอย่างเดียว** ถอดสีออกทั้งหมดแล้วตารางต้องยังอ่านรู้เรื่องครบทุกแถว
  (`verified.test.js` นับจำนวนแถวเทียบกับจำนวนข้อความสถานะให้ตรงกัน)
- ⚠️ **เทา = "ยังไม่มีข้อมูล" ไม่ใช่ "ไม่มี"** ข้อความต้องเขียนให้อ่านออกว่าเรายังไม่รู้
- ⚠️ **ที่ตาบอด (`access:'none'`) และแนวรุกล้ำ (`structures:'encroach'`) ต้องขึ้นแดงเสมอ**
  เป็นข้อมูลที่กระทบการตัดสินใจซื้อโดยตรงที่สุดในตารางนี้ · ล็อกไว้ทั้ง `verified.test.js` และ `contracts.test.js`

### บันได 5 ระดับ

- ใช้ `<details>`/`<summary>` ของเบราว์เซอร์เอง ไม่เขียนตัวกาง/หุบเอง — ได้คีย์บอร์ดและ
  การอ่านออกเสียงมาฟรี และไม่มีอะไรพังเมื่อ JS โหลดไม่สำเร็จ
- แถวที่ `status==='issue'` **กางไว้ตั้งแต่แรก** — ประเด็นที่ผู้ซื้อควรทราบต้องไม่ต้องกดหา
- **รายการ "ยังไม่ได้ตรวจ" สำคัญพอๆ กับรายการที่ตรวจแล้ว ห้ามซ่อนเพื่อให้หน้าดูสวย**
- ป้ายบนการ์ด (`badgeHtml`) **ไม่ขึ้นเมื่อยังไม่ถึงระดับใดเลย** — ป้าย "0/5" อ่านแล้วแย่กว่าไม่มีป้าย
  และแปลงที่ทีมยังไม่ได้ไล่กรอกจะดูเหมือนถูกตัดสินไปแล้ว
- ป้ายนี้ **ไม่ได้แทนที่** ป้ายเหลือง-เขียวเดิม สองอย่างตอบคนละคำถาม
  (เดิม = "รังวัดแล้วหรือยัง" · ใหม่ = "ตรวจไปแล้วกี่ระดับจาก 5")
- ⚠️ **ข้อความปิดท้ายของทั้งสามแผงห้ามถอด** เป็นเส้นแบ่งระหว่าง "เราตรวจอะไรไปแล้ว" กับ "เรารับประกันอะไร"
  `contracts.test.js` ล็อกไว้ว่าสามไฟล์นี้ห้ามมีคำรับประกัน

### หน้ารายละเอียดเปลี่ยนมาใช้เส้นทางแปลงเดียว

`land.js` → `GET /api/public/listings/:id` (เดิมโหลด `/api/public/listings` ทั้งก้อนแล้วคัดเอาแปลงเดียว
= ดาวน์โหลดข้อมูลของทุกแปลงทั้งเว็บทุกครั้งที่มีคนเปิดหน้าเดียว) · **ห้ามกลับไปโหลดทั้งก้อนอีก**
· 404 = แปลงถูกถอด/ขายแล้ว ซึ่งคนละเรื่องกับ "โหลดไม่สำเร็จ" ต้องขึ้นคนละข้อความ

### `?api=` — ชี้ API ไปเซิร์ฟเวอร์ทดสอบบนเครื่อง

เติม `?api=http://127.0.0.1:8792` ตอนเปิดจาก localhost (แนวเดียวกับ `?photos=dev`)
⚠️ **ทำงานเฉพาะ localhost และรับเฉพาะที่อยู่ที่ชี้กลับมาที่เครื่องเอง** เปิดกว้างกว่านี้ =
ใครส่งลิงก์ `njteedinsure.com?api=<เว็บของเขา>` ให้ลูกค้า แล้วหน้าเว็บของเราจะดึงข้อมูลแปลง
จากเซิร์ฟเวอร์ของคนอื่นมาแสดงในนามเรา

### แก้เรื่องที่ค้างมาก่อนหน้านี้ไปด้วย

`land.css` และ `compare.css` **ไม่เคยมีกฎ `:focus` เลยสักข้อ** ทั้งที่มีปุ่มควบคุมสไลด์ภาพ จุดนำทาง
ปุ่มคัดลอกรหัสทรัพย์ ปุ่มดูเบอร์ ปุ่มดาวน์โหลด PDF และปุ่ม "เทียบ" — คนที่เดินหน้าเว็บด้วยคีย์บอร์ด
มองไม่ออกเลยว่าตอนนี้โฟกัสอยู่ที่ปุ่มไหน · เพิ่ม `:focus-visible` แล้ว (ไม่ใช่ `:focus` เพราะกรอบ
จะเด้งตอนคลิกเมาส์ ซึ่งเป็นเหตุผลที่คนมักถอดกฎนี้ทิ้ง)

### ทดสอบแล้ว

`node verified.test.js` 58 ข้อ · `node contracts.test.js` 51 ข้อ (เพิ่มหัวข้อ 7 · 24 ข้อ) ·
`compare/feecalc/valuecalc` ชุดเดิมยังผ่านครบ
· เบราว์เซอร์จริง 3 แปลง (ครบทุกอย่าง / มีบางส่วน / แปลงเก่าไม่มีข้อมูลใหม่) ที่ 375px และ 1000px:
ไม่ล้นแนวนอน · ไม่มี console error · **แปลงเก่าไม่มีแผงใหม่โผล่เลยแม้แต่แผงเดียว**

## นัดตรวจแปลงก่อนซื้อ · ติดตามงานซื้อ 11 ขั้น (2026-09-10 · Phase 2)

branch `phase2/web` · ✅ **merge เข้า main และขึ้นเว็บจริงแล้ว** (ตรวจ 2026-09-11) · ตอน deploy ต้องขึ้นฝั่ง `nj-survey-system`
(branch `phase2/foundation`) **ก่อน** เสมอ เพราะหน้าใหม่ทั้งสองเรียกเส้นทาง API ที่เพิ่งเพิ่ม

### ไฟล์ใหม่

| ไฟล์ | ทำอะไร |
|---|---|
| `inspect.html` / `inspect.js` / `inspect.css` | ฟอร์มขอนัดตรวจแปลง **และ** หน้าติดตามคำขอ — ไฟล์เดียวกัน |
| `deal.html` / `deal.js` / `deal.css` | ผู้ซื้อติดตามเส้นทาง 11 ขั้นของตัวเอง |

### หน้าเดียวทำสองหน้าที่ — ตัดสินจาก query string

`inspect.html` ไม่มี `?id=&t=` = ฟอร์มขอนัด · มีครบทั้งคู่ = หน้าติดตามสถานะ
· โหมดติดตามใส่คลาส `ins-tracking` ที่ `<body>` แล้ว CSS ซ่อนข้อความขายกับ FAQ ทิ้ง
(คนที่เปิดลิงก์ติดตามส่งคำขอมาแล้ว ข้อความชวนให้ส่งคำขอไม่ใช่สิ่งที่เขามาหา)

### ⚠️ กติกาที่ห้ามผ่อน

1. **ห้ามมีฟอร์มเข้าสู่ระบบในทั้งสองหน้าเด็ดขาด** — กติกาเดียวกับ `portal.html`
   เว็บนี้เป็นสแตติกบน GitHub Pages ไม่มีฝั่งเซิร์ฟเวอร์ที่ถือ token ได้ปลอดภัย
   และการรับรหัสผ่านที่โดเมนหนึ่งแล้วส่งข้ามไปอีกโดเมน = สอนลูกค้าให้กรอกรหัสในที่ที่ไม่ใช่ระบบจริง
   ทางเข้ามีทางเดียวคือ **ตั๋วในลิงก์** (`?t=`) ที่ได้ตอนส่งคำขอ หรือที่ทีมงานส่งให้ทางไลน์
2. **รายการบริการ 5 แบบต้องตรงกับ `LEAD_SERVICES` ใน `lib/leads.js` เป๊ะ**
   และ `SOURCES` ต้องตรงกับ `LEAD_SOURCES` — เซิร์ฟเวอร์ทิ้งคีย์ที่ไม่รู้จักโดยไม่มี error
   (`contracts.test.js` หัวข้อ 8 เทียบให้ทุกครั้งที่รัน)
3. **ไม่ติ๊กยินยอม PDPA = ส่งไม่ได้** ปิดที่ปุ่มด้วย และเซิร์ฟเวอร์ปฏิเสธซ้ำอีกชั้น
   สองชั้นเพราะชั้นหน้าจอมีไว้บอกผู้ใช้ ส่วนชั้นเซิร์ฟเวอร์มีไว้บังคับจริง
4. **บังคับกรอกแค่ชื่อกับเบอร์** (กติกาเดียวกับหน้าฝากหาที่ดิน) — คนที่ยังไม่รู้ว่าอยากให้ตรวจอะไร
   ก็ยังเป็นลีดที่คุยต่อได้ · บังคับครบทุกช่อง = เขากดถอยตั้งแต่ยังไม่ได้คุยกับใคร
5. **ต้องเขียนให้ชัดว่ายังไม่มีค่าใช้จ่ายและยังไม่ถือเป็นการว่าจ้าง** (`.ins-free`)
   ระบบยังไม่มีการชำระเงิน ถ้าไม่บอก คนกดส่งแล้วกลัวว่าจะถูกเรียกเก็บเงิน
6. **ขั้นที่ยังไม่ถึงใน `deal.js` ต้องขึ้นว่า "ยังไม่ถึงขั้นนี้" ห้ามเขียนว่า "รอ"**
   กติกาเดียวกับผลตรวจ 7 หัวข้อและบันได 5 ระดับ — "ยังไม่ถึงคิว" กับ "กำลังทำอยู่" คนละเรื่อง
   เขียนว่ารออยู่ = รับปากล่วงหน้าแทนสิ่งที่ยังไม่เกิด
7. **ตั๋วอยู่ใน query string จึงต้องกันไม่ให้หลุดไปกับหัว `Referer`** —
   ทั้งสองหน้ามี `<meta name="referrer">` และลิงก์ออกนอกเว็บทุกอันมี
   `rel="noopener noreferrer" referrerpolicy="no-referrer"` · `robots.txt` ก็กัน `?t=` ไว้ด้วย
   และ `deal.html` เป็น `noindex` ทั้งหน้า (เนื้อหาขึ้นกับตั๋ว ไม่ใช่หน้าสาธารณะ)

### 🐞 บั๊กที่เจอตอนทดสอบ — `progress` เป็นก้อน ไม่ใช่ตัวเลข

`GET /api/public/deal/:id` ส่ง `progress: {done, total}` มา · โค้ดรอบแรกอ่านเป็น `Number(dl.progress)`
ได้ `NaN` แล้วแถบความคืบหน้าเป็น **0% ตลอด โดยไม่มี error ให้เห็น**
· และ `done` ของเซิร์ฟเวอร์คือ "เสร็จติดต่อกันจากขั้นแรก" ซึ่งไม่เท่ากับจำนวนขั้นที่เสร็จทั้งหมด
**ใช้ค่าของเซิร์ฟเวอร์เสมอ ห้ามนับเอง** ไม่งั้นตัวเลขบนหน้าจอกับในระบบหลังบ้านไม่ตรงกัน

### ตารางเปรียบเทียบเพิ่ม 6 แถว

`comparepage.js` เพิ่ม: ระดับการตรวจสอบ (จาก 5 ระดับ) · ข้อมูลตรวจล่าสุด · สภาพแปลงจากรายงานสุขภาพ ·
สิ่งที่ยังต้องตรวจเพิ่ม · หน้ากว้าง × ลึก · ค่าใช้จ่ายวันโอน

- ⚠️ **หน้ารวมประกาศส่งข้อมูลมาแบบย่อ** (`slimLand` ตัด `health`/`plot` ทิ้ง และเหลือ `verify`
  แค่ `{reached,total}`) จึงต้อง `fetchDetails()` ดึงรายละเอียดของ **เฉพาะแปลงที่เลือกไว้**
  (สูงสุด 4 ใบ) เพิ่มอีกรอบ · แปลงใดโหลดไม่สำเร็จ ช่องของแปลงนั้นขึ้น "—" ไม่ใช่พังทั้งตาราง
- ⚠️ **แถวค่าใช้จ่ายวันโอนไม่ใส่ตัวเลขให้** ยื่นลิงก์ไปเครื่องคำนวณที่หน้าแปลงแทน —
  ค่าโอนคิดจากราคาประเมินราชการซึ่งเว็บนี้ตั้งใจไม่เผยแพร่ (กติกาข้อ 12)
- ⚠️ **ไม่คูณ หน้ากว้าง × ลึก เป็นพื้นที่** แปลงส่วนใหญ่ไม่ใช่สี่เหลี่ยม คูณแล้วได้ตัวเลขผิด
  ที่ดูเหมือนตัวเลขจริง · เนื้อที่จริงมีแถวของตัวเองอยู่แล้ว
- `compare.html` โหลด `landvocab.js` + `verified.js` เพิ่ม (คำศัพท์ชุดกลาง ห้ามก๊อปเป็นชุดที่สี่)

### แดชบอร์ดเจ้าของที่ดินเห็น "มีคนสนใจกี่ราย"

`consign.js` → `renderReport()` เพิ่มกล่อง "ผู้ซื้อที่ติดต่อเข้ามาผ่านระบบ" จาก `d.interest`
· ⚠️ **ตัวเลขล้วน** เซิร์ฟเวอร์ส่งมาแค่จำนวน ไม่มีชื่อ เบอร์ หรือข้อความของผู้ซื้อเลย
ผู้ซื้อยินยอมให้ "ทีมงานติดต่อกลับ" ไม่ได้ยินยอมให้ข้อมูลตัวเองถูกส่งต่อให้เจ้าของแปลง
**ห้ามเพิ่มรายละเอียดรายบุคคลลงกล่องนี้ไม่ว่ากรณีใด**

### ทางเข้าหน้านัดตรวจ

`land.js` → `.ld-inspect` วางใต้แถวปุ่มติดต่อในหน้ารายละเอียดแปลง
ลิงก์เป็น `inspect.html?listing=<รหัส>&from=land_page`
· ⚠️ **ไม่ใส่ `data-contact` และไม่ยิงสถิติติดต่อ** — คนที่กดยังไม่ได้ติดต่อใคร เขาไปกรอกฟอร์มต่อ
ซึ่งเซิร์ฟเวอร์นับเป็น `inspect_submit` ให้เองตอนสร้างใบ (กติกาข้อ 6 — นับที่เดียว)
· **ไม่เพิ่มเมนูตัวที่ 6 บนหัวเว็บ** เพราะแถวเมนูเต็มพอดีอยู่แล้วที่ 1440px (ดูหัวข้อ `menu.js`)

### เหตุการณ์สถิติที่เพิ่ม

`inspect_view` ขึ้นทะเบียนทั้งสองฝั่ง · `inspect_submit` **มีเฉพาะฝั่งเซิร์ฟเวอร์**
เพราะเซิร์ฟเวอร์บันทึกเองตอนสร้างใบ (แนวเดียวกับ `consign_submit` และ `inquiry_submit`)

### ทดสอบแล้ว

`node contracts.test.js` **61 ข้อ** (เพิ่มหัวข้อ 8 · 11 ข้อ) · ชุดเดิม verified/compare/feecalc/valuecalc ผ่านครบ
· **เบราว์เซอร์จริง + เซิร์ฟเวอร์จริง** (worktree `nj-wt-p2` ที่พอร์ต 8792 · DATA_DIR ชั่วคราว) ที่ 375px และ 1440px:
ส่งคำขอสำเร็จได้เลขงาน + ลิงก์ติดตาม · ยังไม่ติ๊กยินยอมกดส่งไม่ได้และขึ้นข้อความบอก ·
วันที่ซ้ำถูกตัดเหลือวันเดียว · ส่งซ้ำด้วย `+66` แบบเดียวกันถูกจับว่าเป็นใบซ้ำและขึ้นข้อความถูกต้อง ·
`<script>` ในช่องข้อความไม่กลายเป็น element · ตั๋วผิด / ไม่มีตั๋ว ขึ้นคนละข้อความกัน ·
สิ่งที่เจ้าหน้าที่กรอกในขั้นการซื้อไปโผล่บนหน้าติดตามของผู้ซื้อจริง · ไม่ล้นแนวนอน · ไม่มี console error

## ค้นหาที่ดินตามวัตถุประสงค์ 8 แบบ (2026-09-11 · Phase 3 ข้อ 3)

ไฟล์ใหม่: `purpose.html` · `purpose.js` · `landpurpose.js` (`window.NJPurpose`) · `landpurpose.css`
ที่แก้เพิ่ม: `land.html` + `land.js` (แผง "แปลงนี้เข้าทางอะไรบ้าง") · `listings.html`/`listings.css` (ทางเข้า) ·
`analytics.js` (`purpose_view`) · `sitemap.xml` · `contracts.test.js` (หัวข้อ 10)

วัตถุประสงค์ 8 แบบ: สร้างบ้าน · สร้างโกดัง · โรงงาน · ร้านค้าหรือสำนักงาน · แบ่งขาย · จัดสรรที่ดิน · เกษตร · ลงทุนระยะยาว

### กติกาที่ห้ามผ่อน

1. **⛔ ห้ามมีข้อความรับปาก** — "สร้างได้แน่นอน" · "จัดสรรได้แน่นอน" · "รับประกัน" · "การันตี"
   ถูกล็อกไว้ใน `contracts.test.js` หัวข้อ 10 ทั้งฝั่งเว็บและฝั่งระบบ · การก่อสร้าง การแบ่งแยกแปลง
   และการจัดสรรที่ดิน ต้องขออนุญาตจากหน่วยงานเสมอ เว็บนี้ฟันธงแทนไม่ได้
2. **`disclaim` ที่เซิร์ฟเวอร์ส่งมาต้องพิมพ์ทุกครั้งที่แสดงผล ห้ามถอดและห้ามเขียนใหม่ให้อ่อนลง**
   (กติกาเดียวกับ `SCORE_DISCLAIM` ของคะแนนความพร้อม)
3. **"ยังบอกไม่ได้" (`unknown`) กับ "ยังไม่เข้าทาง" (`weak`) เป็นคนละคำตอบ ห้ามยุบรวม**
   และ **ห้ามซ่อนแปลงกลุ่ม `unknown` ออกจากผลค้น** — เซิร์ฟเวอร์เรียงลำดับมาให้แล้วและไม่ได้คัดใครทิ้ง
   หน้าเว็บมีหน้าที่แสดงให้ครบและบอกจำนวนของแต่ละกลุ่ม (`.njpp-counts`)
   · แปลงที่ขึ้น `unknown` ครบทุกข้อจะมีบรรทัดอธิบายว่าเป็นเพราะยังไม่มีผลตรวจ ไม่ใช่เพราะแปลงไม่ดี
4. **รายชื่อวัตถุประสงค์ดึงจาก `GET /api/public/purposes` เสมอ ห้ามฝังรายการไว้ในไฟล์เว็บ**
   ฝังเมื่อไหร่ = คีย์บนปุ่มกับคีย์ในตัวคำนวณเลื่อนออกจากกัน แล้วกดแล้วได้ผลว่างโดยไม่มีอะไรเตือน
   (บทเรียนเดียวกับ `messenger_click` ที่เคยตกหล่นใน `analytics.js`) — มีเทสต์ล็อกไว้
5. **ห้ามคำนวณความเข้ากันเองฝั่งเว็บ** เซิร์ฟเวอร์คิดมาให้แล้วทั้งคะแนนและเหตุผลทั้ง 5 กลุ่ม
   (บทเรียนเดียวกับราคาต่อตารางวาและคะแนนความพร้อม)
6. **แผงในหน้าแปลงคืนสตริงว่างเมื่อ API ไม่ส่ง `purposes` มา** — แปลงเก่าหน้าตาเหมือนเดิมเป๊ะ

### รูปร่างข้อมูลที่ใช้

- `land.html` อ่าน `l.purposes` (`{at, disclaim, items[]}`) — **อยู่ระดับบนสุดของประกาศ ไม่ได้อยู่ใน `l.land`**
  เหมือน `l.score` · แต่ละ item มี `reasons` (`ok`/`partial`/`caution`) · `checked` · `missing` · `watch` · `services`
- `purpose.html` ยิง `/api/public/listings?purpose=<key>` แล้วอ่าน `l.purpose` (รูปย่อ) ของแต่ละแปลง
  พร้อม `data.purpose.counts` สำหรับบรรทัดสรุป
- `NJL.normalize()` ตัดช่องที่ไม่รู้จักทิ้ง จึงต้องหิ้ว `raw.purpose` มาแปะกลับเองหลังเรียก (มีคอมเมนต์กำกับใน `purpose.js`)

### ทดสอบแล้ว

`node contracts.test.js ../nj-wt-p2` **146 ข้อ** (เพิ่มหัวข้อ 10) · ชุดเดิมผ่านครบ
· **เบราว์เซอร์จริง + เซิร์ฟเวอร์จริง** (worktree `nj-wt-p2` ที่พอร์ต 8792 · DATA_DIR ชั่วคราว) ที่ 375px:
ปุ่ม 8 อันสร้างจากรายชื่อของเซิร์ฟเวอร์ · กดแล้วเรียงผลถูกต้องและที่อยู่หน้าเปลี่ยนเป็น `?p=` ·
เปิดลิงก์ `?p=allocate` ตรงๆ ได้ผลเดิม · แปลงที่ยังไม่มีผลตรวจยังอยู่ในผลพร้อมป้าย "ยังบอกไม่ได้" ·
ที่ตาบอดในผังอนุรักษ์อยู่ท้ายสุด · แผงในหน้าแปลงกางดูเหตุผลครบ 5 กลุ่ม ·
แปลงที่ยังไม่มีผลตรวจขึ้นบรรทัดอธิบายแทนการถูกตัดสิน · ไม่ล้นแนวนอน · ไม่มี console error

## ห้องข้อมูลแปลงสำหรับผู้ซื้อ (2026-09-11 · Phase 3 ข้อ 4)

ไฟล์ใหม่: `room.html` · `room.js` · `room.css`
ที่แก้เพิ่ม: `land.js` + `land.css` (ทางเข้า `.ld-room`) · `analytics.js` (`dataroom_view`) ·
`robots.txt` · `contracts.test.js` (หัวข้อ 11)

หน้าเดียวทำสองโหมดแบบเดียวกับ `inspect.html`:
ไม่มีตั๋วในลิงก์ → ฟอร์มขอสิทธิ์ · มีตั๋ว (`?id=..&t=..`) → สถานะคำขอ + รายการเอกสารที่เปิดได้

### กติกาที่ห้ามผ่อน

1. **⛔ หน้านี้ห้ามถูกเก็บเข้าดัชนีเสิร์ชเอนจิน** — กันสองชั้น: `<meta name="robots" content="noindex, nofollow, noarchive">`
   ในหน้า และ `Disallow: /room.html` ใน `robots.txt` (เหตุผลเดียวกับ `deal.html`)
   · **ไม่อยู่ใน `sitemap.xml`** และมีเทสต์ยืนยันว่าไม่ได้ถูกใส่เข้าไป
2. **ลิงก์ที่มีตั๋วคือกุญแจ** — ข้อความบนหน้าต้องเตือนให้เก็บไว้กับตัวและห้ามส่งต่อ
   เพราะใครถือลิงก์ก็เปิดเอกสารในนามผู้ขอได้ และชื่อผู้ขอจะไปประทับอยู่บนเอกสารนั้น
3. **⛔ ห้ามเขียนว่าจะได้ "ไฟล์ต้นฉบับ" หรือ "ดาวน์โหลดต้นฉบับ"** — ไม่มีทางนั้นอยู่จริงในระบบ
   สิ่งที่ผู้ขอได้คือ PDF ที่ประทับชื่อเขาไว้ทุกหน้าเสมอ (มีเทสต์ล็อกคำต้องห้ามไว้)
4. **ไม่ยินยอม PDPA = ปุ่มส่งถูกปิดไว้** และเซิร์ฟเวอร์ตรวจซ้ำอีกชั้น
5. **ไม่ฝังรายชื่อชนิดเอกสารไว้ฝั่งเว็บ** — อ่าน `kindTh` ที่เซิร์ฟเวอร์ส่งมาเสมอ
   (บทเรียนเดียวกับรายชื่อวัตถุประสงค์ของข้อ 3)
6. **ทางเข้าในหน้ารายละเอียดแปลงต้องบอกว่าต้องผ่านการยืนยันตัวตนก่อน** ไม่ใช่ทำให้ดูเหมือนปุ่มดาวน์โหลด
   ไม่งั้นคนกดแล้วคาดหวังว่าจะได้ไฟล์ทันที แล้วผิดหวังตั้งแต่หน้าแรก
   · แถบข้างของกล่องนี้เป็นสีกรมท่า ต่างจากกล่องนัดตรวจแปลง (สีเขียว) เพราะเป็นคนละเรื่องกัน:
     อันบนคือ "ให้เราไปตรวจให้" อันนี้คือ "ขอดูเอกสารที่ตรวจไว้แล้ว"
7. **เหตุการณ์สถิติ**: `dataroom_view` ยิงจากเบราว์เซอร์ · `dataroom_request` **เซิร์ฟเวอร์บันทึกเอง**
   ตอนสร้างคำขอ ฝั่งเว็บห้ามยิงซ้ำ (กติกาเดียวกับ `inspect_submit`)
   · `dataroom_request` นับรวมใน `formLeads` ไม่ใช่ `leads`

### ทดสอบแล้ว

`node contracts.test.js ../nj-survey-system` **175 ข้อ** (เพิ่มหัวข้อ 11) · ชุดเดิมผ่านครบ
· **เบราว์เซอร์จริง + เซิร์ฟเวอร์จริง** ที่ 375px: ติ๊กยินยอมแล้วปุ่มส่งถึงจะกดได้ ·
ส่งคำขอได้เลขที่และลิงก์ติดตาม · เปิดลิงก์แล้วเห็นสถานะ "รอทีมงานพิจารณา" พร้อมข้อความว่ายังไม่มีเอกสารให้เปิด ·
ทีมอนุมัติแล้วรายการเอกสารโผล่ · กดเปิดได้ไฟล์ PDF ที่ประทับลายน้ำจริง ·
ไม่ล้นแนวนอน · ไม่มี console error

## สถานะจริงของเว็บ ณ 11 ก.ย. 2569 (ตรวจกับเว็บจริง ไม่ใช่จากบันทึก)

- **ทุกหน้าออนไลน์อยู่จริงทั้ง 16 หน้า** (ยิงเช็คได้ 200 ทั้งหมด รวม `purpose.html` `room.html` `notify.html`)
- **มี 21 แปลงขึ้นเว็บจริง** — ระดับ 1 จำนวน 16 แปลง · ระดับ 2 (รังวัดยืนยันแล้ว) 5 แปลง
- ⚠️ **แต่ 0 แปลงมีบันได 5 ระดับ และ 0 แปลงมีคะแนนความพร้อม**
  แผงบันได 5 ระดับ · รายงานสุขภาพแปลง · แผนที่แนวเขต · ห้องข้อมูล จึงยังไม่โผล่บนหน้าแปลงเลยสักแปลง
  **นี่คือพฤติกรรมที่ถูกต้องตามกติกาข้อแรกของ Phase 1** (ไม่มีข้อมูล = ซ่อนทั้งหัวข้อ ห้ามวาดกล่องว่าง)
  ไม่ใช่บั๊ก — รอทีมกรอกข้อมูลฝั่งระบบหลังบ้าน
  · ฝั่งระบบมีการ์ด "🧩 แปลงที่ขึ้นเว็บแล้ว — ข้อมูลที่ยังขาด" บอกทีละแปลงว่าต้องกรอกอะไรบ้าง
- ชุดเทสต์ของ repo นี้ผ่านครบทุกไฟล์: contracts 184 · compare 10 · feecalc 50 · valuecalc 28 ·
  verified 58 · njchat 45

**ตรวจกับของจริงก่อนเชื่อสถานะในไฟล์นี้เสมอ** — 11 ก.ย. 69 พบสองหัวข้อที่เขียนว่า "⛔ ยังไม่ deploy"
ทั้งที่ขึ้นเว็บไปแล้วนานแล้ว (กับดักเดียวกับที่ `nj-survey-system/CLAUDE.md` เจอ 7 จุดในวันเดียวกัน)
