# Claude Code Guide — ที่ดินชัวร์

## เป้าหมายผลิตภัณฑ์

เว็บตลาดซื้อขายที่ดินของบริษัท เอ็นเจ แอนด์ คอนซัลติ้ง จำกัด จุดต่างหลักคือข้อมูลแนวเขตจากงานรังวัดจริง ไม่ใช่เว็บรวมประกาศทั่วไป

## โครงสร้างสำคัญ

- `index.html`, `home.css` หน้าแรก — โทนน้ำเงินเข้ม–ทอง (Sarabun + Trirong)
  · **หน้าแรกไม่โหลด `marketplace.css`** คนละชุดสีกัน โหลดคู่กันแล้วฟอนต์และพื้นหลังตีกัน
    สไตล์การ์ดแปลงที่ดินถูกเขียนซ้ำไว้ใน `home.css` แทน — แก้หน้าตาการ์ดต้องแก้ที่นี่ ไม่ใช่ `marketplace.css`
  · โครงหน้าเป็น inline style ตามที่ดีไซน์ส่งมา · `home.css` เก็บเฉพาะ base, media query,
    คลาส hover และคอมโพเนนต์ที่ดีไซน์ไม่ได้ให้มา
  · **คลาส `.hv-*` ต้องใช้ `!important`** เพราะ inline style ชนะ class เสมอ — ถอด `!important` ออกเมื่อไหร่ hover ตายทันที
  · **โรงฉายวิดีโอ `.nj-stage` ถูกถอดออกแล้ว 2026-09-07** — เจ้าของสั่งเอาภาพโมชั่นกราฟิก 3D ออก
    แล้วใช้คลิปพรีเซนเตอร์แทนในตำแหน่งเดิม (ระหว่างเมนูค้นหาด่วนกับ `#listings`)
    · HTML กลายเป็นแถบ `.nj-intro` ตัวที่สอง (`#ตรวจก่อนโอน`) ดูหัวข้อ `njintro.js` ข้างล่าง
    · ตัวควบคุม IntersectionObserver ท้าย `surveyquote.js` ถูกลบทิ้งพร้อมกัน (ไม่มี element ให้คุมแล้ว)
    · **CSS `.nj-stage*` ยังอยู่ท้าย `home.css`** ตั้งใจไม่ลบ เผื่ออยากได้แถบคลิปเงียบวนลูปกลับมา
      ไฟล์ `video/njsurvey.mp4` + โปสเตอร์ก็ยังอยู่ใน repo เหมือนกัน
- `njintro.js` + แถบ `.nj-intro` **คลิปพรีเซนเตอร์แนวตั้ง 9:16 — ตอนนี้มี 2 แถบ** (2026-09-07)
  · `#ตรวจก่อนโอน` โฆษณาที่ดินชัวร์ 30 วิ (`video/teedinsure-ad.mp4` ~4 MB) — บนสุด แทนที่ `.nj-stage` เดิม
  · `#แนะนำระบบ` แนะนำระบบทำงาน 1:18 (`video/nj-intro.mp4` ~11 MB) — คั่นระหว่าง "บริการ" กับ "ขั้นตอน"
  · **`njintro.js` วนทุก `.nj-intro` ไม่ผูก id เดี่ยวแล้ว** เพิ่มแถบที่สามก็แค่เขียน HTML ตามโครงเดิม
    หาปุ่ม/วิดีโอ "ในแถบของตัวเอง" ด้วย `sec.querySelector` — ห้ามกลับไปใช้ `getElementById`
  · **กดเล่นแถบหนึ่ง ต้องหยุดอีกแถบ** ไม่งั้นเสียงพูดสองคลิปทับกัน (คลิปทั้งคู่มีเสียง)
  · **คลิปมีเสียงพูดทั้งคู่ จึงห้ามเล่นเอง**
    (`preload="none"` ด้วย — ไฟล์ 11 MB เล่นเองคือกินเน็ตมือถือลูกค้าฟรีๆ)
  · **HTML ใส่ `controls` ไว้ตั้งแต่แรก แล้ว `njintro.js` เป็นคนถอดออก ห้ามทำกลับด้าน**
    ไฟล์ JS โหลดไม่สำเร็จเมื่อไหร่ ต้องเหลือวิดีโอที่กดเล่นได้ตามปกติ ไม่ใช่ภาพนิ่งที่กดอะไรไม่ได้เลย
    · ปุ่มเล่นที่เราวาดเองก็ซ่อนไว้ใน CSS จนกว่า JS จะใส่คลาส `.js-ready` ให้ ด้วยเหตุผลเดียวกัน
    · **สองสถานะห้ามซ้อนกัน**: ยังไม่เล่น = โปสเตอร์ + ปุ่มของเรา · เล่นแล้ว = แถบควบคุมของเบราว์เซอร์
      มีพร้อมกันเมื่อไหร่ แถบควบคุมจะทับปุ่มมุมล่างจนอ่านไม่ออก (เจอจริงตอนพรีวิว)
  · ⚠️ **`.nj-intro-frame video` ต้องมี `height:auto`** — แอตทริบิวต์ `height="1920"` ใน HTML
    กลายเป็น `height:1920px` ซึ่งชนะ `aspect-ratio` แล้วจอวิดีโอยืดเป็นแถบเปล่ายาวทั้งหน้า
  · ⚠️ `@media (max-width:900px)` ของแถบนี้ต้องมี `padding-left:0 !important` เพื่อล้มกฎรวม
    `section{padding:18px !important}` ที่ 640px ไม่งั้นคลิปเยื้องไปทางขวาบนมือถือ
- **ปุ่ม "ดูคลิปแนะนำระบบ" ที่ hero** (`[data-nj-intro-play]` ใน `index.html` · ตัวจัดการอยู่ใน `njintro.js`)
  · ทำเป็น**ลิงก์เปล่า + วงกลมทองมีสามเหลี่ยมเล่น ไม่ใช่ปุ่มกรอบเต็มใบ** — สองปุ่มแรกในแถวนั้น
    (ฝากขาย / คู่มือ) เป็น CTA หลัก ถ้าปุ่มที่สามหน้าตาเท่ากัน แถวจะกลายเป็นสามทางเลือกที่ดูสำคัญ
    เท่ากันหมด แล้วปุ่มฝากขายซึ่งเป็นเป้าหมายจริงของหน้าจะจม
  · **ต้องเรียก `start()` ในจังหวะคลิกทันที ห้ามรอให้เลื่อนจอถึงแถบคลิปก่อน** — นโยบายสื่อของเบราว์เซอร์
    อนุญาตให้เล่นคลิปที่มีเสียงเฉพาะตอนที่ผู้ใช้เพิ่งกดเท่านั้น หน่วงเมื่อไหร่ `play()` ถูกปฏิเสธ
    แล้วผู้ใช้จะเลื่อนไปเจอจอนิ่งที่ต้องกดซ้ำอีกที
  · **ไม่ `preventDefault`** ปล่อยให้ `href="#แนะนำระบบ"` พาไปตามปกติ — `njintro.js` ไม่ทำงานเมื่อไหร่
    ลิงก์ยังต้องพาไปดูคลิปได้เหมือนลิงก์ธรรมดา
  · ผูกด้วย `querySelectorAll('[data-nj-intro-play]')` ไม่ใช่ id — จะเพิ่มปุ่มดูคลิปที่จุดอื่นของเว็บ
    ก็แค่ติดแอตทริบิวต์นี้ ไม่ต้องแก้ JS
  · **ค่าของแอตทริบิวต์ = id ของแถบที่จะให้เล่น** (2026-09-07) · ไม่ใส่ค่า = `#แนะนำระบบ` ตามเดิม
    ปุ่มที่ hero จึงยังเล่นคลิปแนะนำระบบเหมือนเดิม ไม่ใช่คลิปโฆษณาที่มาใหม่
- `marketplace.css` design system ของ `guides.html` `land.html` `videos.html` (หน้าแรกไม่ใช้แล้ว)
- `marketplace.js` โหลดประกาศ ค้นหา กรอง และบันทึกรายการบนหน้าจอ
  · **ผูกกับ id เหล่านี้แบบไม่เช็ก null**: `search-form` `search-input` `type-filter` `price-filter`
    `listing-grid` `result-note` `listings` `year` + `[data-purpose]` `[data-quick]`
    ขาดตัวใดตัวหนึ่งคือ JS ตายทั้งไฟล์ ประกาศไม่ขึ้นเลย — จัดหน้าแรกใหม่ต้องเก็บครบทุกตัว
  · **หน้าแรกแบ่งหน้าละ 9 แปลง** (`PAGE_SIZE` · `renderPager()` · `#listing-pager` · เจ้าของสั่ง 2026-09-13)
    พร้อมย่อรูปการ์ดใน `home.css` จาก 220 → 160px · กรอง/ค้นหาใหม่ (`render(message)`) กลับหน้า 1 เสมอ
    · ⚠️ **`#listing-pager` ต้องอยู่นอก `#listing-grid`** — `compare.js` เฝ้า DOM ในตะแกรง (ดูกับดัก MutationObserver)
    · `#listing-pager` เป็น id ที่เช็ก null แล้ว ไม่อยู่ในรายชื่อบังคับข้างบน
    · หน้ารวมประกาศ (`listings.js`) **ยังไม่แบ่งหน้า** แสดงทุกแปลงตามเดิม
- `listingcard.js` **ตัวเรนเดอร์การ์ดกลาง** (`window.NJListing`) — `card()` `normalize()` `emptyHtml()`
  `loadFailedHtml()` `fetchListings()` `bindGrid()` · ใช้ร่วมกันทั้งหน้าแรกและหน้ารวมประกาศ
  · **ต้องโหลดก่อน `marketplace.js` / `listings.js` เสมอ** (ทั้งสองไฟล์อ่าน `window.NJListing` ตอนเริ่มทำงานทันที)
  · **ห้ามก๊อป `card()` กลับไปไว้ในไฟล์ของหน้าใดหน้าหนึ่ง** — กติกาข้อ 4/5 จะกลายเป็นมีสองที่ให้ลืมแก้
  · ⚠️ สไตล์การ์ดยังแยกสองชุดตามชุดสี (`home.css` หน้าแรก · `marketplace.css` หน้าอื่น)
    เพิ่มคลาสใหม่ต้องเพิ่ม**ทั้งสองไฟล์** ใส่ที่เดียว = อีกหน้าไม่มีสไตล์โดยไม่มี error
- `listings.html`, `listings.js`, `listings.css` **หน้ารวมประกาศ + ตัวกรอง** (เพิ่ม 2026-09-04)
  · เหตุผลที่ต้องมี: หน้าแรกมีแค่ช่องค้นหา + กรองราคาชั้นเดียว และเป็นหน้าขายแบรนด์ ไม่ใช่หน้าไล่ดูของ
    เวลายิงโฆษณาหาผู้ซื้อจึงไม่มีหน้าปลายทางให้ส่งคนไปลง
  · กรองได้: คำค้น · ประเภท · จังหวัด · ช่วงราคา · ช่วงเนื้อที่ · เอกสารสิทธิ์ · ผังสี · สิ่งที่แปลงมี
    เรียงได้: อัปเดตล่าสุด · ราคารวม · ราคาต่อ ตร.ว. · เนื้อที่
  · **รายชื่อจังหวัดสร้างจากแปลงที่มีอยู่จริง ไม่ใช่รายชื่อ 77 จังหวัด** — เลือกจังหวัดที่ไม่มีของ
    แล้วเจอผลว่างเปล่าจะดูเหมือนเว็บพัง
  · ⚠️ **กติกาที่ห้ามผ่อน — "ไม่ตรงเงื่อนไข" กับ "ยังไม่ได้ระบุ" ต้องแยกกัน**
    แปลงที่ยังไม่ได้กรอกช่องที่กำลังกรอง จะไม่ถูกนับว่าตรง (บอกว่าตรงทั้งที่ไม่รู้ = โกหกผู้ซื้อ)
    **แต่ต้องขึ้นข้อความบอกจำนวนที่ถูกซ่อนด้วยเหตุนี้เสมอ** (`#unknown-note`)
    ไม่งั้นแปลงหายเงียบ ผู้ซื้อคิดว่าไม่มีของ และทีมงานก็ไม่รู้ว่าต้องไปกรอกอะไรเพิ่ม
  · ค่าใน dropdown ต้องตรงกับ `LAND_DEEDS` / `LAND_ZONES` / `PARCEL_FEATURES` ใน `server.js` เป๊ะ
    ไม่ตรง = กรองไม่เจออะไรเลยทั้งที่ข้อมูลมีอยู่
  · **"น.ส.3ก" ในตัวกรองหมายถึง "น.ส.3ก ขึ้นไป"** — โฉนดนับว่าตรงด้วย (กติกาเดียวกับฝั่ง server)
- `consign.html`, `consign.js`, `ui.css` หน้าฝากขายเดิม ห้ามแก้โดยไม่ทดสอบฟอร์ม
- **กล่อง "รังวัดยืนยันเขตก่อนประกาศขาย" ในฟอร์มฝากขาย** (`#cs-survey` · `setupSurvey()` · เพิ่ม 2026-09-06)
  · เกาะช่องที่มีอยู่แล้ว 2 ช่อง ไม่ได้เพิ่มช่องให้กรอกใหม่: **จังหวัด** → ตัดสินว่าบังคับรังวัดไหม ·
    **เนื้อที่** → ยิงเข้าตารางราคางานรังวัดจริง ได้ค่ารังวัดประมาณการทันทีขณะพิมพ์
  · ⚠️ **บังคับเฉพาะ กทม. + ปริมณฑล 5 จังหวัด** (`SURVEY_REQUIRED_PROVINCES`) —
    **ต้องตรงกับรายชื่อชื่อเดียวกันใน `server.js` เป๊ะ** ไม่ตรง = หน้าเว็บบอกว่าเลือกได้
    แต่พอบันทึกแล้วค่ากลายเป็น "ต้องรังวัด" โดยไม่มีคำอธิบาย
  · ⚠️ **"บังคับ" = บังคับก่อนขึ้นประกาศ ไม่ใช่บังคับก่อนส่งฟอร์ม — ห้ามเอาไปบล็อกปุ่มบันทึก**
    คนที่ยังไม่พร้อมจ่ายค่ารังวัดก็ยังเป็นลีดที่ทีมขายคุยต่อได้ (กติกาเดียวกับหน้าฝากหาที่ดิน)
  · ในเขตบังคับ: ล็อกเหลือ "ต้องการรังวัด" ทางเดียว แต่ยังแสดงตัวเลือกอื่นแบบจาง
    ให้เห็นว่าเป็นข้อจำกัดของพื้นที่ ไม่ใช่ฟอร์มพัง · และ**ซ่อนตารางข้อดี–ข้อเสีย**
    (ไม่ต้องโน้มน้าวคนที่ไม่มีทางเลือก — อ่านแล้วเหมือนขายของทั้งที่เลือกไม่ได้)
  · นอกเขตบังคับ: เลือกได้ 3 ทาง (`yes`/`no`/`undecided`) พร้อมตาราง **ข้อดีและสิ่งที่ต้องแลก**
    เขียนทั้งสองด้านตามจริง — เจ้าของที่รู้ต้นทุนล่วงหน้าแล้วยังเลือกรังวัด คือคนที่ไม่ถอยตอนได้ใบเสนอราคา
  · ตอนเปิดใบเดิมกลับมาใช้ `d.surveyRequired` ที่เซิร์ฟเวอร์ส่งมาเสมอ (`applyFromLead`) ไม่คำนวณเองซ้ำ
  · **ปุ่ม "ฝากขายแปลงถัดไป" ต้องเรียก `SV.reset()`** — `form.reset()` คืนค่า radio ให้ก็จริง
    แต่ธง "บังคับ" ของแปลงก่อนหน้ายังค้าง แปลงใหม่ในต่างจังหวัดจะขึ้นว่าบังคับรังวัด
- `surveyquote.js` **เปิด `window.NJSurveyQuote` ให้หน้าอื่นเรียกได้แล้ว** (2026-09-06)
  · `load()` แคช promise ของ `pricing.js` ไว้ตัวเดียว (กติกาเดียวกับ `NJFeeCalc.loadBuildingPrices`)
  · `quoteFromWa(totalWa, jobType, {combo:true})` — คิดส่วนลด "รังวัด + ฝากขาย 5%" ผ่าน `adjust`
    ของ `pricing.js` ไม่ใช่ลบเอาเองทีหลัง ไม่งั้นบรรทัดแจกแจงจะไม่มีส่วนลดโผล่
  · ⚠️ **ตารางราคายังอยู่ที่ระบบหลังบ้านที่เดียวเหมือนเดิม ห้ามก๊อปตัวเลขมาไว้ในเว็บ**
    โหลดไม่ได้ = ไม่แสดงราคาเลย แล้วให้ทักไลน์แทน ห้าม fallback เป็นตัวเลขที่เดา
  · `consign.html` โหลดไฟล์นี้ด้วยแล้ว (กล่องเช็กราคาและโรงฉายวิดีโอข้างในเช็ก element ของตัวเองก่อนทำงาน
    หน้าฝากขายไม่มีทั้งคู่ จึงไม่ทำอะไรนอกจากเปิด API ให้)
- `landform.js` ช่อง "ที่ตั้ง · เนื้อที่ · ราคา" ของฟอร์มฝากขาย (พิมพ์ค้นหา/เลือก จังหวัด-อำเภอ-ตำบล + คำนวณราคาอัตโนมัติ)
  · ใช้ `input + datalist` **ไม่ใช่ `select`** — พิมพ์ 2-3 ตัวอักษรแล้วเบราว์เซอร์กรองให้ เร็วกว่าเลื่อนหาใน 77 จังหวัดมาก
  · เป็นช่องพิมพ์ได้อิสระ ค่าที่ไม่ตรงรายชื่อก็ส่งไปให้ทีมขายอยู่ดี (ขึ้นข้อความเตือนตอนออกจากช่อง ไม่บล็อกการส่ง)
  · คณิตศาสตร์อยู่ใน `NJLandForm.calc` แยกจาก DOM เพื่อทดสอบด้วย node ได้ตรงๆ
  · **`initAreaPrice` มี `opt.onChange` แล้ว** (2026-09-06) ยิงทุกครั้งที่เนื้อที่/ราคาเปลี่ยน —
    กล่องรังวัดในหน้าฝากขายใช้ตัวนี้คำนวณค่ารังวัดตามเนื้อที่ใหม่ (`initAddress` มี `onChange` อยู่ก่อนแล้ว)
    · ⚠️ **ตัวแจ้งอยู่ในตัวห่อ `render()` ไม่ใช่ต่อท้าย `paint()`** — `paint()` มี early return หลายทาง
      (ยังไม่ใส่เนื้อที่ · ยังไม่ใส่ราคา) ซึ่งเป็นทางที่ผู้เรียกต้องรู้พอๆ กับทางที่คำนวณได้
      เอาไปต่อท้ายเมื่อไหร่ ค่ารังวัดบนหน้าฝากขายจะค้างตัวเลขเก่าตอนผู้ใช้ลบเนื้อที่ทิ้ง
- `data/thai-admin.json` รายชื่อ 77 จังหวัด / 930 อำเภอ / 7,452 ตำบล พร้อมรหัสไปรษณีย์ (~271 KB, gzip ~57 KB)
  · สร้างด้วย `node data/build-admin.js` จากชุดข้อมูล kongvut/thai-province-data · **ห้ามแก้ไฟล์ json ด้วยมือ**
  · โหลดแบบไม่บล็อกหน้า · โหลดไม่สำเร็จก็ยังกรอกได้ เพราะช่องเป็น `input` ธรรมดาอยู่แล้ว (ไม่ต้องมีช่องสำรองแยก)
- **หน้าฝากขายเป็น 2 จังหวะ ไม่ใช่กดส่งครั้งเดียวจบ** (แก้เมื่อ 2026-09-02)
  1. **"บันทึกข้อมูล"** → สร้างใบฝากขายในระบบ · **ฟอร์มไม่ถูกซ่อน** ยังแก้ทุกช่องแล้วกดบันทึกซ้ำได้ไม่จำกัด
     (ปุ่มเปลี่ยนข้อความเป็น "💾 บันทึกการแก้ไข" · ยิง `PATCH` แทน `POST`)
  2. **"ส่งให้ทีมงานตรวจสอบ"** (`#cs-send`) → ยิงแจ้งเตือนหาทีมงาน · กดซ้ำได้หลังแก้ข้อมูลเพิ่ม
  · กุญแจคือ **ตั๋ว** (`uploadToken`) ที่ได้ตอนบันทึกครั้งแรก — ไม่มีระบบสมาชิก ไม่มีรหัสผ่าน
    เก็บใน `localStorage['njConsignTicket']` (อ่าน/เขียนใน try/catch เสมอ · โหมดส่วนตัวเขียนไม่ได้)
    เปิดหน้านี้ใหม่ภายใน 14 วันแล้ว `restoreLead()` เติมค่ากลับลงฟอร์มให้ครบ · รองรับลิงก์ `?id=OP-xxx&t=<ตั๋ว>` ด้วย
  · **`fillForm()` ต้องเติมที่ตั้งตามลำดับ จังหวัด → อำเภอ → ตำบล** และ dispatch `input` ทุกช่อง
    เพราะรายชื่อชั้นล่างถูกสร้างจากชั้นบนใน `landform.js` · ติ๊ก `pdpa` คืนให้ด้วย (ยินยอมไปแล้วตอนบันทึกครั้งแรก)
  · **ไม่ยิงสถิติเหตุการณ์ใหม่ตอนกดส่ง** — ลีดถูกนับไปแล้วตอนบันทึกครั้งแรก (`consign_submit` ฝั่งเซิร์ฟเวอร์)
    เพิ่มชนิดเหตุการณ์ใหม่ต้องขึ้นทะเบียนทั้ง `analytics.js` และ `server.js` ก่อน ไม่งั้นถูกทิ้งเงียบๆ
- **ปุ่ม "ยกเลิกการฝากขาย" เป็นทางออกของเจ้าของ ไม่ใช่ปุ่มของทีมงาน** (`#cs-cancel` · `cancelLead()` · เพิ่มเมื่อ 2026-09-02)
  · **กดได้ทุกขั้น รวมถึงหลังประกาศขึ้นเว็บแล้ว — ห้ามผูกปุ่มนี้กับ `d.editable` เด็ดขาด**
    ช่วงที่ต้องใช้จริงที่สุดคือ "ขายได้เองแล้ว" ซึ่งเป็นช่วงที่เจ้าของแก้ข้อมูลเองไม่ได้แล้วพอดี
  · 2 จังหวะเสมอ: ลิงก์ตัวเล็กสีเทา → กล่องยืนยันโทนแดง (กรอกเหตุผลได้ ไม่บังคับ) → กดยืนยันถึงยิงจริง
    กดคืนเองไม่ได้ ต้องให้พนักงานปลดให้ในระบบหลังบ้าน จึงห้ามทำเป็นปุ่มแดงเต็มใบที่เผลอกดระหว่างกรอกฟอร์ม
  · **`applyCancelled()` ต้องถูกเรียกจาก `setLead()` ที่เดียว** — ทุกเส้นทาง (เปิดใบเดิม · บันทึก · แนบไฟล์ ·
    กดยกเลิก) วิ่งผ่าน `setLead()` หมด · ไปเรียกกระจายเองเมื่อไหร่ หน้าจอจะค้างสถานะ "ยังฝากขายอยู่"
    ทั้งที่ฝั่งเซิร์ฟเวอร์ยกเลิกไปแล้ว
  · ยกเลิกแล้วต้อง**ปิดทุกทางที่เขียนข้อมูลบนหน้าจอ**ให้ตรงกับที่เซิร์ฟเวอร์ปิดไปแล้ว
    (ซ่อน `#cs-up` `#cs-send` `#cs-keep` + `disabled` ทั้งฟอร์ม) — ไม่ปิดฝั่งนี้ = กดได้แต่เจอ 409 ทุกครั้ง
    ซึ่งอ่านแล้วเหมือนระบบพัง ไม่ใช่เหมือนถูกปิด
  · **`d.cancelled` กับ `d.editable === false` คนละเรื่องกัน ห้ามใช้ปนกัน** — `editable` เป็นเท็จได้จาก
    "ทีมงานรับเรื่องไปแล้ว" ด้วย ซึ่งต้องขึ้นข้อความคนละแบบโดยสิ้นเชิง
  · ยกเลิกไม่สำเร็จ **ห้ามเงียบ** ต้องขึ้นข้อความ + บอกให้ทักไลน์ ไม่งั้นเจ้าของเข้าใจว่ายกเลิกแล้ว
    ทั้งที่ประกาศยังอยู่บนเว็บ
- **รูป/เอกสารที่แนบแล้วขึ้นเป็นการ์ดพร้อมปุ่มลบ** (`.cs-thumbs` / `.cs-thumb` ใน `ui.css`)
  · **แยก 2 สถานะบนหน้าจอตามสิทธิ์จริงที่ฝั่งเซิร์ฟเวอร์**: รอตรวจ = มีปุ่ม ✕ ลบเองได้ ·
    ตรวจแล้ว = แถบเขียว "ทีมงานตรวจแล้ว" ลบเองไม่ได้ (รูปที่ผ่านการตรวจอาจขึ้นหน้าประกาศไปแล้ว)
  · ปุ่มลบใช้ **event delegation** ที่กล่อง ไม่ใช่ผูกรายใบ — การ์ดถูกวาดใหม่ทุกครั้งที่ข้อมูลเปลี่ยน
  · **ชื่อไฟล์มาจากผู้ใช้ ใส่ผ่าน `textContent` เท่านั้น** ห้ามต่อเป็นสตริง HTML
- **หน้าฝากขายมีช่องแนบไฟล์หลังบันทึกฟอร์มสำเร็จ** (`#cs-up` ใน `consign.html` · `setupUpload()` ใน `consign.js`)
  · แนบได้เฉพาะ**หลัง**บันทึกฟอร์ม เพราะต้องใช้ตั๋วที่ได้จาก response — ไม่ใช่ช่องอัปโหลดลอยๆ ที่ใครก็ยิงไฟล์เข้ามาได้
  · **แยก 2 ช่องเด็ดขาด**: รูปที่ดิน (ขึ้นเว็บได้หลังแอดมินอนุมัติ) กับ เอกสาร (ทีมงานเห็นเท่านั้น ไม่มีทางขึ้นเว็บ)
  · ย่อรูปด้วย canvas ก่อนส่งเสมอ (ยาวสุด 1800px) — รูปมือถือใบละ 3-8 MB อัปดิบๆ คือรอเป็นนาทีบนเน็ตมือถือ
  · ไฟล์ทุกไฟล์เข้ากล่อง "รอตรวจ" ในระบบหลังบ้าน **ไม่ขึ้นเว็บทันที** (กติกาล็อกไว้ที่ `server.js`)
- `wanted.html`, `wanted.js` **หน้าฝากหาที่ดิน — ลีดฝั่งผู้ซื้อ** (เพิ่ม 2026-09-04)
  · กลับด้านกับหน้าฝากขาย: ผู้ซื้อบอกโจทย์ว่าอยากได้ที่ดินแบบไหน ส่งเข้า `POST /api/public/buyer-request`
  · **หน้านี้ทำงานได้แม้คลังแปลงยังว่าง** เพราะสิ่งที่เสนอคือ "เราจะไปหาให้" ไม่ใช่ "มาดูของที่เรามี"
    จึงเป็นหน้าเดียวที่ยิงโฆษณาหาผู้ซื้อได้ตั้งแต่ตอนที่ยังมีแปลงขึ้นเว็บไม่กี่แปลง
  · **ทำเลใส่ได้ถึง 3 ย่าน** — สร้าง `NJLandForm.initAddress` แยกคนละชุดต่อย่าน (ไม่มีสถานะร่วมกัน)
    ย่านที่ 2-3 ซ่อนไว้ก่อน กดเพิ่มเอง · `areaValues()` **ต้องข้ามย่านที่ยังซ่อนอยู่**
    ไม่งั้นค่าที่เบราว์เซอร์เติมอัตโนมัติไว้จะถูกส่งไปเป็นโจทย์ที่ผู้ซื้อไม่ได้กรอก
  · **หน่วยเนื้อที่ส่งค่าดิบ + `areaUnit` ให้เซิร์ฟเวอร์แปลงเอง ห้ามแปลงที่ฝั่งนี้**
    แปลงสองที่เมื่อไหร่ ตัวเลขจะเพี้ยนคนละทางโดยไม่มีอะไรเตือน
  · **ไม่บังคับกรอกทำเล/งบ/เนื้อที่** — คนที่ยังไม่รู้ว่าตัวเองอยากได้อะไรก็ยังเป็นลีดที่คุยต่อได้
    บังคับให้กรอกครบ = เขากดถอยตั้งแต่ยังไม่ได้คุยกับใคร
  · ไม่ยิง `buyer_request_submit` จากเบราว์เซอร์ — เซิร์ฟเวอร์บันทึกให้แล้วตอนสร้างใบ (นับที่เดียว)
- **กล่อง "ไม่พบแปลง" ต้องยื่นปุ่มฝากหาที่ดินเป็นทางออกหลัก** (`app.js` หน้าแรก · `marketplace.js`)
  · คนที่ยืนอยู่หน้ารายการแปลงคือ**ผู้ซื้อ** ยื่นให้แต่ปุ่มฝากขายคือตอบไม่ตรงกับสิ่งที่เขามาหา
  · จังหวะ "ค้นแล้วไม่เจอ" คือจังหวะที่รู้โจทย์ของผู้ซื้อชัดที่สุดในทั้งเว็บ
    ปล่อยให้จบแค่ "ลองเปลี่ยนคำค้น" คือทิ้งลีดที่บอกความต้องการมาแล้วเต็มๆ
- `njservices.js`, `njservices.css` **บริการเพิ่มเติมหลังซื้อที่ดิน** (`window.NJServices` · เพิ่ม 2026-09-06)
  · จุดต่างที่เว็บประกาศทั่วไปไม่มี — ผู้ซื้อติ๊กบอกได้ตั้งแต่วันแรกว่าหลังซื้อแล้วอยากให้ดูแลอะไรต่อ
    (โอนกรรมสิทธิ์ · ขออนุญาตก่อสร้าง · ผู้รับเหมา) แล้วส่งเข้า `POST /api/public/inquiry`
  · อยู่ 2 หน้า: `index.html#บริการเพิ่มเติม` (ไม่ผูกกับแปลง) และ `land.html` (แนบรหัสทรัพย์ให้อัตโนมัติ)
  · **รายการบริการอยู่ใน `LIST` ที่เดียว ห้ามเขียนรายการซ้ำในหน้าใดหน้าหนึ่ง** —
    ไม่ตรงกันเมื่อไหร่ สถิติ "คนสนใจบริการไหนมากที่สุด" อ่านไม่ได้เลย
  · **คีย์ต้องตรงกับ `NJ_SERVICES` ใน `server.js` เป๊ะ** เซิร์ฟเวอร์ทิ้งคีย์ที่ไม่รู้จักโดยไม่มี error
  · ⚠️ **`PARTNERS` ตั้งใจปล่อยว่างไว้จนกว่าจะมีสัญญาจริง** — บริการที่ `by:'partner'`
    จะขึ้นป้ายว่า "ทีมงานจัดหาบริษัทพันธมิตรให้" **ห้ามใส่ชื่อบริษัทสมมติเพื่อให้หน้าเว็บดูสมบูรณ์**
    (กติกาข้อ 4 และ 9) · มีสัญญาแล้วเติมชื่อลง `PARTNERS` ที่เดียว ทุกหน้าขึ้นชื่อจริงพร้อมกัน
  · บริการที่ `by:'nj'` 4 ตัวแรกคือสิ่งที่แถบ "บริการ" หน้าแรกประกาศไว้อยู่แล้วว่าบริษัททำเอง
    (ฝากขาย · ตรวจกรรมสิทธิ์ · รังวัด · ยื่นจัดสรร · ประสานงานโอน) จึงเขียนชื่อทีมงานได้ตามจริง
  · หน้าไหนอยากได้ฟอร์มนี้ ใส่ `<div data-njsv-form>` พอ (ดู `boot()`) —
    หน้ารายละเอียดแปลงเรียก `mount()` เองเพราะต้องรอรหัสแปลงจาก API ก่อน
  · ⚠️ **`.njsv-sec` (แถบบนหน้าแรก) ต้องเป็นพื้นสว่างเท่านั้น** — มันวางระหว่างแถบ "บริการ" (`#E7EEF5`)
    กับ `.nj-intro` (`#050F1D`) ทำเป็นพื้นเข้มเมื่อไหร่ = พื้นเข้มสองแถบติดกัน จังหวะสีของหน้าแรกพัง
- `compare.js`, `compare.css`, `compare.html`, `comparepage.js` **เปรียบเทียบแปลง** (เพิ่ม 2026-09-06)
  · `compare.js` = ตัวเก็บรายการที่เลือก + ปุ่ม "เทียบ" บนการ์ด + แถบลอยด้านล่าง (`window.NJCompare`)
    `comparepage.js` = ตัววาดตารางในหน้า `compare.html` · โหลด `compare.js` ก่อนเสมอ
  · **ปุ่มบนการ์ดถูกแปะทีหลังด้วย `decorate()` ไม่ได้เขียนไว้ใน `listingcard.js`** —
    หน้าที่ไม่ได้โหลด `compare.js` (เช่น `/s/`) จึงได้การ์ดหน้าตาเดิมเป๊ะ
    และสไตล์ปุ่มอยู่ใน `compare.css` ที่เดียว ไม่ต้องเขียนซ้ำใน `home.css` + `marketplace.css`
  · **ตัวดักคลิกต้อง `stopPropagation` ในเฟส capture** — `bindGrid()` ดักคลิกทั้งการ์ดเพื่อพาไปหน้ารายละเอียด
    ไม่หยุดไว้ = กดปุ่ม "เทียบ" แล้วเด้งออกจากหน้าทันที
  · เก็บใน **sessionStorage ไม่ใช่ localStorage** — การเทียบเป็นเรื่องของการเข้าเว็บรอบนี้ ไม่ใช่ข้ามวัน
  · ⚠️ **ป้ายจุดแข็งมอบให้เฉพาะแปลงที่มีข้อมูลช่องนั้นจริง** (`winners()` ข้ามแปลงที่ค่าเป็น null)
    ไม่รู้ ≠ ชนะ และไม่รู้ ≠ แพ้ · ช่องที่ไม่มีข้อมูลขึ้น "—" และ**ข้อความอธิบายว่า "—" แปลว่า
    ยังไม่ได้ระบุ ไม่ใช่ไม่มี ห้ามถอด** (กติกาข้อ 5 · กติกาเดียวกับ `#unknown-note` ของหน้ารวมประกาศ)
  · ป้าย "รังวัดยืนยันแล้ว" เป็นข้อเท็จจริงของแปลงนั้นเอง **ไม่ใช่ผลการเทียบ** จึงขึ้นได้แม้เทียบแปลงเดียว
  · เทียบได้สูงสุด 4 แปลง · `compare.html` ตั้ง `noindex` เพราะเนื้อหาขึ้นกับสิ่งที่ผู้ใช้เลือกเอง
- `portal.html`, `portal.css` **ระบบติดตามงานรังวัด — หน้าอธิบาย + ทางเข้าพอร์ทัลลูกค้า** (เพิ่ม 2026-09-08)
  · ตัวระบบจริงคือ `https://app.njteedinsure.com` (ระบบหลังบ้าน `nj-survey-system` บัญชี `role=customer`)
    หน้านี้เป็นแค่หน้าอธิบาย + ปุ่มพาไป **ไม่ดึง API และไม่มีสถานะงานของใครอยู่ในหน้านี้เลย**
  · ⚠️ **ห้ามใส่ฟอร์มเข้าสู่ระบบในหน้านี้เด็ดขาด** เว็บนี้เป็นสแตติกบน GitHub Pages ไม่มีฝั่งเซิร์ฟเวอร์
    ที่จะถือ token ได้ปลอดภัย · และการรับรหัสผ่านที่โดเมนหนึ่งแล้วส่งข้ามไปอีกโดเมน = สอนลูกค้า
    ให้กรอกรหัสในที่ที่ไม่ใช่ระบบจริง ซึ่งเป็นพฤติกรรมเดียวกับหน้าฟิชชิ่ง
    (โทเคนของระบบผูกกับชื่อโดเมนอยู่แล้ว กรอกที่นี่ก็ใช้ที่นั่นไม่ได้)
  · ⚠️ **หัวข้อ "เข้าไปแล้วเห็นอะไร" 4 ข้อ = 4 แท็บจริงในพอร์ทัล** (สถานะงาน · รายงาน/ไฟล์ ·
    ใบแจ้งหนี้/ชำระ · แผนที่แปลง — ดู `portalBody()` ใน `nj-survey-system/public/app.js`)
    และ "11 ขั้นตอน" มาจาก `SURVEY_STEPS` ที่นั่น · ระบบเปลี่ยนเมื่อไหร่ต้องแก้หน้านี้ตาม
    เขียนสิ่งที่ระบบยังไม่มี = ลูกค้าเข้าไปหาไม่เจอแล้วโทรถาม ซึ่งตรงข้ามกับเหตุผลที่ทำหน้านี้
  · ⚠️ **ปุ่มติดต่อในหน้านี้ห้ามใส่ `data-contact` และห้ามยิงสถิติติดต่อ** — คนที่กดคือลูกค้าเดิม
    ที่มาขอรหัส/ลืมรหัส ไม่ใช่ลีดใหม่ · นับรวมเมื่อไหร่ ยอดลีดที่ใช้ตัดสินเกณฑ์ Phase 2 จะสูงกว่าความจริง
    (กติกาข้อ 6 มองจากอีกด้าน — ข้อนั้นกันลีดตกหล่น ข้อนี้กันลีดปลอม)
  · ทางเข้า: ลิงก์ `.nj-portal-link[data-njportal]` บนหัวเว็บ **8 หน้า** (สไตล์อยู่ใน `menu.css`)
    · **หน้าแรกไม่มีบนหัวเว็บ** เพราะแถวหัวเว็บเต็มพอดีจนโลโก้ถูกบีบอยู่แล้ว (เมนู 982px + โลโก้ 136
      + ปุ่ม 118 + ช่องไฟ 52 = 1288 บนพื้นที่จริง 1216 ที่จอ 1440) เติมอีกอันเมนูตัดสองบรรทัดทันที
      — หน้าแรกใช้ปุ่มบนการ์ด 04 "ติดตามผลผ่านระบบ" ในแถบบริการ + ลิงก์ในฟุตเตอร์แทน
    · จอ ≤640px ซ่อนลิงก์บนหัวเว็บ แล้ว `menu.js` ย้ายไปท้ายลิ้นชัก (`.njmenu-portal`)
      **`[data-njportal]` ต้องมีจุดเดียวต่อหน้า** — `menu.js` ใช้ `querySelector` ตัวเดียว
- `menu.js`, `menu.css` **เมนูมือถือ (ลิ้นชัก)** — โหลดครบทั้ง 9 หน้า (เพิ่ม 2026-09-07)
  · ปัญหาที่แก้: **สไตล์ทั้งสามชุดซ่อนเมนูหลักทิ้งบนจอเล็กโดยไม่มีอะไรมาแทน**
    `home.css` @1040px `header nav{display:none !important}` · `marketplace.css` @1079px `.topbar nav{display:none}` (เดิม 900 · ขยับ 2026-09-10 ดูเหตุผลท้ายไฟล์นั้น)
    · `ui.css`/`style.css` `.header-nav{display:none}` @984px (เดิม 860 · ขยับ 2026-09-10 · ดูเหตุผลท้าย `style.css`) — เหลือแค่แถบล่าง 5 ปุ่ม คนบนมือถือจึงเข้า
    มาตรฐาน · ทีมเรา · บริการ · บริการหลังซื้อที่ดิน · ขั้นตอน · ตรวจทรัพย์ก่อนซื้อ · วิดีโอ · ที่ดินประกาศขาย **ไม่ได้เลย**
  · ⚠️ **สร้างรายการจาก `<nav>` ของหน้านั้นเอง ห้ามพิมพ์รายการเมนูซ้ำใน `menu.js` เด็ดขาด**
    9 หน้ามีเมนูไม่เหมือนกัน · พิมพ์ซ้ำ = วันหนึ่งเพิ่มเมนูบนเดสก์ท็อปแล้วมือถือไม่มี โดยไม่มี error
    (กับดักเดียวกับสไตล์การ์ดสองไฟล์ และ `messenger_click` ที่เคยมีฝั่งเดียว)
  · ⚠️ **ตัดสินว่าจะโชว์ปุ่มไหม จาก `getComputedStyle(nav).display` ไม่ใช่เลข breakpoint**
    สามชุดใช้จุดตัดคนละค่า เขียนเลขไว้ = ต้องแก้ให้ตรงกันสี่ที่ และจะมีช่วงความกว้างที่
    "เมนูหายแต่ปุ่มยังไม่มา" หรือ "มีทั้งสองอัน" โดยไม่มีใครสังเกต · เช็คซ้ำตอน resize ด้วย
  · ⚠️ **z-index ต้องสูงกว่า 900** — `.cs-bottombar` (800) และแบนเนอร์คุกกี้ (900) เป็น fixed ทั้งคู่
    ต่ำกว่านั้นแถบล่างจะทับลิ้นชักแล้วดูเหมือนเมนูพัง (เจอจริงตอนพรีวิวหน้าฝากขาย)
  · ปุ่มสร้างด้วย JS ทั้งหมด — ไฟล์โหลดไม่สำเร็จ = ไม่มีปุ่มที่กดแล้วไม่มีอะไรเกิดขึ้น (กลับไปสภาพเดิมพอดี)
  · คีย์บอร์ด: Escape ปิด · โฟกัสถูกขังในลิ้นชัก (ประกาศ `aria-modal` เฉยๆ เบราว์เซอร์ไม่กันให้)
    · ปิดแล้วโฟกัสกลับไปที่ปุ่มเสมอ ไม่ปล่อยค้างบนของที่เพิ่งถูกซ่อน
  · **ตัดปุ่ม "ฝากขายฟรี" ออกจากหัวเว็บที่ ≤640px** — ที่ 390px ชื่อแบรนด์กับปุ่มถูกบีบจนตัดบรรทัดกลางคำ
    ทางเข้ายังมีอีก 3 ทางบนจอเดียวกัน (แถบล่าง · ปุ่มใน hero · ท้ายลิ้นชัก)
    และหน้าที่ใช้ `marketplace.css` ซ่อน `.post-btn` ที่ 640px อยู่ก่อนแล้ว กติกาจึงตรงกันทั้งเว็บ
  · **ลิ้นชักหยิบลิงก์นอก `<nav>` มาได้ 3 อย่างเท่านั้น** — ปุ่มหลัก (`CTA_SELECTORS`)
    · ทางเข้าพอร์ทัลลูกค้า (`PORTAL_SELECTOR` = `[data-njportal]`) · ทางเข้าแชท (`CHAT_SELECTOR` = `[data-njchat]`
    ใช้เฉพาะ `index.html` · ข้ามให้เองถ้าหน้านั้นมี href เดียวกันใน `<nav>` แล้ว) · ทั้งสามอ่าน `href`/ข้อความ
    จากลิงก์จริงบนหน้า **ไม่ได้พิมพ์ค่าซ้ำไว้ใน `menu.js`** จึงไม่ขัดกับกติกาห้ามพิมพ์เมนูซ้ำข้างบน
- `analytics.js` API base, analytics, consent banner และ **ที่อยู่ช่องทางติดต่อ** ใช้ร่วมกันทุกหน้า
  · `NJ_MESSENGER_URL` = `m.me/<ชื่อผู้ใช้เพจ>` — **ผูกกับชื่อผู้ใช้เพจ ไม่ใช่ชื่อเพจ** เปลี่ยนชื่อผู้ใช้เมื่อไหร่ลิงก์พังทันที
- `guides.html`, `guides.css` หน้าคู่มือที่ดิน 7 หัวข้อ (มี anchor รายหัวข้อ)
- `valuecalc.js`, `valuecalc.css` เครื่องมือ "ประเมินหลักการราคาที่ดิน" ใน `guides.html#valuation`
  · **คนละเรื่องกับ `feecalc.js` อย่าเอามาปนกัน** — `feecalc.js` คำนวณ "ราคาประเมินราชการ" เพื่อภาษี/ค่าธรรมเนียม
    ส่วนตัวนี้จำลอง "หลักการประเมินราคาตลาด" แบบ WQS (ให้คะแนนปัจจัยแล้วถ่วงน้ำหนัก) เพื่อการศึกษา
  · น้ำหนักปัจจัย (ทำเล 50% / รูปร่าง 25% / สาธารณูปโภค 15% / อื่นๆ 10%) และเพดาน ±40% เป็นค่ากลางแบบง่าย
    **ไม่ใช่สูตรทางการของสภาวิชาชีพการประเมินมูลค่าทรัพย์สิน** — คำเตือนใต้ผลลัพธ์ห้ามถอด
  · ส่วนสิ่งปลูกสร้างเรียก `NJFeeCalc.estimateBuilding()` ตัวเดียวกับเครื่องคำนวณค่าโอน จะได้ไม่ให้ตัวเลข 2 ที่ขัดกัน
    จึงต้องเลือกจังหวัดเหมือนกัน ไม่เลือก = คืน `null` ต้องขึ้นข้อความบอกเหตุผล **ห้ามเดาตัวเลขแทน**
  · บัญชีราคาโหลดผ่าน `NJFeeCalc.loadBuildingPrices()` ซึ่งแคช promise ไว้ — 2 เครื่องมือบนหน้าเดียวกันยิงเน็ตครั้งเดียว
  · แก้สูตรเมื่อไหร่ต้องรัน `node valuecalc.test.js` ให้ผ่านครบก่อนเสมอ (28 ข้อ)
- `warm.css` ชั้น "คนและความอบอุ่น" ของ `guides.html` `land.html` `videos.html`
- `photos.js` **ยังไม่มีหน้าไหนเรียกใช้** ตั้งแต่หน้าแรกเปลี่ยนดีไซน์ (2026-08-29)
  หน้าแรกใหม่ฝัง `<img src="photos/...">` ตรงๆ ไม่ได้ผ่านตัวจัดการช่องรูปของ `photos.js`
  พอมีรูปถ่ายจริงครบแล้วจะต่อช่องรูปกลับเข้าหน้าแรกหรือทิ้ง `photos.js` ไปเลย ให้ตัดสินใจตอนนั้น
- **รูป 6 ใบบนหน้าแรกตอนนี้เป็นภาพสต็อก ไม่ใช่รูปทีมงานจริง** (เจ้าของสั่งใช้เอง 2026-08-29)
  `hero.jpg` `survey.jpg` `site.jpg` ครอปจากภาพช่างรังวัดหน้าไซต์ก่อสร้าง ·
  `marker.jpg` `marker-wide.jpg` `house.jpg` ครอปจากภาพหมุดหลักเขตในสนามหญ้า
  · **ผ่านข้อ 9 ได้เพราะ `alt` ไม่ได้อ้างว่าเป็นทีมงานเราหรือแปลงลูกค้า** (ข้อ 9 แก้ใหม่ 2026-09-07)
    เขียน `alt` ใหม่ให้อ้างความเป็นเจ้าของเมื่อไหร่ = ผิดข้อ 9 ทันที · ยังขัดกับ `photos/README.md` อยู่
    เก็บบรรทัดนี้ไว้ให้คนที่มาทีหลังรู้ว่าไม่ใช่ความพลาด
  · คำบรรยาย `alt` เลี่ยงการอ้างว่าเป็นทีมงานของเราหรือแปลงของลูกค้า อย่าเขียนใหม่ให้อ้างแบบนั้น
  · ถ่ายรูปจริงมาแล้วให้ทับไฟล์ชื่อเดิมได้เลย ขนาด/สัดส่วนอยู่ในแอตทริบิวต์ `width`/`height` ของแต่ละ `<img>`
- `photos/` รูปจริงของทีมงาน · **ดู photos/README.md ก่อนเพิ่มรูป**
  ตั้งชื่อไฟล์ตามสเปกแล้วรูปขึ้นเอง ไม่ต้องแก้โค้ด
  · **เว็บจริง**: ช่องที่ยังไม่มีรูปถูกซ่อน (ทั้งกลุ่มไม่มีรูป = ซ่อนทั้งกลุ่ม) ลูกค้าไม่เห็นกรอบว่าง
  · **ดูว่าขาดรูปไหน**: เติม `?photos=dev` ท้าย URL → กรอบประบอกสเปกทุกช่อง
  · **ดูว่าลูกค้าเห็นอะไร** (ตอนเปิดจาก localhost): เติม `?photos=live`
- `land.html`, `land.js`, `land.css` หน้ารายละเอียดแปลง — เรียกด้วย `land.html?id=OP-xxx`
  · **ลำดับบนหน้าตั้งใจให้ราคาและค่าโอนอยู่บนสุด**: รูป → ราคา → เครื่องคำนวณค่าโอน → ชื่อแปลง →
    ข้อมูลแปลง → แผนที่ → ผลตรวจ → ปุ่มติดต่อ → ปุ่ม PDF (ย้ายขึ้นมาเมื่อ 2026-08-30 ตามคำสั่งเจ้าของ)
  · **สไลด์ภาพ** `galleryHtml()`/`initGallery()` — เลื่อนเองทุก 4.2 วิ · ผู้ใช้แตะอะไรก็หยุดทันที
    เคารพ `prefers-reduced-motion` (ไม่เลื่อนเอง) · รูปเดียวไม่สร้างปุ่มควบคุมเลย
  · **สถานที่ใกล้เคียง** `nearbyHtml()` — แสดงเมื่อ API ส่ง `land.nearby` มาเท่านั้น ไม่มีก็ซ่อนทั้งหัวข้อ (กติกาข้อ 5)
  · **แผนที่** `mapHtml()` มี 2 โหมด — มีหมุด `pinLat/pinLng` = ปักตำแหน่งจริง + ลิงก์ขอเส้นทาง ·
    ไม่มีหมุด = ค้นด้วย `locality` ได้แผนที่ระดับพื้นที่ · **ห้ามเปลี่ยนไปอ่าน `land.lat/lng`** (ดูกติกาข้อ 3)
  · **ปุ่ม PDF** ใช้ `window.print()` + `@media print` ใน `land.css` ไม่ได้ใช้ไลบรารีใดๆ
    สไตล์พิมพ์ซ่อนเมนู/ฟุตเตอร์/ฟอร์มคำนวณ/แผนที่ (iframe พิมพ์ไม่ติดในเบราว์เซอร์ส่วนใหญ่)
  · **แถบรหัสทรัพย์ `.ld-code`** (`codeHtml()` · เพิ่ม 2026-09-06) — วางใต้ชื่อแปลง
    เดิมผู้ซื้อทักไลน์มาว่า "สนใจที่แปลงนึงในเว็บ" โดยไม่บอกว่าแปลงไหน ทีมจับคู่กลับไม่ได้
    แปลว่า **เจ้าของที่ดินไม่เคยรู้ว่ามีคนสนใจแปลงของตัวเอง** ซึ่งเป็นสิ่งที่เราสัญญากับเขาไว้
    · **ห้ามซ่อนตอนสั่งพิมพ์** — คนที่พิมพ์ประกาศออกมาอ่านคือคนที่ต้องใช้รหัสนี้ตอนโทรมาที่สุด
    · ปุ่มคัดลอกต้องมีทางถอย (`prompt()`) เสมอ — `navigator.clipboard` ใช้ไม่ได้บน http:// และในเว็บวิวบางตัว
      ล้มแล้วเงียบ = ผู้ซื้อคิดว่าคัดลอกแล้ว วางในไลน์ไม่ติด แล้วก็ไม่แจ้งรหัสอยู่ดี
  · **กล่อง "สนใจแปลงนี้" `.ld-inq`** — `NJServices.mount()` พร้อม `listingId` ที่ล็อกไว้อ่านอย่างเดียว
    ผู้ซื้อจึงไม่มีทางพิมพ์รหัสผิดจนแอดมินตรวจไม่เจอ · ซ่อนตอนพิมพ์ (`@media print`)
- `s/index.html`, `s/share.css` **หน้ารับคนดูจากลิงก์ที่พนักงานแชร์** — `njteedinsure.com/s/?e=<รหัสพนักงาน>`
  · หน้าเดียวในเว็บที่ไม่ใช้ `marketplace.css` โดยตั้งใจ — คนมาจาก TikTok/Facebook ไม่ได้มาหาแปลงที่ดิน
    เมนู แถบค้นหา และ bottom nav คือสิ่งรบกวนล้วนๆ · **ตัวแปรสีถูกคัดลอกไว้ ไม่ได้ import** แก้โทนแบรนด์ต้องแก้ 2 ไฟล์
  · `?e=` = รหัสพนักงานผู้แชร์ · `?v=clipN` = เลือกคลิป (ค่าตั้งต้น `clip5`)
    **รหัสถูกเอาไปต่อในข้อความที่ลูกค้าจะกดส่งเข้า LINE** จึงกรองเหลือ `[a-z0-9]{2,8}` เท่านั้น ผิดรูปแบบให้ทิ้ง
  · เก็บรหัสใน **sessionStorage ไม่ใช่ localStorage** — เครดิตผูกกับการเข้าครั้งนี้ ไม่ใช่ข้ามเดือน
  · แบนเนอร์ PDPA บนหน้านี้อยู่ **ด้านบน** ต่างจากหน้าอื่น เพราะแบบล่างทับปุ่ม "ทักไลน์" พอดี (วัดแล้วบนจอ 375×812)
  · ปุ่มติดต่อมี `href` ใช้งานได้ตั้งแต่ใน HTML แล้ว JS ค่อยเติมรหัสให้ — `analytics.js` ล่มแล้วปุ่มต้องไม่ตาย
- `brand/` โลโก้ favicon และ social preview

## น้องเอ็นเจชัวร์ — แชท AI ผู้ช่วยลูกค้า (2026-09-09)

`njchat.js` · `njchat.css` · `chat.html` (หน้าเต็ม) · `njchat.test.js` (42 ข้อ) · `njchat.e2e.js` (Playwright 55 ข้อ · รันมือ)
วิดเจ็ตมุมจอโหลดครบทั้ง 12 หน้า (`index listings land compare guides portal consign verify wanted videos deal inspect`)
· ฝั่งหลังบ้าน: `POST /api/public/njchat` + `lib/njchat.js` + `test/njchat.test.js` ใน `nj-survey-system`

### ทำงานสองชั้น — กฎก่อน แล้วค่อย AI
1. **กฎในไฟล์ (`route()`) ตอบเองก่อน** ไม่มีค่าใช้จ่าย ทำงานได้แม้ AI ล่ม:
   ค้นแปลง (จังหวัด/อำเภอ/งบ/เนื้อที่/เอกสารสิทธิ์/ผังสี/สิ่งที่แปลงมี/เรียง) · เทียบแปลง · ค่ารังวัด ·
   ขั้นตอนฝากขาย/ยื่นรังวัด · เอกสาร · คิว · ค่าโอน (ลิงก์เครื่องคำนวณ) · ราคาประเมิน · ตรวจทรัพย์ · ฝากหา · พอร์ทัล · ติดต่อ
   · **ข้อพิพาทแนวเขต/เพื่อนบ้านรุกล้ำ** (เพิ่ม 2026-09-11) — ถามบ่อยและเป็นงานหลักของเราโดยตรง ปล่อยให้ AI ตอบแล้วเคยได้
     คำมั่ว ("ส่วนราชการ (อสม./สมุนไขมัน)") บนของจริง · กฎนี้ตอบเองและ **ห้ามให้คำแนะนำทางกฎหมาย**
     ตอบได้แค่ "ยืนยันแนวเขตด้วยการรังวัดก่อน" แล้วส่งต่อทนาย/ทีมงาน (มีเทสต์ล็อก)
     ⚠️ "เพื่อนบ้าน/ข้างบ้าน" เดี่ยวๆ ไม่พอ ต้องมีคำที่บ่งว่ามีปัญหา หรือคู่กับคำเรื่องแนวเขต
     ไม่งั้น "ที่ดินข้างบ้านขายไหม" (คนอยากซื้อ) จะถูกดูดมาตอบผิดเรื่อง
2. **จับกฎไม่ได้ → `POST /api/public/njchat`** ให้ Claude ตอบจากฐานความรู้ + แปลงที่ประกาศอยู่จริง
   · เซิร์ฟเวอร์ถือ `ANTHROPIC_API_KEY` (เว็บนี้สแตติก **ห้ามมี key ที่นี่** — กติกาเดียวกับ `portal.html`)
   · ตอบ `{ok:false}` (ไม่มี key / AI ล่ม / โควตา) = หน้าเว็บถอยไปให้ทักไลน์/โทร ไม่พัง · ข้อความจาก AI ติดป้าย "🤖 ตอบโดย AI"

### กติกาที่ห้ามผ่อน (สืบทอดจากข้อ 4 · 5 · 10 · 14)
- **แปลงมาจาก `NJListing.fetchListings()` เท่านั้น** การ์ดใช้ `NJListing.card()` (ห้ามก๊อป) · `compare.js` แปะปุ่ม "เทียบ" บนการ์ดในแชทให้เอง
- **"ยังไม่ระบุ" ≠ "ไม่ตรง"** — `applySearch()` ถอดจาก `listings.js` ทั้งก้อน: ช่องที่ว่างไม่นับว่าตรง
  แต่ต้องบอกจำนวนที่ถูกซ่อนเสมอ (`hiddenUnknown`) · ตารางเทียบในแชทขึ้น "—" พร้อมคำอธิบาย
- **ประเภทสิ่งปลูกสร้าง + จำนวนชั้น กรองได้จริงตั้งแต่ 2026-09-11** (`land.propertyType` · `land.floors`)
  ตัวกรองบนหน้ารวมประกาศคือ `#f-prop` + `#f-floors` · `njchat.js` อ่านเป็น `f.prop`/`f.floors` แล้วกรองด้วยกติกาเดียวกัน
  · **เดิมข้อนี้เขียนว่า "API ไม่มีช่องให้กรอง ต้องบอกตามจริง"** — ตอนนั้น `f.building` เป็นแค่ธง
  แล้วคัดตามทำเลให้แทน พร้อมนับแปลงที่ "ประกาศเอ่ยถึงสิ่งปลูกสร้าง" จาก `parcelInfo`/`blurb` · ตอนนี้ไม่ต้องแล้ว
  · ⚠️ **ค่าว่าง = ยังไม่ได้กรอก ไม่ใช่ "ที่ดินเปล่า"** — แปลงที่ยังว่างต้องถูกนับเป็น `hiddenUnknown`
  แล้วบอกจำนวน เหมือนช่องอื่นทุกช่อง ห้ามเดาแทนหรือซ่อนเงียบๆ (ตอนเขียนข้อนี้ ทั้ง 21 แปลงบนเว็บยังว่างหมด)
  · ⚠️ ห้ามใช้คำว่า "บ้าน" โดดๆ จับโจทย์ — "ที่ดินข้างบ้าน" คือคนอยากซื้อที่ดิน ไม่ได้อยากได้บ้าน
  · ⚠️ `landvocab.js` ต้องโหลด**ก่อน** `listings.js` — ตัวกรองเติมชื่อไทยตอนสร้างตัวเลือก ถ้ามาทีหลังช่องจะว่างเงียบๆ
- **ค่ารังวัดคิดจาก `NJPricing.computeQuote` เท่านั้น** (โหลด `pricing.js` แบบเดียวกับ `surveyquote.js` · ส่ง `nights` ของโซนเสมอ)
  โหลดไม่ได้ = ไม่มีตัวเลข ให้ทักไลน์ · ไม่บอกจังหวัด = เตือน "ยังไม่รวมค่าเดินทาง" · ทุกคำตอบราคากำกับว่าเป็นประมาณการ
  · กทม.+ปริมณฑล อ่านจาก `P.ZONE_PROVINCES.A` (`METRO_FALLBACK` ใช้เฉพาะตอนตารางยังไม่มา — ตรงกับ `SURVEY_REQUIRED_PROVINCES`)
- **ไม่มีตัวเลขค่าโอน/ภาษี/ค่ารังวัดพิมพ์ไว้ในไฟล์** — `KB` มีแค่ข้อเท็จจริงที่หน้าเว็บพิมพ์อยู่แล้ว (ค่านายหน้า 3% · 1 วันทำการ · เวลาทำการ)
- น้องบอกว่าเป็น AI ตั้งแต่ข้อความแรก · ห้ามรับปากแทนทีมงาน · "ขอใบเสนอราคาจริง" = ส่งต่อไลน์ ไม่คิดซ้ำ
- **สถิติ**: `chat_open` ขึ้นทะเบียนแล้วทั้ง `NJ_INTERNAL_EVENTS` และ `PUBLIC_EVENT_TYPES` (ไม่ใช่ลีด · ห้ามบวกเข้า `leads`)
  ปุ่มติดต่อในแชทใช้แมป `CONTACT_TRACK` เดียวกับการ์ด (กติกาข้อ 6) · **บทสนทนาไม่ส่งไป `/api/public/track`** (ไม่มี PII)
  · ประวัติเก็บ `sessionStorage['njchat']` (กติกาเดียวกับ `njCompare`)
- คำที่ใช้จับโหมดค้นต้องเจาะจง — **ห้ามใช้ "หา" "น้ำ" "ถม" "บ้าน" โดดๆ** ("ปัญหา" "น้ำท่วม" "ข้างบ้าน" จะหลุดเข้าโหมดค้น) มีเทสต์ล็อกไว้

### ตำแหน่งบนจอ (กับดักที่เจอตอนทดสอบจริง)
- z-index ปุ่ม 790 · แผง 890 — **ต่ำกว่าแบนเนอร์คุกกี้ (900)** ซึ่งเป็นเรื่องกฎหมายต้องอยู่บนสุด
  → แบนเนอร์บังปุ่มจนกดไม่ได้ทั้งหน้าที่ 390px · แก้ด้วย `dodgeConsent()` ยกปุ่ม/แผงขึ้นเหนือแบนเนอร์
  (วัดจาก `#nj-consent` จริง · เฝ้า `body` childList เพื่อคืนตำแหน่งเมื่อผู้ใช้ตอบ · แก้แค่ `style` จึงไม่วนลูปแบบ compare.js)
- จอเล็ก: ปุ่มยกพ้น `.mobile-nav` (69px) และ `.cs-bottombar` (74px) · แผงเต็มจอ + `html.njchat-open{overflow:hidden}`
- สไตล์การ์ด `.njchat-cards .land-card` ประกาศครบชุดใน `njchat.css` เพราะหน้า `ui.css` (ฝากขาย/ตรวจทรัพย์/ฝากหา)
  ไม่มีสไตล์ `.land-card` เลย — **ใช้เฉพาะในกล่องแชท** ไม่ใช่ชุดที่สามของเว็บ · เพิ่มคลาสใหม่ให้การ์ดต้องมาเพิ่มที่นี่ด้วย
- `njchat.js` ต้องมาหลัง `analytics.js` · `listingcard.js` (+`verified.js` ก่อนหน้า) · `landvocab.js` · `compare.js` และก่อน `menu.js`
  · `njchat.css` โหลดคู่ `compare.css` + `verified.css` ทุกหน้า (หน้าที่ไม่เคยมีการ์ดถูกเติมให้แล้ว)
- **ลิงก์ "แชทกับน้อง" ในเมนู `<nav>`** อยู่ครบ 12 หน้า (7 หน้าชุด `marketplace.css` ใน `<nav aria-label="เมนูหลัก">` ·
  5 หน้าชุด `ui.css` ใน `<nav class="header-nav">` รวม `deal.html` กับ `inspect.html` ที่ได้วิดเจ็ตแชทครบแล้ว
  เช่นกัน) — ลิ้นชักจอเล็กได้ตามไปเองเพราะ `menu.js` สร้างรายการจาก `<nav>` จริง
  · **ยกเว้น `index.html`** ที่แถวหัวเว็บเต็มความกว้างพอดีแล้ว (มีคำเตือนไว้ในไฟล์) — วัดจริงที่ 1440 แล้วเติมลิงก์เดียว
  โลโก้ถูกบีบจาก 153 เหลือ 97px และเมนูทั้ง 10 ข้อขึ้นสองบรรทัด จึงวางลิงก์ไว้ในฟุตเตอร์พร้อม `data-njchat`
  แล้วให้ `menu.js` หยิบเข้าลิ้นชักแทน (กติกาเดียวกับ `data-njportal` · เช็ก href ซ้ำกับใน `<nav>` ก่อนเติม)
  · ⚠️ แถวหัวเว็บทั้งสองชุดพอดีตัวมาก (marketplace เหลือ ~15px ที่จอ 1440) — ช่วงที่แคบกว่านั้น flex จะบีบ
  ข้อความ **ทุกข้อ** ในเมนูให้ขึ้นสองบรรทัดพร้อมกัน ไม่ใช่แค่ข้อที่เพิ่งเติม จึงซ่อนเฉพาะลิงก์แชท
  ที่ `max-width:1224px` (marketplace · กฎอยู่ใน `njchat.css`) และ `1079px` (ui.css · กฎอยู่ใน `style.css`
  ไม่ใช่ `njchat.css` เพราะ `deal.html`/`inspect.html` มีลิงก์แต่ไม่ได้โหลดไฟล์นั้น)
  ส่วนตัวแถบเมนูเองซ่อนที่ `1079px` และ `984px`
  ตามลำดับ — ตัวเลขทั้งหมดวัดจากของจริงโดยยึดหน้าที่แย่ที่สุดของแต่ละชุด ที่มาอยู่ในคอมเมนต์ท้าย
  `njchat.css` · `marketplace.css` · `style.css` (`deal.html` ไม่ต้องขยับ เพราะแถวหัวเว็บไม่มีปุ่มหลัก)
  ลิ้นชักยังมีรายการนี้เสมอเพราะ `menu.js` อ่านจาก DOM ไม่สนใจ `display` · **เพิ่มลิงก์เมนูอีกไม่ได้แล้วโดยไม่วัดใหม่**

### ทดสอบ
`node njchat.test.js` (42 ข้อ · node:vm · ตารางราคาจาก `../nj-survey-system` ถ้ามี) · `node contracts.test.js` ยังผ่าน 86 ข้อ ·
`node njchat.e2e.js` (Playwright · เซิร์ฟเวอร์ปลอมในไฟล์ · pricing.js ของจริง): มือถือ 390 + เดสก์ท็อป 1440 · ทั้ง 10 หน้า ·
โจทย์ "บ้าน 1 ชั้น กทม.ปริมณฑล เปรียบเทียบ" · เทียบ · ค่ารังวัดตรงกับ `computeQuote` ทุกบรรทัด (25,000+10,000+72,000) ·
API ล่ม vs count:0 ขึ้นคนละข้อความ · AI ใช้ไม่ได้ถอยไปไลน์ · ไม่ล้นแนวนอน · ไม่มี console error

## กติกาการพัฒนา

1. รักษาภาษาไทยเป็นภาษาหลักและออกแบบ mobile-first
2. จุดขาย “รังวัดยืนยันแล้ว” ต้องเห็นบนการ์ดทุกแปลง
3. ห้ามเผยเลขโฉนด ชื่อเจ้าของ หรือข้อมูลส่วนบุคคลโดยไม่ได้รับความยินยอม
   · **เรื่องพิกัดเปลี่ยนกติกาแล้ว 2026-08-30** — API ส่ง `land.pinLat`/`land.pinLng` ออกมาได้
     ซึ่งเป็น**หมุดที่ทีมงานกดเลือกเองรายแปลง**ในระบบหลังบ้าน ไม่ใช่พิกัดจากงานรังวัด
     `land.lat`/`land.lng` (พิกัดภายใน) ยังถูกตัดออกเสมอเหมือนเดิม — ห้ามเปลี่ยนมาอ่านสองค่านั้น
   · แปลงที่ไม่ได้ปักหมุด = `mapHtml()` ถอยไปค้นด้วยข้อความ `locality` ได้แผนที่ระดับพื้นที่แทน
4. **ประกาศทุกแปลงมาจาก `GET /api/public/listings` เท่านั้น — ห้ามมีข้อมูลแปลงตัวอย่างในโค้ดเด็ดขาด**
   ถ้า API ตอบ `count: 0` ให้แสดงกล่อง "ยังไม่มีแปลงประกาศ" พร้อมทางไปต่อ (ฝากขาย/ทักไลน์) ไม่ใช่แสดงแปลงสมมติ
   เหตุผล: การ์ดทุกใบติดป้าย "รังวัดยืนยันแล้ว" ผู้ซื้อจึงอ่านทุกบรรทัดเป็นข้อเท็จจริงที่ตรวจสอบได้
   แปลงสมมติบนเว็บจริงทำลายสิ่งเดียวที่แบรนด์นี้ขาย และขัดกับประตูกันการเผยแพร่ที่ล็อกไว้ใน server.js
   (เคยมีของจริงหลุดเข้ามาแล้วครั้งหนึ่ง — แปลงสมมติ 3 แปลงพร้อมราคาและทำเลจริง ถูกถอดออกเมื่อ 2026-08-25)
5. **ห้ามเติมข้อความแทนช่องที่ API ไม่ได้ส่งมา** เช่น "มีทางเข้าออก" "โฉนด" "ตรวจสอบเนื้อที่แล้ว"
   หรือจำนวนรูปขั้นต่ำ — ช่องไหนไม่มีข้อมูลให้ซ่อนบรรทัดนั้นไป
   API ส่งมาแค่: `id · type · parcelInfo · estValue · blurb · photos · updatedAt · tier · land`
   **· เพิ่ม 2026-09-04: `totalWa` · `pricePerWa` · `pricePerRai`** — คำนวณที่เซิร์ฟเวอร์เสมอ
     **ห้ามหารเองในเบราว์เซอร์** ไม่งั้นหน้ารวมกับหน้ารายละเอียดได้ตัวเลขคนละค่าเมื่อปัดเศษต่างกัน
     ไม่มีเนื้อที่หรือไม่มีราคา = ส่ง 0 มา ต้องซ่อนบรรทัดนั้น ห้ามแสดงเป็น "฿0/ตร.ว."
   · ใน `land` มี: `deedArea zoning locality frontage pinLat pinLng nearby verified verifiedAt verifiedBy checks`
     **· เพิ่ม 2026-09-04 (ช่องแบบเลือกจากรายการ ใช้ทำตัวกรอง): `deedType` `zoneColor` `roadSurface`
       `facing` `roadLanes` `features[]` `province` `amphoe` `tambon`**
       ทุกช่องว่างได้ · **ว่าง = "ยังไม่ได้ระบุ" ซึ่งไม่เหมือน "ไม่มี"** ห้ามแสดงว่าไม่มี
       `zoneColor` เป็นค่าตายตัวที่กรองได้ **แยกจาก `zoning` ที่เป็นข้อความอิสระ ห้ามยุบรวม**
       `features[]` ใช้คีย์ชุดเดียวกับหน้าฝากหาที่ดิน (`BR_FEATURES`) เพื่อให้จับคู่ "คนหา ↔ แปลง" ได้
     (ไม่มี `lat` `lng` `ravaeng` `landNo` `aiAnalysis` `sellerType` `holdYears` — ถูกตัดที่ `publicLand()` ใน server.js)
   · `land.nearby` = สถานที่ใกล้เคียงจาก Google Places `{groups:[{key,label,icon,items:[{name,km}]}],total,at}`
     **เป็นข้อมูลบุคคลที่สาม ไม่ใช่ผลตรวจของทีมช่างรังวัด** — หน้าตาต้องต่างจากแผงผลตรวจ 7 หัวข้อ
     และต้องบอกที่มา + วันที่สำรวจเสมอ ห้ามนำเสนอเป็นคำยืนยันว่าวันนี้สถานที่นั้นยังอยู่
10. **ระดับความน่าเชื่อถือของประกาศมี 2 ระดับ ห้ามแสดงปนกัน**
   - `tier: 1` = ยังไม่รังวัด → ป้าย**เหลือง** “◐ ข้อมูลเบื้องต้น” · **ห้ามขึ้นป้าย “รังวัดยืนยันแล้ว” เด็ดขาด**
   - `tier: 2` = ตรวจเชิงลึกแล้ว → ป้าย**เขียว** “✓ รังวัดยืนยันแล้ว” + ผลตรวจ 7 หัวข้อ + วันที่ตรวจ
   - ผลตรวจที่ `status` ว่าง = **“ยังไม่ได้ตรวจ”** ต้องแสดงเป็นขีด “—” เท่านั้น
     ห้ามแสดงเป็นติ๊กถูกหรือซ่อนบรรทัดไป เพราะ “ยังไม่ตรวจ” กับ “ตรวจแล้วไม่มีปัญหา” คนละเรื่องกัน
   - ข้อความผลตรวจเรื่องอายัด/คดี/ภาษี เป็นข้อเท็จจริง **ณ วันที่ตรวจ** ต้องแสดงวันที่เสมอ
     และห้ามใช้คำรับประกัน — ส่วนนี้ควรให้ที่ปรึกษากฎหมายตรวจก่อนแก้ข้อความ
6. ค่าติดต่อหลักคือ `02-162-0405` (สำนักงาน) และ `084-915-8601` (มือถือ) · LINE `@716lffzt` · Messenger `m.me/NJTeeDinSure`
   · **สองเบอร์นี้ต้องมาคู่กันทุกที่** — ที่ไหนมีเบอร์สำนักงาน ที่นั่นต้องมีเบอร์มือถือด้วย (สั่งโดยเจ้าของ 12 ก.ย. 69)
     ค่าคงที่ชื่อ `COMPANY_TEL` / `COMPANY_TEL_ALT` (`consign.js` `verify.js` `wanted.js` `app.js`) ·
     `TEL_TXT` / `TEL2_TXT` (`land.js` `njchat.js`) · `TEL` / `TEL2` (`listingcard.js`)
   · ลิงก์โทรของเบอร์มือถือใช้ `data-contact="tel"` และเหตุการณ์ `tel_click` **ตัวเดิม** —
     เบอร์ที่สองไม่ใช่ช่องทางใหม่ จึงไม่ต้องขึ้นทะเบียนเพิ่มที่ `analytics.js` / `server.js`
   **เพิ่มช่องทางติดต่อใหม่ต้องแก้ 4 ที่เสมอ** ไม่งั้นตัวเลขลีดจะต่ำกว่าความจริงแล้วสรุปผิดว่าไม่ผ่านเกณฑ์ Phase 2:
   (1) ปุ่มบนหน้าเว็บ + `data-contact` (2) **`NJ_INTERNAL_EVENTS` ใน `analytics.js`**
   (3) `PUBLIC_EVENT_TYPES` ใน `server.js` (4) ผลรวม `leads` ใน `/api/public/track/summary`
   · ตัวดักคลิกต้องเป็น **แมป** ไม่ใช่ `if/else` สองทาง — ของเดิมเป็น if/else ช่องทางที่ 3 เลยถูกนับเป็น "กดโทร"
   · ที่ (2) เคยตกหล่นมาก่อน: `messenger_click` มีอยู่ใน `server.js` แต่ไม่มีใน `analytics.js`
     คลิก Messenger ทุกครั้งจึงถูกทิ้งตั้งแต่ฝั่งเบราว์เซอร์ **โดยไม่มี error ให้เห็น** (แก้แล้ว 2026-08-28)
7. คง semantic HTML, keyboard navigation, contrast และ `prefers-reduced-motion`
8. หลีกเลี่ยง framework จนกว่าจะมีเหตุผลด้าน routing, authentication หรือ state ที่ชัดเจน
9. **ภาพและวิดีโอ — แยกตามว่าสิ่งนั้น "อ้างอะไร" ไม่ใช่แยกตามว่าสร้างด้วยอะไร** (แก้ 2026-09-07)
   หลักการเดิมจากข้อ 4 ไม่เปลี่ยน: เว็บนี้ขายความจริงที่ตรวจสอบได้ · สิ่งที่ห้ามคือ**ภาพที่ทำให้คนเข้าใจผิด
   ว่าเป็นหลักฐาน** ไม่ใช่ห้ามงานที่ AI ช่วยสร้างทุกชิ้น · ของเดิมเขียนห้ามแบบเหมารวม แล้วเราก็ทำผิดกติกา
   ตัวเองอยู่ 2 จุด (ภาพสต็อก 6 ใบ · คลิปพรีเซนเตอร์ 2 ตัว) กติกาที่ไม่มีใครทำตามได้คือกติกาที่ใช้ไม่ได้
   · **ห้ามเด็ดขาด — ภาพที่อ้างถึงแปลงใดแปลงหนึ่ง** รูปแปลง รูปหมุด รูปแนวเขต รูปเอกสารสิทธิ์
     ต้องเป็นภาพถ่ายจริงของแปลงนั้นเท่านั้น **ห้าม AI ห้ามสต็อก ห้ามยืมรูปแปลงอื่น**
     ผู้ซื้ออ่านรูปพวกนี้เป็นหลักฐาน ปลอมเมื่อไหร่คือโกหกเรื่องทรัพย์ที่เขากำลังจะจ่ายเงินซื้อ
   · **ห้าม — ภาพที่อ้างว่าเป็นทีมงานหรือผลงานจริง** เช่น "ช่างของเราลงพื้นที่" "ผลงานที่เราทำ"
     ถ้ายังไม่มีรูปจริง ให้เขียน `alt` แบบไม่อ้างความเป็นเจ้าของ (ดูหมายเหตุภาพสต็อก 6 ใบข้างบน)
   · **อนุญาต — คลิป/ภาพเชิงแบรนด์และโฆษณาที่ไม่ได้อ้างว่าเป็นหลักฐาน** เช่น พรีเซนเตอร์พูดอธิบายบริการ
     กราฟิกอธิบายขั้นตอน ภาพประกอบเชิงแนวคิด · ใช้ AI สร้างได้ **ภายใต้ 3 เงื่อนไข**:
     1. **ใบหน้าที่ใช้ต้องได้รับความยินยอมจากเจ้าของใบหน้าก่อนเสมอ (PDPA)** — รวมถึงกรณีสร้างจากรูปถ่ายจริง
        เจ้าของสั่งใช้อย่างเดียวไม่พอ ต้องเป็นความยินยอมของ**คนในรูป**
     2. **ห้ามให้พรีเซนเตอร์พูดอ้างข้อเท็จจริงที่ตรวจสอบไม่ได้** เช่น ตัวเลขลูกค้า สถิติ หรือคำรับประกันผล
        พูดได้เฉพาะสิ่งที่บริษัททำจริงและมีหลักฐานรองรับ (ใบอนุญาต · บริการที่มีอยู่จริง)
     3. **ห้ามทำให้ดูเหมือนฟุตเทจหน้างานจริง** — ฉากต้องอ่านออกว่าเป็นสตูดิโอ/กราฟิก ไม่ใช่การถ่ายทำในสนาม
   · **ของที่ใช้อยู่จริงตอนนี้** (ทั้งคู่เข้าเงื่อนไขข้อ "อนุญาต" ข้างบน):
     `video/nj-intro.mp4` พรีเซนเตอร์ผู้ชาย = ภาพลักษณ์ของเจ้าของบริษัทเอง ·
     `video/teedinsure-ad.mp4` พรีเซนเตอร์ผู้หญิง = เจ้าของยืนยันความยินยอมของเจ้าของภาพแล้ว 2026-09-07
     · ทั้งสองคลิปสร้างด้วย Airova (omni_flash) จากรูปถ่ายจริง — **ไม่ใช่ฟุตเทจหน้างาน**
11. **ราคารวมที่ฟอร์มคำนวณให้เป็นค่าประมาณ ห้ามนำเสนอเป็นราคาที่ตกลงแล้ว**
   หน่วยที่ดินไทย: 1 ไร่ = 4 งาน = 400 ตร.ว. · ถ้าเจ้าของกรอกราคาต่อหน่วยแต่ยังไม่กรอกเนื้อที่
   ต้องขึ้นข้อความว่ายังคำนวณไม่ได้ **ห้ามเดาเป็นราคารวม** เพราะจะกลายเป็นตัวเลขผิดที่ดูเหมือนตัวเลขจริง
   และเซิร์ฟเวอร์คำนวณราคารวมใหม่เองเสมอ ไม่เชื่อ `estValue` ที่หน้าเว็บส่งไป

12. **เครื่องคำนวณค่าโอน (`feecalc.js`) ห้ามเติมราคาประเมินราชการให้อัตโนมัติ**
   · **ตอนนี้อยู่ 3 หน้า**: `land.html` (mount เอง ใต้ราคา) · `guides.html#valuation` · `index.html` (หน้าแรก)
     หน้าแรกใช้ `bootPage()` ในตัว `feecalc.js` เอง — ปุ่ม `[data-fc-open]` ในแถบบริการกางกล่อง `#fee-calc`
     mount ครั้งเดียวตลอดอายุหน้า ถ้า mount ซ้ำตัวเลขที่ผู้ใช้กรอกค้างไว้จะหายหมด
   · **`bootPage()` ต้องมี `typeof document !== 'undefined'` ครอบเสมอ** — `feecalc.test.js` รันไฟล์นี้ด้วย
     node ที่ไม่มี DOM ถอดเงื่อนไขออกเมื่อไหร่เทสต์ 50 ข้อพังทันทีตั้งแต่บรรทัดแรก (เคยพลาดมาแล้ว)
   ค่าธรรมเนียมโอนคือ 2% ของราคาประเมิน ใครเห็นตัวเลขค่าโอนของแปลงก็หารกลับได้ราคาประเมินทันที
   ถ้าเติมให้ = เท่ากับประกาศราคาประเมินของแปลงนั้นต่อสาธารณะ เจ้าของที่ดิน (ลูกค้าเรา) เสียเปรียบตอนต่อรอง
   ระบบหลังบ้านตั้งใจไม่ส่งราคาประเมินออกทาง `/api/public/*` อยู่แล้ว (ดู `publicLand()` ใน `server.js`)
   · หน้าเว็บเติมให้แค่ "ราคาซื้อขาย" ซึ่งเป็นตัวเลขที่ประกาศอยู่แล้ว
   · ไม่กรอกราคาประเมิน = ใช้ราคาขายแทน ได้ตัวเลข**สูงกว่าความจริง** ต้องบอกผู้ใช้ตรงๆ ทุกครั้ง
   · **ที่ดินเปล่าไม่ได้สิทธิลดค่าโอนเหลือ 0.01%** (มาตรการนั้นใช้เฉพาะที่อยู่อาศัย ≤ 7 ล้านบาท)
     ห้ามคำนวณให้เป็น 0.01% เพราะผู้ซื้อจะไปเจอตัวเลขจริงที่สำนักงานที่ดินแล้วดีลพัง
   · แก้สูตรภาษีเมื่อไหร่ ต้องรัน `node feecalc.test.js` ให้ผ่านครบก่อนเสมอ (50 ข้อ เทียบกับการคำนวณมือ)

14. **เครื่องประเมินราคารังวัด (`surveyquote.js`) ต้องถามจังหวัดเสมอ และต้องบวกค่าดำเนินการนอกพื้นที่**
   (เพิ่ม 2026-09-08 · อยู่ 2 ที่: กล่อง "เช็กราคางานรังวัด" บนหน้าแรก และกล่องประเมินในหน้าฝากขาย)
   · **เคสจริงที่ทำให้ต้องเขียนกติกานี้:** โซนเดินทาง 7 ขั้นขึ้นระบบหลังบ้าน 8 ก.ย. 2569 แต่เว็บ
     ไม่ได้บวกให้เลย — 1 ไร่ สอบเขต จ.เชียงใหม่ เว็บบอก **฿25,000** ขณะที่ใบเสนอราคาจริงคือ
     **฿97,000** (ต่ำไป 72,000) ลูกค้าที่เชื่อตัวเลขนี้แล้วมาคุยกับเซลส์ = ดีลพังตั้งแต่ยังไม่เริ่ม
   · **ตารางโซนและรายชื่อจังหวัดอ่านจาก `pricing.js` เท่านั้น** (`P.zoneOf` · `P.TRAVEL_ZONES` ·
     `P.ZONE_PROVINCES`) กติกาเดียวกับตารางราคา — ห้ามก๊อปมาไว้ฝั่งเว็บ **รวมถึงตัวเลขในข้อความ**
     ช่วง "12,000–75,000" ต้องมาจาก `travelRangeText()` ไม่ใช่พิมพ์ลงไปในประโยค
   · **ต้องส่ง `nights` ไปด้วยเสมอ = จำนวนคืนที่แนะนำของโซนนั้น** ให้ตรงกับ `travelNightsDefault()`
     ในฟอร์มใบเสนอราคาของระบบหลังบ้าน · ไม่ส่ง = `computeTravel` คิดคืนพักเป็น 0
     แล้วเว็บบอกราคาต่ำกว่าใบจริง 4,000–16,000 บาทแบบเงียบๆ
   · **ส่วนลดคอมโบ 5% ห้ามกินค่าเดินทาง** — คิดจาก `comboBase()` ที่หัก `travelTotal` ออกแล้ว
     กติกาเดียวกับที่ `pricing.js` กันส่วนลดลูกค้าเดิม 40% ออกจากค่าเดินทาง
     (ค่าโซน = ค่าน้ำมัน ค่าที่พัก ค่าเสียเวลาทีมบนถนน — เป็นเงินสดที่จ่ายออกจริง ลดแล้วขาดทุนจริง)
   · **ยังไม่เลือกจังหวัด = ต้องขึ้นคำเตือนว่ายอดนี้ยังไม่รวมค่าเดินทาง** ห้ามปล่อยให้อ่านเหมือนราคาครบ
   · หมายเหตุที่ลูกค้าเห็นต้องบอกด้วยว่าค่าโซนคิดเป็นขั้นตาม**จังหวัด** ไม่ได้วัดจากที่ตั้งแปลงจริง
   · ล็อกไว้ด้วย `node contracts.test.js` หัวข้อ 6 (9 ข้อ) ซึ่ง `require('pricing.js')` มาคิดจริง

13. **เครื่องคำนวณรองรับสิ่งปลูกสร้างแล้ว — 2 ค่านี้ห้ามคำนวณให้เองโดยผู้ใช้ไม่ได้สั่ง**
   · **อัตราลดค่าธรรมเนียมตามมาตรการรัฐ** ค่าตั้งต้นคือ **2% ตามกฎหมาย** เสมอ
     ผู้ใช้ต้องติ๊กเองถึงจะลดเหลือ 0.01% (หรือ 1%) — เพราะแหล่งข้อมูลสาธารณะ ณ ส.ค. 2569
     ยังขัดกันเองทั้งวันสิ้นสุด (30 มิ.ย. 2569 หรือ 2570) และอัตรา (0.01% หรือ 1%)
     ล็อกไว้ด้วยว่าติ๊กได้เฉพาะประเภทที่เป็น "ที่อยู่อาศัย" (`home:true` ใน `TYPES`)
     ที่ดินเปล่าและโรงเรือน/โกดังติ๊กแล้วต้องไม่มีผล — มีเทสต์คุมไว้
   · **ราคาประเมินสิ่งปลูกสร้าง** ให้กรอกเองเป็นช่องหลัก ตัวช่วยประมาณเป็นทางเลือกรอง
     เรตต่อ ตร.ม. **เป็นบัญชีจริงของกรมธนารักษ์แล้ว** (2026-08-30) โหลดจาก
     `https://app.njteedinsure.com/buildingprice.json` — 67 ประเภท × 77 จังหวัด
     สร้างด้วย `node landprice/refresh-building.js` ใน repo nj-survey-system
     · ราคาต่างกันตามจังหวัด **จึงบังคับให้เลือกจังหวัดก่อน** ไม่มีค่ากลางทั้งประเทศให้ใช้
     · โหลดบัญชีไม่ได้ หรือไม่เลือกจังหวัด = ประมาณไม่ได้ ต้องกรอกราคาเอง **ห้าม fallback เป็นตัวเลขที่เดา**
     · **ค่าเสื่อมตามอายุยังเป็นค่าประมาณ** (`decay`/`cap` ใน `TYPES`) เพราะกรมธนารักษ์
       ไม่ได้เปิดตารางหักค่าเสื่อมเป็นข้อมูลเปิด — คำเตือนที่ขึ้นต้องพูดถึงเฉพาะค่าเสื่อม
       ไม่ใช่บอกว่าราคาทั้งก้อนเป็นค่าประมาณ เพราะจะทำให้ผู้ใช้ไม่เชื่อตัวเลขที่จริงอยู่แล้ว
     · เทสต์ใส่บัญชีราคาเองผ่าน `setBuildingPrices()` จะได้ไม่ต้องต่อเน็ตและผลไม่เปลี่ยนตามรอบบัญชี
   · ยกเว้นภาษีธุรกิจเฉพาะเมื่อมีชื่อในทะเบียนบ้าน ≥ 1 ปี ใช้ได้เฉพาะที่อยู่อาศัยและบุคคลธรรมดา
     ที่ดินเปล่าไม่มีทะเบียนบ้าน ติ๊กแล้วต้องไม่มีผล

## กับดักที่เคยเจอมาแล้ว

- **เขียน `textContent` ทับด้วยค่าเดิม ไม่ใช่ "การไม่เปลี่ยนอะไร" — มันคือลบ text node แล้วสร้างใหม่**
  ซึ่งนับเป็น childList mutation · จับคู่กับ `MutationObserver` ที่เฝ้า `childList + subtree`
  ของ element เดียวกันเมื่อไหร่ = **ลูปไม่รู้จบ กินซีพียู 100% แท็บค้างจนต้องปิด**
  · เจอจริง 2026-09-06 ใน `compare.js`: `decorate()` แปะปุ่มลงใน `#listing-grid` แล้วเฝ้าตะแกรงเดียวกัน
    `syncMarks()` เขียนป้ายปุ่มทับด้วยค่าเดิม → observer ยิง → วน · **หน้าแรกกับหน้ารวมประกาศ
    ค้างทั้งสองหน้าตั้งแต่ประกาศโหลดเสร็จ** (หน้าอื่นไม่เป็นเพราะไม่มี `#listing-grid`)
  · อาการหลอกมาก: ไม่มี error ใน console เลย · พาเนลเบราว์เซอร์ในเครื่องมือก็แค่ "timeout"
    ซึ่งอ่านเหมือนเครื่องมือเสีย ไม่ใช่เว็บเสีย — เสียเวลาไล่ผิดทางอยู่นาน
  · แก้ 2 ชั้น: (1) เทียบค่าเดิมก่อนเขียนเสมอ (2) `mo.disconnect()` ก่อนแก้ DOM แล้วค่อย observe ใหม่
  · **มีเทสต์คุมไว้แล้ว — `node compare.test.js`** ยืนยันแล้วว่าเวอร์ชันเก่าทำเทสต์แดง 6 ข้อ

- **`[hidden]` ใช้ไม่ได้กับ element ที่ถูกตั้ง `display` ในสไตล์ชีตของเรา** — `.cs-field{display:grid}`
  ชนะกฎ `[hidden]{display:none}` ของเบราว์เซอร์ ทำให้ช่องที่ตั้งใจซ่อนไว้โผล่ขึ้นเว็บจริง
  แก้ด้วย `[hidden]{display:none !important}` ใน `ui.css` แล้ว — อย่าลบกฎนี้ทิ้ง
  · **เจอซ้ำอีกครั้ง 2026-08-30** ที่ `.fc-rate{display:flex}` ใน `feecalc.css`
    หน้าที่โหลด `feecalc.css` โดยไม่ได้โหลด `ui.css`/`home.css` จะไม่มีกฎ `!important` นั้น
    จึงต้องประกาศซ้ำไว้ใน `feecalc.css` เอง — อย่าลบออก

- **หลายเซสชันแก้ repo นี้พร้อมกัน — ตรวจ `git diff --stat` ก่อนเชื่อว่า working tree เป็นของเราคนเดียว**
  (2026-09-06 เจอจริง: กำลังจะ commit ปุ่ม hero แล้วพบว่า `index.html` มีงานแถบ "บริการเพิ่มเติม"
  ของอีกเซสชันเพิ่มเข้ามาระหว่างทาง รวม 12 ไฟล์ที่เขายังทำไม่เสร็จ)
  · คัดเฉพาะก้อนของตัวเองเข้า commit — `git commit -a` จะลากงานที่เขายังทำไม่เสร็จติดไปด้วย
  · ⚠️ **`git apply --cached --unidiff-zero` วางบรรทัดผิดตำแหน่งได้** patch แบบ `-U0` ไม่มีบริบท
    ให้ git ยึด รอบนี้บรรทัดไปโผล่นอกแถวปุ่ม 3 บรรทัด **ต้องอ่าน `git diff --cached` ยืนยันตำแหน่ง
    ก่อน commit เสมอ อย่าเชื่อว่า apply ผ่านแล้วถูก**
  · วิธีที่ชัวร์กว่าเมื่อของเราเป็นการแก้ไม่กี่บรรทัด: ประกอบเนื้อไฟล์ = `git show HEAD:<ไฟล์>`
    บวกบรรทัดของเรา แล้ว `git hash-object -w --stdin` + `git update-index --cacheinfo`
    — stage ได้ตรงเป๊ะโดยไม่แตะไฟล์ในโฟลเดอร์ทำงานที่อีกเซสชันกำลังใช้อยู่
  · **repo นี้เก็บไฟล์เป็น LF แต่ `core.autocrlf=true` แปลงเป็น CRLF ในโฟลเดอร์ทำงาน**
    สคริปต์ที่แก้ไฟล์ในโฟลเดอร์ต้องใช้ตัวจบบรรทัดแบบ CRLF ส่วนสคริปต์ที่สร้าง blob ให้ git
    ต้องใช้ LF — ใช้ผิดตัวแล้ว anchor จะหาไม่เจอเงียบๆ

## Design tokens

**เว็บมี 2 ชุดสีอยู่พร้อมกันตอนนี้** เพราะหน้าแรกเปลี่ยนดีไซน์ก่อนหน้าอื่น (2026-08-29)
หน้าอื่นยังเป็นชุดเดิม — จะไล่เปลี่ยนตามทีหลังหรือคงไว้ ให้ตัดสินใจก่อนแก้ ไม่ใช่เผลอผสมสองชุดในหน้าเดียว

ชุดใหม่ — หน้าแรก (`home.css`)
- Ink `#14170F`: พื้นเข้ม/ตัวหนังสือหลัก
- Gold `#DFC98F` บนพื้นเข้ม · Bronze `#8A6A2E` บนพื้นสว่าง · Brass `#C0A163` เส้นขอบ hover
- Paper `#F7F4EE` พื้นหลังหน้า · Sand `#EDE8DD` พื้นส่วนบริการ
- Thai/Latin: Sarabun (300/400/500) · หัวข้อ: Trirong (400/500)
- ไม่มี border-radius — ดีไซน์นี้ใช้มุมเหลี่ยมทั้งหมด

ชุดเดิม — `guides.html` `land.html` `videos.html` `consign.html` (`marketplace.css`, `ui.css`)
- Navy `#0B2549`: ความน่าเชื่อถือและข้อความหลัก (หน้ารอง)
- **หน้าแรกใช้ชุดสีของตัวเองใน `home.css`** โทนน้ำเงินเข้ม–ทอง (เปลี่ยนจากครีม–ทอง 2026-08-30)
  `--ink:#0B1F3A` · `--gold:#D8B96B` · `--bronze:#1B4B7A` · `--paper:#F4F7FA`
  · สีฝังเป็น inline style ใน `index.html` เยอะมาก เปลี่ยนโทนทีต้องแก้ทั้ง 2 ไฟล์ให้ตรงกัน
  · `s/share.css` คัดลอกตัวแปรสีไว้เอง **ยังเป็นโทนเดิมอยู่** ถ้าจะให้ทั้งเว็บโทนเดียวกันต้องแก้ไฟล์นั้นด้วย
- Green `#1CA45C`: verified/action
- Mist `#F4F7F8`: พื้นหลังตลาด
- Thai font: IBM Plex Sans Thai
- Latin numbers: Manrope
- Card radius: 12px; panel radius: 16px

## การทดสอบขั้นต่ำ

- **`node compare.test.js` ต้องผ่านครบก่อนเสมอ** (10 ข้อ · เพิ่ม 2026-09-06)
  ล็อกบั๊กลูป MutationObserver ที่เคยทำให้ 2 หน้าค้างทั้งแท็บ (ดู "กับดักที่เคยเจอมาแล้ว")
  · ใช้ DOM ปลอมที่เขียนเองใน `node:vm` **ไม่พึ่งไลบรารีภายนอก** — repo นี้เป็นเว็บสแตติกล้วน
    ไม่มี `package.json` และไม่ควรมี `node_modules` เพียงเพื่อเทสต์ไม่กี่ข้อ
- **`node contracts.test.js` ต้องผ่านครบก่อนเสมอ** (27 ข้อ · เพิ่ม 2026-09-06 · +9 ข้อเรื่องโซนเดินทาง 2026-09-08)
  เทียบ "รายการค่าตายตัวที่ผูกกันข้าม repo" ระหว่างเว็บกับ `nj-survey-system`:
  ชนิดเหตุการณ์สถิติ · คีย์บริการเพิ่มเติม · จังหวัดที่บังคับรังวัด · `LAND_DEEDS`/`LAND_ZONES`/`PARCEL_FEATURES`
  · เขียนขึ้นเพราะความผิดพลาดที่แพงที่สุดของโปรเจกต์นี้ไม่ใช่โค้ดพัง แต่คือ
    **รายการสองฝั่งเลื่อนออกจากกันโดยไม่มี error ให้เห็น** (เคยเกิดจริงกับ `messenger_click` 2026-08-28)
  · หา `nj-survey-system` จาก `../nj-survey-system` · อยู่ที่อื่นให้ส่ง path เป็นอาร์กิวเมนต์แรก
    ไม่เจอ = ข้ามเงียบๆ (exit 0) ไม่ใช่ล้มเหลว — คนที่เช็คเอาต์เฉพาะ repo เว็บก็ยังรันเทสต์อื่นได้
- เปิดผ่าน local HTTP server (ไม่เปิดด้วย `file://`)
- ทดสอบที่ความกว้าง 390px, 768px และ 1440px
- **ทุกหน้าต้องมีทางเข้าเมนูบนจอเล็ก** — ย่อจอให้แคบกว่าจุดตัดของหน้านั้นแล้วปุ่มแฮมเบอร์เกอร์ต้องโผล่
  และรายการในลิ้นชักต้องตรงกับเมนูเดสก์ท็อปของหน้านั้นเป๊ะ (`menu.js` สร้างจาก `<nav>` จริง)
- ทดสอบค้นหา กรองราคา ปุ่มบันทึก โทร LINE และลิงก์ฝากขาย
- **หน้าฝากขาย**: เลือกจังหวัดในเขตบังคับ (เช่น กรุงเทพมหานคร) แล้วตัวเลือกรังวัดต้องล็อกเหลือ "ต้องการรังวัด"
  · เลือกจังหวัดนอกเขต (เช่น ระยอง) แล้วต้องปลดล็อก + ขึ้นตารางข้อดี–ข้อแลกเปลี่ยน
  · ใส่เนื้อที่แล้วต้องขึ้นค่ารังวัดประมาณการจาก `pricing.js` จริง (ไม่ใช่ตัวเลขที่ฝังไว้)
- **หน้าเปรียบเทียบ**: กด "เทียบ" บนการ์ดแล้วต้อง **ไม่** เด้งเข้าหน้ารายละเอียด (ดูเหตุผลที่ `compare.js`)
- ตรวจ console และ network error · **API ล่มต้องแสดงว่า "โหลดไม่สำเร็จ" ไม่ใช่แสดงแปลงตัวอย่าง**
  (คนละกรณีกับ API ตอบ count: 0 ซึ่งต้องแสดงกล่อง "ยังไม่มีแปลงประกาศ" — สองอย่างนี้ห้ามสลับกัน)

## Teedin Sure Verified — บันได 5 ระดับ · รายงานสุขภาพแปลง · แผนที่แนวเขต (2026-09-09 · Phase 1)

branch `phase1/verified-status` · ✅ **merge เข้า main และขึ้นเว็บจริงแล้ว** (ตรวจกับ git และเว็บจริง 2026-09-11)

### ⚠️ กติกาข้อแรกของทั้งระลอกนี้ — แปลงเก่าต้องหน้าตาเหมือนเดิมเป๊ะ

ทั้งสามแผงคืน **สตริงว่าง** เมื่อ API ไม่ได้ส่งข้อมูลมา → หน้าเว็บซ่อนทั้งหัวข้อ (กติกาข้อ 5)
**ห้ามเปลี่ยนเป็นวาดกล่องว่างพร้อมข้อความ "ยังไม่มีข้อมูล"** — บันไดว่าง 5 ขีดสีเทาอ่านแล้วเหมือน
แปลงถูกตัดสินไปแล้ว ทั้งที่ความจริงคือยังไม่มีใครไปตรวจ · แปลงที่ประกาศอยู่ตอนนี้อยู่ในกลุ่มนี้ทั้งหมด

⚠️ **ตารางสุขภาพกั้นด้วย `L.health` ไม่ใช่กั้นด้วย "ประกอบแถวสีเขียวได้ไหม"** — แปลงเก่ามี
`deedType`/`deedArea`/`checks` อยู่แล้วทุกแปลง ถ้ากั้นแบบหลัง ตารางจะโผล่ทุกแปลงในวันที่ deploy
ทั้งที่ยังไม่มีใครไปตรวจอะไรเพิ่ม และซ้ำกับแผงผลตรวจ 7 หัวข้อเดิมที่อยู่ล่างลงไป
(กรอกช่องไหนก็ได้แค่ช่องเดียวฝั่งพนักงาน ตารางก็ขึ้นทั้งใบพร้อมข้อมูลเดิมที่มีอยู่แล้ว)

### ไฟล์ใหม่

| ไฟล์ | ทำอะไร |
|---|---|
| `landvocab.js` | **คำศัพท์ชุดกลาง** (`window.NJVocab`) — ไฟล์ใหม่ทุกไฟล์ให้ใช้ชุดนี้ ห้ามก๊อปเพิ่ม |
| `verified.js` / `verified.css` | บันได 5 ระดับ (`NJVerified.ladderHtml` / `badgeHtml`) |
| `health.js` / `health.css` | รายงานสุขภาพแปลง 14 หัวข้อ 4 สถานะ (`NJHealth.tableHtml`) |
| `parcelmap.js` / `parcelmap.css` | แผนที่แนวเขต SVG (`NJParcelMap.mapHtml` / `init`) |
| `verified.test.js` | 58 ข้อ · รันด้วย `node verified.test.js` |

⚠️ **ยังมีสำเนาคำศัพท์เดิมอยู่ใน `listings.js` และ `comparepage.js`** (เขียนไว้ก่อน `landvocab.js` จะมี)
`contracts.test.js` หัวข้อ 7 เทียบสำเนาทั้งหมดให้แล้ว — แก้ที่ `landvocab.js` แล้วเทสต์จะบอกเองว่าต้องตามแก้ที่ไหนอีก

### ลำดับการโหลดสคริปต์ (ห้ามสลับ)

`land.html`: `landvocab.js` → `verified.js` → `health.js` → `parcelmap.js` → `land.js`
· `health.js` อ่านคำศัพท์จาก `NJVocab` · `land.js` เรียกทั้งสามตัวตอนวาดหน้า
`index.html` / `listings.html`: `verified.js` **ต้องมาก่อน** `listingcard.js` (ป้ายระดับบนการ์ด)

### แผนที่แนวเขตเป็น SVG ล้วน ไม่เพิ่มไลบรารีแผนที่

เหตุผล: (1) กติกาข้อ 10 ห้ามเพิ่มไลบรารีใหญ่โดยไม่จำเป็น ทั้งเว็บไม่มีไลบรารี JS เลยสักตัว
(2) **โหมดเริ่มต้นที่เซิร์ฟเวอร์ส่งมาไม่มีพิกัดเลย** (ส่งเป็นระยะเป็นเมตรจากจุดกึ่งกลาง เพื่อไม่ให้
แนวเขตระดับรังวัดหลุดสู่สาธารณะ) ไม่มีพิกัด = วางบนกระเบื้องแผนที่ไม่ได้อยู่แล้ว

- วาดรูปแปลง หมุดพร้อมเลขกำกับ ความยาวแต่ละด้าน ไม้บรรทัด และเข็มทิศ
- **หมุดเป็น `<button>` จริง** ไม่ใช่จุดเล็กๆ บน SVG ที่ต้องจิ้มให้แม่น — ได้การใช้คีย์บอร์ดและ
  การแตะบนมือถือมาฟรี · กดแล้วเปิดแผงรูป/หมายเหตุช่าง/วันที่สำรวจ
- ⚠️ **ห้ามคำนวณความยาวด้าน/พื้นที่/การเรียงหมุดเองที่ฝั่งนี้** เซิร์ฟเวอร์คิดมาให้แล้ว
  คิดซ้ำ = สองที่ปัดเศษไม่เหมือนกัน แล้วตัวเลขบนรูปกับในตารางไม่ตรงกันโดยไม่มีอะไรเตือน
- ⚠️ **y ของ SVG ชี้ลง แต่ y ของเราคือทิศเหนือซึ่งชี้ขึ้น ต้องกลับด้าน** ไม่กลับ = รูปแปลงพลิกบน-ล่าง
  ซึ่งดูเหมือนถูกจนกว่าจะเอาไปเทียบกับของจริง
- ⚠️ **`.njp-svg` ต้องมีเพดานความสูง** แปลงแคบยาว (พบบ่อยมาก เช่นแปลงริมถนน) จะได้ SVG
  สูงหลายพันพิกเซล แล้วดันทุกอย่างใต้แผนที่ตกจอไปหมด
- ข้อจำกัดที่บอกผู้ใช้ตรงๆ: ไม่มีชั้นภาพถ่ายดาวเทียม ไม่มีถนนโดยรอบ และไม่ได้วางทับพิกัดจริงบนโลก

### รายงานสุขภาพแปลง — 4 สถานะ ทุกสถานะมีข้อความกำกับ

เขียว ตรวจแล้วไม่พบประเด็นสำคัญ · เหลือง ต้องตรวจเพิ่มเติม · แดง พบประเด็นที่ผู้ซื้อควรทราบ · เทา ยังไม่มีข้อมูล

- ⚠️ **ห้ามใช้สีอย่างเดียว** ถอดสีออกทั้งหมดแล้วตารางต้องยังอ่านรู้เรื่องครบทุกแถว
  (`verified.test.js` นับจำนวนแถวเทียบกับจำนวนข้อความสถานะให้ตรงกัน)
- ⚠️ **เทา = "ยังไม่มีข้อมูล" ไม่ใช่ "ไม่มี"** ข้อความต้องเขียนให้อ่านออกว่าเรายังไม่รู้
- ⚠️ **ที่ตาบอด (`access:'none'`) และแนวรุกล้ำ (`structures:'encroach'`) ต้องขึ้นแดงเสมอ**
  เป็นข้อมูลที่กระทบการตัดสินใจซื้อโดยตรงที่สุดในตารางนี้ · ล็อกไว้ทั้ง `verified.test.js` และ `contracts.test.js`

### บันได 5 ระดับ

- ใช้ `<details>`/`<summary>` ของเบราว์เซอร์เอง ไม่เขียนตัวกาง/หุบเอง — ได้คีย์บอร์ดและ
  การอ่านออกเสียงมาฟรี และไม่มีอะไรพังเมื่อ JS โหลดไม่สำเร็จ
- แถวที่ `status==='issue'` **กางไว้ตั้งแต่แรก** — ประเด็นที่ผู้ซื้อควรทราบต้องไม่ต้องกดหา
- **รายการ "ยังไม่ได้ตรวจ" สำคัญพอๆ กับรายการที่ตรวจแล้ว ห้ามซ่อนเพื่อให้หน้าดูสวย**
- ป้ายบนการ์ด (`badgeHtml`) **ไม่ขึ้นเมื่อยังไม่ถึงระดับใดเลย** — ป้าย "0/5" อ่านแล้วแย่กว่าไม่มีป้าย
  และแปลงที่ทีมยังไม่ได้ไล่กรอกจะดูเหมือนถูกตัดสินไปแล้ว
- ป้ายนี้ **ไม่ได้แทนที่** ป้ายเหลือง-เขียวเดิม สองอย่างตอบคนละคำถาม
  (เดิม = "รังวัดแล้วหรือยัง" · ใหม่ = "ตรวจไปแล้วกี่ระดับจาก 5")
- ⚠️ **ข้อความปิดท้ายของทั้งสามแผงห้ามถอด** เป็นเส้นแบ่งระหว่าง "เราตรวจอะไรไปแล้ว" กับ "เรารับประกันอะไร"
  `contracts.test.js` ล็อกไว้ว่าสามไฟล์นี้ห้ามมีคำรับประกัน

### หน้ารายละเอียดเปลี่ยนมาใช้เส้นทางแปลงเดียว

`land.js` → `GET /api/public/listings/:id` (เดิมโหลด `/api/public/listings` ทั้งก้อนแล้วคัดเอาแปลงเดียว
= ดาวน์โหลดข้อมูลของทุกแปลงทั้งเว็บทุกครั้งที่มีคนเปิดหน้าเดียว) · **ห้ามกลับไปโหลดทั้งก้อนอีก**
· 404 = แปลงถูกถอด/ขายแล้ว ซึ่งคนละเรื่องกับ "โหลดไม่สำเร็จ" ต้องขึ้นคนละข้อความ

### `?api=` — ชี้ API ไปเซิร์ฟเวอร์ทดสอบบนเครื่อง

เติม `?api=http://127.0.0.1:8792` ตอนเปิดจาก localhost (แนวเดียวกับ `?photos=dev`)
⚠️ **ทำงานเฉพาะ localhost และรับเฉพาะที่อยู่ที่ชี้กลับมาที่เครื่องเอง** เปิดกว้างกว่านี้ =
ใครส่งลิงก์ `njteedinsure.com?api=<เว็บของเขา>` ให้ลูกค้า แล้วหน้าเว็บของเราจะดึงข้อมูลแปลง
จากเซิร์ฟเวอร์ของคนอื่นมาแสดงในนามเรา

### แก้เรื่องที่ค้างมาก่อนหน้านี้ไปด้วย

`land.css` และ `compare.css` **ไม่เคยมีกฎ `:focus` เลยสักข้อ** ทั้งที่มีปุ่มควบคุมสไลด์ภาพ จุดนำทาง
ปุ่มคัดลอกรหัสทรัพย์ ปุ่มดูเบอร์ ปุ่มดาวน์โหลด PDF และปุ่ม "เทียบ" — คนที่เดินหน้าเว็บด้วยคีย์บอร์ด
มองไม่ออกเลยว่าตอนนี้โฟกัสอยู่ที่ปุ่มไหน · เพิ่ม `:focus-visible` แล้ว (ไม่ใช่ `:focus` เพราะกรอบ
จะเด้งตอนคลิกเมาส์ ซึ่งเป็นเหตุผลที่คนมักถอดกฎนี้ทิ้ง)

### ทดสอบแล้ว

`node verified.test.js` 58 ข้อ · `node contracts.test.js` 51 ข้อ (เพิ่มหัวข้อ 7 · 24 ข้อ) ·
`compare/feecalc/valuecalc` ชุดเดิมยังผ่านครบ
· เบราว์เซอร์จริง 3 แปลง (ครบทุกอย่าง / มีบางส่วน / แปลงเก่าไม่มีข้อมูลใหม่) ที่ 375px และ 1000px:
ไม่ล้นแนวนอน · ไม่มี console error · **แปลงเก่าไม่มีแผงใหม่โผล่เลยแม้แต่แผงเดียว**

## นัดตรวจแปลงก่อนซื้อ · ติดตามงานซื้อ 11 ขั้น (2026-09-10 · Phase 2)

branch `phase2/web` · ✅ **merge เข้า main และขึ้นเว็บจริงแล้ว** (ตรวจ 2026-09-11) · ตอน deploy ต้องขึ้นฝั่ง `nj-survey-system`
(branch `phase2/foundation`) **ก่อน** เสมอ เพราะหน้าใหม่ทั้งสองเรียกเส้นทาง API ที่เพิ่งเพิ่ม

### ไฟล์ใหม่

| ไฟล์ | ทำอะไร |
|---|---|
| `inspect.html` / `inspect.js` / `inspect.css` | ฟอร์มขอนัดตรวจแปลง **และ** หน้าติดตามคำขอ — ไฟล์เดียวกัน |
| `deal.html` / `deal.js` / `deal.css` | ผู้ซื้อติดตามเส้นทาง 11 ขั้นของตัวเอง |

### หน้าเดียวทำสองหน้าที่ — ตัดสินจาก query string

`inspect.html` ไม่มี `?id=&t=` = ฟอร์มขอนัด · มีครบทั้งคู่ = หน้าติดตามสถานะ
· โหมดติดตามใส่คลาส `ins-tracking` ที่ `<body>` แล้ว CSS ซ่อนข้อความขายกับ FAQ ทิ้ง
(คนที่เปิดลิงก์ติดตามส่งคำขอมาแล้ว ข้อความชวนให้ส่งคำขอไม่ใช่สิ่งที่เขามาหา)

### ⚠️ กติกาที่ห้ามผ่อน

1. **ห้ามมีฟอร์มเข้าสู่ระบบในทั้งสองหน้าเด็ดขาด** — กติกาเดียวกับ `portal.html`
   เว็บนี้เป็นสแตติกบน GitHub Pages ไม่มีฝั่งเซิร์ฟเวอร์ที่ถือ token ได้ปลอดภัย
   และการรับรหัสผ่านที่โดเมนหนึ่งแล้วส่งข้ามไปอีกโดเมน = สอนลูกค้าให้กรอกรหัสในที่ที่ไม่ใช่ระบบจริง
   ทางเข้ามีทางเดียวคือ **ตั๋วในลิงก์** (`?t=`) ที่ได้ตอนส่งคำขอ หรือที่ทีมงานส่งให้ทางไลน์
2. **รายการบริการ 5 แบบต้องตรงกับ `LEAD_SERVICES` ใน `lib/leads.js` เป๊ะ**
   และ `SOURCES` ต้องตรงกับ `LEAD_SOURCES` — เซิร์ฟเวอร์ทิ้งคีย์ที่ไม่รู้จักโดยไม่มี error
   (`contracts.test.js` หัวข้อ 8 เทียบให้ทุกครั้งที่รัน)
3. **ไม่ติ๊กยินยอม PDPA = ส่งไม่ได้** ปิดที่ปุ่มด้วย และเซิร์ฟเวอร์ปฏิเสธซ้ำอีกชั้น
   สองชั้นเพราะชั้นหน้าจอมีไว้บอกผู้ใช้ ส่วนชั้นเซิร์ฟเวอร์มีไว้บังคับจริง
4. **บังคับกรอกแค่ชื่อกับเบอร์** (กติกาเดียวกับหน้าฝากหาที่ดิน) — คนที่ยังไม่รู้ว่าอยากให้ตรวจอะไร
   ก็ยังเป็นลีดที่คุยต่อได้ · บังคับครบทุกช่อง = เขากดถอยตั้งแต่ยังไม่ได้คุยกับใคร
5. **ต้องเขียนให้ชัดว่ายังไม่มีค่าใช้จ่ายและยังไม่ถือเป็นการว่าจ้าง** (`.ins-free`)
   ระบบยังไม่มีการชำระเงิน ถ้าไม่บอก คนกดส่งแล้วกลัวว่าจะถูกเรียกเก็บเงิน
6. **ขั้นที่ยังไม่ถึงใน `deal.js` ต้องขึ้นว่า "ยังไม่ถึงขั้นนี้" ห้ามเขียนว่า "รอ"**
   กติกาเดียวกับผลตรวจ 7 หัวข้อและบันได 5 ระดับ — "ยังไม่ถึงคิว" กับ "กำลังทำอยู่" คนละเรื่อง
   เขียนว่ารออยู่ = รับปากล่วงหน้าแทนสิ่งที่ยังไม่เกิด
7. **ตั๋วอยู่ใน query string จึงต้องกันไม่ให้หลุดไปกับหัว `Referer`** —
   ทั้งสองหน้ามี `<meta name="referrer">` และลิงก์ออกนอกเว็บทุกอันมี
   `rel="noopener noreferrer" referrerpolicy="no-referrer"` · `robots.txt` ก็กัน `?t=` ไว้ด้วย
   และ `deal.html` เป็น `noindex` ทั้งหน้า (เนื้อหาขึ้นกับตั๋ว ไม่ใช่หน้าสาธารณะ)

### 🐞 บั๊กที่เจอตอนทดสอบ — `progress` เป็นก้อน ไม่ใช่ตัวเลข

`GET /api/public/deal/:id` ส่ง `progress: {done, total}` มา · โค้ดรอบแรกอ่านเป็น `Number(dl.progress)`
ได้ `NaN` แล้วแถบความคืบหน้าเป็น **0% ตลอด โดยไม่มี error ให้เห็น**
· และ `done` ของเซิร์ฟเวอร์คือ "เสร็จติดต่อกันจากขั้นแรก" ซึ่งไม่เท่ากับจำนวนขั้นที่เสร็จทั้งหมด
**ใช้ค่าของเซิร์ฟเวอร์เสมอ ห้ามนับเอง** ไม่งั้นตัวเลขบนหน้าจอกับในระบบหลังบ้านไม่ตรงกัน

### ตารางเปรียบเทียบเพิ่ม 6 แถว

`comparepage.js` เพิ่ม: ระดับการตรวจสอบ (จาก 5 ระดับ) · ข้อมูลตรวจล่าสุด · สภาพแปลงจากรายงานสุขภาพ ·
สิ่งที่ยังต้องตรวจเพิ่ม · หน้ากว้าง × ลึก · ค่าใช้จ่ายวันโอน

- ⚠️ **หน้ารวมประกาศส่งข้อมูลมาแบบย่อ** (`slimLand` ตัด `health`/`plot` ทิ้ง และเหลือ `verify`
  แค่ `{reached,total}`) จึงต้อง `fetchDetails()` ดึงรายละเอียดของ **เฉพาะแปลงที่เลือกไว้**
  (สูงสุด 4 ใบ) เพิ่มอีกรอบ · แปลงใดโหลดไม่สำเร็จ ช่องของแปลงนั้นขึ้น "—" ไม่ใช่พังทั้งตาราง
- ⚠️ **แถวค่าใช้จ่ายวันโอนไม่ใส่ตัวเลขให้** ยื่นลิงก์ไปเครื่องคำนวณที่หน้าแปลงแทน —
  ค่าโอนคิดจากราคาประเมินราชการซึ่งเว็บนี้ตั้งใจไม่เผยแพร่ (กติกาข้อ 12)
- ⚠️ **ไม่คูณ หน้ากว้าง × ลึก เป็นพื้นที่** แปลงส่วนใหญ่ไม่ใช่สี่เหลี่ยม คูณแล้วได้ตัวเลขผิด
  ที่ดูเหมือนตัวเลขจริง · เนื้อที่จริงมีแถวของตัวเองอยู่แล้ว
- `compare.html` โหลด `landvocab.js` + `verified.js` เพิ่ม (คำศัพท์ชุดกลาง ห้ามก๊อปเป็นชุดที่สี่)

### แดชบอร์ดเจ้าของที่ดินเห็น "มีคนสนใจกี่ราย"

`consign.js` → `renderReport()` เพิ่มกล่อง "ผู้ซื้อที่ติดต่อเข้ามาผ่านระบบ" จาก `d.interest`
· ⚠️ **ตัวเลขล้วน** เซิร์ฟเวอร์ส่งมาแค่จำนวน ไม่มีชื่อ เบอร์ หรือข้อความของผู้ซื้อเลย
ผู้ซื้อยินยอมให้ "ทีมงานติดต่อกลับ" ไม่ได้ยินยอมให้ข้อมูลตัวเองถูกส่งต่อให้เจ้าของแปลง
**ห้ามเพิ่มรายละเอียดรายบุคคลลงกล่องนี้ไม่ว่ากรณีใด**

### ทางเข้าหน้านัดตรวจ

`land.js` → `.ld-inspect` วางใต้แถวปุ่มติดต่อในหน้ารายละเอียดแปลง
ลิงก์เป็น `inspect.html?listing=<รหัส>&from=land_page`
· ⚠️ **ไม่ใส่ `data-contact` และไม่ยิงสถิติติดต่อ** — คนที่กดยังไม่ได้ติดต่อใคร เขาไปกรอกฟอร์มต่อ
ซึ่งเซิร์ฟเวอร์นับเป็น `inspect_submit` ให้เองตอนสร้างใบ (กติกาข้อ 6 — นับที่เดียว)
· **ไม่เพิ่มเมนูตัวที่ 6 บนหัวเว็บ** เพราะแถวเมนูเต็มพอดีอยู่แล้วที่ 1440px (ดูหัวข้อ `menu.js`)

### เหตุการณ์สถิติที่เพิ่ม

`inspect_view` ขึ้นทะเบียนทั้งสองฝั่ง · `inspect_submit` **มีเฉพาะฝั่งเซิร์ฟเวอร์**
เพราะเซิร์ฟเวอร์บันทึกเองตอนสร้างใบ (แนวเดียวกับ `consign_submit` และ `inquiry_submit`)

### ทดสอบแล้ว

`node contracts.test.js` **61 ข้อ** (เพิ่มหัวข้อ 8 · 11 ข้อ) · ชุดเดิม verified/compare/feecalc/valuecalc ผ่านครบ
· **เบราว์เซอร์จริง + เซิร์ฟเวอร์จริง** (worktree `nj-wt-p2` ที่พอร์ต 8792 · DATA_DIR ชั่วคราว) ที่ 375px และ 1440px:
ส่งคำขอสำเร็จได้เลขงาน + ลิงก์ติดตาม · ยังไม่ติ๊กยินยอมกดส่งไม่ได้และขึ้นข้อความบอก ·
วันที่ซ้ำถูกตัดเหลือวันเดียว · ส่งซ้ำด้วย `+66` แบบเดียวกันถูกจับว่าเป็นใบซ้ำและขึ้นข้อความถูกต้อง ·
`<script>` ในช่องข้อความไม่กลายเป็น element · ตั๋วผิด / ไม่มีตั๋ว ขึ้นคนละข้อความกัน ·
สิ่งที่เจ้าหน้าที่กรอกในขั้นการซื้อไปโผล่บนหน้าติดตามของผู้ซื้อจริง · ไม่ล้นแนวนอน · ไม่มี console error

## ค้นหาที่ดินตามวัตถุประสงค์ 8 แบบ (2026-09-11 · Phase 3 ข้อ 3)

ไฟล์ใหม่: `purpose.html` · `purpose.js` · `landpurpose.js` (`window.NJPurpose`) · `landpurpose.css`
ที่แก้เพิ่ม: `land.html` + `land.js` (แผง "แปลงนี้เข้าทางอะไรบ้าง") · `listings.html`/`listings.css` (ทางเข้า) ·
`analytics.js` (`purpose_view`) · `sitemap.xml` · `contracts.test.js` (หัวข้อ 10)

วัตถุประสงค์ 8 แบบ: สร้างบ้าน · สร้างโกดัง · โรงงาน · ร้านค้าหรือสำนักงาน · แบ่งขาย · จัดสรรที่ดิน · เกษตร · ลงทุนระยะยาว

### กติกาที่ห้ามผ่อน

1. **⛔ ห้ามมีข้อความรับปาก** — "สร้างได้แน่นอน" · "จัดสรรได้แน่นอน" · "รับประกัน" · "การันตี"
   ถูกล็อกไว้ใน `contracts.test.js` หัวข้อ 10 ทั้งฝั่งเว็บและฝั่งระบบ · การก่อสร้าง การแบ่งแยกแปลง
   และการจัดสรรที่ดิน ต้องขออนุญาตจากหน่วยงานเสมอ เว็บนี้ฟันธงแทนไม่ได้
2. **`disclaim` ที่เซิร์ฟเวอร์ส่งมาต้องพิมพ์ทุกครั้งที่แสดงผล ห้ามถอดและห้ามเขียนใหม่ให้อ่อนลง**
   (กติกาเดียวกับ `SCORE_DISCLAIM` ของคะแนนความพร้อม)
3. **"ยังบอกไม่ได้" (`unknown`) กับ "ยังไม่เข้าทาง" (`weak`) เป็นคนละคำตอบ ห้ามยุบรวม**
   และ **ห้ามซ่อนแปลงกลุ่ม `unknown` ออกจากผลค้น** — เซิร์ฟเวอร์เรียงลำดับมาให้แล้วและไม่ได้คัดใครทิ้ง
   หน้าเว็บมีหน้าที่แสดงให้ครบและบอกจำนวนของแต่ละกลุ่ม (`.njpp-counts`)
   · แปลงที่ขึ้น `unknown` ครบทุกข้อจะมีบรรทัดอธิบายว่าเป็นเพราะยังไม่มีผลตรวจ ไม่ใช่เพราะแปลงไม่ดี
4. **รายชื่อวัตถุประสงค์ดึงจาก `GET /api/public/purposes` เสมอ ห้ามฝังรายการไว้ในไฟล์เว็บ**
   ฝังเมื่อไหร่ = คีย์บนปุ่มกับคีย์ในตัวคำนวณเลื่อนออกจากกัน แล้วกดแล้วได้ผลว่างโดยไม่มีอะไรเตือน
   (บทเรียนเดียวกับ `messenger_click` ที่เคยตกหล่นใน `analytics.js`) — มีเทสต์ล็อกไว้
5. **ห้ามคำนวณความเข้ากันเองฝั่งเว็บ** เซิร์ฟเวอร์คิดมาให้แล้วทั้งคะแนนและเหตุผลทั้ง 5 กลุ่ม
   (บทเรียนเดียวกับราคาต่อตารางวาและคะแนนความพร้อม)
6. **แผงในหน้าแปลงคืนสตริงว่างเมื่อ API ไม่ส่ง `purposes` มา** — แปลงเก่าหน้าตาเหมือนเดิมเป๊ะ

### รูปร่างข้อมูลที่ใช้

- `land.html` อ่าน `l.purposes` (`{at, disclaim, items[]}`) — **อยู่ระดับบนสุดของประกาศ ไม่ได้อยู่ใน `l.land`**
  เหมือน `l.score` · แต่ละ item มี `reasons` (`ok`/`partial`/`caution`) · `checked` · `missing` · `watch` · `services`
- `purpose.html` ยิง `/api/public/listings?purpose=<key>` แล้วอ่าน `l.purpose` (รูปย่อ) ของแต่ละแปลง
  พร้อม `data.purpose.counts` สำหรับบรรทัดสรุป
- `NJL.normalize()` ตัดช่องที่ไม่รู้จักทิ้ง จึงต้องหิ้ว `raw.purpose` มาแปะกลับเองหลังเรียก (มีคอมเมนต์กำกับใน `purpose.js`)

### ทดสอบแล้ว

`node contracts.test.js ../nj-wt-p2` **146 ข้อ** (เพิ่มหัวข้อ 10) · ชุดเดิมผ่านครบ
· **เบราว์เซอร์จริง + เซิร์ฟเวอร์จริง** (worktree `nj-wt-p2` ที่พอร์ต 8792 · DATA_DIR ชั่วคราว) ที่ 375px:
ปุ่ม 8 อันสร้างจากรายชื่อของเซิร์ฟเวอร์ · กดแล้วเรียงผลถูกต้องและที่อยู่หน้าเปลี่ยนเป็น `?p=` ·
เปิดลิงก์ `?p=allocate` ตรงๆ ได้ผลเดิม · แปลงที่ยังไม่มีผลตรวจยังอยู่ในผลพร้อมป้าย "ยังบอกไม่ได้" ·
ที่ตาบอดในผังอนุรักษ์อยู่ท้ายสุด · แผงในหน้าแปลงกางดูเหตุผลครบ 5 กลุ่ม ·
แปลงที่ยังไม่มีผลตรวจขึ้นบรรทัดอธิบายแทนการถูกตัดสิน · ไม่ล้นแนวนอน · ไม่มี console error

## ห้องข้อมูลแปลงสำหรับผู้ซื้อ (2026-09-11 · Phase 3 ข้อ 4)

ไฟล์ใหม่: `room.html` · `room.js` · `room.css`
ที่แก้เพิ่ม: `land.js` + `land.css` (ทางเข้า `.ld-room`) · `analytics.js` (`dataroom_view`) ·
`robots.txt` · `contracts.test.js` (หัวข้อ 11)

หน้าเดียวทำสองโหมดแบบเดียวกับ `inspect.html`:
ไม่มีตั๋วในลิงก์ → ฟอร์มขอสิทธิ์ · มีตั๋ว (`?id=..&t=..`) → สถานะคำขอ + รายการเอกสารที่เปิดได้

### กติกาที่ห้ามผ่อน

1. **⛔ หน้านี้ห้ามถูกเก็บเข้าดัชนีเสิร์ชเอนจิน** — กันสองชั้น: `<meta name="robots" content="noindex, nofollow, noarchive">`
   ในหน้า และ `Disallow: /room.html` ใน `robots.txt` (เหตุผลเดียวกับ `deal.html`)
   · **ไม่อยู่ใน `sitemap.xml`** และมีเทสต์ยืนยันว่าไม่ได้ถูกใส่เข้าไป
2. **ลิงก์ที่มีตั๋วคือกุญแจ** — ข้อความบนหน้าต้องเตือนให้เก็บไว้กับตัวและห้ามส่งต่อ
   เพราะใครถือลิงก์ก็เปิดเอกสารในนามผู้ขอได้ และชื่อผู้ขอจะไปประทับอยู่บนเอกสารนั้น
3. **⛔ ห้ามเขียนว่าจะได้ "ไฟล์ต้นฉบับ" หรือ "ดาวน์โหลดต้นฉบับ"** — ไม่มีทางนั้นอยู่จริงในระบบ
   สิ่งที่ผู้ขอได้คือ PDF ที่ประทับชื่อเขาไว้ทุกหน้าเสมอ (มีเทสต์ล็อกคำต้องห้ามไว้)
4. **ไม่ยินยอม PDPA = ปุ่มส่งถูกปิดไว้** และเซิร์ฟเวอร์ตรวจซ้ำอีกชั้น
5. **ไม่ฝังรายชื่อชนิดเอกสารไว้ฝั่งเว็บ** — อ่าน `kindTh` ที่เซิร์ฟเวอร์ส่งมาเสมอ
   (บทเรียนเดียวกับรายชื่อวัตถุประสงค์ของข้อ 3)
6. **ทางเข้าในหน้ารายละเอียดแปลงต้องบอกว่าต้องผ่านการยืนยันตัวตนก่อน** ไม่ใช่ทำให้ดูเหมือนปุ่มดาวน์โหลด
   ไม่งั้นคนกดแล้วคาดหวังว่าจะได้ไฟล์ทันที แล้วผิดหวังตั้งแต่หน้าแรก
   · แถบข้างของกล่องนี้เป็นสีกรมท่า ต่างจากกล่องนัดตรวจแปลง (สีเขียว) เพราะเป็นคนละเรื่องกัน:
     อันบนคือ "ให้เราไปตรวจให้" อันนี้คือ "ขอดูเอกสารที่ตรวจไว้แล้ว"
7. **เหตุการณ์สถิติ**: `dataroom_view` ยิงจากเบราว์เซอร์ · `dataroom_request` **เซิร์ฟเวอร์บันทึกเอง**
   ตอนสร้างคำขอ ฝั่งเว็บห้ามยิงซ้ำ (กติกาเดียวกับ `inspect_submit`)
   · `dataroom_request` นับรวมใน `formLeads` ไม่ใช่ `leads`

### ทดสอบแล้ว

`node contracts.test.js ../nj-survey-system` **175 ข้อ** (เพิ่มหัวข้อ 11) · ชุดเดิมผ่านครบ
· **เบราว์เซอร์จริง + เซิร์ฟเวอร์จริง** ที่ 375px: ติ๊กยินยอมแล้วปุ่มส่งถึงจะกดได้ ·
ส่งคำขอได้เลขที่และลิงก์ติดตาม · เปิดลิงก์แล้วเห็นสถานะ "รอทีมงานพิจารณา" พร้อมข้อความว่ายังไม่มีเอกสารให้เปิด ·
ทีมอนุมัติแล้วรายการเอกสารโผล่ · กดเปิดได้ไฟล์ PDF ที่ประทับลายน้ำจริง ·
ไม่ล้นแนวนอน · ไม่มี console error

## สถานะจริงของเว็บ ณ 11 ก.ย. 2569 (ตรวจกับเว็บจริง ไม่ใช่จากบันทึก)

- **ทุกหน้าออนไลน์อยู่จริงทั้ง 16 หน้า** (ยิงเช็คได้ 200 ทั้งหมด รวม `purpose.html` `room.html` `notify.html`)
- **มี 21 แปลงขึ้นเว็บจริง** — ระดับ 1 จำนวน 16 แปลง · ระดับ 2 (รังวัดยืนยันแล้ว) 5 แปลง
- ⚠️ **แต่ 0 แปลงมีบันได 5 ระดับ และ 0 แปลงมีคะแนนความพร้อม**
  แผงบันได 5 ระดับ · รายงานสุขภาพแปลง · แผนที่แนวเขต · ห้องข้อมูล จึงยังไม่โผล่บนหน้าแปลงเลยสักแปลง
  **นี่คือพฤติกรรมที่ถูกต้องตามกติกาข้อแรกของ Phase 1** (ไม่มีข้อมูล = ซ่อนทั้งหัวข้อ ห้ามวาดกล่องว่าง)
  ไม่ใช่บั๊ก — รอทีมกรอกข้อมูลฝั่งระบบหลังบ้าน
  · ฝั่งระบบมีการ์ด "🧩 แปลงที่ขึ้นเว็บแล้ว — ข้อมูลที่ยังขาด" บอกทีละแปลงว่าต้องกรอกอะไรบ้าง
- ชุดเทสต์ของ repo นี้ผ่านครบทุกไฟล์: contracts 184 · compare 10 · feecalc 50 · valuecalc 28 ·
  verified 58 · njchat 45

**ตรวจกับของจริงก่อนเชื่อสถานะในไฟล์นี้เสมอ** — 11 ก.ย. 69 พบสองหัวข้อที่เขียนว่า "⛔ ยังไม่ deploy"
ทั้งที่ขึ้นเว็บไปแล้วนานแล้ว (กับดักเดียวกับที่ `nj-survey-system/CLAUDE.md` เจอ 7 จุดในวันเดียวกัน)

## ทรัพย์หน่วยงาน · เครื่องมือคำนวณ · ปุ่มแชร์เข้าระบบ (2026-09-17 · P1)

branch `p1-tools-agency` (worktree `nj-wt-p1`) · ต่อยอดจากต้นแบบที่เจ้าของอนุมัติ (วิเคราะห์ TNR MapHub)
· ✅ **merge แล้ว (PR #1) และขึ้นเว็บจริง 2026-09-17** · branch/worktree ลบแล้ว

| ไฟล์ | ทำอะไร |
|---|---|
| `agencies.js` | `window.NJAgency` — รายชื่อเว็บหน่วยงาน + `detect()` อ่านลิงก์ที่ลูกค้าวาง/แชร์ + `checklist()` + `verifyHref()` |
| `agency.html` / `agency.js` / `agency.css` | หน้า "ทรัพย์หน่วยงาน": ช่องวางลิงก์ → บอกแหล่ง + รายการที่ทีมจะตรวจ → ปุ่มไป `verify.html` ที่กรอกไว้แล้ว |
| `loancalc.js` | `window.NJLoanCalc` — ค่างวดสินเชื่อ (บ้าน / ที่ดินเปล่า) |
| `tools.html` / `tools.js` | หน้า "เครื่องมือคำนวณ" = `NJLoanCalc` + `NJFeeCalc` ตัวเดิม (ใช้ `agency.css` ร่วม) |
| `manifest.webmanifest` | ให้เว็บติดตั้งลงหน้าจอหลักได้ + `share_target` (GET `/agency.html?title=&text=&url=`) |
| `agency.test.js` · `loancalc.test.js` | 69 + 25 ข้อ · `njchat.test.js` เพิ่ม 5 ข้อ (วางลิงก์ในแชท) |

ที่แก้เพิ่ม: `verify.js` (`prefillFromQuery()`) · `verify.html` (ลิงก์ไปหน้าหน่วยงานใต้ช่องลิงก์) ·
`index.html` (เมนูด่วน 07–08 + manifest) · `home.css` (`.quick` 5 → 4 คอลัมน์) · `njchat.js` (กฎวางลิงก์) · `sitemap.xml`

### กติกาที่ห้ามผ่อน
1. **ไม่ดึงข้อมูลจากเว็บหน่วยงาน (ห้าม scraping)** — อ่านแค่ตัวลิงก์ ไม่ fetch ปลายทาง (มีเทสต์ล็อก)
2. **ลิงก์ที่ลูกค้าส่งมาเป็นข้อมูลไม่น่าเชื่อถือ** — รับเฉพาะ http/https · ห้ามทำเป็น `<a>` บนหน้าเรา ·
   `esc()` ทุกครั้ง · ลิงก์ย่อไม่ขยายให้ (บอกให้วางลิงก์เต็ม) · แชทไม่ส่งลิงก์ให้ AI
3. **`source` ต้องอยู่ใน `PROPCHECK_SOURCES`** (server.js + verify.js) · `agency.test.js` เทียบกับ verify.js ให้
   · โดเมนเทียบแบบ "ตรงหรือเป็นโดเมนย่อย" เท่านั้น — `led.go.th.evil.com` ต้องไม่นับเป็นกรมบังคับคดี
4. **ลิงก์ยาวสุด 300 ตัว** (`PROPCHECK_MAX.sourceUrl`) — ตัด `utm_*`/`fbclid` ก่อน ยาวเกินต้องเตือนลูกค้า
   · `verify.js` รับ `?source=` เฉพาะค่าในรายการ และ `?url=` เฉพาะ http(s) ≤ 300 · ใส่ด้วย `.value` เท่านั้น
5. **รายการตรวจแยก "ตรวจเอกสาร" กับ "ลงพื้นที่" เสมอ** — คนละราคา และงานลงพื้นที่คือจุดต่างของเรา
6. **เครื่องคำนวณสินเชื่อไม่รวมค่าโอนให้เอง** — ปุ่ม "คิดค่าใช้จ่ายวันโอนต่อ" ส่งแค่ราคาซื้อขาย (+ประเภท
   ที่ดินเปล่า) ไปที่ `NJFeeCalc` แล้วโฟกัสช่องราคาประเมินให้ผู้ใช้กรอกเอง (กติกาข้อ 12)
   · ห้ามฝังดอกเบี้ย/วงเงินของธนาคารใด · คำเตือนค่าประมาณห้ามถอด
7. **กล่องค่าโอนใน `tools.html` ใช้ id `tl-fee` ห้ามใช้ `fee-calc`** — `bootPage()` จะซ่อนกล่องนั้นรอปุ่มของหน้าแรก
8. ลิงก์หน้าแรกของหน่วยงานตรวจแล้ว 17 ก.ย. 69 (กสิกรไทยตอบ 403 กับการตรวจอัตโนมัติ · IAM เข้าไม่ได้จึงไม่ใส่ลิงก์)

### เมนู (วัดจริงแล้ว — อย่าเพิ่มลิงก์โดยไม่วัดใหม่)
- **ไม่ได้เพิ่มลงเมนูหัวเว็บของหน้าเดิม** เพราะแถวเต็มพอดีแล้วทุกชุด (ดูหัวข้อ `menu.js`) · ทางเข้าคือ
  เมนูด่วน 07–08 บนหน้าแรก · ลิงก์ใต้ช่องลิงก์ใน `verify.html` · ปุ่มปิดท้ายของสองหน้าใหม่ชี้หากัน · แชท
- `agency.html` / `tools.html` มีเมนู 6 ข้อ · ซ่อน "ความรู้เรื่องที่ดิน" (`[data-nav-wide]`) ที่ ≤1199px
  และใช้ลิ้นชักตั้งแต่ ≤1023px (`.page-agency .header-nav`) — วัดที่ 985/990/1024/1100/1199/1200/1440 ไม่ตัดสองบรรทัด
- ⚠️ ปุ่ม Enter แบบจำลองของเครื่องมือทดสอบไม่ทำให้ฟอร์มส่งเอง จึงดัก `keydown` ที่ช่องลิงก์ด้วย
  (ชื่อปุ่มในเครื่องมือทดสอบต้องเป็น `Enter` — `Return` ไม่ถูกแปลงเป็นปุ่ม Enter)

### ปุ่มแชร์ (Web Share Target) — ข้อจำกัดที่ต้องบอกเจ้าของ
- ใช้ได้เมื่อ **ติดตั้งเว็บลงหน้าจอหลักแล้ว** และส่วนใหญ่เป็น Chrome บน Android · iPhone (Safari) ยังไม่รองรับ
- เว็บนี้ **ไม่มี service worker** (ตั้งใจ — กันปัญหาแคชค้าง) Chrome รุ่นใหม่ติดตั้งได้โดยไม่ต้องมี
  แต่ **ยังไม่ได้ทดสอบบนมือถือจริง** · Android หลายเครื่องใส่ลิงก์มาใน `text` ไม่ใช่ `url` → `agency.js` รวมทั้งสามช่อง

### ทดสอบแล้ว
`agency` 69 · `loancalc` 25 · `njchat` 51 · `compare` 10 · `feecalc` 50 · `valuecalc` 28 · `verified` 58 ·
`contracts` 194 — ผ่านครบ · เบราว์เซอร์ (python http.server) ที่ 375 และ 1440: วางลิงก์ BAM → ตัด fbclid →
ปุ่มไป verify.html ที่กรอกแหล่ง/ลิงก์ไว้ · `?source=<b>x&url=javascript:` ถูกทิ้ง · ลิงก์แชร์ผ่าน `?text=` ตรวจให้ทันที ·
ที่ดินเปล่า 3 ล้าน → ดาวน์ 40% 10 ปี ค่างวด 19,535 (ตรงกับคำนวณมือ) → ส่งราคาไปเครื่องคิดค่าโอนและโฟกัสช่องราคาประเมิน ·
เมนูด่วนหน้าแรก 4×2 ที่ 1440 · ไม่ล้นแนวนอน · ไม่มี console error

## คู่มือผังสี + ทางไปตรวจผังของหน่วยงาน (2026-09-17 · P3 รอบแรก)

branch `p3-zoning-guide` · เจ้าของสั่ง "เริ่มงานชั้นผังสี" · ✅ **merge แล้ว (PR #3) และขึ้นเว็บจริง 2026-09-17**

### ⚠️ ทำไมยังไม่มี "ชั้นระบายผังสีบนแผนที่"
ตรวจแหล่งข้อมูลเมื่อ 17 ก.ย. 69 — **เรียกจากเบราว์เซอร์ได้จริงแต่ไม่มีสิทธิ์นำมาแสดงบนเว็บสาธารณะ**
| แหล่ง | สถานะ |
|---|---|
| กรมโยธาฯ `dptgis.dpt.go.th/arcgis/rest/services/dds2/PLLU{จังหวัด}/MapServer` (export/identify/WMS ใช้ได้) | ไม่ประกาศสัญญาอนุญาต · เป็นระบบเบื้องหลัง · ไม่มี กทม./นนทบุรี |
| `landuseplan.dpt.go.th` | "All right reserved" · ระบบหลังบ้านของเราใช้ภายในผ่าน `lib/dptzone.js` แล้ว |
| กทม. `cityplangis.bangkok.go.th/.../comprehensiveplan_2556/MapServer` (GeoJSON + FAR/OSR) | ดีที่สุดทางเทคนิค · ไม่มีสัญญาอนุญาต · ต้องขอ สนผ. กทม. |
| Longdo `cityplan_dpt` | Longdo แจ้งว่าเป็นสำเนาภาพของกรมฯ ให้ติดต่อกรมฯ เอง · ต้องถามเงื่อนไขเชิงพาณิชย์ |
| data.go.th (`dpt_01_01`, `dpt1_001_002`, `dpt_02_02`) | ใช้ได้ (Open Data Common / CC-BY) แต่มีแค่ **รายชื่อผัง/ขอบเขตผัง ไม่มีผังสี** |
⚠️ **หนังสือขอ API กรมฯ ฉบับเดิมรับปากว่า "ไม่นำข้อมูลไปเผยแพร่ต่อบุคคลภายนอก"**
→ การแสดงผังสีบนเว็บสาธารณะต้องขออนุญาตแยกอีกฉบับ ห้ามอ้างหนังสือฉบับเดิม
· กติกาเดิมของ server.js ยังอยู่: `land.zoneInfo` (ข้อกำหนดฉบับเต็ม) ห้ามขึ้นเว็บ ขึ้นได้แค่ `zoning`/`zoneColor` ที่คนกดบันทึกเอง

### ไฟล์ในรอบนี้
| ไฟล์ | ทำอะไร |
|---|---|
| `zoneguide.js` / `zoneguide.css` | `window.NJZoneGuide` — ความหมาย 12 ประเภท (คีย์ชุดเดียวกับ `LAND_ZONES`) + ลิงก์ตรวจผัง 5 ระบบ + ปุ่มให้ทีมตรวจ |
| `zoneguide.test.js` | 31 ข้อ |
ที่แก้เพิ่ม: `tools.html` (ส่วน `#zoning` + ลิงก์กระโดด) · `tools.js` · `agency.css` · `agencies.js` (ลิงก์กรมฯ ชี้ระบบตรวจผังโดยตรง) ·
`njchat.js` (ถามความหมาย/วิธีตรวจผังสี → คู่มือ · ไม่ฟันธงว่าสร้างอะไรได้)

### กติกาที่ห้ามผ่อน
1. คำอธิบายเป็นภาพรวม · **ห้ามรับปาก** (มีเทสต์คำต้องห้าม) · `DISCLAIM` ขึ้นทุกครั้ง
2. ช่วงรหัส (ย.1–ย.10 ฯลฯ) ต่างกันตามผัง → เขียน "ในผัง กทม." กำกับ · จัดสีตาม "ประเภท" ไม่ใช่ตามเลข
3. ชื่อสีตามข้อความกฎกระทรวง · hex มาจากตัวแสดงผลของบริการทางการ = สีโดยประมาณ
4. ร่างผัง กทม. ต้องติดป้าย "ร่าง ยังไม่มีผลบังคับใช้" เสมอ
5. แชท: ต้องมีคำถามกำกับ ("คืออะไร" "สร้าง…ได้ไหม" "เช็คผัง") — "หาที่ดินผังเหลือง…" ยังเป็นโหมดค้นแปลง (มีเทสต์)

### ทดสอบแล้ว
`zoneguide` 31 · ชุดเดิมผ่านครบ (contracts 205 กับ server main ล่าสุด · njchat 51) ·
เบราว์เซอร์ 375px: การ์ด 12 ใบ ลิงก์ 5 ระบบ คำเตือนขึ้น ไม่ล้นแนวนอน ไม่มี console error ·
ลิงก์หน่วยงานทั้ง 5 ตอบ 200 (17 ก.ย. 69)

## ทางเข้าเครื่องมือจากหน้าคู่มือ · หัวข้อกรอกล่วงหน้าในฟอร์มตรวจทรัพย์ (2026-09-17)

branch `p4-guide-links`
- `guides.html` / `guides.css`: แถบ `.gd-tools` 3 ใบ (ค่างวดสินเชื่อ · ผังสี · ทรัพย์หน่วยงาน) · หัวข้อ `#zoning` · ลิงก์ระบบตรวจผังของกรมโยธาฯ ใน `.gd-links`
  · `.gd-tools` เป็น `<nav>` ใน `<main>` — `menu.js` อ่านเฉพาะ `header nav` / `.topbar nav` ลิ้นชักมือถือจึงไม่ดึงไปปน (ตรวจแล้ว)
- `verify.js`: `?topic=` → กรอกช่อง "ต้องการให้ตรวจเรื่องอะไร" จาก `PREFILL_TOPICS` (ตอนนี้มี `zoning`)
  · ⚠️ **รับเฉพาะคีย์ในรายการ (`hasOwnProperty`) ห้ามรับข้อความอิสระจาก URL** และไม่ทับข้อความที่ลูกค้าพิมพ์ไว้แล้ว
  · ใช้โดย `zoneguide.js` (ปุ่มให้ทีมตรวจ) และ `njchat.js` (กฎถามเรื่องผังสี)
- ทดสอบ: `zoneguide` 35 · ชุดเดิมผ่านครบ · เบราว์เซอร์: `?topic=zoning` กรอกให้ · `?topic=__proto__` และ `?topic=<img>` ไม่กรอก ·
  `guides.html` ที่ 375px ไม่ล้นแนวนอน ลิ้นชักไม่มีลิงก์เครื่องมือปน ไม่มี console error

## ป้ายผังสีบนการ์ดประกาศ + ตัวกรอง "ตรวจผังสีแล้ว" (2026-09-18 · ทางที่ 1 ของงานผังสี)

branch `p5-zone-chip` · เจ้าของสั่งทำระหว่างรอหนังสืออนุญาตจากกรมโยธาฯ (ดู `แผน-ผังสีระหว่างรอหนังสืออนุญาต.md`)

**หลักการ:** ผังสีที่ **ทีมอ่านผังแล้วกดบันทึกเอง** (`land.zoneColor`) เป็นข้อมูลของบริษัท เผยแพร่ได้
— ต่างจากภาพผังของหน่วยงานซึ่งยังไม่มีสิทธิ์ (ชั้นภาพใน `areamap.js` ยังปิดสวิตช์ `ZONING_OVERLAY=false`)

| ไฟล์ | สิ่งที่เพิ่ม |
|---|---|
| `landvocab.js` | `ZONE_HEX` · `ZONE_HATCH` · `zoneShort()` — **สีผังอยู่ที่นี่ที่เดียว** |
| `listingcard.js` | `zoneChip()` → ป้าย `.card-zone` ใต้ `.card-meta` |
| `home.css` · `marketplace.css` · `njchat.css` | สไตล์ `.card-zone` (ครบทั้งสามชุดตามกติกาการ์ด) |
| `listings.js` | ตัวเลือกกรอง `checked` = "ตรวจผังสีแล้ว (ทุกสี)" |
| `zoneguide.js` | เลิกเก็บรหัสสีเอง อ่านจาก `NJVocab` (`hexOf`/`hatchOf`) |
| `tools.html` | ย้าย `landvocab.js` ขึ้นมาก่อน `zoneguide.js` |
| `zonechip.test.js` | 18 ข้อ · `zoneguide.test.js` เพิ่มเป็น 39 ข้อ |

### กติกาที่ห้ามผ่อน
1. **ไม่มี `zoneColor` = ยังไม่ได้ตรวจ → ไม่ขึ้นป้ายอะไรเลย** ห้ามขึ้น "ไม่มีผังสี" (กติกาข้อ 5)
2. **ค่าผังสีที่ไม่รู้จักต้องไม่ขึ้นป้าย** (ไม่เดาสี) — มีเทสต์
3. **ตัวกรอง `checked` ยังนับแปลงที่ไม่มีผังสีเป็น `hiddenUnknown`** และขึ้นข้อความบอกจำนวนเหมือนช่องอื่น
4. **รหัสสีอยู่ใน `landvocab.js` ที่เดียว** — hex คัดจากตัวแสดงผลของบริการทางการ = สีโดยประมาณ ห้ามอ้างว่าเป็นสีตามกฎหมาย
5. เขียวลายขาว (`green_diag`) ต้องวาดเป็นลายทแยง ไม่ใช่สีทึบ
6. ⚠️ **`zoneguide.js` ต้องโหลดหลัง `landvocab.js`** ไม่งั้นคู่มือผังสีวาดสวอตช์เป็นสีขาวหมดโดยไม่มี error

### ทดสอบแล้ว
`zonechip` 18 · `zoneguide` 39 · ชุดเดิมผ่านครบ (contracts 205) ·
เบราว์เซอร์กับ API จริง: 22 แปลง มีป้าย 2 แปลง (ผังส้ม · ผังเขียว) · เลือก "ตรวจผังสีแล้ว" เหลือ 2 แปลง
พร้อมข้อความ "อีก 20 แปลงไม่ได้แสดง เพราะยังไม่ได้ระบุ…" · ไม่ล้นแนวนอนที่ 375/1440 ทั้ง listings และหน้าแรก · ไม่มี console error

⚠️ **ของจริงยังเห็นป้ายน้อย เพราะทีมกรอกผังสีไว้แค่ 2 จาก 22 แปลง** — ยิ่งทีมกรอกเพิ่ม ป้ายและตัวกรองยิ่งมีประโยชน์

## หน้านโยบาย 3 หน้า + ทางถอนความยินยอมคุกกี้ (2026-09-20 · Sprint 1 · C-01)

branch `feature/homepage-ux-platform-audit` · ทำหลังส่งรายงานตรวจเว็บ 7 ฉบับ (`docs/audit/`)

**ปัญหาที่แก้:** เว็บมีแบนเนอร์คุกกี้ขอความยินยอมทุกหน้า และมีฟอร์มเก็บข้อมูลส่วนบุคคล 8 ชุด
(รวมอัปโหลดสำเนาโฉนด) แต่**ไม่มีหน้านโยบายความเป็นส่วนตัว นโยบายคุกกี้ หรือข้อกำหนดการใช้งานเลย**
ผู้ใช้จึงให้ความยินยอมโดยไม่มีเอกสารให้อ่าน

| ไฟล์ | ทำอะไร |
|---|---|
| `privacy.html` | นโยบายความเป็นส่วนตัว (PDPA) — เก็บอะไร ส่งให้ใคร เก็บนานเท่าไร สิทธิ์ 8 ข้อ |
| `cookie.html` | นโยบายคุกกี้ — ตารางของที่เก็บในเบราว์เซอร์จริงทุกตัว + ปุ่มเปิดแถบตั้งค่า |
| `terms.html` | ข้อกำหนดการใช้งาน — ขอบเขตข้อมูล · ความหมายของระดับตรวจสอบ · ค่าบริการ |
| `legal.css` | สไตล์ของสามหน้านี้ **และแถบ `.nj-legal` ท้ายทุกหน้า** |
| `consent.test.js` | 28 ข้อ · DOM ปลอมใน `node:vm` (แนวเดียวกับ `compare.test.js`) |

### กติกาที่ห้ามผ่อน

1. ⚠️ **กล่องเหลือง `.lg-todo` ในหน้า privacy/terms ห้ามลบทิ้งเฉยๆ**
   ในนั้นคือข้อมูลที่กฎหมายต้องมีแต่ยังไม่มีในระบบ (ที่อยู่จดทะเบียน · เลขทะเบียนนิติบุคคล ·
   ช่องทางรับคำขอใช้สิทธิ์ · ระยะเวลาเก็บข้อมูลที่อนุมัติแล้ว) · ลบได้ก็ต่อเมื่อเติมครบจริงแล้ว
   ลบทิ้งเฉยๆ = หน้านโยบายที่ขาดข้อมูลตามกฎหมายโดยไม่มีอะไรเตือน
2. ⚠️ **การถอนความยินยอมต้องง่ายพอๆ กับการให้** — ของเดิมกดตอบครั้งเดียวแล้วจบถาวร
   เปลี่ยนใจได้ทางเดียวคือล้างข้อมูลเว็บไซต์ทิ้งทั้งก้อน **ซึ่งลบตั๋วใบฝากขายของลูกค้าไปด้วย**
   ทางกลับคือปุ่ม `[data-njconsent]` ในแถบ `.nj-legal` ท้ายทุกหน้า → `NJConsent.open()`
3. ⚠️ **`bindConsentLinks()` ต้องถูกเรียกก่อนบรรทัดที่ return ออกเมื่อเคยตอบแล้ว**
   ผูกทีหลัง = ลิงก์ "ตั้งค่าคุกกี้" ตายสนิทสำหรับคนที่เคยตอบไปแล้ว ซึ่งคือคนทั้งหมดที่ต้องใช้มัน
4. ⚠️ **กดปฏิเสธหลังโหลด Pixel ไปแล้ว ต้อง `location.reload()`** — ถอนสคริปต์ที่รันไปแล้วไม่ได้จริง
   ไม่โหลดใหม่ = สภาพจริงไม่ตรงกับสิ่งที่ผู้ใช้เพิ่งเลือก (มีเทสต์ล็อก)
5. ⚠️ **`paintConsentState()` เทียบค่าเดิมก่อนเขียน `textContent` เสมอ** — กับดัก MutationObserver
   เดียวกับที่เคยทำให้สองหน้าค้างทั้งแท็บ (มีเทสต์ล็อก)
6. ⚠️ **`legal.css` ต้องโหลดทุกหน้า ไม่ใช่เฉพาะสามหน้านโยบาย** เพราะแถบ `.nj-legal` อยู่ท้ายทุกหน้า
   เขียนกฎนี้ลงสามชุดสไตล์เดิมแทน = ของซ้ำสามที่ให้ลืมแก้ (กับดักเดียวกับสไตล์การ์ดสองไฟล์
   และ `.nj-consent` ที่ตอนนี้ยังซ้ำอยู่สี่ไฟล์ — หนี้ก้อนนั้นจะเก็บตอนรวม design token)
7. ปุ่มทุกอันในแถบ `.nj-legal` สูง 44px พอดีตามเกณฑ์เป้ากดบนมือถือ
8. ปุ่ม "ตั้งค่าคุกกี้" เป็น `<button>` จริง ไม่ใช่ `<a href="#">` — มันไม่ได้พาไปหน้าไหน

### ยังไม่ได้ทำในรอบนี้
- แผงติ๊กหมวดคุกกี้แยกหมวด — ตอนนี้มีของให้เลือกจริงหมวดเดียว (การตลาด) ทำแผงติ๊กหมวดเดียว
  คือเพิ่มขั้นตอนโดยไม่เพิ่มทางเลือก · วันไหนมีหมวดที่สองค่อยเปลี่ยน
- Skip link ยังมีแค่สามหน้าใหม่ (สไตล์อยู่ใน `legal.css` พร้อมแล้ว) — ไล่ใส่ทั้ง 24 หน้าใน Sprint 1 ข้อถัดไป

### ทดสอบแล้ว
`consent` 28 · `landlabel` 13 · ชุดเดิมผ่านครบ **รวม 583 ข้อ · ไม่ผ่าน 0** · `linkcheck` ลิงก์เสีย 0 ·
`sitemap.xml` 17 URL (เพิ่มสามหน้าใหม่) · เบราว์เซอร์ที่ 375px: ไม่ล้มแนวนอน · ปุ่มในแถบนโยบายสูง 44px ครบ ·
ตารางในหน้านโยบายเลื่อนแนวนอนเองไม่ดันหน้าให้ล้น · ไม่มี console error

## Design System กลาง + หัวเว็บชุดเดียว (2026-09-20 · Sprint 1 · งานที่ 1–2)

branch `feature/homepage-ux-platform-audit` · ต่อจากงานแก้ Critical 3 ข้อ

**ปัญหาที่แก้:** รายงานตรวจเว็บ (`docs/audit/`) ชี้ว่าเว็บมี **design system 3 ชุด + ฟอนต์ 3 ตระกูล**
และหัวเว็บถูกคัดลอกลง HTML ทีละหน้าจนเกิด **เมนู 10 รูปแบบใน 21 หน้า** สิ่งเดียวกันถูกเรียก 4 ชื่อ
(ซื้อที่ดิน / แปลงที่ดิน / ที่ดินประกาศขาย / ดูประกาศทั้งหมด) และไม่มี active state เลยสักหน้า

| ไฟล์ใหม่ | ทำอะไร |
|---|---|
| `tokens.css` | **Design token กลาง** — สี · ตัวอักษร · ระยะห่าง · มุมโค้ง · เงา · z-index |
| `components.css` | คอมโพเนนต์กลาง `nj-*` — Button · Form · Card · Badge · Alert · Modal · Loading · Empty · Error |
| `header.css` | หัวเว็บชุดเดียว (sticky · เมนูย่อย · active state) |
| `build/pages.js` | **สร้างหัวเว็บ + สไตล์ชีตกลางลงทุกหน้า** (`--check` ไว้ใช้ตอนตรวจ) |
| `header.test.js` | 97 ข้อ |

### ⚠️ กติกาที่ห้ามผ่อน

1. **`tokens.css` ต้องเป็นสไตล์ชีตตัวแรกของทุกหน้า** ไฟล์อื่นทับค่าที่นี่ได้ แต่ที่นี่ทับใครไม่ได้
   · `build/pages.js` เป็นคนใส่ให้ **อย่าใส่ด้วยมือ**
2. **ห้ามเพิ่มค่าสี/ขนาดตัวอักษรใหม่ลงไฟล์อื่นอีก** ต้องการค่าที่ยังไม่มีให้เพิ่ม token
   (กติกาเดียวกับ `landvocab.js` ที่เป็นแหล่งคำศัพท์เดียวของทั้งเว็บ) · มีเทสต์ล็อกว่า
   `header.css` และ `components.css` ห้ามมีรหัสสีดิบ
3. ⚠️ **HTML ระหว่างเครื่องหมาย `NJ:HEADER` / `NJ:CORECSS` ห้ามแก้ด้วยมือ** รอบถัดไปถูกเขียนทับ
   แก้ที่ `NAV` ใน `build/pages.js` ที่เดียว แล้วรัน `node build/pages.js`
   · **ผลลัพธ์ถูก commit ตามเดิม ไม่ได้เปลี่ยนวิธี deploy** GitHub Pages ยังเสิร์ฟจากรากของ repo
     (แนวเดียวกับ `build/sitemap.js`)
4. ⚠️ **แถวเมนูแรกมีได้ 6 กลุ่มเท่านั้น** ของเดิม 11 ลิงก์จนเต็มความกว้างพอดีที่จอ 1440
   ต้องการเพิ่มปลายทางใหม่ให้ใส่เป็น `items` ใต้กลุ่มที่มีอยู่ **ไม่ใช่เพิ่มกลุ่มที่ 7**
5. ⚠️ **`href` ของตัวกลุ่มต้องเป็นหน้าจริง ไม่ใช่ `#`** — เมนูย่อยเปิดด้วย hover/โฟกัส
   คนที่กดตัวกลุ่มตรงๆ (และคนที่ JS ไม่ทำงาน) ต้องไปถึงที่ที่ใช้ได้ · มีเทสต์ตรวจว่าทุก href มีไฟล์จริง
6. ⚠️ **เมนูย่อยเปิดด้วย `:hover` + `:focus-within` ล้วนๆ ไม่พึ่ง JS** และ **ขาเปิดไม่มี transition**
   ถ้าให้ opacity ค่อยๆ ไล่ขึ้น จะมีช่วงที่แผง "กดได้แล้วแต่ยังมองไม่เห็น" และในสภาพที่เบราว์เซอร์
   หน่วงการวาด ช่วงนั้นยืดจนกลายเป็นเมนูที่เปิดแล้วแต่ใสสนิท (เจอจริงตอนทดสอบ)
   · **ห้ามใส่ `visibility` ลงในรายการ transition ที่มีระยะเวลา** ค่ามันไม่ไล่ระดับ
7. ⚠️ **active state ผูกกับ `[aria-current="page"]` ไม่ใช่คลาสที่ตั้งเอง** — `build/pages.js` ใส่ให้
   ตามชื่อไฟล์ · ความหมาย (เครื่องอ่านหน้าจอ) กับหน้าตา (คนมองเห็น) จะได้ไม่มีทางหลุดจากกัน
8. **แถวเมนูซ่อนที่ต่ำกว่า 1024px** (ของเดิม 1280px ทำให้ iPad และโน้ตบุ๊กเหลือแค่แฮมเบอร์เกอร์)
   · `menu.js` ยังตัดสินจาก `getComputedStyle(nav).display` จริง **ไม่ต้องไปเขียนเลขซ้ำในไฟล์นั้น**
9. **ลิ้นชักจอเล็กสร้างจาก `<nav>` จริงเหมือนเดิม** เปลี่ยนแค่วิธีจัดเรียง (หัวกลุ่ม `.njmenu-group`
   + ลูก `.njmenu-sub`) · **ยังห้ามพิมพ์รายการเมนูซ้ำใน `menu.js`**
   · ลิงก์พอร์ทัลย้ายเข้า `<nav>` แล้ว จึงต้องมีตัวกันซ้ำ (มีเทสต์ล็อก)
10. ⚠️ **ฟอนต์ตระกูลเดียวทั้งเว็บ: IBM Plex Sans Thai** (เจ้าของตัดสิน 20 ก.ย. 2569)
    ถอด **Trirong** (68 จุด) และ **Manrope** (35 จุด) ออกหมดแล้ว · ทุกหน้าโหลดชุดฟอนต์เดียวกัน
    เบราว์เซอร์จึงใช้ไฟล์ที่แคชไว้ข้ามหน้าได้ · **เพิ่มตระกูลที่สองเมื่อไหร่ ปัญหา "เหมือนคนละเว็บ" กลับมาทันที**
    (มีเทสต์ล็อกว่าห้ามมี Trirong/Sarabun/Manrope เหลือ และทุกหน้าต้องโหลดชุดเดียวกัน)

### ผลที่วัดได้ (เทียบกับรายงานตรวจ)

| | ก่อน | หลัง |
|---|---|---|
| รูปแบบเมนู | 10 แบบใน 21 หน้า | **1 แบบใน 24 หน้า** |
| ลิงก์แถวแรก | 11 ลิงก์ (เต็มพอดี เพิ่มไม่ได้) | **6 กลุ่ม + ปุ่ม** (เหลือที่ว่าง) |
| แถวเมนูเดสก์ท็อปเริ่มแสดงที่ | 1280px | **1024px** |
| Active state | ไม่มี | ทุกหน้า |
| Skip link | ไม่มีเลย | **ทุกหน้า** |
| ตระกูลฟอนต์ | 3 (Trirong · Sarabun · IBM Plex · Manrope) | **1** |
| H1 หน้าแรกเดสก์ท็อป | 58px | **52px** (ในช่วง 48–56 ตามโจทย์) |
| H1 ที่จอ 375px | 4 บรรทัด | **3 บรรทัด** (360px ลดเป็น 32px อีกขั้น) |
| ความสูง hero เดสก์ท็อป | 759px | **692px** |
| หน้ากำพร้า | 3 | **1** (เหลือ `land.html` ซึ่งเข้าจากการ์ด) |

### ยังไม่ได้ทำในรอบนี้
- **ยังไม่ได้ไล่เปลี่ยนชุดสไตล์เดิมสามชุดให้อ่านจาก token ทุกบรรทัด** รอบนี้ทำแค่วาง token
  รวมฟอนต์ และตั้งสเกลตัวอักษร · การไล่แทนค่าที่ฝังไว้เป็นงานของสปรินต์ถัดไป
- หน้าแรกยังมี inline style ~270 จุด (H1 ย้ายมาใช้ token แล้วจุดเดียว)
- คอมโพเนนต์ `nj-*` ยังไม่ได้เอาไปแทนของเดิม (`.post-btn` `.land-card` ฯลฯ) — ตั้งใจ
  ไล่เปลี่ยนทั้งเว็บในคอมมิตเดียวทดสอบไม่ไหว

### ทดสอบแล้ว
`header` 97 · `consent` 28 · `landlabel` 13 · ชุดเดิมผ่านครบ **รวม 680 ข้อ · ไม่ผ่าน 0** ·
`node build/pages.js --check` ผ่าน (24 หน้าตรงกับสคริปต์) · `linkcheck` ลิงก์เสีย 0 ·
เบราว์เซอร์จริง 10 ความกว้าง (360–1920): ไม่ล้นแนวนอนสักขนาด · เมนูไม่ตัดบรรทัดสักขนาด ·
เมนูย่อยกางแล้วไม่ล้นขอบจอ · ลิ้นชักจอเล็กได้เมนูครบ 19 ลิงก์จัดเป็นกลุ่ม · ไม่มี console error

⚠️ **ข้อควรระวังตอนทดสอบเอง**: เบราว์เซอร์แคช CSS/JS เก่าไว้แรงมาก เจอสามครั้งในรอบนี้ว่า
หน้าจอแสดงของเก่าทั้งที่ไฟล์บนเซิร์ฟเวอร์ถูกแล้ว — ตรวจด้วย `fetch(..., {cache:'no-store'})`
ก่อนสรุปว่าโค้ดผิด

## หน้าแรกจัดใหม่ — Hero · Search · Trust Bar · ลำดับบล็อก (2026-09-20 · Sprint 2 · งานที่ 3–6)

branch `feature/homepage-ux-platform-audit`

**ปัญหาที่แก้ (จาก `docs/audit/HOMEPAGE_UX_AUDIT.md`)** — หน้าแรกยาว 27 จอบนมือถือ ·
ช่องค้นหาอยู่ลึก 1,494 px · hero มีปุ่ม 3 ปุ่มและไม่มีปุ่มไหนพาไปค้นหาทรัพย์เลย ·
ตัวเลขความน่าเชื่อถือถูกดันตกจอแรก · เครื่องคำนวณเต็มบล็อก 2 ตัวอยู่ก่อนช่องค้นหาและก่อนประกาศ

### ไฟล์ใหม่
| ไฟล์ | ทำอะไร |
|---|---|
| `services.html` | หน้าบริการ — รับของที่ย้ายออกจากหน้าแรก (`#บริการ` `#บริการเพิ่มเติม` `#ขั้นตอน` + คลิป 2 แถบ) |
| `homepage.test.js` | 70 ข้อ |
| `photos/hero.webp` · `hero-mobile.webp` · `hero-1280.webp` · `hero-1280.jpg` | ภาพ hero 3 ขนาด × 2 รูปแบบ |

### ลำดับบล็อกหน้าแรกตอนนี้ (ห้ามสลับโดยไม่อ่านเหตุผล)
Header → **Hero** → **Search** → **Trust Bar** → ทรัพย์แนะนำ 6 → จุดต่าง 3 ข้อ → เลือกบริการ 4 →
เครื่องมือและความรู้ → ทีมงาน → Final CTA → Footer

### ⚠️ กติกาที่ห้ามผ่อน

1. ⚠️ **id ที่ `marketplace.js` ผูกไว้แบบไม่เช็ก null ต้องอยู่ครบทุกตัว**
   `search-form` `search-input` `type-filter` `price-filter` `listing-grid` `result-note` `listings` `year`
   ขาดตัวเดียว = JS ตายทั้งไฟล์ ประกาศไม่ขึ้นเลย **และไม่มี error ให้เห็น** (`homepage.test.js` หัวข้อ 1 ล็อกไว้)
2. ⚠️ **ปุ่มหลักใน hero ได้ไม่เกิน 2 ปุ่ม** (งานที่ 3) ทางที่สามเป็น**ลิงก์ข้อความ** ไม่ใช่ปุ่ม
   ของเดิมมี 3 ปุ่มขนาดใกล้กัน ทำให้ไม่มีปุ่มไหนเด่นจริง · มีเทสต์นับจำนวนไว้
3. ⚠️ **`.nj-hero` ที่จอเล็กต้องมี `!important`** — `home.css` มีกฎเก่า
   `[data-r="hero"]{min-height:560px !important}` ใน `@media (max-width:1040px)` ที่ชนะกฎธรรมดา
   ถอด `!important` ออกเมื่อไหร่ hero กลับไปสูง 560px บนมือถือทันที (เจอจริงตอนวัด)
4. ⚠️ **แถบ Trust Bar มี 4 ข้อเท่านั้น** และทุกข้อต้องตรวจสอบได้จริง
   **ห้ามเขียนอะไรที่อ่านแล้วเข้าใจว่าทุกแปลงรังวัดแล้ว** (มีเทสต์คำต้องห้าม)
5. ⚠️ **รายการเติมคำของช่องทำเลสร้างจากแปลงที่มีอยู่จริง** (`fillLocations` อ่าน `land.province/amphoe/tambon`)
   **ห้ามพิมพ์รายชื่อ 77 จังหวัดไว้ในไฟล์** — เลือกทำเลที่ไม่มีของแล้วเจอผลว่างเปล่าจะดูเหมือนเว็บพัง
6. ⚠️ **ค่าจาก URL ต้องตรวจกับตัวเลือกที่มีจริงก่อนใช้** (`allowed.indexOf(pr)`) ห้ามยัดลงช่องตรงๆ
7. **`pushState` เฉพาะตอนผู้ใช้สั่งเอง · `replaceState` ตอนโหลดครั้งแรก**
   ไม่งั้นประวัติเต็มไปด้วยรายการที่ผู้ใช้ไม่ได้กด แล้วกดย้อนกลับสิบครั้งถึงจะออกจากหน้า
8. ⚠️ **ย้อนกลับมาที่ "ไม่มีตัวกรอง" ต้องเก็บข้อความสรุปผลเดิมทิ้งด้วย** (`noteFor()`)
   ไม่งั้นการ์ดกลับเป็น 6 ใบแล้วแต่บรรทัดบนยังเขียนว่า "พบ 5 รายการ" (เจอจริงตอนทดสอบ)
9. **หน้าแรกแสดง 6 แปลง ไม่ใช่ 9** — เจ้าของสั่งใหม่ 19 ก.ย. 2569 (งานที่ 6 ข้อ 5)
   ทับคำสั่งเดิมเมื่อ 13 ก.ย. ที่ให้ใช้ 9 · หน้าแรกเป็นหน้า "แนะนำ" ไม่ใช่หน้าไล่ดูของ
10. ⚠️ **วิดเจ็ตสองตัวย้ายออกจากหน้าแรกแล้ว** — `#survey-quote` ไป `tools.html#survey` ·
    `#fee-calc` ไม่ต้องมีเพราะ `tools.html#fee` มีอยู่แล้ว
    · ปุ่ม `[data-sq-open]`/`[data-fc-open]` บนการ์ดบริการหน้าแรกกลายเป็น**ลิงก์**
      เพราะ `surveyquote.js`/`feecalc.js` ผูกปุ่มเฉพาะเมื่อเจอ element ของตัวเอง — ไม่มี element = ปุ่มตาย
    · หน้าแรกเลิกโหลด `surveyquote.js` `feecalc.js` `njintro.js` `njservices.js` แล้ว (−~140 KB)
11. **จุดยึดที่ย้ายไป `services.html` ถูกแก้ลิงก์ให้แล้วทั้งเว็บ** (`compare.html` · `portal.html`)
    ย้ายบล็อกไปหน้าอื่นอีกเมื่อไหร่ ต้องรัน `node build/linkcheck.js` ทุกครั้ง

### ผลที่วัดได้ (เทียบกับรายงานตรวจ)

| | ก่อน | หลัง |
|---|---|---|
| ช่องค้นหาอยู่ในจอแรก | **ไม่อยู่สักขนาดเดียว** | **อยู่ครบทุกขนาด (360–1920)** |
| ตำแหน่งช่องค้นหา @390px | 1,494 px | **646 px** |
| ตำแหน่งช่องค้นหา @1366px | 989 px | **663 px** |
| ความสูง hero @1366 | 759 px | **470 px** |
| ความสูง hero @390 | 1,003 px | **471 px** |
| ความสูงหน้าแรก @1366 | 10,914 px (14 จอ) | **5,670 px (7.1 จอ)** |
| ความสูงหน้าแรก @390 | 21,510 px (26 จอ) | **11,795 px (14.5 จอ)** |
| ปุ่มหลักใน hero | 3 ปุ่ม (ไม่มีปุ่มค้นหา) | **2 ปุ่ม + ลิงก์** |
| บล็อกบนหน้าแรก | 17 | **9** |
| แชร์ผลการค้นหา / ปุ่มย้อนกลับ | ทำไม่ได้ | **ทำได้** |
| เติมคำทำเลอัตโนมัติ | ไม่มี | **46 ทำเลจากแปลงจริง** |
| ภาพ hero | JPEG ขนาดเดียว | **WebP + 3 ขนาด** |

### ยังไม่ถึงเป้า
- **หน้าแรกบนมือถือยังยาว 14.5 จอ** (เป้าคือไม่เกิน 8) — ที่เหลือมาจากการ์ดประกาศ 6 ใบ
  ซึ่งเรียงคอลัมน์เดียว (3,873 px) และฟุตเตอร์ (1,456 px) · ทั้งสองอย่างเป็นงานของสปรินต์ถัดไป
  (การ์ดอยู่ในงานที่ 7 · ฟุตเตอร์ยังไม่มีในโจทย์ข้อไหน — ต้องถามเจ้าของก่อน)
- **ยังไม่ได้เพิ่มเหตุการณ์สถิติ `homepage_search` / `filter_property`** — ต้องขึ้นทะเบียนทั้ง
  `analytics.js` และ `PUBLIC_EVENT_TYPES` ใน `server.js` (คนละรีโป) ไม่งั้นถูกทิ้งเงียบๆ
  และ `contracts.test.js` จะแดง · ทำพร้อมงานที่ 16 ในสปรินต์ 5
- **ยังไม่มี Bottom Sheet ของตัวกรองบนมือถือ** (งานที่ 4) — ตัวกรองเต็มอยู่ที่ `listings.html`
  ซึ่งเป็นงานของสปรินต์ 3

### ทดสอบแล้ว
`homepage` 70 · ชุดเดิมผ่านครบ **รวม 752 ข้อ · ไม่ผ่าน 0** · `build/pages.js --check` ผ่าน (25 หน้า) ·
`linkcheck` ลิงก์เสีย 0 · `sitemap.xml` 18 URL · เบราว์เซอร์จริง 8 ความกว้าง (360–1920):
ไม่ล้นแนวนอนสักขนาด · ค้นหา → URL เปลี่ยน → ย้อนกลับ → เดินหน้า คืนค่าถูกทุกรอบ ·
เมนูย่อยและลิ้นชักยังทำงานปกติ · `services.html` และ `tools.html#survey` เปิดใช้ได้จริง · ไม่มี console error

⚠️ **ตอนทดสอบต้องบังคับโหลด asset ใหม่** — เบราว์เซอร์แคช CSS/JS เก่าแรงมาก
วิธีที่ใช้ได้จริงคือสร้างไฟล์ชั่วคราวที่เติม `?cb=<เวลา>` ท้าย href/src ทุกอัน แล้วเปิดไฟล์นั้นแทน
(เจอปัญหานี้ซ้ำหลายรอบจนเกือบสรุปผิดว่าโค้ดพัง)

## การ์ดประกาศ · หน้ารายละเอียดแปลง · บันทึกแปลง (2026-09-20 · Sprint 3 · งานที่ 7–8)

branch `feature/homepage-ux-platform-audit`

**ปัญหาที่แก้ (จาก `docs/audit/`)** — การ์ดไม่แสดงรหัสทรัพย์ทั้งที่ทีมต้องใช้รหัสจับคู่ผู้ซื้อกับเจ้าของ ·
ระดับการตรวจสอบขึ้นเป็นเศษส่วน "3/5" ที่ไม่มีใครอ่านออกว่าแปลว่าอะไร · ป้าย "ข้อมูลเบื้องต้น"
ความต่างสี 3.68:1 (ต่ำกว่าเกณฑ์ AA) · **H1 หน้าแปลงยาว 200+ ตัวอักษรเพราะเอา `parcelInfo` ทั้งก้อนมาเป็นชื่อ** ·
ไม่มี breadcrumb ไม่มี meta description ไม่มี og รายแปลง (แชร์เข้า LINE/Facebook ได้การ์ดเดียวกันทุกแปลง) ·
แปลงที่ถูกถอดแล้วยังไม่ประกาศ noindex (soft 404) · ไม่มีที่เก็บแปลงที่สนใจเลย

### ไฟล์ใหม่
| ไฟล์ | ทำอะไร |
|---|---|
| `save.js` / `save.css` | `window.NJSave` — บันทึกแปลงที่สนใจข้ามวัน (localStorage) |
| `detail.test.js` | 78 ข้อ |
| `.github/workflows/ci.yml` | รันชุดทดสอบ + `pages --check` + `sitemap --check` + `linkcheck` ทุก push/PR |

### ⚠️ กติกาที่ห้ามผ่อน

1. ⚠️ **`save.js` ต้องโหลด*ก่อน* `listings.js` / `marketplace.js`** ทั้งสองไฟล์อ่าน `window.NJSave`
   ตอนเริ่มทำงานทันที · มาทีหลัง = ตัวเลข "เฉพาะที่บันทึกไว้" ค้างว่าง **โดยไม่มี error ให้เห็น**
   (เจอจริงตอนทดสอบ · `detail.test.js` หัวข้อ 4 ล็อกไว้ทั้ง 10 หน้า)
2. ⚠️ **`save.js` ใช้ localStorage ส่วน `compare.js` ใช้ sessionStorage — ตรงข้ามกันโดยตั้งใจ**
   การเทียบคือเรื่องของการเข้าเว็บรอบนี้ · การบันทึกคือ "เก็บไว้กลับมาดูอีก" ผู้ซื้อที่ดิน
   กลับมาดูซ้ำข้ามสัปดาห์ · เก็บผิดที่ = ปิดแท็บแล้วของที่เลือกไว้หายหมด
3. ⚠️ **เก็บแค่รหัสแปลง ไม่เก็บข้อมูลแปลง** ราคาและสถานะเปลี่ยนได้ตลอด
   เก็บทั้งก้อน = ผู้ซื้อกลับมาเห็นราคาเก่าที่ไม่ตรงกับความจริงแล้ว (กติกาเดียวกับ `njSeen`)
4. ⚠️ **กับดัก MutationObserver อีกครั้ง** — `decorate()` แปะปุ่มลงในตะแกรงที่ตัวเองเฝ้าอยู่
   `syncMarks()` จึงต้องเทียบค่าเดิมก่อนเขียนทุกครั้ง และ `watch()` ต้อง `mo.disconnect()`
   ก่อนแก้ DOM (บทเรียนเดียวกับ `compare.js` ที่เคยทำให้สองหน้าค้างทั้งแท็บ)
5. ⚠️ **ตัวดักคลิกต้อง `stopPropagation` ในเฟส capture** — `bindGrid()` ดักคลิกทั้งการ์ด
   ไม่หยุดไว้ = กดบันทึกแล้วเด้งเข้าหน้ารายละเอียดทันที
6. ⚠️ **`save_property` นับเฉพาะตอนกดเพิ่ม ไม่นับตอนเอาออก** กดสลับไปมาหนึ่งครั้ง = หนึ่งเหตุการณ์ ไม่ใช่สอง
7. ⚠️ **ตัวกรอง "เฉพาะที่บันทึกไว้" ไม่นับแปลงที่ไม่ได้บันทึกเป็น `hiddenUnknown`**
   ช่องอื่นนับเพราะ "ยังไม่ได้กรอก ≠ ไม่ตรง" แต่ช่องนี้ผู้ใช้รู้อยู่แล้วว่าตัวเองบันทึกอะไรไว้
   ขึ้นข้อความว่า "อีก 21 แปลงถูกซ่อนเพราะยังไม่ได้ระบุ" ที่นี่คืออ่านไม่รู้เรื่อง
8. ⚠️ **แผงตัวกรองจอเล็กซ่อนด้วยคลาส `ls-js` ที่ JS เป็นคนใส่** (`body.ls-js .ls-filters`)
   ซ่อนใน CSS ตรงๆ = JS โหลดไม่สำเร็จเมื่อไหร่ ตัวกรองหายทั้งชุดโดยไม่มีทางเปิด
9. ⚠️ **ชื่อระดับเปลี่ยนแค่คำที่แสดง คีย์ในฐานข้อมูลเหมือนเดิมทุกตัว** (เจ้าของเลือก 20 ก.ย. 2569)
   `owner` → ระดับ 1 ข้อมูลจากเจ้าของ · `document` → ระดับ 2 ตรวจเอกสารเบื้องต้น ·
   `site` → ระดับ 3 ลงพื้นที่ตรวจสอบ · `survey` → ระดับ 4 รังวัดยืนยันแนวเขต ·
   `transfer` → ระดับ 5 พร้อมรายงานตรวจสอบ · **ไม่มี migration และไม่แตะข้อความฝั่งเซิร์ฟเวอร์**
10. ⚠️ **แถบติดต่อติดหนึบบนมือถือใช้ `--nj-z-sticky` (150) ซึ่งต่ำกว่าแบนเนอร์คุกกี้ (900)**
    เรื่องกฎหมายต้องอยู่บนสุดเสมอ · และยก `bottom:69px` ให้พ้น `.mobile-nav`
11. ⚠️ **ปุ่ม "แจ้งข้อมูลไม่ถูกต้อง" ไม่ใส่ `data-contact` และไม่ยิงสถิติติดต่อ**
    คนที่กดคือคนที่มาบอกว่าประกาศผิด ไม่ใช่ลีด (กติกาข้อ 6 มองจากด้านกันลีดปลอม เหมือน `portal.html`)
    · แต่ต้องแนบรหัสแปลงไปในข้อความเสมอ ไม่งั้นทีมงานไม่รู้ว่าแปลงไหน
12. ⚠️ **แปลงที่ถูกถอดแล้ว (`markGone`) ต้อง noindex + ถอด canonical ทิ้ง**
    ปล่อย canonical ชี้ไปหน้าที่ไม่มีเนื้อหาแล้ว = บอกเสิร์ชเอนจินว่าหน้านี้ยังเป็นของจริง
13. ⚠️ **บล็อกแปลงใกล้เคียง/แปลงที่เพิ่งดู โหลดไม่สำเร็จ = ไม่ขึ้นอะไรเลย** ไม่ใช่กล่องว่าง
    (กติกาข้อแรกของ Phase 1) · และใช้ `NJListing.card` ตัวกลาง **ห้ามก๊อปตัวเรนเดอร์การ์ด**

### เหตุการณ์สถิติที่เพิ่ม 6 ตัว — ⚠️ ต้อง deploy `nj-survey-system` ก่อนเว็บ

`homepage_search` · `filter_property` · `save_property` · `share_property` · `use_calculator` · `chat_to_human`
ขึ้นทะเบียนแล้วทั้ง `NJ_INTERNAL_EVENTS` (เว็บ) และ `PUBLIC_EVENT_TYPES` (`server.js`) · **ไม่มีตัวไหนเป็นลีด**
· ตรวจกับของจริง 20 ก.ย. 69: `app.njteedinsure.com` ยังตอบ **400** ให้หกชนิดนี้ (ชนิดเดิมตอบ 204)
เพราะฝั่งระบบยังไม่ deploy · `sendBeacon` ไม่สนใจผลลัพธ์ หน้าเว็บจึงไม่พัง **แต่เหตุการณ์หายทั้งหมด**
→ **ขึ้น `nj-survey-system` ก่อนเสมอ** (กติกาเดียวกับ Phase 2)

### ผลที่วัดได้
| | ก่อน | หลัง |
|---|---|---|
| รหัสทรัพย์บนการ์ด | ไม่มี | ทุกใบ |
| ป้ายระดับการตรวจสอบ | "3/5" | **"ระดับ 3/5 ลงพื้นที่ตรวจสอบ"** |
| ความต่างสีป้าย "ข้อมูลเบื้องต้น" | 3.68:1 (ตก AA) | **7.4:1** |
| H1 หน้าแปลง | 200+ ตัวอักษร | **74 ตัวอักษร** (ของเต็มย้ายลง `.ld-desc`) |
| meta description / og รายแปลง | ไม่มี | **มีทุกแปลง (og:image = รูปของแปลงนั้น)** |
| breadcrumb + BreadcrumbList | ไม่มี | ทุกแปลง |
| แปลงที่ถูกถอด | 200 + ไม่มี noindex | **noindex + ถอด canonical** |
| เก็บแปลงที่สนใจ | ไม่มี | บันทึกข้ามวัน + กรองดูเฉพาะที่บันทึก |
| ตัวกรองบนมือถือ | แผงยาวดันเนื้อหาตกจอ | **Bottom sheet (งานที่ 4 ที่ค้างจากสปรินต์ 2)** |
| CI | ไม่มี | รันทุก push/PR |

### ยังไม่ได้ทำในรอบนี้
- **หน้าแปลงยังไม่อยู่ใน `sitemap.xml`** (H-06) และ **og ยังเขียนด้วย JS บอต LINE/Facebook จึงอ่านไม่เห็น** (H-07)
  ทั้งสองข้อต้องมีหน้าสแตติกรายแปลงที่สร้างตอน build — เป็นงานของสปรินต์ 5
  · ⚠️ **อย่าเพิ่งใส่ `land.html?id=…` ลง sitemap ก่อนมีหน้าสแตติก** เท่ากับเชิญบอตมาอ่าน HTML เปล่า
- **รูปการ์ดยังเป็นไฟล์ต้นฉบับ** (H-04 · ใบละ ~458 KB เข้าช่อง 390×160) — ต้องย่อที่ฝั่งระบบตอนอัปโหลด
- **ยังไม่เก็บ UTM / Source / First touch / Last touch** (H-11) — สปรินต์ 5
- หน้าแรกบนมือถือยัง 14.5 จอ (เป้า 8) · ที่เหลือคือการ์ด 6 ใบ (3,873 px) และฟุตเตอร์ (1,456 px)
  **ฟุตเตอร์ต้องถามเจ้าของก่อน** ยังไม่อยู่ในโจทย์ข้อไหน

### ทดสอบแล้ว
`detail` 78 · ชุดเดิมผ่านครบ **รวม 830 ข้อ · ไม่ผ่าน 0** · `pages --check` ผ่าน (25 หน้า) ·
`sitemap --check` ผ่าน · `linkcheck` ลิงก์เสีย 0 · หน้ากำพร้า 1 (`land.html` เข้าจากการ์ด) ·
เบราว์เซอร์จริงกับ API จริงที่ 390px และ 1440px: การ์ดขึ้นรหัสและปุ่มดูรายละเอียด ·
breadcrumb "หน้าแรก › ประกาศทั้งหมด › สมุทรปราการ › OP-072" · og:image เป็นรูปของแปลงนั้นจริง ·
แถบติดหนึบ `display:flex` `bottom:69px` `z-index:150` ปุ่มสูง 44px · แปลงใกล้เคียง 3 ใบในจังหวัดเดียวกัน ·
กรอง "เฉพาะที่บันทึกไว้" 22 → 1 แล้วตัวเลขขยับเป็น (2) สด · bottom sheet เปิด/ปิด/Escape ได้ ·
ไม่ล้นแนวนอน · ไม่มี console error

## ฟอร์ม · ที่มาของลีด · แบนเนอร์คุกกี้ · แชท · คู่มือ (2026-09-20 · Sprint 4 · งานที่ 10–12)

branch `feature/homepage-ux-platform-audit`

**ปัญหาที่แก้** — ฟอร์มเก็บลีด 7 ชุดขึ้นข้อความผิดพลาด**เป็นกล่องเดียวบนหัวฟอร์ม** ซึ่งบนมือถือ
เลื่อนพ้นจอไปแล้วตอนผู้ใช้กดส่ง เขาจึงเห็นแต่ปุ่มที่กดแล้วเงียบ · ไม่มีการจัดรูปเบอร์โทรเลยสักช่อง ·
**ไม่เก็บที่มาของลีดเลย** (มีแต่ `ref` ของหน้าฝากขายที่ตัด query string ดิบมา 60 ตัวอักษร) ·
แบนเนอร์คุกกี้ (z 900) **ทับแถบติดต่อติดหนึบของหน้าแปลง (z 150) เต็มๆ** จนกดปุ่มโทร/ไลน์ไม่ได้

### ไฟล์ใหม่
| ไฟล์ | ทำอะไร |
|---|---|
| `attrib.js` | `window.NJAttrib` — เก็บ UTM · แหล่งอ้างอิง · สัมผัสแรก/สัมผัสสุดท้าย (โหลดทุกหน้าผ่าน `build/pages.js`) |
| `njform.js` | `window.NJForm` — จัดรูปเบอร์/เงิน · ข้อความผิดพลาดรายช่อง · กติกาลีดขั้นต่ำ · เก็บร่าง |
| `forms.test.js` | 86 ข้อ |

### ⚠️ กติกาที่ห้ามผ่อน

1. ⚠️ **`attrib.js` ห้ามเก็บหรือส่งข้อมูลส่วนบุคคลเด็ดขาด** (ข้อกำหนดข้อ 11 และ 16)
   สิ่งที่เก็บคือ "ลิงก์ไหนพาเขามา" ไม่ใช่ "เขาเป็นใคร"
2. ⚠️ **หน้าที่เข้ามาและแหล่งอ้างอิง เก็บเฉพาะ path ตัด query ทิ้งเสมอ**
   `inspect.html?id=..&t=..` · `room.html?..&t=..` · `deal.html?t=..` พก**ตั๋ว**มาใน query
   ซึ่งใครถือก็เปิดเอกสารในนามลูกค้าได้ · เก็บทั้ง URL = ตั๋วไปโผล่ในใบลีดแล้ววิ่งต่อเข้าระบบหลังบ้าน
   · **ของเดิมผิดข้อนี้อยู่จริง** — `ref: location.search.slice(1,60)` ในหน้าฝากขายและกล่องสนใจบริการ
   · เซิร์ฟเวอร์ตัดซ้ำอีกชั้นที่ `attribCut()` ไม่พึ่งฝั่งเว็บอย่างเดียว
3. ⚠️ **"สัมผัส" นับเฉพาะตอนมี UTM หรือมาจากเว็บอื่นจริงๆ** เดินหน้าต่อหน้าในเว็บเราเอง
   ต้องไม่ทับค่าสัมผัสสุดท้าย ไม่งั้นทุกลีดกลายเป็น "มาจากหน้าแรกของเราเอง" หมด
4. ⚠️ **สัมผัสแรกเขียนครั้งเดียว ห้ามทับ** และต้องแยกจากสัมผัสสุดท้ายเสมอ —
   คนที่เห็นโฆษณาเฟซบุ๊กครั้งแรก แล้วอีกสองสัปดาห์ค่อยค้นกูเกิลกลับมากรอกฟอร์ม
   ถ้าให้เครดิตแค่ช่องทางสุดท้าย เราจะสรุปว่าโฆษณาไม่ได้ผลแล้วหยุดยิง ทั้งที่มันพาเขามารู้จักเรา
5. ⚠️ **`attrib.js` ต้องโหลดทุกหน้า ไม่ใช่เฉพาะหน้าที่มีฟอร์ม** — คนคลิกโฆษณาเข้าหน้าแปลง
   อ่านสามหน้า แล้วค่อยไปกรอกฟอร์ม · `build/pages.js` มีบล็อก `NJ:COREJS` ใส่ให้ **อย่าใส่ด้วยมือ**
6. ⚠️ **กติกาลีดขั้นต่ำอยู่ใน `njform.js` ที่เดียว** (ชื่อ · เบอร์ · ยินยอม) **ห้ามเพิ่มช่องบังคับ**
   คนที่ยังไม่รู้ว่าตัวเองอยากได้อะไรก็ยังเป็นลีดที่คุยต่อได้ · และทุกฟอร์มต้องมีทางถอย
   เมื่อ `njform.js` โหลดไม่สำเร็จ (ตรวจได้ครบเหมือนเดิม แค่ไม่มีข้อความชี้ช่อง)
7. ⚠️ **ขึ้นข้อความผิดพลาดสองที่เสมอ** — กล่องสรุปบนหัวฟอร์ม และใต้ช่องที่ผิดจริง
   พร้อมย้ายโฟกัสไปที่ช่องนั้น · และผูก `aria-invalid` + `aria-describedby` ให้เครื่องอ่านหน้าจอ
   · **ห้ามบอกว่าผิดด้วยสีอย่างเดียว** (`.njf-err::before` มีสัญลักษณ์ ⚠ กำกับ)
8. ⚠️ **จัดรูปเบอร์เฉพาะตอนเคอร์เซอร์อยู่ท้ายช่อง** — เขียนทับทั้งช่องตอนแก้กลางข้อความ
   ทำให้เคอร์เซอร์กระโดดไปท้ายทุกครั้ง แล้วแก้เบอร์ที่พิมพ์ผิดตัวเดียวไม่ได้เลย
9. ⚠️ **ห้ามเก็บข้อมูลส่วนบุคคลลงร่าง** — `NJForm.draft()` ปฏิเสธชื่อช่องที่เข้ารูปแบบ `PII` เสมอ
   เครื่องที่ใช้ร่วมกันในครอบครัวหรือร้านเน็ตไม่ควรมีชื่อกับเบอร์ของคนก่อนหน้าค้างอยู่
10. ⚠️ **ฟอร์มที่ JS วาดเองต้องเรียก `NJForm.attach()` เอง** — ตัวติดตั้งอัตโนมัติทำงานตอน
    DOMContentLoaded จึงไม่เห็นฟอร์มที่วาดทีหลัง (`njservices` · `room` · `partner-apply` · `inspect`)

### แบนเนอร์คุกกี้ (งานที่ 12 ครึ่งแรก)
- ถ้อยคำสั้นลง **110 → 92 ตัวอักษร** · ปุ่มเป็น **"เฉพาะที่จำเป็น" / "ยอมรับทั้งหมด"**
  (ของเดิม "ปฏิเสธ / ยอมรับ" ซึ่งไม่บอกว่าปฏิเสธแล้วเสียอะไร) · ลิงก์ "ตั้งค่าคุกกี้" → `cookie.html`
- ⚠️ **ยังไม่ทำแผงติ๊กหมวด — ตั้งใจ** ตอนนี้มีของให้เลือกจริงหมวดเดียว (การตลาด)
  แผงติ๊กหมวดเดียวคือเพิ่มขั้นตอนโดยไม่เพิ่มทางเลือก · วันไหนมีหมวดที่สองค่อยเปลี่ยน
- ⚠️ **แบนเนอร์ต้องบอกความสูงตัวเองผ่าน `--nj-consent-h`** (`measureConsent()` ใน `analytics.js`)
  แล้วให้ของที่ติดขอบล่างบวกเอง — `.ld-sticky` ใช้ `bottom: calc(69px + var(--nj-consent-h, 0px))`
  · วัดจากตัวแบนเนอร์จริง ไม่ใช่ตัวเลขที่เดา · ตอบแล้วต้องคืนพื้นที่ทันที (`setConsentH(0)`)
  · เทียบค่าเดิมก่อนเขียนเสมอ (กับดัก MutationObserver)

### แชท (งานที่ 12 ครึ่งหลัง)
- **ส่ง `listingId` ของแปลงที่ผู้ใช้กำลังดูไปให้ AI ด้วย** — เดิมคนพิมพ์ "แปลงนี้" แล้ว AI ต้องเดาเอง
  · ฝั่งเว็บอ่านจาก `?id=` เฉพาะหน้า `land.html` · **เซิร์ฟเวอร์กรองรูปแบบซ้ำอีกชั้น** (`validateBody`)
- **ปุ่มแชทโผล่หลัง 18 วินาที หรือเมื่อผู้ใช้เลื่อนจอลง 400px** (ข้อกำหนดให้ 15–30 วิ)
  · ⚠️ **ข้อห้ามเดิมไม่เปลี่ยน — ยังห้ามเปิดแผงเอง** ที่โผล่ทีหลังคือ "ปุ่ม" ไม่ใช่ "แผง"
  · ⚠️ **ซ่อนด้วยคลาส `njchat-hold` ที่ JS ใส่เอง** ห้ามซ่อนใน CSS ตรงๆ (JS พัง = ปุ่มหายถาวร)
  · ⚠️ **ขาโผล่ไม่มี transition + ต้องมี `visibility:hidden` ด้วย** — กติกาเดียวกับเมนูย่อยบนหัวเว็บ
    ใส่ transition ที่กฎฐานเมื่อไหร่ **และเบราว์เซอร์หยุดวาด (แท็บพื้นหลัง) ปุ่มจะใสสนิทแต่กดได้**
    เจอจริงตอนวัด — opacity ค้างที่ 0 ขณะที่ `pointer-events` เป็น `auto` ไปแล้ว

### คู่มือที่ดิน (งานที่ 10 ครึ่งที่ทำได้)
- เพิ่มแถบผู้เรียบเรียง + **วันที่ตรวจทาน** (`<time datetime>`) + แหล่งอ้างอิงทางการเพิ่มเป็น 5 แห่ง
- ⚠️ **ยังไม่ใส่ชื่อ "ผู้ตรวจทาน" เพราะยังไม่มีชื่อที่ยืนยันแล้ว** — ขึ้นกล่อง `.gd-todo` บอกตรงๆ
  แทนการใส่ชื่อสมมติ (กติกาข้อ 4 และ 9 · แนวเดียวกับ `.lg-todo` ในหน้านโยบาย)
- ⚠️ **แก้เนื้อหาคู่มือเมื่อไหร่ ต้องแก้วันที่ใน `<time>` ด้วยทุกครั้ง** เทสต์ตรวจได้แค่ว่า
  "มีและไม่เป็นวันในอนาคต" ตรวจแทนไม่ได้ว่าตรงกับของจริงไหม

### 🐞 บั๊กที่เจอระหว่างวัดจริง (ไม่ได้เกิดจากรอบนี้ แต่เก็บไปด้วย)
- **หน้ารายละเอียดแปลงเลื่อนแนวนอนได้ 91px ที่จอ 375px** — `.ld-val` ตั้ง `white-space:nowrap`
  **และ `flex:none`** ซึ่งถูกสำหรับค่าสั้น (วันที่ · "โฉนด") แต่ช่องนี้รับข้อความอิสระด้วย
  เช่นชื่อโครงการยาวๆ · ⚠️ **ปลด nowrap อย่างเดียวไม่พอ** ต้องมี `flex:0 1 auto` + `min-width:0`
  ไม่งั้นมันยังกว้างเท่าเนื้อหาอยู่ดี (วัดแล้ว 394px บนจอ 375px)
- ~~**`partner-apply.html` อยู่บนเว็บจริงแต่ `POST /api/public/partner-apply` ตอบ 404** —
  เส้นทางฝั่งระบบอยู่ใน PR ที่ยังไม่ merge~~
  ⚠️ **ข้อนี้เขียนผิด แก้เมื่อ 21 ก.ย. 2569** — 404 ไม่ได้แปลว่าเส้นทางไม่มี
  แต่เป็น**พฤติกรรมที่ออกแบบไว้ของสวิตช์ฟีเจอร์** (`server.js` บรรทัด 4187 → `featureflags.isOn('partners')`)
  · กติกาใน `lib/featureflags.js` คือ "ทุกสวิตช์ปิดเป็นค่าเริ่มต้น" และ "ปิดอยู่ = ตอบ 404 ไม่ใช่ 403
  เพื่อไม่บอกใบ้ว่ามีฟีเจอร์ซ่อนอยู่" · เจ้าของเปิดสวิตช์แล้ว ตรวจซ้ำได้ 204
  · **บทเรียน: เจอรหัสตอบกลับผิดปกติ ให้ไล่หาจุดที่คืนค่านั้นในโค้ดก่อนเสมอ อย่าสรุปสาเหตุเอง**
  (ข้อนี้ถูกยกเป็น "งานด่วนที่สุด" ติดกันสามสปรินต์ทั้งที่ไม่ใช่ปัญหา)

### ฝั่งระบบ (`nj-survey-system`) — ⚠️ ต้อง deploy ก่อนเว็บเหมือนเดิม
- `publicAttrib()` / `attribLine()` ใน `server.js` — คัดเฉพาะช่องที่รู้จัก ตัดความยาว 120
  ตัด query ทิ้ง และทิ้งค่าที่รูปร่างไม่ถูก · เก็บลงใบลีด 5 เส้นทาง (ฝากขาย · ตรวจทรัพย์ ·
  สนใจบริการ · นัดตรวจ · ห้องข้อมูล) พร้อมบรรทัดสรุปในบันทึกการติดต่อ
- `lib/njchat.js` รับ `listingId` เพิ่มและใส่ลงคำสั่งระบบ

### ทดสอบแล้ว
`forms` 86 · `consent` 39 (เพิ่มหัวข้อ 9–10) · ชุดเดิมผ่านครบ **รวม 927 ข้อ · ไม่ผ่าน 0** ·
ฝั่งระบบ `npm test` ผ่านครบ · `pages --check` 25 หน้า · `sitemap --check` ผ่าน · ลิงก์เสีย 0 ·
เบราว์เซอร์จริงที่ 375px: เก็บ UTM จากลิงก์โฆษณาได้จริง · เบอร์จัดรูปเป็น `081-234-5678` ขณะพิมพ์ ·
ส่งฟอร์มว่างแล้วขึ้นข้อความใต้ช่อง + `aria-invalid` + โฟกัสไปที่ช่องนั้น ·
แบนเนอร์คุกกี้สูง 121px และ **ไม่ทับแถบติดต่อติดหนึบแล้ว** (ยก 203px ตามที่วัดได้ · ตอบแล้วคืนเป็น 69px) ·
ปุ่มแชทซ่อนอยู่ 18 วินาทีแล้วโผล่ แผงยังไม่เปิดเอง · หน้าแปลงไม่ล้นแนวนอนแล้ว (91px → 0) ·
ยิงเข้าเซิร์ฟเวอร์จริงบนเครื่อง: ที่มาถูกเก็บครบ และ payload ที่แนบตั๋วมาถูกตัดทิ้งทุกช่อง

## หน้าแปลงสแตติก · การเข้าถึง · ประสิทธิภาพ · สถิติ (2026-09-20 · Sprint 5 · งานที่ 13–16)

branch `feature/homepage-ux-platform-audit`

### ไฟล์ใหม่
| ไฟล์ | ทำอะไร |
|---|---|
| `landmeta.js` | **ชื่อหน้า/คำโปรย/ที่ตั้งของแปลง — ชุดกลาง ใช้ได้ทั้งเบราว์เซอร์และ Node** |
| `build/properties.js` | สร้าง `p/<รหัส>.html` + `sitemaps/properties.xml` จากข้อมูลจริง |
| `njchatload.js` | ตัวโหลดแชทแบบหน่วง (4.5 KB แทน 101 KB) |
| `a11y.test.js` · `seo.test.js` | 209 + 234 ข้อ |

### งานที่ 15 — หน้าแปลงมีเนื้อหาอยู่ใน HTML ต้นทางแล้ว (H-06 + H-07 ที่ค้างมาตั้งแต่ Sprint 3)

**ปัญหา:** `land.html?id=…` ส่ง **HTML เปล่า** ให้บอตเสมอ เนื้อหามาทีหลังจาก JS ·
บอตของ LINE และ Facebook **ไม่รันสคริปต์** ลิงก์แปลงที่พนักงานส่งให้ลูกค้าทางไลน์ทุกแปลง
จึงขึ้นหัวข้อและรูปเหมือนกันหมด · และ Google ไม่มีเนื้อหาให้จัดอันดับเลย

**ทางแก้:** `node build/properties.js` สร้างไฟล์จริงหนึ่งไฟล์ต่อหนึ่งแปลงที่ `p/<รหัส>.html`

1. ⚠️ **ต้นแบบคือ `land.html` ตัวจริง ไม่ได้เขียนโครงหน้าใหม่**
   แก้หัวเว็บ/สคริปต์ที่ `land.html` แล้ว **ต้องรัน `build/properties.js` ใหม่ทุกครั้ง**
   (ลำดับ: `node build/pages.js` → `node build/properties.js`)
2. ⚠️ **การ์ดทุกใบยังลิงก์ `land.html?id=` เหมือนเดิม ไม่มีทางพาไป 404**
   หน้านั้นชี้ `canonical` มาที่หน้าสแตติก · แปลงที่เพิ่งขึ้นหลังรอบ build ล่าสุดจะยังไม่มี
   หน้าสแตติก → Google ข้าม canonical แล้วเก็บ `land.html` แทน = เท่ากับพฤติกรรมเดิม
   **ไม่มีจังหวะไหนที่ผู้ใช้เจอหน้าเสีย**
3. ⚠️ **ดึงข้อมูลไม่สำเร็จ = ไม่แตะไฟล์เดิมเลยแม้แต่ไฟล์เดียว** แล้วออกด้วยรหัสไม่ศูนย์
   (API ตอบว่างเปล่าก็ไม่ลบของเดิมเช่นกัน) · แปลงที่หายจาก API แล้วจึงถูกลบทิ้ง
4. ⚠️ **หน้าอยู่ในโฟลเดอร์ `p/` จึงใช้ที่อยู่ไฟล์แบบเต็ม (`/tokens.css`)** — เว็บอยู่ที่รากโดเมน
5. ⚠️ **`landmeta.js` เป็นแหล่งเดียวของชื่อหน้าและคำโปรย** — `land.js` และ `build/properties.js`
   เรียกตัวเดียวกัน · สองที่ประกอบเองแยกกันเมื่อไหร่ สิ่งที่ Google เห็นกับสิ่งที่ผู้ใช้เห็น
   จะไม่ตรงกัน ซึ่งเป็นเกณฑ์ที่ Google ตัดสิทธิ์**ทั้งเว็บ** ไม่ใช่แค่หน้าเดียว
6. ⚠️ **`robots.txt` ชี้แผนผังสองไฟล์** (`sitemap.xml` หน้าคงที่ · `sitemaps/properties.xml` หน้าแปลง)
7. ⚠️ **⛔ ห้ามใส่ Review / AggregateRating ในข้อมูลโครงสร้างเด็ดขาด** เว็บนี้ไม่มีรีวิวจริง
   สักรายการ · `seo.test.js` หัวข้อ 3 ล็อกไว้ · และ `offers.price` ใส่เฉพาะแปลงที่มีราคาจริง
   โดยเทียบกับตัวเลขที่แสดงบนหน้าจอจริงๆ

🐞 **บั๊กที่เจอตอนเขียนเทสต์**: `metaDesc()` ต่อทุกอย่างแล้วค่อยตัดที่ 300 ตัวอักษร
ผลคือแปลงที่เจ้าของเขียนโฆษณายาว (3 จาก 22 แปลง) **เสียท่อนท้ายไปทั้งหมด**
ซึ่งคือ "รหัสทรัพย์" ที่ทีมใช้จับคู่ผู้ซื้อกับเจ้าของ และบรรทัดใบอนุญาตที่เป็นจุดต่างของแบรนด์
— สองอย่างที่ขาดไม่ได้ที่สุด กลับเป็นสองอย่างแรกที่หายไป · แก้เป็นตัดที่ข้อความของเจ้าของแทน

### งานที่ 14 — การเข้าถึง

🐞 **บั๊กที่เจอและแก้**: Sprint 1 ใส่ลิงก์ "ข้ามไปเนื้อหาหลัก" (`href="#main"`) ให้ทุกหน้า
**แต่ 21 จาก 25 หน้าไม่มี `id="main"` เลย** ลิงก์จึงพาไปไหนไม่ได้ตั้งแต่ต้น
· แก้โดยให้ `build/pages.js` รับผิดชอบปลายทางด้วย — หน้าที่มี `<main>` เติม id ให้
หน้าที่ยังไม่มี `<main>` (9 หน้า) ถูกห่อให้ระหว่างท้ายหัวเว็บกับ `<footer>`
ได้ทั้งที่ยึดของลิงก์ข้าม และได้ landmark ที่เครื่องอ่านหน้าจอใช้กระโดด

อื่นๆ ที่แก้: `#result-note` บนหน้าแรกประกาศ `aria-live` แล้ว (เดิมค้นแล้วเงียบสำหรับ
คนที่ใช้เครื่องอ่านหน้าจอ) · ชื่อหน้าที่ยาวเกิน 75 ตัวอักษร 3 หน้าถูกย่อ

⚠️ `a11y.test.js` ตรวจได้เฉพาะสิ่งที่อ่านจาก HTML ต้นทางได้ — **ความต่างสี การใช้คีย์บอร์ดจริง
และขนาดเป้ากดหลัง CSS ทำงาน ต้องวัดในเบราว์เซอร์**

### งานที่ 13 — ประสิทธิภาพ

**`njchat.js` 101 KB ถูกโหลดใน 20 หน้าตั้งแต่วินาทีแรก** ทั้งที่คนส่วนใหญ่ไม่เปิดแชทเลย
· เปลี่ยนเป็น `njchatload.js` (4.5 KB) ที่ดึงไฟล์จริงเมื่อ: ผ่านไป 18 วินาที ·
เลื่อนจอลง 400px · แตะ/กดปุ่มอะไรก็ได้ · กดลิงก์ `[data-njchat]` (โหลดแล้วเปิดให้เลย)

- ⚠️ **`chat.html` ห้ามใช้ตัวโหลด** — หน้านั้นคือตัวแชทเอง
- ⚠️ **ตัวโหลดต้องตั้ง `window.NJCHAT_LAZY` ก่อนแทรกสคริปต์** ไม่งั้น `revealLater()`
  หน่วงปุ่มอีก 18 วินาทีซ้อน ผู้ใช้ต้องรอรวม 36 วินาที
- ⚠️ **ตัวโหลดพัง = ไม่มีแชท ไม่ใช่หน้าเว็บพัง** ปุ่มติดต่ออื่นเป็นลิงก์จริงใน HTML อยู่แล้ว

รายงานเต็มอยู่ที่ `docs/audit/PERFORMANCE_BEFORE_AFTER.md` พร้อม**สิ่งที่ยังแก้ไม่ได้และเหตุผล**
(รูปประกาศ 458 KB/ใบ ต้องแก้ที่ฝั่งระบบ · หน้ารวมประกาศยังโหลดรูปรวม ~10 MB)

### งานที่ 16 — สถิติ

⚠️ **รายการ 20 ชื่อในข้อกำหนด มีอยู่แล้ว 18 ชื่อใต้ชื่อเดิมของระบบ — ห้ามเปลี่ยนชื่อตาม**
เปลี่ยนเมื่อไหร่ = ตัวเลขเก่าขาดตอน และยอด `leads` ใน `/api/public/track/summary` เพี้ยน

| ชื่อในข้อกำหนด | ชื่อจริงในระบบ | | ชื่อในข้อกำหนด | ชื่อจริงในระบบ |
|---|---|---|---|---|
| `view_property` | `listing_view` | | `start_consign` | `consign_view` |
| `compare_property` | `compare_open` | | `complete_consign` | `consign_submit` |
| `schedule_viewing` | `inspect_submit` | | `start_wanted` | `buyer_request_view` |
| `click_line` | `line_click` | | `complete_wanted` | `buyer_request_submit` |
| `click_phone` | `tel_click` | | `start_verify` | `propcheck_view` |
| `open_chat` | `chat_open` | | `complete_verify` | `propcheck_submit` |

**เพิ่มใหม่จริง 2 ตัว** (ขึ้นทะเบียนทั้งสองรีโป · ไม่ใช่ลีดทั้งคู่):
`view_survey_level` (กางดูบันได 5 ระดับ — **นับครั้งเดียวต่อการเปิดหน้า** ไม่ใช่ทุกแถว
ไม่งั้นคนที่สนใจมากถูกนับ 5 ครั้ง) · `download_report` (กดดาวน์โหลด/พิมพ์ประกาศ)

### ทดสอบแล้ว
`a11y` 209 · `seo` 234 · `landlabel` 13 → 27 (เขียนใหม่ให้ **เรียกฟังก์ชันจริง** แทนการไล่จับข้อความ
เพราะ `landmeta.js` require เข้า Node ได้แล้ว) · ชุดเดิมผ่านครบ **รวม 1,384 ข้อ · ไม่ผ่าน 0** ·
ฝั่งระบบ `npm test` ผ่านครบ · `pages --check` 25 หน้า · `sitemap --check` ผ่าน · ลิงก์เสีย 0 ·
เบราว์เซอร์จริงที่ 375px: หน้าแปลงสแตติกขึ้น `<h1>` จาก HTML ต้นทาง (domContentLoaded 89 ms)
แล้ว JS ต่อยอดเป็นหน้าเต็มได้ครบ · เปิดหน้าแรกแล้ว `njchat.js` **ไม่ถูกโหลด** ·
แตะจอครั้งเดียวแล้วโหลดและปุ่มโผล่ทันที แผงยังไม่เปิดเอง · ไม่ล้นแนวนอน


## ทดสอบรวบยอดก่อนขึ้นจริง (2026-09-20 · Sprint 6 · งานที่ 18)

branch `feature/homepage-ux-platform-audit` · รายงานเต็มอยู่ที่ `docs/audit/FINAL_QA_REPORT.md`

### ไฟล์ใหม่
| ไฟล์ | ทำอะไร |
|---|---|
| `build/lint.js` | ตรวจไวยากรณ์ JS · ที่อยู่ไฟล์ที่หน้าชี้ไป · id ซ้ำ · **ลำดับการโหลดสคริปต์** · ลิงก์ในหน้าแปลงสแตติก |
| `docs/audit/FINAL_QA_REPORT.md` | ผลทดสอบทั้งหมด + สิ่งที่ทดสอบไม่ได้ + ที่ค้างก่อนขึ้นจริง |

⚠️ **`build/lint.js` รันใน CI ทุก push แล้ว** — ด่านนี้จับ "พิมพ์ชื่อไฟล์ผิด" และ
"สลับลำดับสคริปต์" ซึ่งเป็นสองอย่างที่พังเงียบที่สุดในเว็บที่ไม่มี bundler
(เคยเจอจริงสองครั้ง: `save.js` มาหลัง `listings.js` · `zoneguide.js` มาก่อน `landvocab.js`)

### 🐞 บั๊กที่เจอตอนยิงฟอร์มจริง — ฟอร์มตรวจทรัพย์ไม่เก็บที่มาของลีดเลย

Sprint 4 ต่อ `attrib` ให้ 5 เส้นทาง แล้ว **เขียนในบันทึกว่าครอบคลุม "ฟอร์มตรวจทรัพย์"
ทั้งที่ช่องนั้นเป็นของฟอร์มฝากหาที่ดิน** (`buyerRequests`) · ฟอร์มตรวจทรัพย์สร้างใบเป็น
`opportunities` คนละบล็อกกัน จึงถูกข้ามทั้งเส้นทาง — และมันคือปลายทางของโฆษณา
สายทรัพย์บังคับคดี/ธนาคาร ซึ่งเป็นสายที่ต้องวัดผลโฆษณามากที่สุด

⚠️ **ล็อกด้วย `contracts.test.js` หัวข้อ 12 แล้ว** — นับว่าเส้นทางที่รับ `b.attrib` ครบ 6 จุด
· บทเรียน: **เทสต์ที่อ่านแต่โค้ดไม่จับข้อนี้ ต้องยิงของจริงเข้าเซิร์ฟเวอร์ทดสอบถึงเจอ**

### ⚠️ `contracts.test.js` ต้องชี้ไปสาขาที่ถูกต้อง

ระหว่าง Sprint 6 พบว่าโฟลเดอร์ `nj-survey-system` ถูกสลับไปสาขาอื่นโดยอีกเซสชัน
ทำให้เทสต์ล้ม 1 ข้อโดยที่โค้ดของเราไม่ได้ผิดอะไร · วิธีที่ถูกคือสร้าง worktree ของสาขาตัวเอง
แล้วส่ง path เข้าไป: `node contracts.test.js ../nj-wt-s6`
· **อย่าสลับสาขาในโฟลเดอร์ที่อีกเซสชันใช้อยู่** (กับดักเดิมของ repo นี้)

### ผลทดสอบ

- `build/lint.js` ผ่าน (62 ไฟล์ JS · 47 หน้า) · ชุดเทสต์เว็บ **1,392 ข้อ ผ่านครบ** ·
  ฝั่งระบบ `npm test` ผ่านครบ · `pages/sitemap --check` ผ่าน · ลิงก์เสีย 0
- **การแสดงผล 21 หน้า × 10 ความกว้าง (360–1920) = 210 ช่อง → ไม่ล้นแนวนอนเลยสักช่อง**
- **ยิงฟอร์มจริง 4 ชุดเข้าเซิร์ฟเวอร์ทดสอบบนเครื่อง** (ไม่แตะ production ตามข้อกำหนด):
  ตรวจทรัพย์ · ฝากขาย · ฝากหาที่ดิน · นัดตรวจแปลง — ได้เลขใบครบ และที่มาของลีดถูกเก็บครบ
  · **สัมผัสแรกถูกเก็บข้ามการทดสอบคนละรอบคนละวัน** ยืนยันว่ากลไกทำงานจริง

### ⛔ สิ่งที่ทดสอบไม่ได้ ต้องให้คนอื่นทำต่อ

1. **Safari / Mobile Safari** — เครื่องนี้มีแต่เบราว์เซอร์ตระกูล Chromium
   · 6 จุดเสี่ยงที่ต้องให้คนมี iPhone เปิดเช็ค อยู่ในรายงานหัวข้อ 5.1
2. **Staging** — ยังไม่มี environment ให้ deploy ก่อนของจริงเลย มีแต่ production เดียว
   · เสนอ 3 ทางเลือกไว้ในรายงานหัวข้อ 5.2 (แนะนำ Cloudflare Pages/Netlify ที่ให้ preview ต่อ PR)
3. **Core Web Vitals จริง** — ต้องวัดบน production หลัง deploy

## แพ็กเกจบริการที่ดินชัวร์ — หน้าเว็บ (2026-09-19 · รอบ 4 ของโมดูลแพ็กเกจ)

branch `pkg-4-web` (worktree `nj-wt-pkgweb`) · ต่อจากรอบ 1–3ก ฝั่งระบบ (`nj-survey-system` branch `pkg-1-core`)
⚠️ **ต้อง deploy ฝั่งระบบก่อนเสมอ** หน้าใหม่ทั้งสองเรียกเส้นทางที่เพิ่งเพิ่ม และเส้นทางทั้งหมดอยู่หลังสวิตช์ `packages`
ซึ่ง **ปิดอยู่เป็นค่าเริ่มต้น** — เปิดสวิตช์ที่หน้าจัดการผู้ใช้ของระบบก่อน ไม่งั้นหน้าเว็บจะขึ้นกล่อง "ทักไลน์หาทีมงาน"

| ไฟล์ | ทำอะไร |
|---|---|
| `packages.html` · `packages.js` · `packages.css` | แคตตาล็อก 3 กลุ่ม · การ์ดแพ็กเกจ · เปรียบเทียบ · เครื่องคำนวณราคา · แบบสอบถาม 12 ข้อ · ฟอร์มส่งข้อมูล · ค่านายหน้า |
| `package-order.html` · `package-order.js` · `package-order.css` | หน้าติดตามใบสั่งงานของลูกค้า (เปิดด้วยลิงก์ที่มีตั๋ว) |
ที่แก้เพิ่ม: `analytics.js` (6 ชนิดเหตุการณ์) · `index.html` (เมนูด่วน 09 + ข้อความค่านายหน้า) ·
`consign.html` (3 จุด) · `njchat.js` + `njchat.e2e.js` (ฐานความรู้ของแชท) · `sitemap.xml` · `robots.txt` · `contracts.test.js` (หัวข้อแพ็กเกจ)

### ⚠️⚠️ กติกาที่ห้ามผ่อน

1. **ห้ามพิมพ์ราคา ชื่อแพ็กเกจ รายการที่ได้รับ หรืออัตราค่านายหน้าไว้ในไฟล์ของหน้าแพ็กเกจ**
   ทุกอย่างมาจาก `/api/public/packages` · `/api/public/package-quote` · `/api/public/package-recommend`
   (ข้อกำหนดข้อ 4 ของเจ้าของกิจการ: แก้ราคาได้จากหลังบ้าน ไม่ใช่แก้ที่หน้าเว็บ)
   · โหลดไม่ได้ = ไม่แสดงราคาเลย แล้วให้ทักไลน์/โทร **ห้ามใส่ตัวเลขสำรองที่เดาเอง** (กติกาเดียวกับ `surveyquote.js`)
   · `contracts.test.js` สแกน `packages.js` ว่าไม่มีเลข 4 หลักขึ้นไปหลงเหลือ (ยกเว้นเบอร์โทร/ความยาวช่องกรอก)
2. **คำถามของแบบสอบถามมาจาก `data.questions` ทั้งชุด** — ระบบเพิ่ม/ลดคำถามแล้วหน้านี้ตามทันทีโดยไม่ต้องแก้
   (เทสต์ล็อกว่าไม่มีคีย์คำถามฝังอยู่ในไฟล์เว็บ)
3. **ห้ามใช้ถ้อยคำรับประกัน** กรรมสิทธิ์ ราคาขาย หรือผลการขาย · ใช้ "ตามขอบเขตบริการ" ·
   ข้อความกำกับท้ายหน้ามาจาก `config.estimateNote` ของเซิร์ฟเวอร์ + ประโยคปฏิเสธที่เขียนไว้ตายตัว
   (เทสต์ตรวจคำต้องห้าม โดยไม่นับประโยคที่ขึ้นต้นว่า "ไม่ใช่การรับประกัน…")
4. **ไม่ติ๊กยินยอม PDPA = ปุ่มส่งถูกปิด** และเซิร์ฟเวอร์ปฏิเสธซ้ำอีกชั้น · กับดักบอทใช้ช่อง `hp`
5. **หน้าติดตามของลูกค้า: ปุ่มที่กดได้มาจาก `order.moves` ของเซิร์ฟเวอร์เท่านั้น**
   ห้ามพิมพ์ตารางสถานะไว้ในไฟล์เว็บ (ตัดสินที่ `lib/packages/orders.js` ที่เดียว)
   · `ACT_TH` ในไฟล์นั้นเป็น **คำบนปุ่ม** เท่านั้น (ชื่อสถานะอ่านบนปุ่มแล้วไม่รู้ว่ากดแล้วเกิดอะไร)
     สถานะที่ไม่มีในตารางใช้ชื่อจากเซิร์ฟเวอร์ตามเดิม
6. **ชนิดเอกสารที่ลูกค้าเลือกตอนอัปโหลดมาจาก `docKindTh` ของเซิร์ฟเวอร์** ไม่ใช่รายการที่พิมพ์เอง
7. **ตั๋วอยู่ใน query string** — `package-order.html` จึงมี `referrer` policy + `noindex` ในหน้า
   และ `Disallow: /package-order.html` ใน `robots.txt` · **ห้ามเก็บตั๋วลง localStorage** (เทสต์ล็อก)
8. **การกดไลน์/โทรบนหน้าแพ็กเกจใช้ `line_click`/`tel_click` เดิม ห้ามสร้างชนิดใหม่** —
   ข้อกำหนดเรียกมันว่า contact_line/call_staff แต่ชนิดใหม่ = นับคนเดิมสองครั้ง

### ค่านายหน้าขั้นบันได — แก้ข้อความ 5 จุดพร้อมกัน

เจ้าของกิจการอนุมัติ 18 ก.ย. 2569: เลิกใช้ 3% ทุกช่วงราคา เปลี่ยนเป็น
**3% (ไม่เกิน 5 ล้าน) · 2.5% (5–20 ล้าน) · 2% (20–50 ล้าน) · เกิน 50 ล้านเสนอเป็นรายกรณี · ขั้นต่ำ 50,000 บาท · ไม่บวก VAT**

| ที่ | ข้อความ |
|---|---|
| `consign.html` meta description | แบบสั้น |
| `consign.html` หัวข้อจุดขาย | แบบสั้น |
| `consign.html` คำถามที่พบบ่อย | แบบเต็มทุกขั้น |
| `index.html` แถบ "ทำไมต้องฝากขายกับเรา" | แบบสั้น |
| `njchat.js` ฐานความรู้ของแชท | แบบเต็มทุกขั้น |

⚠️ **ต้นทางของอัตราคือขั้นค่านายหน้าใน `pricing_rules` ของระบบหลังบ้าน** — `contracts.test.js`
เทียบอัตราในข้อความกับ `SEED_RULES` ของ `lib/packages/pricing.js` ทุกครั้งที่รัน
· หน้า `packages.html` ไม่ได้พิมพ์อัตราไว้เลย ดึงจาก `config.commission` สดทุกครั้ง
· **สัญญาที่ลงนามไปแล้วใช้อัตราตามสัญญาฉบับนั้น** — ข้อความบนเว็บบอกไว้แล้วว่าอัตราระบุในสัญญา

### ทางเข้าหน้าแพ็กเกจ

เมนูด่วนหน้าแรกช่อง **09** + `sitemap.xml` · **ไม่เพิ่มในเมนูหัวเว็บ** เพราะแถวเต็มพอดีแล้วทุกชุด
(เหตุผลเดียวกับ 07–08 ของรอบ P1 · ดูหัวข้อ `menu.js`)

### 🐞 สิ่งที่เจอตอนทดสอบกับเซิร์ฟเวอร์จริง

1. **แพ็กเกจที่เริ่มต้นฟรี (`priceFrom: 0`) ขึ้นว่า "฿0 – ฿990"** ซึ่งอ่านแล้วเหมือนระบบยังไม่ได้ราคา
   → `money()` แปลง 0 เป็น "ฟรี" (เฉพาะการแสดงผล ไม่แตะตัวเลขที่เซิร์ฟเวอร์ส่งมา)
2. **ลิงก์โทรของหน้าติดตามพัง** — เซิร์ฟเวอร์ส่งเบอร์มาสองเบอร์ในข้อความเดียว ("084-xxx, 02-xxx")
   แล้วโค้ดตัดอักขระที่ไม่ใช่ตัวเลขทิ้งจนได้เบอร์ยาวผิด → `telHref()` ใช้เบอร์แรกเสมอ
3. **🔴 บั๊กของรอบ 2 ฝั่งระบบ: ชื่อไฟล์ภาษาไทยเพี้ยนทั้งหมด** — เส้นทางแนบเอกสารของใบสั่งงาน
   (ทั้งฝั่งลูกค้าและฝั่งทีมงาน) ลืมเรียก `fixName()` ที่ระบบมีอยู่แล้ว ทำให้ "โฉนดทดสอบ.pdf"
   กลายเป็น "à¹à¸..." · แก้ที่ `server.js` แล้วและมีเทสต์ล็อกใน `test/packages-admin-api.test.js`
   **เจอเพราะทดสอบอัปโหลดจากเบราว์เซอร์จริง ไม่ใช่จากชุดเทสต์**

### ทดสอบแล้ว

`node contracts.test.js ../nj-wt-pkg` **253 ข้อ** (เพิ่มหัวข้อแพ็กเกจ) · ชุดเดิมผ่านครบทุกไฟล์
(agency 69 · compare 10 · feecalc 50 · loancalc 25 · njchat 51 · valuecalc 28 · verified 58 · zonechip 18 · zoneguide 39)
· **เบราว์เซอร์จริง + เซิร์ฟเวอร์จริง 2 ตัว** (ระบบที่ 8799 · เว็บสถิตที่ 8798 · DATA_DIR ชั่วคราว ไม่แตะข้อมูลจริง)
ที่ 1280 · 768 · 375px:
แคตตาล็อก 18 แพ็กเกจจาก API จริง · เครื่องคำนวณได้ราคาจากตารางจริง (ตรวจเอกสาร 19,900 + รังวัด 25,000 +
ค่าเดินทางโซน F 60,000 = 104,900–114,900 ก่อน VAT · 112,243–122,943 รวม VAT) ·
แบบสอบถาม 12 ข้อจบแล้วได้แพ็กเกจแนะนำ 3 ตัวพร้อมเหตุผลและคำเตือนเรื่องกำหนดเวลา ·
**ยังไม่ติ๊กยินยอมกดส่งไม่ได้** → ติ๊กแล้วส่งได้เลข PKL-00001 · เปรียบเทียบ 2 แพ็กเกจได้ตาราง 6 แถว ·
เปิดใบสั่งงานจากลีดแล้วเปิดหน้าติดตามด้วยตั๋วจริง: สถานะ · เอกสารที่ต้องเตรียม · อัปโหลด PDF ชื่อไทยได้ถูกต้อง ·
ขอนัดหมายได้ · ปุ่มเป็นคำกริยา · ไม่ล้นแนวนอนทุกความกว้าง · ไม่มี console error · ไม่มีคำว่า `undefined`

## ทะเบียน SEO กลาง + ข้อมูลโครงสร้าง JSON-LD (2026-09-20 · Phase 10 สปรินต์ 2)

branch `feature/phase-10-sprint-2-web` (worktree `nj-wt-p10s2web`) · **ต้อง deploy ฝั่ง `nj-survey-system`
(branch `feature/phase-10-sprint-2`) ก่อน** เพราะตัวสร้างดึงทะเบียนจากเส้นทางที่เพิ่งเพิ่ม

เลิกไล่แก้หัวไฟล์ 20 กว่าหน้าด้วยมือ — ทีมตั้งค่าที่เมนู **"SEO และการตลาด"** ในระบบหลังบ้าน
แล้วรันตัวสร้างสองตัวเขียนลงไฟล์จริง

| ไฟล์ | ทำอะไร |
|---|---|
| `build/seo.js` | ดึง `GET /api/public/seo/pages` → เขียน title/description/canonical/robots/og/twitter ลงหัวไฟล์ · `--check` · `--from <ไฟล์>` · `--offline` |
| `build/schema.js` | เขียนบล็อก JSON-LD: Organization + WebSite + WebPage + BreadcrumbList ทุกหน้าที่เก็บดัชนีได้ · FAQPage 5 หน้า · VideoObject 18 คลิป |
| `seo.test.js` (48) · `schema.test.js` (41) | ล็อกกติกาทุกข้อด้านล่าง |

**ลำดับการรัน: `node build/seo.js` → `node build/schema.js` → `node build/sitemap.js`**
(ชื่อหน้าใน breadcrumb และคำอธิบายใน WebPage อ่านจาก `<title>`/`<meta description>` ที่อยู่ในไฟล์ตอนนั้น)

### กติกาที่ห้ามผ่อน (เขียนเต็มไว้หัวไฟล์ทั้งสองตัว — อ่านก่อนแก้ทุกครั้ง)

1. **ไม่เขียนเนื้อหาให้เอง** — title · description · og:title · og:description · og:image
   มาจากทะเบียนเท่านั้น · ตัวสร้างคำนวณเองได้แค่ 3 ค่าที่เป็นกลไก: **canonical · og:url · twitter:card**
2. **ไม่เขียน twitter:title/twitter:description ซ้ำกับ og** — X ถอยไปอ่าน og ให้เองอยู่แล้วเมื่อไม่มี twitter:*
   เขียนซ้ำ = สร้างที่ที่สองให้ลืมแก้ (ช่องพวกนี้ยังใช้ได้ถ้าคนตั้งในทะเบียนเองว่าอยากให้ข้อความบน X ต่างจากที่อื่น)
3. **ช่องที่ทะเบียนเว้นว่าง = ไม่แตะของเดิมในไฟล์** ไม่ใช่สั่งลบ (กติกาเดียวกับข้อ 2 ที่หัว `lib/seo.js` ฝั่งระบบ)
4. **หน้า noindex ไม่ได้ค่าที่คำนวณเองเลยสักตัว** (404 · compare · deal · notify · quote · room)
   — สั่งบอตว่าอย่าเก็บแล้วยังชี้ canonical กลับมาหาตัวเองคือคำสั่งที่ขัดกันเอง
   · และหน้าพวกนี้เป็นหน้าใช้งานเฉพาะกิจ จะให้มีข้อความตอนแชร์หรือไม่เป็นการตัดสินใจของคน
5. **⚠️ `land.html` ไม่ได้ canonical/og:url** (`NO_URL` ใน `build/seo.js`) — ที่อยู่จริงคือ `land.html?id=…`
   คนละหน้าต่อหนึ่งแปลง · ใส่ canonical ชี้ `land.html` เปล่าๆ = บอก Google ว่าทุกแปลงคือหน้าเดียวกัน
   (ของจริงตั้งจาก `land.js` ตอนเปิดหน้า · หน้าที่เขียนค่าลงไฟล์จริงมาในสปรินต์ 3 พร้อม `sitemaps/properties.xml`)
   · `build/schema.js` ข้าม `land.html` ทั้งหน้าด้วยเหตุผลเดียวกัน (`SKIP`)
6. **ข้อมูลบริษัทใน JSON-LD อ่านจากบล็อกที่เขียนด้วยมือใน `index.html`** (`readBrand()`) ห้ามพิมพ์ซ้ำในโค้ด
   — เบอร์ · ชื่อนิติบุคคล · ที่อยู่ · เลขใบอนุญาต อยู่ที่ `index.html` ที่เดียว แก้ที่นั่นแล้วทุกหน้าตามเอง
   · **ตัวสร้างไม่แตะบล็อกเดิมของ `index.html` เลย** มันเขียนบล็อกของตัวเองแยก (หน้าแรกจึงมี 2 บล็อก)
   · อ่านไม่ออก = **ล้มดังๆ** ไม่ใช่เดาข้อมูลบริษัทเอง
7. **FAQPage มาจาก `<details><summary>` จริงในหน้า** — หน้าที่ไม่มีหัวข้อ "คำถามที่พบบ่อย" ไม่ถูกใส่
   แม้จะมี `<details>` อยู่ (Google ห้าม FAQ ที่ไม่ปรากฏให้คนอ่านเห็นจริง)
   · ตอนนี้มี 5 หน้า: `consign` `inspect` `portal` `verify` `wanted`
8. **VideoObject เฉพาะคลิปที่มีไฟล์อยู่จริงบนดิสก์** · `uploadDate` = วันที่ commit ของไฟล์คลิป
   (กติกาเดียวกับ `lastmod` ใน `sitemap.js`) · **ไม่ใส่ `duration`** เพราะอ่านความยาวจริงไม่ได้โดยไม่ลง ffprobe
   · ไม่มีไฟล์/ไม่เคย commit = ข้ามพร้อมคำเตือน ไม่หายเงียบ
9. **ไม่มีรีวิว เรตติ้ง ราคา เวลาทำการ หรือคำรับประกันในข้อมูลโครงสร้าง** (เทสต์ล็อกคำต้องห้าม)
   Google ลงโทษข้อมูลปลอมด้วยการตัดสิทธิ์แสดงผลพิเศษ **ทั้งเว็บ ไม่ใช่แค่หน้าเดียว**
10. **รันซ้ำได้ผลเท่าเดิม** — บล็อก `nj-seo:start/end` และ `nj-schema:start/end` ถูกสร้างใหม่ทั้งบล็อกทุกรอบ
    ⚠️ **ห้ามแก้ข้างในบล็อกด้วยมือ** · แท็กที่มีอยู่เดิมนอกบล็อกถูกแก้ค่าในที่เดิม ไม่เกิดแท็กซ้ำ
11. **ทะเบียนปิดอยู่ (API ตอบ 404) ไม่ใช่ข้อผิดพลาด** — สวิตช์ `seo_center` ค่าเริ่มต้นคือปิด
    ตัวสร้างยังทำงานต่อโดยเติมเฉพาะค่าตามข้อ 1 แล้วบอกบนจอว่าทะเบียนยังไม่เปิด

### ⚠️ `<head>` หาด้วย `</head>` เป็นหลัก ห้ามค้น `<head` ตรงๆ

ทุกหน้ามี `<header>` ในเนื้อหา — `sed -n '/<head/,/<\/head>/p'` จึงเปิดช่วงที่สองที่ไม่มีวันปิด
แล้วตัวตรวจอ่านเลยหัวไฟล์ไปถึงท้ายหน้า (เจอจริงตอนสำรวจรอบนี้ · `headRange()` ทั้งสองไฟล์กันไว้แล้ว)

### ผลที่ได้บนไฟล์จริง

- **ปิดช่องว่าง M1** — `twitter:card` ครบทุกหน้าที่มีรูปแชร์ (เดิมขาด 14 จาก 20 หน้า)
- **ปิดช่องว่าง M2** — `og:url` และ `canonical` ครบทุกหน้าที่เก็บดัชนีได้
- JSON-LD เพิ่มจาก **1 หน้า เป็น 14 หน้า**

### ทดสอบแล้ว

`seo` 48 · `schema` 41 · ชุดเดิมผ่านครบ (contracts 205 · agency 69 · verified 58 · njchat 51 ·
feecalc 50 · zoneguide 39 · valuecalc 28 · loancalc 25 · zonechip 18 · compare 10)
· `build/linkcheck.js` ลิงก์เสีย 0 (หน้ากำพร้า 3 หน้าเป็นของค้างจากสปรินต์ 4)
· เบราว์เซอร์จริงที่ 375 และ 1440px: ทุกบล็อกแปลงเป็น JSON ได้จริงในเบราว์เซอร์ ·
`index.html` มีสองบล็อกและบล็อกเดิมไม่ถูกแตะ · `videos.html` มี 18 VideoObject ตรงกับการ์ด 18 ใบ ·
คำถามใน FAQPage ของ `verify.html` ตรงกับ `<summary>` บนหน้าเป๊ะ · ไม่ล้นแนวนอน
· ⚠️ `videos.html` ยังมี console error 403 จาก YouTube Data API (ไม่มีคีย์) — **ของเดิม ไม่ใช่ของรอบนี้**
  หน้ามีทางถอยอยู่แล้ว (คลิปในเครื่อง 18 ตัวขึ้นครบ)

## ที่อยู่หน้าแปลงภาษาไทย + ทะเบียนที่อยู่เดิม (2026-09-21 · Phase 10 สปรินต์ 3 รอบ ก)

หน้าแปลงย้ายจาก `p/<รหัส>.html` ไปอยู่ที่ **`/properties/{ประเภท}/{จังหวัด}/{อำเภอ}/{รหัส}/`**
ตามข้อกำหนดงานที่ 4 · เจ้าของกิจการตัดสิน 21 ก.ย. 2569 ว่า **ใช้สลักภาษาไทย** (แผนข้อ 3.2)

### ไฟล์ที่เกี่ยวข้อง

- `landmeta.js` — `slugSeg()` · `slugPathWith()` · `pagePath(l, vocab)` · `pageUrl(l, vocab)` · `encUrl()` · `legacyPath(id)`
  **⚠️ `pagePath`/`pageUrl` รับ "ทั้งใบ" แล้ว ไม่ใช่แค่รหัส** เพราะที่อยู่ประกอบจากประเภท/จังหวัด/อำเภอ
- `build/properties.js` — เขียน `properties/**/index.html` · วางหน้าพาไปที่ `p/<รหัส>.html` · ดูแล `redirects.json`
- `redirects.json` — ทะเบียน "ที่อยู่เดิม → ที่อยู่ใหม่" **ห้ามลบรายการเก่าทิ้ง**
- `propurl.test.js` (129 ข้อ) — ข้อทดสอบของรอบนี้ทั้งหมด

### ⚠️⚠️ กติกาที่ห้ามผ่อน

1. **ที่อยู่ที่เคยเผยแพร่ออกไปแล้วต้องไม่ตาย** — GitHub Pages ทำ 301 จริงไม่ได้ จึงใช้สามชั้นพร้อมกัน
   ที่หน้าพาไป: `canonical` → `meta refresh` → **ลิงก์ที่กดได้จริง** (ห้ามตัดข้อใดข้อหนึ่งออก)
   · **ห้ามใส่ `noindex` ในหน้าพาไป** — ขัดกับ canonical และตัดค่าที่ควรส่งต่อให้ที่อยู่ใหม่ทิ้ง
2. **ช่องข้อมูลที่ว่างถูกข้ามไป ไม่ใช่เดาแทน** — แปลงที่ยังไม่กรอกจังหวัด/อำเภอได้ที่อยู่สั้นลง
   `kindOf()` คืน `ทรัพย์` เมื่อยังไม่กรอกประเภท **ห้ามเดาว่าเป็นที่ดินเปล่า** (กติกาเดิมของ landmeta.js)
3. **ที่อยู่ในหน้าเว็บและในแผนผังต้องเข้ารหัสแล้วเสมอ** (`encUrl`) — อักษรไทยดิบใน `href`/`<loc>` พังกับตัวอ่านบางตัว
4. **หน้าพาไปห้ามอยู่ในแผนผังเว็บ** — แผนผังคือรายการหน้าที่เราอยากให้เก็บเข้าดัชนี
5. **การ์ดประกาศยังลิงก์ไป `land.html?id=` เหมือนเดิม** (ลิงก์นี้ไม่มีทางพาไป 404) และ `land.html`
   ตั้ง canonical ไปที่อยู่ใหม่ให้เอง · จะเปลี่ยนลิงก์ในการ์ดเป็นที่อยู่ใหม่ค่อยทำตอนงานลิงก์ภายใน (สปรินต์ 4)

### ⚠️ ที่อยู่จะเปลี่ยนอีกครั้งเมื่อทีมกรอก "ประเภททรัพย์"

ตอนนี้ทั้ง 10 แปลงยังไม่กรอก `land.propertyType` ที่อยู่จึงออกมาเป็น `/properties/ทรัพย์/…`
พอทีมกรอก (เช่นเป็น "ที่ดินเปล่า") ที่อยู่จะกลายเป็น `/properties/ที่ดินเปล่า/…` โดยอัตโนมัติ
และตัวสร้างจะ **วางหน้าพาไปไว้ที่อยู่เดิมให้เอง** พร้อมจดลง `redirects.json`
**เจ้าของกิจการรับทราบและเลือก "สร้างเลย แล้วค่อยเปลี่ยนทีหลัง" เมื่อ 21 ก.ย. 2569**

### ที่ยังไม่ได้ทำในรอบนี้ (ตั้งใจ)

- **หน้าจังหวัด/อำเภอ** — ข้อกำหนดสั่งว่า "ต้องให้แอดมินกดเผยแพร่ก่อน" ซึ่งต้องมีสวิตช์ฝั่งระบบหลังบ้าน
  จึงยกไปรอบ ข ที่แตะสอง repo พร้อมกัน
- **แผนผังเว็บ 6 ไฟล์ + sitemap index** — ตอนนี้มีเนื้อหาจริงแค่ 2 ชุด (หน้าคงที่ + หน้าแปลง)
  สร้างไฟล์เปล่าอีก 4 ไฟล์ไม่ได้ช่วยอะไรและทำให้ Search Console ขึ้นเตือน · ทำพร้อมสปรินต์ 4 ที่มีบทความ
- **allowlist ตัวกรองที่ให้เก็บดัชนี** ของหน้ารวมประกาศ — ทำพร้อมงานลิงก์ภายใน

### ทดสอบแล้ว

`node propurl.test.js` 129 ข้อ · `seo.test.js` 126 ข้อ (ปรับให้อ่านโฟลเดอร์ใหม่) · `landlabel.test.js` 31 ข้อ
· ชุดทดสอบเว็บทั้งหมดผ่าน · `build/lint.js` (ตรวจ `properties/**` ด้วยแล้ว) · `build/linkcheck.js` ลิงก์เสีย 0
· `build/pages.js --check` · `build/sitemap.js --check` · `build/schema.js --check` ผ่านหมด
· `contracts.test.js ../nj-wt-p2b` ผ่าน 261 ข้อ

**ตรวจบนเบราว์เซอร์จริงแล้ว** (เสิร์ฟไฟล์บนเครื่อง พอร์ต 8975):
ที่อยู่ใหม่เปิดได้ 200 · `<title>`/H1/canonical เป็นของแปลงนั้นจริง · JSON-LD 4 ก้อน · ไม่มี console error
· `land.html?id=` ยังเปิดได้และตั้ง canonical ไปที่อยู่ใหม่ · 375px ไม่ล้นแนวนอน

⚠️ **`contracts.test.js` ต้องส่ง path ของสาขาที่ตรงกัน** — รันเปล่า ๆ จะเทียบกับ `../nj-survey-system`
ซึ่งอีกเซสชันสลับไปสาขาอื่นไว้ แล้วขึ้นแดง 5 ข้อทั้งที่โค้ดถูก

## หน้าจังหวัด/อำเภอ สร้างจากทะเบียน SEO (2026-09-21 · Phase 10 สปรินต์ 3 รอบ ข)

branch `p10-s3b-locations` · **ฝั่งระบบ ([#44](https://github.com/jugkapongsurvey-cyber/nj-survey-system/pull/44)) merge และ deploy แล้ว**
ตัวสร้างดึงจาก `GET /api/public/seo/areas` ซึ่งคืนเฉพาะรายการที่แอดมินกดเผยแพร่

| ไฟล์ | ทำอะไร |
|---|---|
| `locations.html` | ดัชนีพื้นที่ **และเป็นต้นแบบของหน้าจังหวัด/อำเภอ** (แนวเดียวกับ `land.html` → `properties/**`) |
| `locations.css` | สไตล์ของหน้าพื้นที่ |
| `build/locations.js` | เขียน `locations/**/index.html` · `sitemaps/locations.xml` · บรรทัด Sitemap ใน `robots.txt` |
| `locations.test.js` | 82 ข้อ |

**ลำดับทั้งชุด: `pages.js` → `properties.js` → `locations.js` → `schema.js` → `sitemap.js`**
(ไฟล์นี้เปลี่ยนสถานะเก็บดัชนีของ `locations.html` ได้ · สองตัวหลังอ่านสถานะนั้น)

### ⚠️⚠️ กติกาที่ห้ามผ่อน

1. **หน้าพื้นที่เกิดจาก "แอดมินกดเผยแพร่" เท่านั้น** (ข้อกำหนดงานที่ 6)
   ⛔ **ห้ามสร้างหน้าจากรายชื่อจังหวัดที่มีแปลงประกาศอยู่** ไม่ว่าจะดูสมเหตุสมผลแค่ไหน —
   นั่นคือ "หน้าจังหวัดที่เปลี่ยนแค่ชื่อ" ที่ข้อกำหนดห้ามไว้ตรงๆ · ปิดสวิตช์เมื่อไหร่ รอบถัดไปหน้านั้นถูกลบ
2. **เนื้อหาของพื้นที่มาจากช่อง `intro` ที่คนเขียนเอง** ระบบไม่แต่งประโยคให้
   (ฝั่งระบบบังคับแล้วว่าต้องยาว ≥ 80 ตัวอักษรก่อนกดเผยแพร่ได้) · เทสต์ล็อกว่าไม่มีประโยคโฆษณาสำเร็จรูปในตัวสร้าง
3. ⚠️ **ที่อยู่ของหน้ามาจากช่อง `path` ของทะเบียน แต่ต้องตรวจรูปแบบก่อนเขียนทุกครั้ง**
   รับเฉพาะ `locations/<...>[/<...>]/` — **ของจริงตั้ง path เป็น `index.html` ได้** (ระบบไม่ได้ห้าม)
   ไม่มีด่านนี้ = รายการเดียวเขียนทับหน้าแรกของเว็บทั้งหน้า · ทดสอบกับของจริงแล้วว่าตั้งได้จริง
4. **ดึงข้อมูลไม่สำเร็จ = ไม่แตะไฟล์เดิมเลยสักไฟล์** (กติกาเดียวกับ `build/properties.js` ข้อ 4)
5. **รายการแปลงบนหน้าเป็นข้อมูล ณ ตอนสร้างไฟล์** หน้านี้ไม่มีสคริปต์ดึงข้อมูลสด
   บรรทัด "ราคาและรายละเอียดที่แสดงเป็นข้อมูลล่าสุดตอนสร้างหน้านี้" **ห้ามตัดทิ้ง**
   (จะทำให้สดต้องเพิ่มสคริปต์ฝั่งเบราว์เซอร์ ไม่ใช่ลบคำเตือน)
6. **หัวข้อของหน้าอยู่ในกล่องที่ตัวสร้างเขียน** ไม่งั้นหน้าพื้นที่ได้หัวข้อของหน้าดัชนีติดมาจากต้นแบบ
   กลายเป็น H1 สองอันคนละเรื่องในหน้าเดียว (เจอจริงตอนทดสอบ)
7. **ไม่เติมคำว่า "อำเภอ" หรือ "เขต" ให้เอง** — กทม.ใช้ "เขต" ต่างจังหวัดใช้ "อำเภอ"
   เดาผิด = พิมพ์ผิดบนหัวข้อหน้าที่ขึ้น Google
8. **แปลงที่เข้าหน้าไหน เทียบชื่อจังหวัด/อำเภอแบบตรงตัว ไม่เดาจาก `parcelInfo`**
   แปลงที่ยังไม่กรอกจังหวัดจะไม่ขึ้นหน้าไหนเลย ซึ่งถูกแล้ว

### ⚠️ ขอบเขตของกล่องที่ตัวสร้างเขียน ใช้คอมเมนต์ ไม่ใช่ `</div>`

เนื้อหาข้างในมี div ซ้อน (`section > div.shell`) ตัวแทนที่ที่จับ `</div>` ตัวแรกจึงหยุดกลางทาง
แล้วเนื้อหาเก่าค้างต่อท้ายของใหม่ — **รันสองรอบได้หน้าที่มีเนื้อหาซ้ำสองชุด** (เจอจริง)
ตอนนี้ใช้ `nj-locbody:start` / `nj-locbody:end` และมีเทสต์ยืนยันว่ารันซ้ำได้ผลเท่าเดิม

### ⚠️ ดัชนีที่ยังว่าง = noindex และไม่มีบล็อก JSON-LD

ดัชนีเปล่าคือหน้าบาง จึงติด `noindex, follow` ในกล่อง `nj-loc` จนกว่าจะมีพื้นที่เผยแพร่
⚠️ **แต่ `build/schema.js` ถอดบล็อก JSON-LD ของหน้า noindex ออก** ตัวสร้างจึงอ่านต้นแบบไม่ได้
แล้วตายทันทีที่ทีมเผยแพร่พื้นที่แรก (เจอจริง) → ตอนนี้ถอยไปอ่านก้อนจาก `listings.html` แทน
และ `render()` **แทรกบล็อกใหม่เมื่อต้นแบบไม่มี** ไม่ใช่แค่แทนที่

### ⚠️ ยังไม่ใส่ลิงก์ในเมนูหัวเว็บ — ตั้งใจ

ตอนนี้ยังไม่มีพื้นที่ไหนเผยแพร่ ดัชนีจึงว่าง · **ลิงก์ไปหน้าที่ว่างเปล่าแย่กว่าไม่มีลิงก์**
ใส่ในรอบที่ทีมเผยแพร่หน้าพื้นที่หน้าแรก (แถวเมนูเต็ม 6 กลุ่มแล้ว ต้องใส่เป็น `items` ใต้กลุ่มเดิม)
· `header.test.js` มี `locations.html` อยู่ในรายการหน้าที่ตั้งใจไม่ใส่เมนู พร้อมเหตุผล

### 🐞 บั๊กของตัวตรวจลิงก์ที่เจอไปด้วย

`build/linkcheck.js` เทียบที่อยู่ในหน้ากับไฟล์บนดิสก์แบบตรงตัว — ที่อยู่ในหน้าเข้ารหัสแล้ว
(`/locations/%E0%B8...`) แต่ชื่อโฟลเดอร์เป็นภาษาไทย จึงรายงานว่าลิงก์เสียทั้งที่ไฟล์อยู่ครบ
**ลิงก์ภายในที่เข้ารหัสเพิ่งมีครั้งแรกในรอบนี้** (หน้าแปลงลิงก์ด้วย `land.html?id=` ซึ่งไม่ต้องเข้ารหัส)
· แก้ที่ `exists()` ให้ถอดรหัสก่อนเทียบ

### ที่ตั้งใจยังไม่ทำ

- **ไม่มีสคริปต์ดึงข้อมูลสดบนหน้าพื้นที่** — รายการแปลงเป็นของ ณ ตอน build (มีคำกำกับบนหน้า)
- **ไม่มีข้อมูลตั้งต้น** ทะเบียนเริ่มจากว่าง ทีมเพิ่มหน้าเอง
- **ไม่ลิงก์จากหน้าแปลงกลับหน้าพื้นที่** — รอให้มีหน้าพื้นที่จริงก่อน (งานลิงก์ภายใน สปรินต์ 4)

### ทดสอบแล้ว

`node locations.test.js` **82 ข้อ** · ชุดเทสต์เว็บทั้งหมดผ่าน · `build/lint.js` ·
`pages/schema/sitemap --check` · `linkcheck` ลิงก์เสีย 0
· `node contracts.test.js ../nj-wt-p2b` ผ่าน 261 ข้อ
· **เบราว์เซอร์จริง + เซิร์ฟเวอร์ระบบจริงบนเครื่อง** (DATA_DIR ชั่วคราว) ที่ 1280 และ 375px:
เผยแพร่ 3 พื้นที่แล้วได้หน้าจังหวัด/อำเภอพร้อมรายการแปลงที่กรองถูกต้อง · หน้าที่ไม่มีแปลงบอกตรงๆ ·
ปิดสวิตช์แล้วหน้าถูกลบ · ปิดหมดแล้วแผนผังและบรรทัดใน robots.txt หายไปด้วย ·
รายการที่ตั้ง path เป็น `index.html` ถูกข้ามพร้อมคำเตือน **หน้าแรกของเว็บไม่ถูกแตะ** ·
ไม่ล้นแนวนอน · ไม่มี console error · ไม่มีคำว่า `undefined`

## ตัวอย่างประกาศแบบสดข้างฟอร์มฝากขาย (2026-09-21 · ระลอก 2 ข้อ 3)

branch `p6-consign-preview` (worktree `nj-wt-p6pv`) · **แก้เฉพาะ repo เว็บ ไม่แตะฝั่งระบบ**
ไม่มีเส้นทาง API ใหม่ ไม่มีชนิดเหตุการณ์สถิติใหม่ จึง deploy ได้เลยโดยไม่ต้องรอ `nj-survey-system`

**ปัญหาที่แก้:** ฟอร์มฝากขายยาวมาก (ที่ตั้ง · เนื้อที่ · ราคา · รังวัด · ไฟล์แนบ) แต่สิ่งที่เจ้าของ
ที่ดินอยากรู้จริงๆ คือ "ประกาศของฉันจะออกมาหน้าตาแบบไหน" ซึ่งเดิมไม่มีทางเห็นเลยจนกว่าทีมงาน
จะเอาขึ้นเว็บ · คนที่กรอกไปครึ่งทางแล้วไม่มีอะไรตอบกลับก็ปิดหน้าไปเฉยๆ
และคอขวดของธุรกิจตอนนี้คือ **จำนวนแปลงที่ขึ้นเว็บ** ไม่ใช่ฟีเจอร์ฝั่งผู้ซื้อ

| ไฟล์ | ทำอะไร |
|---|---|
| `consignpreview.js` | `window.NJConsignPreview` — **ตรรกะล้วน ไม่แตะ DOM** · `model()` `cardHtml()` `todoHtml()` `missing()` `started()` `unitText()` |
| `consignpreview.test.js` | 100 ข้อ |
ที่แก้เพิ่ม: `consign.html` (กล่อง `#cs-preview` + สคริปต์) · `consign.js` (`livePreview()` + ตัวแปร `PV`) ·
`ui.css` (คลาส `.cs-pv-*`) · `build/lint.js` (ล็อกลำดับ `consignpreview.js` ก่อน `consign.js`)

### ⚠️⚠️ กติกาที่ห้ามผ่อน (เขียนเต็มไว้หัว `consignpreview.js` — อ่านก่อนแก้ทุกครั้ง)

1. **ตรรกะอยู่ใน `consignpreview.js` ที่เดียว · `consign.js` เป็นแค่ตัวต่อ DOM**
   ("อ่านค่าจากฟอร์ม → ส่งให้ NJConsignPreview → เขียนลง DOM") · ตัดสินใจอะไรใน `consign.js`
   เมื่อไหร่ = กติกามีสองที่ให้ลืมแก้ และเทสต์จับไม่ได้เพราะเรียกจาก node ไม่ได้
2. ⛔ **ห้ามขึ้นป้าย "✓ รังวัดยืนยันแล้ว" เด็ดขาด** (กติกาข้อ 10) แม้เจ้าของจะเลือก
   "ต้องการให้รังวัด" ไว้แล้ว · ป้ายเขียวเป็นข้อเท็จจริงหลังช่างลงสนามจริง ไม่ใช่ความตั้งใจ
   ตัวอย่างขึ้นป้ายเหลือง `◐ ข้อมูลเบื้องต้น` เสมอ แล้วอธิบายเป็นประโยคว่าป้ายจะเปลี่ยนเมื่อไหร่
   (เทสต์หัวข้อ 3 ดึงข้อความในแท็ก `.cs-pv-tier` ออกมาเทียบทั้งสามตัวเลือก)
3. **ทุกบรรทัดมาจากสิ่งที่เจ้าของพิมพ์เอง** (กติกาข้อ 5) ช่องว่าง = ไม่ขึ้นบรรทัดนั้น
   แล้วไปโผล่ในรายการ "ยังเติมอะไรได้อีก" แทน · **ไม่หารอะไรทั้งสิ้น** — `unitText()`
   สะท้อนหน่วยที่เขาเลือกกับตัวเลขที่เขาพิมพ์เท่านั้น
4. **ยังไม่กรอกอะไรเลย = ไม่แสดงกล่อง ห้ามวาดการ์ดเปล่า** (กติกาข้อแรกของ Phase 1)
   · ชื่อกับเบอร์ไม่นับว่า "เริ่มกรอกประกาศแล้ว" เพราะสองช่องนั้นไม่ได้ขึ้นประกาศ
5. **ประกาศขึ้นเว็บแล้ว (`d.listing.live`) หรือยกเลิกแล้ว = ไม่แสดงตัวอย่าง**
   กล่องรายงาน `#cs-report` มีของจริงพร้อมลิงก์ไปหน้าประกาศอยู่แล้ว ยื่นตัวอย่างซ้อนของจริง
   คือชวนให้สับสนว่าอันไหนจริง
6. **รายการที่ยังขาดเป็นคำชวน ไม่ใช่ช่องบังคับ** — กติกาลีดขั้นต่ำมีแค่ ชื่อ · เบอร์ · ยินยอม
   (อยู่ใน `njform.js` ที่เดียว) **ห้ามเอารายการนี้ไปบล็อกปุ่มบันทึกเด็ดขาด** · เทสต์ล็อก
   ว่าหัวรายการต้องมีคำว่า "ไม่ได้บังคับ" และห้ามมีคำว่า "ต้องกรอก" / "จำเป็นต้อง"
7. **รูปที่ทีมงานยังไม่ได้ตรวจต้องติดป้าย "รอทีมงานตรวจ" บนรูป** — รูปที่ยังไม่ผ่านตรวจ
   ไม่มีทางขึ้นหน้าประกาศ (ล็อกที่ `server.js`) ปล่อยให้เข้าใจเองคือให้ความหวังผิด
8. ⚠️ **ไม่ใส่ `aria-live` ที่กล่องนี้** — มันเปลี่ยนทุกครั้งที่พิมพ์ตัวอักษรเดียว
   ประกาศให้โปรแกรมอ่านหน้าจอทุกรอบคือเสียงรบกวนจนใช้ฟอร์มไม่ได้
   (ผลคำนวณที่ต้องประกาศจริงๆ อยู่ที่ `#cs-area-out` / `#cs-price-out` ซึ่งมี `aria-live` อยู่แล้ว)
9. ⚠️ **เทียบ HTML เดิมก่อนเขียนเสมอ** (`lastCard` / `lastTodo` ใน `livePreview()`) —
   กล่องนี้ถูกวาดใหม่ทุกครั้งที่ผู้ใช้พิมพ์ ซึ่งเป็นครึ่งหนึ่งของกับดักลูป MutationObserver
   ที่เคยทำให้สองหน้าค้างทั้งแท็บ

### ทำไม `money()` ถึงยอมให้มีสองที่

ไฟล์นี้ต้อง `require` เข้า node ได้โดยไม่มี DOM จึงเรียก `NJListing.money` ข้ามไฟล์ไม่ได้
· แทนที่จะปล่อยให้เลื่อนออกจากกันเงียบๆ **`consignpreview.test.js` หัวข้อ 1 โหลด `listingcard.js`
เข้า `node:vm` แล้วเทียบผลของสองตัวนี้ให้ทุกครั้งที่รัน** (วิธีเดียวกับ `contracts.test.js`)

### ⚠️ ไม่ใช่ชุดสไตล์การ์ดชุดที่สี่

หน้าฝากขายใช้ `ui.css` ซึ่งไม่มีสไตล์ `.land-card` เลย · คลาสของกล่องนี้จึงขึ้นต้นด้วย `.cs-pv-`
ทั้งหมดและใช้เฉพาะหน้านี้ — **ห้ามเอาไปใช้ที่อื่น และห้ามก๊อป `NJListing.card()` มาไว้ที่นี่**
· กรอบเส้นประ + คำว่า "ตัวอย่างประกาศของคุณ" มีไว้ให้อ่านออกทันทีว่ายังไม่ใช่ของจริงบนเว็บ
**ทำให้เนียนเหมือนประกาศจริงเมื่อไหร่ = เจ้าของเข้าใจว่าแปลงขึ้นเว็บแล้ว**
· `.cs-preview` มี `min-width:0` เพราะ `#consign-form` เป็น grid (บทเรียนเดียวกับ `.ld-val`
ที่เคยทำหน้าแปลงเลื่อนแนวนอนได้ 91px)

### ทดสอบแล้ว

`consignpreview` 100 ข้อ · ชุดเทสต์เว็บทั้งหมดผ่านครบ (a11y 233 · seo 126 · propurl 129 ·
header 102 · forms 86 · locations 80 · detail 78 · homepage 70 · agency 69 · verified 58 ·
njchat 51 · feecalc 50 · seocenter 48 · schema 41 · consent 39 · zoneguide 39 · landlabel 31 ·
valuecalc 28 · loancalc 25 · zonechip 18 · compare 10) · `node contracts.test.js ../nj-wt-sysmain`
ผ่าน **261 ข้อ** · `build/lint.js` ผ่าน (70 ไฟล์ JS · 48 หน้า · ด่านลำดับสคริปต์ล็อกคู่ใหม่แล้ว) ·
`build/pages.js --check` ผ่าน (28 หน้า) · `build/linkcheck.js` ลิงก์เสีย 0

⚠️ **ยังไม่ได้ตรวจบนเบราว์เซอร์จริง** — รอบนี้รันจากงานตามกำหนดเวลา (unattended) ซึ่งเปิด
dev server ไม่ได้ · ที่ต้องดูด้วยตาก่อน merge: ความกว้าง 375 / 768 / 1440 ว่าไม่ล้นแนวนอน ·
กล่องโผล่/หายตามที่กรอกจริง · กดปุ่ม "ฝากขายแปลงถัดไป" แล้วการ์ดของแปลงก่อนหน้าหายจริง

⚠️ **`contracts.test.js` ต้องส่ง path ของสาขาที่ตรงกัน** — รอบนี้โฟลเดอร์ `nj-survey-system`
ถูกอีกเซสชันสลับไปสาขา `feat/opp-code-search` ซึ่งยังไม่มี `publicAttrib()` แล้วขึ้นแดง 5 ข้อ
ทั้งที่โค้ดถูก (กับดักเดิมที่บันทึกไว้แล้วตั้งแต่ Sprint 6) · วิธีที่ถูกคือสร้าง worktree ของ
`origin/main` แล้วส่ง path เข้าไป

⚠️ **`build/sitemap.js --check` และ `build/schema.js --check` ขึ้นไม่ตรงบนเครื่อง Windows**
ตรวจกับ worktree สะอาดของ `origin/main` แล้วได้ผลเหมือนกันเป๊ะ = **ของเดิม ไม่ใช่ของรอบนี้**
· สาเหตุคือ repo เก็บไฟล์เป็น LF แต่ `core.autocrlf=true` เขียน CRLF ลงโฟลเดอร์ทำงาน
ตัว `--check` เทียบสตริงที่สร้างใหม่ (LF) กับไฟล์บนดิสก์ (CRLF) จึงไม่มีวันตรงบน Windows
· รันตัวสร้างใหม่แล้ว `git diff` ว่างเปล่า ยืนยันว่าเนื้อหาตรงกันจริง · บน CI (Linux) ผ่านปกติ

## วัดพื้นที่แปลง — ทางลิงก์ออก แทนการฝังแผนที่ (2026-09-22 · P7)

branch `p7-area-link` (worktree `nj-wt-p7area`) · **แก้เฉพาะ repo เว็บ ไม่มีเส้นทาง API ใหม่ ไม่มีชนิดเหตุการณ์สถิติใหม่**
จึง deploy ได้เลยโดยไม่ต้องรอ `nj-survey-system`

**คำสั่งเจ้าของกิจการ 22 ก.ย. 2569:** *"กุญแจ Longdo Map ตรงนี้ มีค่าใช้จ่ายหรือไม่ ถ้ามีให้ยกเลิกคำสั่งนี้ออกไป
จะให้ออกแบบแสดง link ไว้แล้วไปเชื่อมข้อมูลด้านนอกเว็บแทน"*

### ⛔ งาน `p2-longdo-map` ถูกยกเลิก — ไม่ใช่ "รอคีย์" อีกต่อไป

ตรวจค่าบริการ Longdo Map API เมื่อ 22 ก.ย. 69 ที่ `map.longdo.com/products/pricing`:

| แพ็กเกจ | ราคา/เดือน | ไทล์แผนที่ | เรียกบริการ |
|---|---|---|---|
| Free | **฿0** | < 800,000 | < 100,000 |
| Starter | ฿8,250 | < 2,500,000 | < 300,000 |
| Basic | ฿13,750 | < 5,000,000 | < 600,000 |

**ตัวคีย์ไม่มีค่าใช้จ่าย** แพ็กเกจฟรีอยู่ที่ ฿0/เดือน — แต่สามเรื่องนี้คือเหตุผลที่ยกเลิก:
1. **แพ็กเกจฟรีมีเพดานรายวันด้วย ไม่ใช่แค่รายเดือน** — 5,000 ครั้ง/วัน · 60 ครั้ง/นาที
   เว็บสาธารณะคุมจำนวนคนเข้าไม่ได้ ขั้นถัดไปคือ **฿8,250/เดือน = ฿99,000/ปี** ซึ่งเป็น
   ค่าใช้จ่ายที่เกิดจากคนนอก ไม่ได้เกิดจากการตัดสินใจของเรา
2. **หน้าขายฝั่งองค์กรของ Longdo เสนอ "Online API" เริ่มที่ ~฿150,000/ปี** — สิทธิ์ใช้เชิงพาณิชย์
   บนแพ็กเกจฟรียังไม่เคยได้คำยืนยันเป็นลายลักษณ์อักษรจาก Longdo (อีเมลที่ร่างไว้ยังไม่ได้ส่ง)
3. **งานนั้นค้างมาตั้งแต่ 17 ก.ย. เพราะรอคีย์** ทางลิงก์ออกทำได้ทันทีโดยไม่ต้องรออะไรเลย

⚠️ **branch `p2-longdo-map` และ worktree `nj-wt-p2map` ยังอยู่ ไม่ได้ลบ** — โค้ด `areamap.js`
(รวมสูตรคิดพื้นที่แบบ shoelace + haversine และเทสต์ 56 ข้อ) ยังอยู่ครบถ้าวันหนึ่งเจ้าของเปลี่ยนใจ
**แต่ห้าม merge เข้า main โดยไม่มีคำสั่งใหม่เป็นลายลักษณ์อักษร**

### ไฟล์ในรอบนี้

| ไฟล์ | ทำอะไร |
|---|---|
| `arealink.js` / `arealink.css` | `window.NJAreaLink` — ลิงก์ไปวัดที่ระบบภายนอก 3 ราย + ช่องแปลงหน่วยสองทาง + คำเตือน + ปุ่มให้ทีมรังวัดจริง |
| `arealink.test.js` | 51 ข้อ |
ที่แก้เพิ่ม: `tools.html` (ส่วน `#area` + ลิงก์กระโดด + โหลด `landform.js`/`arealink.js`) ·
`tools.js` (mount) · `verify.js` (`PREFILL_TOPICS.area`) · `build/lint.js` (ล็อกลำดับสคริปต์สองคู่)

### ⚠️ กติกาที่ห้ามผ่อน (เขียนเต็มไว้หัว `arealink.js` — อ่านก่อนแก้ทุกครั้ง)

1. ⛔ **ห้ามเพิ่ม `<script>` หรือ `<iframe>` ของผู้ให้บริการแผนที่ลงหน้านี้**
   เหตุผลสองชั้น ห้ามลืมชั้นใดชั้นหนึ่ง: **ค่าใช้จ่ายตามปริมาณการใช้** (ตารางข้างบน) และ
   **สิทธิ์ในข้อมูล** (การแสดงภาพแผนที่ของคนอื่นบนเว็บบริษัท = เผยแพร่ซ้ำ ต้องมีสัญญา)
   · `arealink.test.js` หัวข้อ 2 สแกนทั้ง `arealink.js` · `tools.html` · `tools.js`
   · **ตัดคอมเมนต์ออกก่อนสแกน** ไม่งั้นกติกาที่เขียนไว้เป็นข้อความจะทำให้เทสต์แดงเอง
2. **วัดจากภาพไม่ใช่การรังวัด** — `DISCLAIM` ขึ้นทุกครั้ง · ห้ามเรียกผลว่า "เนื้อที่" เฉยๆ
   ต้องเป็น "เนื้อที่ที่วัดจากภาพ" · มีเทสต์คำต้องห้าม (`ใช้แทนการรังวัด` · `เท่ากับการรังวัด` ฯลฯ)
3. ⚠️ **คณิตหน่วยที่ดินอ่านจาก `NJLandForm.calc` ที่เดียว** (`landform.js` ต้องโหลดก่อน)
   ห้ามพิมพ์ 400 / 100 / 4 ซ้ำในไฟล์นี้ — เทสต์เทียบผลกับ `NJLandForm` ทุกค่าที่ลอง
   · **ไม่มี `NJLandForm` = ไม่วาดช่องแปลงหน่วยเลย** (ลิงก์ยังขึ้น) ห้ามวาดช่องที่กรอกแล้วเงียบ
   · `build/lint.js` ล็อก `landform.js` → `arealink.js` → `tools.js` แล้ว
4. **ตัวเลขที่ผู้ใช้กรอกไม่ถูกส่งไปไหน** — ไม่มี fetch · ไม่มี localStorage · ไม่ยิงสถิติ (มีเทสต์)
5. ⚠️ **บรรทัดสรุปห้ามใช้ `C.fmt`** มันปัดเป็นจำนวนเต็ม — กรอก 12,345.6 แล้วสรุปบอก "12,346"
   อ่านแล้วเหมือนระบบเปลี่ยนตัวเลขที่ผู้ใช้พิมพ์ทิ้ง (เจอจริงตอนทดสอบบนเบราว์เซอร์)
6. **ระบบของหน่วยงานรัฐมาเป็นอันดับแรกเสมอ** — เนื้อที่ตามเอกสารสิทธิ์จาก LandsMaps
   น่าเชื่อถือกว่าการลากหมุดบนภาพ · มีเทสต์ล็อกลำดับ
7. ลิงก์ทั้งสามตรวจแล้วตอบ 200 เมื่อ 22 ก.ย. 69 · ทุกลิงก์มี `rel="noopener noreferrer"`

### ทดสอบแล้ว

`arealink` 51 · ชุดเทสต์เว็บทั้งหมดผ่านครบ ไม่ผ่าน 0 · `build/lint.js` ผ่าน (72 ไฟล์ JS · 48 หน้า) ·
`build/pages.js --check` ผ่าน (28 หน้า) · `build/linkcheck.js` ลิงก์เสีย 0
· **เบราว์เซอร์จริง** (พอร์ต 8812) ที่ 375px และ 1440px: ลิงก์ 3 ราย · 6,400 ตร.ม. → 4-0-0 ไร่ ·
2,000 → 1-1-0 ไร่ · 2 ไร่ → 3,200 ตร.ม. · "6,400" ที่มีเครื่องหมายคั่นหลักพันอ่านได้ ·
12,345.6 คงทศนิยมในบรรทัดสรุป · ลบค่าทิ้งแล้วล้างอีกฝั่งจริง · ไม่ล้นแนวนอน · ไม่มี console error

### ⛔ หนังสือขออนุญาตแสดงผังเมือง 2 ฉบับ — ยกเลิกแล้ว (22 ก.ย. 2569)

เจ้าของกิจการสั่ง: *"จดหมายขออนุญาต 2 ฉบับ ฟังก์ชันนี้จะยกเว้นและตัดปัญหา ให้แสดง link เชื่อมเว็บภายนอกแทน"*

| ฉบับ | สถานะ |
|---|---|
| `หนังสือขออนุญาตแสดงผังเมืองรวมบนเว็บไซต์-กรมโยธาธิการและผังเมือง.docx` | ⛔ ยกเลิก ไม่ส่ง |
| `หนังสือขออนุญาตแสดงผังเมืองรวมบนเว็บไซต์-กรุงเทพมหานคร.docx` | ⛔ ยกเลิก ไม่ส่ง |
| `อีเมลสอบถามเงื่อนไข-Longdo-ชั้นผังเมือง.md` | ⛔ ยกเลิก ไม่ส่ง |

⚠️ **อย่าสับสนกับ `หนังสือขอเชื่อมโยงข้อมูล-API-กรมโยธาธิการและผังเมือง.docx` ซึ่งไม่ได้ถูกยกเลิก**
ฉบับนั้นขอใช้ข้อมูล **ภายในสำนักงาน** (ระบบหลังบ้านใช้อยู่แล้วผ่าน `lib/dptzone.js`)
และรับปากไว้ว่าจะไม่เผยแพร่ต่อบุคคลภายนอก — คนละสิทธิ์กับการแสดงภาพผังบนเว็บสาธารณะ
· บันทึกฉบับเต็มอยู่ที่ `ยกเลิก-หนังสือขออนุญาตแสดงผังเมือง-22ก.ย.69.md` ในโฟลเดอร์โปรเจกต์

**ผลต่อโค้ด:** `zoneguide.js` เพิ่มค่าคงที่ `NO_MAP` ซึ่งขึ้นในกล่อง "ตรวจผังสีของแปลงด้วยตัวเอง" เสมอ
บอกผู้ใช้ตรงๆ ว่าเว็บนี้ไม่ได้แสดงภาพผังเมืองเองและเพราะอะไร — **เพื่อให้อ่านออกว่าเป็นการตัดสินใจ
ไม่ใช่ฟีเจอร์ที่ยังทำไม่เสร็จ** · `zoneguide.test.js` เพิ่มเป็น 42 ข้อ และล็อกว่าไฟล์คู่มือ
ห้ามมี `<iframe>` / `<img>` / `cityplan` / `dptgis` / `MapServer` / `WMS` หลงเหลือ

⛔ **ห้ามนำภาพผังเมืองของหน่วยงานหรือของบุคคลที่สามมาแสดงบนเว็บนี้โดยไม่มีหนังสืออนุญาตเป็นลายลักษณ์อักษร**
ไม่ว่าจะผ่านชั้นภาพบนแผนที่ ภาพนิ่ง หรือ iframe — เผยแพร่ซ้ำ = บริษัทกลายเป็นผู้ยืนยันผังแทนรัฐ
· สิ่งที่ยังแสดงได้ตามเดิมคือผังสีที่ **ทีมอ่านผังแล้วกดบันทึกเอง** (`land.zoneColor`) ซึ่งเป็นข้อมูลของบริษัท
