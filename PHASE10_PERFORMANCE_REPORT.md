# PHASE10_PERFORMANCE_REPORT.md — เส้นฐานประสิทธิภาพ (ก่อนปรับปรุง)

**วันที่วัด:** 18 กันยายน 2569 · **สาขา:** `feature/phase-10-seo-growth`
**สถานะ:** นี่คือ **ค่าก่อนปรับปรุง** เท่านั้น — งานปรับปรุงจริงอยู่ในสปรินต์ 6 ตามแผน
เอกสารนี้จะถูกเติมคอลัมน์ "หลังปรับปรุง" เมื่อถึงรอบนั้น

---

## 1. วิธีวัด

- **น้ำหนักไฟล์**: วัดจากไฟล์จริงใน repo ด้วย `gzip -c <ไฟล์> | wc -c` (ค่าที่ผู้ใช้ดาวน์โหลดจริง
  เพราะ GitHub Pages บีบอัดให้อัตโนมัติ) — ไม่ใช่ขนาดไฟล์ดิบ
- **จำนวนคำขอ**: นับจากแท็กใน HTML จริง (`<link rel=stylesheet>` · `<script src>` · `<img>`)
- **⛔ Core Web Vitals ยังไม่ได้วัด** — ดูหัวข้อ 4

---

## 2. หน้าแรก (`index.html`) — หน้าที่หนักที่สุด

| รายการ | ค่า |
|---|---|
| HTML | 77.2 KB → **14.7 KB** (gzip) |
| ไฟล์ CSS/JS ของเราเอง | **22 ไฟล์ · รวม 114.6 KB** (gzip) |
| — ก้อนใหญ่สุด | `njchat.js` 24.1 KB (ดิบ 98.7 KB) · `surveyquote.js` 9.8 KB · `feecalc.js` 8.4 KB · `home.css` 8.4 KB |
| แท็ก `<script src>` ในหน้า | 13 |
| แท็ก `<link rel=stylesheet>` | 9 |
| รูปในหน้า | 10 (`<img>`) |
| ฟอนต์ | Google Fonts (Sarabun + Trirong) · มี `display=swap` แล้ว ✅ |
| **รวมโดยประมาณก่อนนับรูป** | **~129 KB** |

⚠️ ตัวเลขนี้ยังไม่รวมรูป — `photos/` รวม **1.06 MB** (9 ไฟล์ JPEG ทั้งหมด ไม่มี WebP/AVIF)
ไฟล์ใหญ่สุด `hero.jpg` **275 KB** (มี `<picture>` สลับเป็น `hero-mobile.jpg` 167 KB บนจอ ≤640px — จุดเดียวในเว็บที่ทำไว้)

---

## 3. หน้าอื่น (เฉพาะ HTML · ยังไม่รวม CSS/JS/รูป)

| หน้า | ดิบ | gzip |
|---|---|---|
| `guides.html` | 19.0 KB | 5.0 KB |
| `listings.html` | 13.9 KB | 3.8 KB |
| `purpose.html` | 7.7 KB | 2.3 KB |
| **`land.html`** (หน้าแปลง) | 7.5 KB | **2.3 KB** |

⚠️ **`land.html` เบาเพราะยังไม่มีเนื้อหาอยู่ในนั้นเลย** — เนื้อหาทั้งหมดถูกวาดด้วย JS หลังเรียก API
(ดู `PHASE10_SEO_AUDIT.md` ข้อ C2) · ตัวเลขนี้จะโตขึ้นเมื่อสร้างหน้าแปลงแบบมีเนื้อหาจริงในสปรินต์ 3
**และนั่นคือการเปลี่ยนแปลงที่ถูกต้อง ไม่ใช่การถอยหลังด้านประสิทธิภาพ**

---

## 4. ⛔ Core Web Vitals — ยังไม่ได้วัด

| ตัวชี้วัด | ค่า |
|---|---|
| LCP · INP · CLS (ข้อมูลผู้ใช้จริง) | ยังไม่มี — ต้องมี Google Search Console ที่ยืนยันสิทธิ์โดเมนแล้ว |
| LCP · INP · CLS (วัดในห้องทดลอง) | ยังไม่มี — ต้องรัน Lighthouse หรือ PageSpeed Insights |

**วิธีที่เสนอ (ไม่มีค่าใช้จ่าย ไม่ต้องติดตั้งอะไร):**
เจ้าของกิจการเปิด <https://pagespeed.web.dev> แล้วใส่ URL ทีละหน้า (หน้าแรก · หน้ารวมประกาศ · หน้าแปลง)
เลือกแท็บ **มือถือ** แล้วส่งภาพผลลัพธ์มา — ผมจะบันทึกลงตารางนี้เป็นเส้นฐาน
· ทางเลือกอีกทาง (ต้องอนุมัติก่อน) คือติดตั้ง Lighthouse ในเครื่องเพื่อวัดเองซ้ำได้ทุกรอบ
แต่เป็นแพ็กเกจก้อนใหญ่ และ repo เว็บนี้จงใจไม่มี `package.json`

**สิ่งที่รู้อยู่แล้วโดยไม่ต้องวัด** (จากการอ่านซอร์ส):
- **CLS เสี่ยงสูง** — รูปการ์ดแปลงไม่มี `width`/`height` (`listingcard.js:72`) เบราว์เซอร์จึงจองที่ไม่ถูก
- **LCP น่าจะเป็น `hero.jpg`** (275 KB · JPEG) — เป็นเป้าหมายแรกของการแปลงเป็น WebP
- **INP** ยังไม่มีข้อบ่งชี้ว่ามีปัญหา (ไม่มีสคริปต์ที่คำนวณหนักตอนโหลด)

---

## 5. สิ่งที่ยังไม่ได้ทำ (แผนของสปรินต์ 6)

1. รูป WebP/AVIF พร้อมภาพสำรอง · `width`/`height` ทุกรูป · `srcset` มากกว่าหนึ่งจุด
2. โหลด JS เมื่อต้องใช้ (`njchat.js` 24 KB gzip โหลดทุกหน้าแม้ผู้ใช้ไม่เปิดแชท)
3. ลดจำนวนไฟล์ CSS/JS ที่หน้าแรกต้องรอ (ตอนนี้ 22 ไฟล์)
4. หัว Cache-Control ของเราเอง — **ทำไม่ได้บน GitHub Pages** (ดู audit ข้อ H5)

⚠️ **ห้ามลดคุณภาพหรือซ่อนเนื้อหาสำคัญเพื่อทำคะแนน** ตามข้อกำหนดงานที่ 13 —
เช่นห้ามถอดผลตรวจ 7 หัวข้อหรือรูปแปลงออกจากหน้าเพื่อให้ LCP ดีขึ้น
