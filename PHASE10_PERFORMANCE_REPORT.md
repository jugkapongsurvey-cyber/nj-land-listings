# PHASE10_PERFORMANCE_REPORT.md — ประสิทธิภาพ ก่อน/หลังปรับปรุง (งานที่ 13)

**เส้นฐาน (ก่อน):** 18 กันยายน 2569 · **ปรับปรุง:** สปรินต์ 1–6 (18–24 ก.ย. 2569) · **อัปเดตเอกสารนี้:** 24 กันยายน 2569

> ⚠️ **ที่มาของตัวเลขแต่ละแถวต่างกัน อ่านคอลัมน์ "วัดจาก" ทุกครั้ง**
> - **Lighthouse** วัดบน**เว็บจริง** (มือถือ · ค่ากลาง 3 รอบ) หลัง deploy แต่ละรอบ — ค่าที่บันทึกไว้ตอนนั้น
> - **น้ำหนักไฟล์** วัดจากไฟล์ใน repo ด้วย gzip (วิธีเดียวกับเส้นฐาน) — วัดซ้ำวันนี้
> - **วันนี้ (24 ก.ย.) วัด Lighthouse/PageSpeed ซ้ำไม่ได้** เพราะโควตาฟรีรายวันของ PageSpeed Insights API หมด
>   ตัวเลข Core Web Vitals ในเอกสารนี้จึงเป็นค่าที่วัดไว้หลัง deploy รอบล่าสุด (22 ก.ย.) ไม่ใช่ของวันนี้
>
> ⚠️ **ห้ามลดคุณภาพหรือซ่อนเนื้อหาสำคัญเพื่อทำคะแนน** (ข้อกำหนดงานที่ 13) — ทุกข้อด้านล่างไม่ได้ตัดเนื้อหาออก

---

## 1. สรุปผล

| ตัวชี้วัด | ก่อน | หลัง | วัดจาก |
|---|---|---|---|
| **Lighthouse Performance หน้าแรก (มือถือ)** | **63** | **94** | เว็บจริง · PR เว็บ #18 (22 ก.ย.) |
| Accessibility · Best practices · SEO | — | 100 · 100 · 100 | เว็บจริง · รอบเดียวกัน |
| LCP หน้าแรก (มือถือ) | ยังไม่ได้วัด | **2.1 วินาที** | เว็บจริง |
| CLS หน้าแรก | ยังไม่ได้วัด (รูปการ์ดไม่มีขนาด = เสี่ยงสูง) | **0.011** | เว็บจริง |
| รูปการ์ดประกาศ 6 ใบบนหน้าแรก (มือถือ) | **1,924 KB** | **165 KB (−91%)** | เว็บจริง · ระบบ PR #49–#51 · เว็บ PR #15–#16 |
| ไฟล์แชทที่โหลดทันทีทุกหน้า | `njchat.js` 101 KB (24.7 KB gzip) | `njchatload.js` 4.5 KB (1.7 KB gzip) | ไฟล์ใน repo |
| ภาพ hero บนมือถือ | JPEG 167 KB (แนวตั้ง) | WebP 58 KB / JPEG 74 KB สำรอง | ไฟล์ใน repo |
| ฟอนต์ | Google Fonts 2 โดเมน (บล็อกการวาด) | เสิร์ฟเอง `fonts/` + `fonts.css` | ไฟล์ใน repo |
| สคริปต์ของบุคคลที่สามก่อนผู้ใช้ยินยอมคุกกี้ | ไม่มี | ไม่มี · Meta Pixel โหลดหลังยินยอมเท่านั้น · GA4 ยังไม่เปิด (`GA4_ID` ว่าง) | `analytics.js` |

⚠️ บนเครื่องพัฒนาวัดได้ต่ำกว่าเว็บจริงมาก (บนเครื่อง 85→89 · เว็บจริง 80→94) เพราะไฟล์ทั้งหมดมาจาก localhost
**ตัดสินจากเว็บจริงเสมอ**

---

## 2. รายการตามข้อกำหนดงานที่ 13

| หัวข้อ | สถานะ | ทำอย่างไร / เหตุผล |
|---|---|---|
| Responsive Images | ✅ | การ์ดใช้ `srcset` 400×300 / 800×600 + `sizes` · hero ใช้ `<picture>` 3 ขนาด |
| WebP/AVIF พร้อม Fallback | ✅ WebP | รูปการ์ดเป็น WebP ที่ระบบสร้าง (ไฟล์ต้นฉบับเป็นทางถอย) · hero WebP + JPEG สำรอง · **ยังไม่มี AVIF** |
| Lazy Loading | ✅ | รูปใต้ขอบจอ `loading="lazy"` · แชทโหลดเมื่อผู้ใช้เริ่มโต้ตอบ (`njchatload.js`) |
| Font Loading | ✅ | เสิร์ฟฟอนต์เอง · ไม่ลดน้ำหนักฟอนต์ (500/600 ใช้อยู่ 143 จุด) |
| JavaScript Bundle / Code Splitting | ⚠️ บางส่วน | ไม่มีตัวรวมไฟล์โดยตั้งใจ (เว็บสถิต ไม่มี build step) · ทุกสคริปต์ `defer` · แยกแชทออกเป็นโหลดทีหลัง |
| Cache Headers | ⛔ ทำไม่ได้ฝั่งเว็บ | GitHub Pages ไม่ให้ตั้งหัวเอง (audit ข้อ H5) · ฝั่ง API ตั้งแคชเองแล้ว (P9-7) |
| CDN Compatibility | ✅ | GitHub Pages เสิร์ฟผ่าน CDN อยู่แล้ว · preconnect ไป `app.njteedinsure.com` ทุกหน้า |
| Database Query / API Response | ✅ ฝั่งระบบ | แคชผูกกับรุ่นข้อมูล (`lib/cache.js`) · แบ่งหน้า opt-in · PDF ใน worker (Phase 9 P9-7) |
| Third-party Scripts | ✅ | ก่อนยินยอมคุกกี้ไม่มีสคริปต์ภายนอกเลย · Meta Pixel โหลดหลังผู้ใช้กดยอมรับ · GA4 จะทำแบบเดียวกันเมื่อเจ้าของตั้ง `GA4_ID` |

---

## 3. น้ำหนักต่อหน้า (วัดวันนี้ · ไฟล์ของเราเองเท่านั้น · gzip)

| หน้า | HTML | CSS บล็อกการวาด | CSS โหลดทีหลัง | JS (defer ทั้งหมด) | `<img>` |
|---|---|---|---|---|---|
| `index.html` | 13.9 KB | 14 ไฟล์ · 47.9 KB | 5 ไฟล์ | 16 ไฟล์ · 63.0 KB | 7 |
| `listings.html` | 5.8 KB | 16 · 52.2 KB | 4 | 13 · 55.0 KB | 3 |
| `guides.html` | 7.4 KB | 16 · 47.8 KB | 4 | 12 · 55.6 KB | 2 |
| `services.html` | 9.5 KB | 12 · 38.5 KB | 4 | 12 · 50.2 KB | 2 |
| `knowledge.html` | 3.7 KB | 11 · 37.7 KB | 1 | 6 · 22.7 KB | 2 |
| `checklist.html` (ใหม่) | 5.6 KB | 14 · 51.4 KB | 4 | 11 · 43.3 KB | 1 |
| หน้าแปลงสแตติก (`properties/…/index.html`) | 4.5 KB | 19 · 62.0 KB | **0** | 18 · 98.4 KB | 3 |

เทียบเส้นฐานหน้าแรก: เดิม **22 ไฟล์ · 114.6 KB รวมแชท 24 KB ที่โหลดทันที** และ Google Fonts ภายนอก ·
ตอนนี้ **30 ไฟล์ · 110.9 KB** (ไม่นับ CSS ที่โหลดทีหลัง 5 ไฟล์ · แชทไม่อยู่ในจำนวนนี้แล้ว) · **จำนวนไฟล์เพิ่มขึ้นเพราะแยกสไตล์กลาง (`tokens` `components` `header`) และเสิร์ฟฟอนต์เอง**
แต่ไม่มีไฟล์ไหนโหลดจากโดเมนภายนอก และ JS ทั้งหมดไม่บล็อกการวาด

---

## 4. สิ่งที่เจอวันนี้และยังไม่ได้แก้

1. **✅ แก้แล้ว 25 ก.ย. 2569 (เว็บ #28) · สร้างเองทุก 30 นาทีด้วย `properties.yml`** — ข้อความเดิม: **หน้าแปลงสแตติกยังเป็นไฟล์รุ่นก่อนปรับปรุง** — สร้างครั้งล่าสุด 21 ก.ย. (ก่อนเสิร์ฟฟอนต์เองและก่อน `lazycss` ใน PR #18 วันที่ 22 ก.ย.)
   จึงยังดึง Google Fonts จากภายนอก และมี CSS บล็อกการวาด 19 ไฟล์ โดยไม่มีไฟล์ที่โหลดทีหลังเลย
   · **ไม่ใช่บั๊กของโค้ด** — ต้นแบบ `land.html` ถูกแล้ว เพียงแต่ยังไม่ได้สั่ง `node build/pages.js && node build/properties.js` ใหม่
   · ⚠️ การสั่งสร้างใหม่ดึงข้อมูลประกาศจาก API จริง (แปลงที่ถูกถอนจะถูกลบ แปลงใหม่ถูกเพิ่ม) จึงรอเจ้าของกิจการอนุมัติก่อน
   · ข้อเสนอ: เพิ่ม workflow สร้างหน้าแปลงอัตโนมัติแบบเดียวกับ `knowledge.yml`/`journal.yml` เพื่อไม่ให้หน้าเก่าค้างอีก
2. **`styleLayout` ~1,600 ms** ยังเป็นก้อนใหญ่สุดบนหน้าแรก (CSS ~92 KB ต่อหน้าที่ต้องคำนวณกับ DOM) —
   แก้ได้ด้วยการส่งเฉพาะ CSS ที่หน้านั้นใช้ ซึ่งเสี่ยงกับเว็บ 31 หน้าที่เขียนมือ **ยังไม่ทำ**
3. **หน้ารวมประกาศ** ยังโหลดข้อมูลทุกแปลงทั้งก้อน — ต้องแบ่งหน้าเมื่อแปลงเกิน ~500 (ฝั่งระบบ · ดู CLAUDE.md ของระบบ)
4. **ยังไม่มี AVIF** และ **ยังไม่มีข้อมูล Core Web Vitals จากผู้ใช้จริง** (ต้องยืนยันสิทธิ์ Search Console ก่อน)

---

## 5. วิธีวัดซ้ำ

- **Lighthouse บนเว็บจริง:** เปิด <https://pagespeed.web.dev> ใส่ URL แล้วเลือกแท็บมือถือ ·
  หน้าที่ข้อกำหนดระบุ: หน้าแรก · `services.html` · `knowledge.html` · หน้าแปลงหนึ่งหน้า
- **น้ำหนักไฟล์:** `gzip -c <ไฟล์> | wc -c` เหมือนเส้นฐาน
- **CSS ไม่บล็อกการวาดครบทุกหน้า:** `node build/lazycss.js --check` (อยู่ใน CI แล้ว)

รายละเอียดการปรับปรุงแต่ละรอบ: `docs/audit/PERFORMANCE_BASELINE.md` · `docs/audit/PERFORMANCE_BEFORE_AFTER.md`
