# ที่ดินชัวร์ — Design System

แนวทางภาพรวม: trustworthy land intelligence — สะอาด เป็นมืออาชีพ อ่านง่ายกว่าพอร์ทัลอสังหาทั่วไป และให้ความสำคัญกับข้อมูลแปลงที่ดินก่อนสิ่งตกแต่ง

## ลำดับข้อมูลในการ์ด

1. ภาพและสถานะรังวัด
2. ราคา
3. ข้อมูลแปลง (parcelInfo)
4. เนื้อที่ + ราคาต่อตารางวา (`.card-meta`) — มีเฉพาะแปลงที่ API ส่งค่ามาจริง
5. คำโปรยที่ทีมงานเขียนเอง — ถ้าไม่มี ให้ข้ามไป
6. ผู้ดูแลและช่องทางติดต่อ

> ⚠️ ลำดับนี้แสดง **เฉพาะช่องที่ API ส่งมาจริง** เท่านั้น
> เดิมเคยมี "ราคาต่อตารางวา / เนื้อที่ / ทางเข้าออก / เอกสารสิทธิ์" อยู่ในลำดับนี้ด้วย
> แต่ `/api/public/listings` ไม่ได้ส่งข้อมูลเหล่านั้นมา การใส่ไว้จึงเท่ากับสั่งให้เติมข้อความเอง
> ซึ่งกลายเป็นการอ้างข้อเท็จจริงเกี่ยวกับที่ดินของคนอื่นโดยไม่มีข้อมูลรองรับ
> ถ้าอยากได้ช่องเหล่านี้จริง ต้องเพิ่มฟิลด์ที่ฝั่ง server ก่อน ไม่ใช่เติมที่หน้าเว็บ
>
> **2026-09-04 — ทำตามทางที่ถูกแล้ว**: เพิ่มฟิลด์ที่ฝั่ง server ก่อน แล้วค่อยเอาขึ้นการ์ด
> ตอนนี้ API ส่ง `totalWa` · `pricePerWa` · `pricePerRai` มาจริง (คำนวณที่เซิร์ฟเวอร์ ไม่ได้หารในเบราว์เซอร์)
> จึงเอา "เนื้อที่ + ราคาต่อตารางวา" กลับเข้าลำดับได้อย่างถูกต้อง
> **แปลงที่ยังไม่รู้เนื้อที่จะไม่มีบรรทัดนี้เลย ไม่ใช่ขึ้นเป็น 0** — กติกาเดิมทุกประการ
> ส่วน "ทางเข้าออก / เอกสารสิทธิ์" ยังไม่เอาขึ้นการ์ด แม้ API จะส่งมาแล้ว
> เพราะการ์ดสั้นๆ ใบเดียวใส่ครบไม่ได้ — สองช่องนั้นใช้เป็น **ตัวกรอง** ในหน้ารวมประกาศแทน

## Responsive

- Mobile `< 640px`: การ์ดเลื่อนแนวนอน, bottom navigation, search 2 แถว
- Tablet `640–900px`: grid 2 คอลัมน์
- Desktop `> 900px`: grid 3 คอลัมน์และเมนูเต็ม

## Voice

ใช้ข้อความตรงไปตรงมา สุภาพ และไม่รับประกันเกินข้อมูลจริง เช่น “ผ่านการรังวัดยืนยันแนวเขต” แทน “ปลอดภัย 100%” และระบุเมื่อข้อมูลใดต้องตรวจเพิ่มเติม

## ตัวเรนเดอร์การ์ดมีชุดเดียว

`listingcard.js` (`window.NJListing`) เป็นเจ้าของ `card()` · `normalize()` · `emptyHtml()` ·
`loadFailedHtml()` · `fetchListings()` · `bindGrid()` ใช้ร่วมกันทั้งหน้าแรก (`marketplace.js`)
และหน้ารวมประกาศ (`listings.js`)

**ห้ามก๊อปตัวเรนเดอร์ไปไว้ในไฟล์ของหน้าใดหน้าหนึ่งอีก** — กติกา "แสดงเฉพาะสิ่งที่ API ส่งมาจริง"
จะกลายเป็นมีสองที่ให้ลืมแก้ ซึ่งเป็นความผิดพลาดที่เว็บนี้รับไม่ได้

⚠️ **สไตล์การ์ดยังมีสองชุดตามชุดสีของหน้า** (`home.css` สำหรับหน้าแรก · `marketplace.css` สำหรับหน้าอื่น)
เพิ่มคลาสใหม่ให้การ์ดต้องเพิ่มทั้งสองไฟล์ — ใส่ที่เดียวแล้วอีกหน้าจะไม่มีสไตล์โดยไม่มี error ให้เห็น
