# PHASE10_SEO_AUDIT.md — ผลตรวจ SEO ก่อนเริ่ม Phase 10

**วันที่ตรวจ:** 18 กันยายน 2569
**ตรวจกับของจริง ไม่ใช่จากบันทึกเก่า** — ทุกข้อในเอกสารนี้มีหลักฐานเป็นคำสั่งที่รันจริงหรือเลขบรรทัดในโค้ด
**สาขา:** `feature/phase-10-seo-growth` (แตกจาก `origin/main` = `64c7f37`)
**ยังไม่มีการแก้โค้ดใดๆ** — รอบนี้เป็นการตรวจและวางแผนเท่านั้น ตามข้อกำหนดข้อ 6

---

## 0. ขอบเขตและวิธีตรวจ

ตรวจ 2 ระบบที่เกี่ยวกับ Phase 10:

| ระบบ | ที่อยู่ | repo | โฮสต์ |
|---|---|---|---|
| เว็บสาธารณะ (SEO อยู่ที่นี่เกือบทั้งหมด) | `https://njteedinsure.com` | `nj-land-listings` | **GitHub Pages** (ยืนยันจาก header `Server: GitHub.com`) |
| ระบบหลังบ้าน + API สาธารณะ | `https://app.njteedinsure.com` | `nj-survey-system` | Render (Node/Express) |

วิธีตรวจ:
- ยิง `curl` ที่เว็บจริงทั้งสองโดเมน (header, สถานะ, เนื้อหา HTML ที่เสิร์ชเอนจินได้รับจริงก่อนรัน JS)
- อ่านซอร์สทั้ง 20 หน้า HTML + JS ที่เกี่ยวข้อง และ `server.js` (20,087 บรรทัด)
- ดึงข้อมูลจริงจาก `GET /api/public/listings` มานับ (22 แปลง ณ วันตรวจ)

**สิ่งที่ตรวจจากเครื่องนี้ไม่ได้** อยู่ในหัวข้อ 7 — ไม่เดาแทน

---

## 1. ระบบเดิมที่พบ (สรุปก่อนแก้ ตามข้อกำหนดข้อ 1 และ 6)

### 1.1 เว็บสาธารณะ `njteedinsure.com`

- **สแตติกล้วน ไม่มี build step ไม่มี backend ของตัวเอง** — 20 ไฟล์ HTML วางที่ราก ไม่มี `package.json`
  ไม่มี `node_modules` ไม่มี GitHub Actions (ไม่มีโฟลเดอร์ `.github`) · deploy = push ขึ้น `main` แล้ว GitHub Pages เสิร์ฟ
- **Routing = ชื่อไฟล์ตรงๆ** `land.html?id=OP-072`, `guides.html#deed` · ไม่มี URL แบบ `/properties/...` และไม่มี router
- **ข้อมูลแปลงทั้งหมดมาจาก `GET https://app.njteedinsure.com/api/public/listings`** ทุกครั้งที่เปิดหน้า
  (กติกาข้อ 4 ของ `CLAUDE.md`: ห้ามมีข้อมูลแปลงตัวอย่างในโค้ดเด็ดขาด)
- **ไม่มีฐานข้อมูลของตัวเอง** — ไม่มีอะไรให้ migrate ฝั่งนี้
- **มี SEO พื้นฐานอยู่แล้วและทำไว้ดี**: `robots.txt` มี `Disallow` ครบทุกหน้าที่มีตั๋วในลิงก์,
  `sitemap.xml` 14 URL, canonical 13 หน้า, Open Graph เกือบทุกหน้า, JSON-LD องค์กรที่หน้าแรก
- **สถิติ**: `analytics.js` มี Meta Pixel ID จริงแล้ว (`2478386206003868`) · GA4 ยังว่าง ·
  โหลดสคริปต์ภายนอก **หลังผู้ใช้กดยอมรับเท่านั้น** (ถูกต้องตาม PDPA) · สถิติภายในของเราเอง (ไม่มีคุกกี้ ไม่มี PII) ส่งเข้า `POST /api/public/track` ตลอด
- **ชุดทดสอบ 10 ไฟล์** รันด้วย `node <ไฟล์>` ตรงๆ: `contracts` `compare` `feecalc` `valuecalc` `verified` `njchat` `agency` `loancalc` `zonechip` `zoneguide`

### 1.2 ระบบหลังบ้าน `app.njteedinsure.com`

- Node/Express หน้าเดียว (SPA) · ฐานข้อมูลเป็น JSON blob (`lib/db.js`) บน Postgres
- **สิทธิ์**: `lib/auth.js` มี 47 พื้นที่สิทธิ์ (`AREAS`) แยกตามบทบาท — ยังไม่มีพื้นที่สำหรับงานการตลาด/SEO
- **สวิตช์ฟีเจอร์**: `lib/featureflags.js` — ของใหม่ทุกอย่างเปิด/ปิดได้จากหน้าแอดมิน (Phase 10 จะใช้กติกาเดียวกัน)
- **ของเดิมที่ Phase 10 ต้องต่อยอด ไม่ใช่สร้างใหม่**:
  - `lib/leads.js` — ลีดและสถานะ 9 ขั้น (CRM ที่สั่งให้เชื่อม)
  - `lib/savedsearch.js` — **บันทึกเงื่อนไขค้นหา + แจ้งแปลงใหม่ + แจ้งราคาลด มีอยู่แล้วครบ** (Phase 7 รอบ 4) แต่เป็นระบบในหลังบ้าน ยังไม่ต่อออกมาที่เว็บสาธารณะ
  - `lib/kb.js` — คลังความรู้ที่มี draft → reviewed → published → archived, เวอร์ชัน, วันทบทวน, ผู้ตรวจ **แต่ทำมาเพื่อ AI ภายใน** ไม่มี slug ไม่มีช่อง SEO ไม่มีหน้าเว็บสาธารณะ
  - `PUBLIC_EVENT_TYPES` ใน `server.js:1759` — เหตุการณ์สถิติ 26 ชนิดที่ใช้อยู่จริง
  - `GET /api/public/track/summary` — สรุปลีด (ต้องมีสิทธิ์ `opportunities`)
- **ประตูกันข้อมูลรั่วที่ห้ามแตะ**: แปลงจะขึ้นเว็บได้ต่อเมื่ออยู่ขั้น "ตกลง/เซ็นสัญญา" **และ** PDPA = ได้รับความยินยอมแล้ว · `publicLand()` ตัด `lat/lng/ravaeng/landNo/ราคาประเมิน` ออกเสมอ

### 1.3 ข้อมูลจริง ณ วันตรวจ (ใช้ตัดสินใจเรื่องหน้าจังหวัด)

22 แปลง (ขายทั้งหมด ไม่มีให้เช่า) · ระดับ 1 = 17 แปลง · ระดับ 2 (รังวัดยืนยันแล้ว) = 5 แปลง · มีรูปครบ 22/22 · **มีคำโปรยเพียง 4 แปลง**

| จังหวัด | จำนวนแปลง | อำเภอที่มีของ |
|---|---|---|
| สมุทรปราการ | 5 | พระสมุทรเจดีย์, เมืองสมุทรปราการ, บางพลี |
| กรุงเทพมหานคร | 5 | บางกะปิ, ทวีวัฒนา, ตลิ่งชัน, ภาษีเจริญ |
| ปทุมธานี | 3 | คลองหลวง, ธัญบุรี |
| นนทบุรี | 2 | ไทรน้อย, เมืองนนทบุรี |
| สระบุรี | 2 | แก่งคอย, หนองแซง |
| อยุธยา / สุพรรณบุรี / ชลบุรี / นครนายก / นครปฐม | 1 ต่อจังหวัด | — |
| **สมุทรสาคร** | **0** | — |

⚠️ **สมุทรสาครซึ่งอยู่ในรายชื่อ 6 จังหวัดที่สั่ง ยังไม่มีแปลงเลยสักแปลง** — มีผลกับแผนงานที่ 7 โดยตรง (ดูหัวข้อ 8 ข้อ Q6)

---

## 2. Critical — ต้องแก้ก่อนทำอย่างอื่น

### C1 · ระบบหลังบ้านทั้งโดเมนถูกจัดทำดัชนีได้ และสร้าง URL ขยะได้ไม่จำกัด

**อาการ** `app.njteedinsure.com` ไม่มี `robots.txt` ไม่มี `noindex` และทุกเส้นทางที่ไม่รู้จักตอบ **HTTP 200 พร้อม HTML ของหน้าเข้าสู่ระบบ**

**หลักฐาน**
```
curl -I https://app.njteedinsure.com/robots.txt          → 200, Content-Type: text/html  (ได้ index.html ของ SPA)
curl -I https://app.njteedinsure.com/this-path-does-not-exist-123 → 200 (ไม่ใช่ 404)
```
- `server.js:19509` — `app.get('*', (req,res) => res.sendFile(.../public/index.html))`
- `public/index.html` ของระบบหลังบ้าน **ไม่มี** `<meta name="robots">` เลย
- มีลิงก์เข้าจากเว็บสาธารณะจริง (`portal.html` และปุ่มแชร์) → บอตเดินเข้ามาถึงแน่นอน

**ผลกระทบ** ขัดข้อกำหนดงานที่ 14 ตรงๆ ("หน้า Login, Admin ต้องไม่ถูก Index") · หน้าเข้าสู่ระบบโผล่ในผลค้นหาแบรนด์ตัวเอง · soft 404 ไม่จำกัดจำนวน = งบการไต่ (crawl budget) ถูกผลาญและเว็บถูกประเมินคุณภาพต่ำลง
**ไม่มีข้อมูลลูกค้ารั่ว** — เนื้อหาจริงต้องล็อกอิน สิ่งที่รั่วคือ "หน้าเปล่า 200" จำนวนไม่จำกัด

**วิธีแก้** (ทั้งสามข้อ ไม่ใช่ข้อใดข้อหนึ่ง)
1. เพิ่มเส้นทาง `GET /robots.txt` จริง — **ห้ามใช้ `Disallow: /` เพียงอย่างเดียว** เพราะบอตที่ถูกห้ามไต่จะมองไม่เห็นคำสั่ง `noindex` แล้วยังเก็บ URL เปล่าเข้าดัชนีได้ → ให้ยอมให้ไต่หน้า HTML แต่ห้ามไต่ `/api/` `/files/` `/sandbox/`
2. ใส่ `X-Robots-Tag: noindex, nofollow` ที่ตัวเสิร์ฟ SPA fallback และที่ `public/index.html` (`<meta name="robots" content="noindex,nofollow">`)
3. ให้เส้นทางที่ไม่ใช่ของ SPA จริงๆ ตอบ **404** (แนวเดียวกับที่ Phase 9 P9-6 ทำกับ `/api/*` ไว้แล้วที่ `server.js:19502`)

**ไฟล์** `nj-survey-system/server.js` (ราว 19502–19509), `nj-survey-system/public/index.html`

---

### C2 · หน้าแปลงที่ดินส่ง HTML เปล่าให้เสิร์ชเอนจิน

**อาการ** `land.html?id=…` ซึ่งเป็น **หน้าสินค้าหลักของธุรกิจ** ส่ง HTML ที่มีแค่ "กำลังโหลดข้อมูลแปลงที่ดิน…" เนื้อหาทั้งหมด (ชื่อแปลง ราคา เนื้อที่ ทำเล ผลตรวจ) ถูกวาดด้วย JavaScript หลังเรียก API

**หลักฐาน**
```
curl "https://njteedinsure.com/land.html?id=OP-072"  → 2,269 bytes
เนื้อความใน <body>: "… ← กลับไปดูแปลงทั้งหมด  กำลังโหลดข้อมูลแปลงที่ดิน... …"
```
- `land.js:475` ตั้ง `document.title` หลังโหลดข้อมูลเสร็จ
- `land.js:63` ตั้ง `<link rel=canonical>` จาก JS
- `land.js:118` ฉีด JSON-LD `RealEstateListing` จาก JS

**ผลกระทบ**
- Google เก็บหน้าพวกนี้ได้ แต่ต้องรอ **การเรนเดอร์รอบสอง** ซึ่งช้ากว่ามากและไม่รับประกัน — ประกาศที่เพิ่งลงอาจใช้เวลาหลายวันกว่าจะมีเนื้อหาในดัชนี
- **LINE, Facebook, Messenger ไม่รันสคริปต์เลย** → แชร์ลิงก์แปลงไปในกลุ่มไลน์ ได้การ์ดเดียวกันหมดทุกแปลง ("รายละเอียดแปลงที่ดิน | ที่ดินชัวร์" + รูปแบรนด์) ซึ่งเป็นช่องทางที่ทีมใช้จริงทุกวัน
- ถ้า API ล่มตอนบอตเข้ามา = หน้าว่างถาวรในสายตา Google

**วิธีแก้** สร้างไฟล์ HTML จริงต่อแปลงด้วยสคริปต์ (prerender) แล้ว commit ขึ้น repo — รายละเอียดและเหตุผลที่ไม่เลือกทางอื่นอยู่ใน `PHASE10_IMPLEMENTATION_PLAN.md` §3

**ไฟล์** `land.html`, `land.js`, `listingcard.js`, (ใหม่) `build/prerender.js`

---

### C3 · ทุกหน้าแปลงใช้ title + description + og ชุดเดียวกัน

**อาการ** `land.html` มี `<title>รายละเอียดแปลงที่ดิน | ที่ดินชัวร์</title>` และ description ชุดเดียว ใช้ร่วมกันทั้ง 22 แปลง
`setSeo()` ใน `land.js` ตั้งให้เฉพาะ **canonical + JSON-LD + title** — **ไม่แตะ `meta description`, `og:title`, `og:description`, `og:image`, `og:url` เลย**

**หลักฐาน** `land.html` head (ไม่มี canonical, ไม่มี `og:url`, ไม่มี `twitter:card`) + `land.js:47–120` (`setSeo`)

**ผลกระทบ** duplicate title/description ข้าม 22 หน้า = Google ยุบรวมหรือเลือกเขียนชื่อหน้าให้เอง · การ์ดแชร์ทุกแปลงเหมือนกัน

**วิธีแก้** รวมกับ C2 (prerender เขียนค่าจริงลงไฟล์) + ให้ `setSeo()` ตั้ง description/og/twitter ด้วย สำหรับผู้ใช้ที่เปิดจากในเว็บ

**ไฟล์** `land.html`, `land.js`

---

## 3. High

### H1 · ไม่มีหน้ารองรับคำค้นระดับจังหวัด/อำเภอเลย และตัวกรองไม่เขียนลง URL
ตัวกรองทั้งหมดของ `listings.html` (จังหวัด ราคา เนื้อที่ เอกสารสิทธิ์ ผังสี) เก็บอยู่ในหน่วยความจำ ไม่มี `history.replaceState` (ทั้ง repo พบการเขียน URL เฉพาะที่ `purpose.js:31`)
**ผลดี**: ไม่มีปัญหา URL ซ้ำจากลำดับพารามิเตอร์ (งานที่ 4 ผ่านอยู่แล้วโดยบังเอิญ)
**ผลเสีย**: ไม่มี URL ใดที่ตรงกับคำค้นที่มีมูลค่าสูงสุดของธุรกิจนี้ เช่น "ขายที่ดิน สมุทรปราการ" · แชร์ผลกรองให้คนอื่นไม่ได้ · ยิงโฆษณาลงหน้าเฉพาะทำเลไม่ได้
**แก้** งานที่ 7 (หน้าจังหวัด/อำเภอ) + allowlist ตัวกรองที่ให้เก็บดัชนี
**ไฟล์** `listings.js`, `listings.html`, (ใหม่) หน้าและตัวสร้างหน้าจังหวัด

### H2 · หน้ากำพร้า 2 หน้า
- `partner-apply.html` — **ไม่มีลิงก์เข้าจากหน้าไหนเลยทั้งเว็บ** (พบการอ้างถึงเฉพาะใน `contracts.test.js`) แต่ประกาศไว้ใน `sitemap.xml`
- `inspect.html` — มีลิงก์เข้าจาก `notify.html` `quote.html` `room.html` `partner-apply.html` ซึ่ง **ทั้งหมดเป็นหน้า noindex** และจาก `land.js`/`room.js` ซึ่งเป็นลิงก์ที่เกิดหลังรัน JS → ในสายตาบอตรอบแรก ไม่มีทางเดินถึง
**แก้** เพิ่มลิงก์จากหน้าที่เก็บดัชนีได้ (งานที่ 9)

### H3 · ไม่มี sitemap ของประกาศทรัพย์
`sitemap.xml` มี 14 URL และคอมเมนต์ไว้ว่าจงใจไม่ใส่หน้าแปลง "เพราะเปลี่ยนไปมาตลอด" โดยหวังให้ Google ตามลิงก์เอาเอง — แต่ลิงก์เหล่านั้นเกิดจาก JS (C2) จึงเป็นการฝากความหวังไว้กับสิ่งที่ยังไม่ทำงาน
**แก้** งานที่ 3 — `sitemaps/properties.xml` สร้างจากข้อมูลจริง ใช้ `updatedAt` ของแต่ละแปลงเป็น `lastmod`

### H4 · ไม่มี Google Search Console และ GA4 → ไม่มีเส้นฐานไว้วัดผล
`GA4_ID = ''` (`analytics.js:13`) · ไม่พบหลักฐานการยืนยันสิทธิ์โดเมนใน repo
**ผลกระทบ** ตัวชี้วัด 6 ตัวแรกของงานที่ 12 (Impressions, Clicks, CTR, Average Position, Indexed Pages, Organic Users/Sessions) **ไม่มีทางได้มาโดยไม่มีสองบัญชีนี้** — เป็นงานที่เจ้าของต้องทำเอง ผมทำแทนไม่ได้
**แก้** หัวข้อ 8 ข้อ Q2–Q3

### H5 · ตั้ง Security Header ที่เว็บสาธารณะไม่ได้เลยบนโฮสต์ปัจจุบัน
```
curl -I https://njteedinsure.com/  → ไม่มี CSP, X-Content-Type-Options, Referrer-Policy, HSTS
```
GitHub Pages ไม่เปิดให้ตั้ง header เอง (ต่างจากฝั่งระบบหลังบ้านซึ่งมีครบแล้ว: CSP report-only, HSTS, nosniff, X-Frame-Options, Permissions-Policy)
**แก้** ต้องให้เจ้าของเลือก (หัวข้อ 8 ข้อ Q1) — ย้ายโฮสต์ หรือยอมรับข้อจำกัดแล้วใช้ `<meta http-equiv>` เท่าที่ทำได้

### H6 · ทำ 301 Redirect และ HTTP 410 ไม่ได้บนโฮสต์ปัจจุบัน
ข้อกำหนดงานที่ 4 ("301 Redirect Mapping") และงานที่ 6 ("ส่ง HTTP 410 เมื่อไม่มีคุณค่าต่อผู้ใช้อีก") **บังคับให้ต้องตั้งค่าที่เซิร์ฟเวอร์** ซึ่ง GitHub Pages ทำไม่ได้ (ทำได้แค่ redirect ฝั่งเบราว์เซอร์ ซึ่ง Google ถือว่าเป็น 301 แบบอ่อนและไม่เท่ากัน)
**แก้** ผูกกับข้อ Q1 เดียวกับ H5 · ถ้าไม่ย้ายโฮสต์ แผนจะใช้ "คงหน้าไว้ + ป้ายขายแล้ว + แนะนำแปลงใกล้เคียง" เป็นทางหลัก ซึ่งข้อกำหนดอนุญาตไว้อยู่แล้วและเป็นทางที่ดีต่อผู้ใช้มากกว่า

### H7 · Metadata ถูก hard-code กระจายใน 20 ไฟล์ แก้จากหน้าแอดมินไม่ได้
ขัดข้อกำหนดงานที่ 2 ตรงๆ ("ห้าม Hard-code Metadata ไว้ใน Component หลายแห่ง")
**แก้** งานที่ 2 — สร้างทะเบียน SEO กลางในระบบหลังบ้าน + ตัวสร้างหน้า

### H8 · ยังไม่มีบทความจริงและไม่มี CMS บทความ
`guides.html` เป็น **หน้าเดียวรวมหลายหัวข้อด้วย anchor** (`guides.html#deed`, `#valuation`, `#zoning`) ไม่ใช่บทความแยกหน้า → ไม่มีหน้าใดเจาะคำค้นเฉพาะเรื่องได้ · ไม่มีผู้เขียน ผู้ตรวจ วันที่เผยแพร่ แหล่งอ้างอิง
`lib/kb.js` มีขั้นตอนตรวจ–เผยแพร่–เก็บเข้าคลัง–ประวัติเวอร์ชันครบแล้ว แต่ **ออกแบบมาป้อน AI ภายใน** ไม่มี slug/ช่อง SEO/ช่องทางเผยแพร่สู่เว็บ
**แก้** งานที่ 8 — ต่อยอด `kb.js` ไม่สร้างระบบที่สองขึ้นมาแข่ง

### H9 · ลีดไม่ได้เก็บที่มาแบบที่งานที่ 10 ต้องการ
`lib/leads.js:70` เก็บ `source` เป็นรายการตายตัว 9 ค่า (`land_page`, `listings`, `home`, `compare`, `share`, `line`, `phone`, `walk_in`, `other`)
**ยังไม่มี**: `medium`, `campaign`, `content`, `term`, `landing_page`, `referrer`, `first_touch`, `last_touch`, `conversion_event`, `consent_status`
**ผลกระทบ** ตอบไม่ได้ว่าลีดมาจากบทความไหน จังหวัดไหน แคมเปญไหน — ซึ่งคือหัวใจของงานที่ 12
**แก้** งานที่ 10 + migration (ดูแผน §5)

---

## 4. Medium

| # | เรื่อง | หลักฐาน | วิธีแก้ |
|---|---|---|---|
| M1 | **ไม่มี `twitter:card` 14 จาก 20 หน้า** รวมหน้าแรก | `grep -L 'name="twitter:card"' *.html` → chat, compare, deal, guides, index, land, listings, notify, partner-apply, portal, purpose, quote, room, videos | ใส่ผ่านทะเบียน SEO กลาง (งานที่ 2) |
| M2 | **ไม่มี `og:url`** ที่หน้าแรกและหน้าแปลง | `grep -L 'property="og:url"'` → index, land (+ หน้า noindex) | เดียวกับ M1 |
| M3 | **มี JSON-LD หน้าเดียวทั้งเว็บ** | `grep -c 'application/ld+json' *.html` → มีแต่ `index.html:1` | งานที่ 5 |
| M4 | **มีเนื้อหาคำถาม-คำตอบจริง 5 หน้า แต่ไม่มี FAQPage schema** | consign, inspect, portal, verify, wanted | งานที่ 5 (ใส่เฉพาะคำถามที่แสดงบนหน้าจริง) |
| M5 | **`sitemap.xml` ใส่ `priority`/`changefreq` และ `lastmod` ที่พิมพ์มือ** | ทุก `<url>` ใน `sitemap.xml` · เช่น `/` lastmod 2026-09-06 ทั้งที่ไฟล์แก้ล่าสุด 17 ก.ย. | ข้อกำหนดงานที่ 3 สั่งห้ามไว้ตรงๆ — ตัด `priority`/`changefreq` ทิ้ง และให้ `lastmod` มาจากวันที่ commit ของไฟล์ |
| M6 | **หน้าแรกแบ่งหน้าละ 9 แปลง แต่ไม่มี URL ของหน้าที่ 2** · `listings.html` ไม่แบ่งหน้าเลย | `marketplace.js` `PAGE_SIZE`/`renderPager()` · `listings.js` ไม่มี pager | ทำ URL ของหน้าถัดไปให้ไต่ได้ (งานที่ 1 หัวข้อ Pagination) |
| M7 | **รูปไม่มี `width`/`height`** → เลื่อนหน้าจอกระตุก (CLS) | `listingcard.js:72` มี `loading="lazy"` แต่ไม่มีขนาด | ใส่ขนาดจริง (งานที่ 13) |
| M8 | **`alt` ของรูปการ์ด = `parcelInfo` ทั้งก้อน** ซึ่งเป็นข้อความยาวที่เจ้าของพิมพ์เอง (บางแปลง 200+ ตัวอักษร) | `listingcard.js:72` | เขียน `alt` สั้นจากช่องที่แยกไว้ (ประเภท + ตำบล/อำเภอ/จังหวัด) |
| M9 | **ไม่มีหน้า 404 ของเราเอง** | `curl -o /dev/null -w %{http_code} https://njteedinsure.com/no-such-page-xyz` → 404 หน้าเริ่มต้นของ GitHub | เพิ่ม `404.html` (GitHub Pages รองรับไฟล์นี้อยู่แล้ว ไม่ต้องย้ายโฮสต์) |
| M10 | **กดยอมรับคุกกี้แล้วถอนคืนไม่ได้** | `analytics.js` เก็บ `njts_consent` แล้วแบนเนอร์ขึ้นเฉพาะตอน "ยังไม่เคยตอบ" · ไม่พบลิงก์ "จัดการคุกกี้" ที่ไหนในเว็บ | ข้อกำหนดงานที่ 14 สั่งให้มี "ระบบถอนความยินยอม" — เพิ่มลิงก์ถาวรที่ท้ายเว็บ |
| M11 | **รูปทั้งหมดเป็น JPEG ไม่มี WebP/AVIF** · `hero.jpg` 275 KB · โฟลเดอร์ `photos/` รวม 1.1 MB | `ls photos/` (9 ไฟล์ .jpg) · มี `<picture>` ใช้จริง 1 จุด (hero มี `hero-mobile.jpg`) | งานที่ 13 |
| M12 | **หน้าแรกโหลด JS/CSS ของตัวเอง 22 ไฟล์ ≈ 114 KB (gzip)** ก่อนนับรูปและฟอนต์ · ก้อนใหญ่สุด `njchat.js` 24 KB gzip (98.7 KB ดิบ) | วัดด้วย gzip ต่อไฟล์ | โหลดเมื่อต้องใช้ (งานที่ 13) — **ห้ามซ่อนเนื้อหาเพื่อทำคะแนน** ตามข้อกำหนด |

---

## 5. Low

| # | เรื่อง | หมายเหตุ |
|---|---|---|
| L1 | ไม่มี `hreflang` / ไม่มีฉบับภาษาอังกฤษ | ตั้งใจ — ตลาดเป็นคนไทย ไม่ต้องแก้ |
| L2 | `og:image` ใช้ภาพแบรนด์ภาพเดียวเกือบทั้งเว็บ | ทำภาพต่อหมวดได้ภายหลัง |
| L3 | ไม่มี `BreadcrumbList` ที่หน้าใดเลย | รวมอยู่ในงานที่ 5 |
| L4 | `videos.html` ไม่มี `VideoObject` | รวมอยู่ในงานที่ 5 · ต้องใส่จากคลิปที่มีจริงเท่านั้น |
| L5 | ฟอนต์ Google Fonts ใช้ `display=swap` แล้ว ✅ | ไม่ต้องแก้ · ถ้าจะให้ดีขึ้นอีกคือโฮสต์ฟอนต์เอง (แลกกับความยุ่ง) |
| L6 | `robots.txt` ใช้ `Disallow: /consign.html?*t=` ซึ่งเป็นรูปแบบไวลด์การ์ดที่ Google เข้าใจ แต่บอตอื่นอาจไม่ | หน้าเหล่านั้นมี `noindex` ในหน้าอยู่แล้วอีกชั้น จึงไม่เสี่ยง |

---

## 6. เช็กลิสต์ตามหัวข้อที่สั่งในงานที่ 1

| หัวข้อที่สั่งตรวจ | ผล | อ้างอิง |
|---|---|---|
| Crawlable / Indexable Pages | ⚠️ เว็บสาธารณะผ่าน · **ระบบหลังบ้านไม่ผ่าน** | C1 |
| robots.txt | ⚠️ เว็บสาธารณะดีมาก · ระบบหลังบ้านไม่มี | C1 |
| XML Sitemap | ⚠️ มี 14 URL แต่ไม่มีหน้าแปลง และไม่มี sitemap index | H3, M5 |
| Canonical URL | ⚠️ 13 หน้าถูกต้อง · หน้าแปลงตั้งจาก JS เท่านั้น | C3 |
| Redirect | ❌ ทำที่เซิร์ฟเวอร์ไม่ได้บนโฮสต์ปัจจุบัน | H6 |
| 404 และ Broken Links | ⚠️ 404 ทำงานถูกต้องแต่เป็นหน้าเริ่มต้นของ GitHub · ยังไม่ได้ไล่ลิงก์เสียทั้งเว็บ (ต้องมีเครื่องมือ — ดูหัวข้อ 7) | M9 |
| Duplicate URLs | ✅ ตัวกรองไม่เขียนลง URL จึงไม่มี URL ซ้ำ | H1 |
| Duplicate Title / Meta Description | ❌ **22 หน้าแปลงใช้ชุดเดียวกัน** · 20 หน้าคงที่ไม่ซ้ำกัน | C3 |
| Orphan Pages | ❌ พบ 2 หน้า | H2 |
| Pagination | ❌ ไม่มี URL ต่อหน้า | M6 |
| URL จาก Search / Filter / Sort | ✅ ไม่มีการสร้าง URL จากตัวกรอง (ดีต่อ duplicate แต่เสียโอกาส) | H1 |
| JavaScript Rendering | ❌ **หน้าแปลงทั้งหมดขึ้นกับ JS** | C2 |
| Mobile Responsive | ✅ mobile-first เป็นกติกาข้อ 1 ของ repo · ยังไม่ได้วัดซ้ำในรอบนี้ | หัวข้อ 7 |
| Core Web Vitals | ⛔ **ยังไม่ได้วัด** | หัวข้อ 7 |
| Image Size และ Alt Text | ⚠️ มี lazy + `<picture>` ที่ hero · ไม่มี WebP · ไม่มี width/height · alt ยาวเกิน | M7, M8, M11 |
| Security Headers | ❌ เว็บสาธารณะไม่มีเลย · ✅ ระบบหลังบ้านครบ | H5 |
| Login / Admin / เอกสารลูกค้า ต้องไม่ถูก Index | ⚠️ เอกสารลูกค้า **ผ่าน** (มี `X-Robots-Tag` 11 จุดใน `server.js` + `Disallow` ใน robots.txt + `noindex` ในหน้า) · **Login/Admin ไม่ผ่าน** | C1 |

---

## 7. สิ่งที่ตรวจจากเครื่องนี้ไม่ได้ (ไม่เดาแทน)

1. **สถานะการเก็บดัชนีจริง** (Indexed Pages, Coverage, คำค้นที่ติดอยู่, Impressions/Clicks/CTR/Position)
   → ต้องมี Google Search Console ที่ยืนยันสิทธิ์โดเมนแล้ว ซึ่งยังไม่มี (H4)
2. **Core Web Vitals ของผู้ใช้จริง (field data)** → ต้องใช้ข้อมูล CrUX ผ่าน PageSpeed Insights หรือ Search Console
   ผมวัด lab data ได้ถ้าติดตั้ง Lighthouse (เป็นแพ็กเกจ dev ขนาดใหญ่ · repo นี้จงใจไม่มี `package.json`) → ต้องอนุมัติก่อน (Q4)
3. **ลิงก์เสียทั้งเว็บ** → ต้องมีตัวไต่ลิงก์ ผมเขียนสคริปต์เล็กๆ ด้วย Node ล้วนได้ (ไม่ต้องลงแพ็กเกจ) ในสปรินต์ 1
4. **สถานะบัญชี Meta Pixel `2478386206003868`** ว่าใช้งานอยู่จริงและเป็นของบริษัทหรือไม่ → ต้องเข้าบัญชีโฆษณา
5. **ปริมาณการค้นหาของคำค้น (keyword volume)** → ต้องใช้บริการภายนอกที่มีค่าใช้จ่าย ซึ่งกติกาห้ามไว้จนกว่าจะอนุมัติ
   → แผนนี้จึงเลือกคำค้นจาก **โครงสร้างธุรกิจจริง** (บริการที่มี + จังหวัดที่มีของ) ไม่ใช่จากตัวเลขที่เดาเอง

---

## 8. คำถามที่ต้องการคำตอบก่อนเริ่ม Sprint 1

ข้อกำหนดข้อ 7 สั่งให้ถามก่อน ห้ามเดาแล้วเปลี่ยนระบบหลัก — 6 ข้อนี้เปลี่ยนรูปแผนทั้งหมด

| # | คำถาม | ทำไมต้องตอบก่อน |
|---|---|---|
| **Q1** | **จะย้ายเว็บสาธารณะออกจาก GitHub Pages ไหม?** (เช่นไป Render Static Site ที่ใช้อยู่แล้ว) | เป็นตัวตัดสินว่าทำ **301 redirect · HTTP 410 · Security Header** ได้หรือไม่ (H5, H6) ถ้าไม่ย้าย แผนจะเลี่ยงไปใช้วิธีที่ทำได้จริงแทน และงาน 3 หัวข้อนั้นจะถูกบันทึกว่า "ทำไม่ได้เพราะข้อจำกัดโฮสต์" ไม่ใช่ "ยังไม่ทำ" |
| **Q2** | **ยืนยันสิทธิ์ Google Search Console ให้ `njteedinsure.com` ได้ไหม** (ผมเตรียมไฟล์/เมตาแท็กให้ได้ แต่ต้องกดในบัญชี Google ของบริษัท) | ตัวชี้วัดฝั่ง Search ทั้งหมดของงานที่ 12 ขึ้นกับข้อนี้ |
| **Q3** | **เปิด GA4 ไหม หรือใช้สถิติภายในของเราเองอย่างเดียว?** | สถิติภายในที่มีอยู่ไม่มีคุกกี้ ไม่มี PII และไม่ส่งข้อมูลออกนอกบริษัท — ถ้าพอใช้ ก็ไม่ต้องเพิ่มสคริปต์ภายนอก ซึ่งตรงกับกติกา "ห้ามติดตั้ง Tracking Script โดยไม่ได้รับอนุมัติ" มากกว่า · GA4 ให้ข้อมูลผู้ใช้/เซสชันที่สถิติภายในยังไม่มี |
| **Q4** | **อนุญาตให้ติดตั้ง Lighthouse ไว้วัดผลในเครื่องไหม?** | เป็น dev dependency ก้อนใหญ่ และ repo เว็บจงใจไม่มี `package.json` · ถ้าไม่อนุญาต จะวัดด้วย PageSpeed Insights (เจ้าของเปิดเว็บกดเอง ฟรี) แล้วผมบันทึกผลลง `PHASE10_PERFORMANCE_REPORT.md` แทน |
| **Q5** | **ข้อมูลสำนักงานที่ดินรายอำเภอ เอามาจากไหน?** ผมค้นทั้งสอง repo แล้ว **ไม่มีชุดข้อมูลนี้** (พบแค่ข้อความประกอบในหน้าเว็บ) | งานที่ 7 สั่งให้หน้าจังหวัดมี "สำนักงานที่ดินที่เกี่ยวข้อง" · กติกาห้ามสร้างข้อมูลที่ไม่มีแหล่งอ้างอิง → ถ้าไม่มีแหล่ง หัวข้อนี้จะเว้นไว้ ไม่เขียนเดา |
| **Q6** | **สมุทรสาครยังไม่มีแปลงเลย จะให้ทำหน้าจังหวัดไหม?** | ข้อกำหนดสั่งทั้ง "ต้องมี 6 จังหวัด" และ "ห้ามสร้าง Thin Content" พร้อมกัน · ทางออกที่เสนอ: ทำหน้าโดยให้เนื้อหาหลักเป็น **บริการ + ความรู้เฉพาะพื้นที่ + ขั้นตอนจริง** ไม่ใช่รายการแปลง แล้วแสดงกล่อง "ยังไม่มีแปลงในจังหวัดนี้ — ฝากขาย/ฝากหา" ตามกติกาข้อ 4 ของ repo · ขอคำยืนยันว่าเอาแนวนี้ |

---

## 9. สิ่งที่ยังไม่ได้แตะในรอบนี้

- ยังไม่แก้โค้ดใดๆ · ยังไม่ commit · สร้างแค่สาขา `feature/phase-10-seo-growth` และเอกสาร 2 ฉบับ
- ยังไม่ได้สำรองฐานข้อมูล (ข้อกำหนดข้อ 5) — จะทำ **ก่อน migration จริงในสปรินต์ 2** ซึ่งเป็นรอบแรกที่แตะข้อมูล
- ยังไม่ได้แตะ `nj-survey-system` เลย — สาขาฝั่งนั้นจะสร้างเมื่อเริ่มงานที่ต้องแก้ระบบหลังบ้าน (สปรินต์ 2)
